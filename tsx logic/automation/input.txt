```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene41',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#A6521B';
const PAPER = '#FFF3E2';

const Scene41: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push-in + drift
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { easing: Easing.inOut(Easing.ease) });
  const camX = Math.sin(frame * 0.008) * 8;

  // Impact shake when stamp slams (frame 70)
  const shakeAmp = interpolate(frame, [70, 74, 82], [0, 9, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeX = Math.sin(frame * 3.1) * shakeAmp;
  const shakeY = Math.cos(frame * 2.7) * shakeAmp * 0.6;

  const leftIn = sp(10);
  const rightIn = sp(22);
  const stampIn = sp(66, { damping: 9, stiffness: 200 });
  const barIn = sp(100);

  const marqueeX = -((frame * 2.4) % 900);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(120% 90% at ${50 + Math.sin(frame * 0.01) * 10}% 30%, #C2691F 0%, ${BG} 55%, #7A3A0F 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 720, height: 720, borderRadius: '50%',
        border: `50px solid ${INK}`, opacity: 0.05, left: -220, top: -260,
        transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 560, height: 560, background: YEL, opacity: 0.04,
        right: -180, bottom: -220, transform: `rotate(${20 + frame * 0.05}deg)`,
      }} />

      {/* Motion texture: halftone dots */}
      <AbsoluteFill style={{
        backgroundImage: `radial-gradient(${INK} 2px, transparent 2.6px)`,
        backgroundSize: '44px 44px',
        backgroundPosition: `${frame * 0.3}px ${frame * 0.15}px`,
        opacity: 0.07,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 41);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 99) * 1080) + frame * (0.4 + s)) % 1140 - 30,
            width: 5 + s * 8, height: 5 + s * 8,
            background: i % 3 === 0 ? YEL : i % 3 === 1 ? INK : PAPER,
            opacity: 0.25,
            transform: `rotate(${frame * (1 + s)}deg)`,
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${camX + shakeX}px, ${shakeY}px)` }}>

        {/* Top kicker tape */}
        <div style={{
          position: 'absolute', top: 70, left: 120,
          background: INK, color: PAPER, padding: '14px 34px',
          fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 34,
          letterSpacing: 4, transform: `rotate(-2deg) translateY(${(1 - sp(0)) * -80}px)`,
          boxShadow: `10px 10px 0 rgba(37,52,63,0.35)`,
        }}>
          EPF vs HOME LOAN — MATH DEKHO
        </div>

        {/* Left punk card: EPF */}
        <div style={{
          position: 'absolute', left: 150, top: 280, width: 640, height: 430,
          background: PAPER, border: `8px solid ${INK}`,
          boxShadow: `18px 18px 0 ${INK}`,
          transform: `rotate(-3deg) translateX(${(1 - leftIn) * -900}px) translateY(${Math.sin(frame * 0.05) * 4}px)`,
          padding: 40,
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 46, color: INK, letterSpacing: 2 }}>
            EPF RETURN
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 150, color: INK, lineHeight: 1.05 }}>
            8.25<span style={{ fontSize: 76 }}>%</span>
          </div>
          <div style={{
            display: 'inline-block', background: YEL, border: `5px solid ${INK}`,
            padding: '8px 20px', fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontWeight: 800, fontSize: 34, color: INK, transform: 'rotate(1.5deg)',
          }}>
            कमाई
          </div>
          {/* Tape strip */}
          <div style={{ position: 'absolute', top: -26, left: 200, width: 190, height: 44, background: YEL, opacity: 0.85, transform: 'rotate(-6deg)', border: `3px solid ${INK}` }} />
        </div>

        {/* Right punk card: LOAN */}
        <div style={{
          position: 'absolute', right: 150, top: 280, width: 640, height: 430,
          background: INK, border: `8px solid ${INK}`,
          boxShadow: `18px 18px 0 rgba(37,52,63,0.45)`,
          transform: `rotate(2.5deg) translateX(${(1 - rightIn) * 900}px) translateY(${Math.cos(frame * 0.05) * 4}px)`,
          padding: 40,
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 46, color: ORG, letterSpacing: 2 }}>
            LOAN RATE
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 150, color: YEL, lineHeight: 1.05 }}>
            8.50<span style={{ fontSize: 76 }}>%</span>
          </div>
          <div style={{
            display: 'inline-block', background: ORG, border: `5px solid ${YEL}`,
            padding: '8px 20px', fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontWeight: 800, fontSize: 34, color: INK, transform: 'rotate(-1.5deg)',
          }}>
            भरपाई
          </div>
          <div style={{ position: 'absolute', top: -26, right: 200, width: 190, height: 44, background: PAPER, opacity: 0.8, transform: 'rotate(5deg)', border: `3px solid ${INK}` }} />
        </div>

        {/* Center slam stamp: NET LOSS */}
        <div style={{
          position: 'absolute', left: '50%', top: 430,
          transform: `translateX(-50%) rotate(${-8 + Math.sin(frame * 0.06) * 1.5}deg) scale(${interpolate(stampIn, [0, 1], [3, 1])})`,
          opacity: stampIn,
          background: YEL, border: `10px solid ${INK}`, padding: '26px 50px',
          boxShadow: `14px 14px 0 ${INK}`,
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 40, color: INK, letterSpacing: 3, textAlign: 'center' }}>
            NET LOSS
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 96, color: INK, textAlign: 'center', lineHeight: 1 }}>
            −0.25%
          </div>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 30, color: INK, textAlign: 'center' }}>
            हर साल
          </div>
        </div>

        {/* Bottom marquee strip */}
        <div style={{
          position: 'absolute', bottom: 130, left: 0, right: 0, height: 84,
          background: INK, overflow: 'hidden', opacity: barIn,
          borderTop: `6px solid ${YEL}`, borderBottom: `6px solid ${YEL}`,
        }}>
          <div style={{
            whiteSpace: 'nowrap', transform: `translateX(${marqueeX}px)`,
            fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 44,
            color: YEL, lineHeight: '72px', letterSpacing: 2,
          }}>
            PARTIAL WITHDRAWAL = POWERFUL OPTION — RETIREMENT CORPUS, SO CAREFUL — PARTIAL WITHDRAWAL = POWERFUL OPTION — RETIREMENT CORPUS, SO CAREFUL —
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(20,15,10,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene41;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene42',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#9E4E18';
const PAPER = '#FFF3E2';

const Scene42: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: lateral pan + slight zoom out feel
  const camScale = interpolate(frame, [0, durationInFrames], [1.07, 1], { easing: Easing.inOut(Easing.ease) });
  const camY = Math.sin(frame * 0.01) * 6;

  const titleIn = sp(4);
  const barAIn = sp(28);
  const barBIn = sp(50);
  const cutIn = sp(120, { damping: 8, stiffness: 220 });
  const footIn = sp(160);

  // Bar widths (base 20yr vs aggressive ~15-16yr)
  const barAW = interpolate(sp(30), [0, 1], [0, 1280]);
  const barBW = interpolate(sp(55), [0, 1], [0, 960]);

  // Scissor cut shake
  const shakeAmp = interpolate(frame, [120, 124, 132], [0, 8, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${160 + Math.sin(frame * 0.012) * 8}deg, #B85C1F 0%, ${BG} 50%, #6E340C 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900,
        border: `70px solid ${YEL}`, opacity: 0.035, right: -320, top: -380,
        transform: `rotate(${45 + frame * 0.06}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0,
        borderLeft: '300px solid transparent', borderRight: '300px solid transparent',
        borderBottom: `520px solid ${INK}`, opacity: 0.05, left: -160, bottom: -240,
        transform: `rotate(${-10 + Math.sin(frame * 0.02) * 4}deg)`,
      }} />

      {/* Motion texture: diagonal lines */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(45deg, ${INK} 0 3px, transparent 3px 34px)`,
        backgroundPosition: `${frame * 0.5}px 0px`,
        opacity: 0.06,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 42);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: ((s * 1920) + frame * (0.3 + s * 0.6)) % 1980 - 30,
            top: seededRandom(i + 7) * 1080,
            width: 6 + s * 7, height: 6 + s * 7, borderRadius: i % 2 ? '50%' : 0,
            background: i % 3 === 0 ? YEL : PAPER, opacity: 0.22,
            transform: `rotate(${frame * (1 + s)}deg)`,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY + Math.sin(frame * 3) * shakeAmp}px)` }}>

        {/* Headline block */}
        <div style={{
          position: 'absolute', top: 90, left: 120,
          transform: `translateY(${(1 - titleIn) * -120}px) rotate(-1.2deg)`,
        }}>
          <div style={{
            background: YEL, border: `8px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`,
            padding: '18px 40px', fontFamily: 'Inter, Poppins, sans-serif',
            fontWeight: 900, fontSize: 84, color: INK, letterSpacing: 1,
          }}>
            2 SCENARIOS MODEL KARO
          </div>
        </div>

        {/* Bar A: BASE CASE */}
        <div style={{ position: 'absolute', top: 340, left: 120, opacity: barAIn }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 40, color: PAPER, letterSpacing: 3, marginBottom: 14 }}>
            BASE CASE — NO PREPAYMENT
          </div>
          <div style={{
            width: barAW, height: 110, background: INK, border: `6px solid ${INK}`,
            boxShadow: `10px 10px 0 rgba(37,52,63,0.4)`, position: 'relative',
          }}>
            <div style={{
              position: 'absolute', right: 20, top: 20, fontFamily: 'JetBrains Mono, monospace',
              fontWeight: 800, fontSize: 56, color: ORG,
            }}>
              20 YRS
            </div>
          </div>
        </div>

        {/* Bar B: AGGRESSIVE PREPAY */}
        <div style={{ position: 'absolute', top: 560, left: 120, opacity: barBIn }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 40, color: YEL, letterSpacing: 3, marginBottom: 14 }}>
            AGGRESSIVE EARLY PREPAY
          </div>
          <div style={{
            width: barBW, height: 110, background: YEL, border: `6px solid ${INK}`,
            boxShadow: `10px 10px 0 ${INK}`, position: 'relative',
          }}>
            <div style={{
              position: 'absolute', right: 20, top: 20, fontFamily: 'JetBrains Mono, monospace',
              fontWeight: 800, fontSize: 56, color: INK,
            }}>
              15–18 YRS
            </div>
          </div>

          {/* Torn-off saved chunk */}
          <div style={{
            position: 'absolute', left: 990, top: 54, opacity: cutIn,
            transform: `rotate(${6 + Math.sin(frame * 0.07) * 3}deg) scale(${interpolate(cutIn, [0, 1], [2.2, 1])})`,
            background: ORG, border: `6px dashed ${INK}`, padding: '16px 28px',
          }}>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 44, color: INK, whiteSpace: 'nowrap' }}>
              2–5 साल छोटा
            </div>
          </div>
        </div>

        {/* Scissor cut line SVG */}
        <svg width={220} height={220} style={{
          position: 'absolute', left: 1080, top: 520, opacity: cutIn,
          transform: `rotate(${Math.sin(frame * 0.1) * 6}deg)`,
        }}>
          <line x1={20} y1={110} x2={200} y2={110} stroke={PAPER} strokeWidth={6} strokeDasharray="18 14"
            strokeDashoffset={-frame * 1.2} />
          <circle cx={60} cy={80} r={22} fill="none" stroke={PAPER} strokeWidth={8} />
          <circle cx={60} cy={140} r={22} fill="none" stroke={PAPER} strokeWidth={8} />
          <line x1={78} y1={94} x2={150} y2={122} stroke={PAPER} strokeWidth={10} />
          <line x1={78} y1={126} x2={150} y2={98} stroke={PAPER} strokeWidth={10} />
        </svg>

        {/* Footer action strip */}
        <div style={{
          position: 'absolute', bottom: 120, left: 120, right: 120, opacity: footIn,
          transform: `translateY(${(1 - footIn) * 90}px)`,
          background: PAPER, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`,
          padding: '22px 36px', display: 'flex', gap: 30, alignItems: 'center',
        }}>
          <div style={{ background: INK, color: YEL, padding: '10px 22px', fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 34 }}>
            RM SE SHEET
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 44, color: INK }}>
            {'>>'}
          </div>
          <div style={{ background: INK, color: ORG, padding: '10px 22px', fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 34 }}>
            YA ONLINE EMI CALCULATOR
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(20,15,10,0.52) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene42;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene43',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#A6521B';
const PAPER = '#FFF3E2';

const Scene43: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: orbit-ish tilt
  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1.09], { easing: Easing.inOut(Easing.ease) });
  const camRot = Math.sin(frame * 0.006) * 0.6;

  // Rate ladder needle: from 8.5 down to 7.58
  const needleT = interpolate(sp(70, { damping: 14 }), [0, 1], [0, 1]);
  const ladderTop = 250;
  const ladderH = 620;
  const rateToY = (r: number) => ladderTop + ((8.6 - r) / (8.6 - 7.0)) * ladderH;
  const needleY = interpolate(needleT, [0, 1], [rateToY(8.5), rateToY(7.58)]);

  const chips = [
    { label: 'BankBazaar', d: 20 },
    { label: 'Bajaj Finance', d: 32 },
    { label: 'SBI', d: 44 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(100% 120% at 80% ${20 + Math.sin(frame * 0.01) * 8}%, #C2691F 0%, ${BG} 55%, #6E340C 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        background: INK, opacity: 0.045, left: -300, bottom: -350,
        transform: `scale(${1 + Math.sin(frame * 0.018) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 460, height: 460, border: `40px solid ${YEL}`,
        opacity: 0.04, right: -140, top: -160, transform: `rotate(${frame * 0.09}deg)`,
      }} />

      {/* Motion texture: grid */}
      <AbsoluteFill style={{
        backgroundImage: `linear-gradient(${INK} 2px, transparent 2px), linear-gradient(90deg, ${INK} 2px, transparent 2px)`,
        backgroundSize: '90px 90px',
        backgroundPosition: `${-frame * 0.25}px ${frame * 0.2}px`,
        opacity: 0.05,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const s = seededRandom(i + 43);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 3) * 1080) - frame * (0.5 + s)) % 1140,
            width: 5 + s * 9, height: 5 + s * 9,
            background: i % 4 === 0 ? YEL : i % 4 === 1 ? ORG : PAPER,
            opacity: 0.2, transform: `rotate(${45 + frame * s}deg)`,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        {/* Title */}
        <div style={{
          position: 'absolute', top: 80, left: 120,
          transform: `translateX(${(1 - sp(2)) * -600}px) rotate(-1.5deg)`,
          background: INK, padding: '16px 38px', border: `6px solid ${YEL}`,
          boxShadow: `12px 12px 0 rgba(37,52,63,0.5)`,
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 72, color: YEL, letterSpacing: 1 }}>
            BALANCE TRANSFER RATE LADDER
          </div>
        </div>

        {/* Rate ladder */}
        <div style={{ position: 'absolute', left: 320, top: ladderTop - 20, opacity: sp(20) }}>
          <div style={{ width: 26, height: ladderH + 40, background: PAPER, border: `5px solid ${INK}` }} />
          {[8.5, 8.0, 7.65, 7.58, 7.10].map((r, i) => (
            <div key={r} style={{
              position: 'absolute', left: -60, top: rateToY(r) - ladderTop - 4,
              display: 'flex', alignItems: 'center', gap: 16,
              opacity: sp(30 + i * 10),
              transform: `translateX(${(1 - sp(30 + i * 10)) * -200}px)`,
            }}>
              <div style={{ width: 150, height: 10, background: r === 7.58 ? YEL : INK }} />
              <div style={{
                fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: r === 7.10 || r === 7.58 ? 58 : 44,
                color: r === 7.58 ? YEL : r === 7.10 ? ORG : PAPER,
              }}>
                {r.toFixed(2)}%
              </div>
            </div>
          ))}
        </div>

        {/* Needle: AAPKA RATE sliding down */}
        <div style={{
          position: 'absolute', left: 700, top: needleY - 55,
          transform: `rotate(${-2 + Math.sin(frame * 0.08) * 1.2}deg)`,
          background: YEL, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`,
          padding: '14px 30px', opacity: sp(60),
        }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 34, color: INK }}>
            आपका 8.5%+ → market ~7.58%
          </div>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 48, color: INK, letterSpacing: 2 }}>
            SAVINGS DEKHO
          </div>
        </div>

        {/* Arrow SVG pointing from needle to ladder */}
        <svg width={140} height={80} style={{ position: 'absolute', left: 560, top: needleY - 10, opacity: sp(70) }}>
          <line x1={130} y1={40} x2={20} y2={40} stroke={INK} strokeWidth={10} />
          <polygon points="30,20 0,40 30,60" fill={INK} />
        </svg>

        {/* Tool chips - sticker collage right side */}
        <div style={{ position: 'absolute', right: 140, top: 300 }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 40, color: PAPER, letterSpacing: 3, marginBottom: 26, opacity: sp(14) }}>
            FREE TOOLS
          </div>
          {chips.map((c, i) => (
            <div key={c.label} style={{
              background: i % 2 ? PAPER : YEL, border: `6px solid ${INK}`,
              boxShadow: `10px 10px 0 ${INK}`, padding: '18px 32px', marginBottom: 26,
              transform: `rotate(${i % 2 ? 2 : -2}deg) translateX(${(1 - sp(c.d)) * 700}px) translateY(${Math.sin(frame * 0.05 + i) * 3}px)`,
              fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 52, color: INK,
              width: 440,
            }}>
              {c.label}
            </div>
          ))}
          {/* Landing zone note */}
          <div style={{
            background: INK, color: ORG, padding: '14px 26px', border: `5px solid ${ORG}`,
            transform: `rotate(-1deg) scale(${interpolate(sp(110, { damping: 9 }), [0, 1], [0, 1])})`,
            fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 38, width: 440,
          }}>
            LAND ZONE: 7.65–8.50%
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(20,15,10,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene43;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene44',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#994B16';
const PAPER = '#FFF3E2';

const Scene44: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: push in on the counter
  const camScale = interpolate(frame, [0, 200, durationInFrames], [1, 1.05, 1.02], { easing: Easing.inOut(Easing.ease) });

  // Animated money counter
  const counterVal = Math.round(interpolate(frame, [30, 120], [0, 50000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));
  const counterStr = counterVal.toLocaleString('en-IN');
  const pulse = 1 + Math.sin(frame * 0.15) * (frame > 120 ? 0.012 : 0);

  // Timeline progress draw
  const lineT = interpolate(sp(140), [0, 1], [0, 1]);

  // Shake on big-number lock (frame 120)
  const shakeAmp = interpolate(frame, [118, 122, 130], [0, 7, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${200 + Math.sin(frame * 0.01) * 10}deg, #BC5F1E 0%, ${BG} 45%, #63300B 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1000, height: 1000, borderRadius: '50%',
        border: `90px solid ${PAPER}`, opacity: 0.03, left: '50%', top: '50%',
        transform: `translate(-50%,-50%) scale(${1 + Math.sin(frame * 0.015) * 0.06}) rotate(${frame * 0.04}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 380, height: 380, background: YEL, opacity: 0.045,
        left: -120, top: 120, transform: `rotate(${12 + frame * 0.07}deg)`,
      }} />

      {/* Motion texture: concentric circles */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-radial-gradient(circle at 50% 45%, transparent 0 60px, ${INK} 60px 62px)`,
        opacity: 0.05,
      }} />

      {/* Floating particles: rupee confetti */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 44);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 11) * 1080) + frame * (0.8 + s * 1.4)) % 1160 - 40,
            fontFamily: 'JetBrains Mono, monospace', fontWeight: 800,
            fontSize: 18 + s * 22, color: i % 3 === 0 ? YEL : i % 3 === 1 ? PAPER : ORG,
            opacity: 0.28, transform: `rotate(${frame * (1 + s) * 0.8}deg)`,
          }}>
            {'\u20B9'}
          </div>
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${Math.sin(frame * 3.2) * shakeAmp}px)` }}>

        {/* Context chip */}
        <div style={{
          position: 'absolute', top: 90, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(2)) * -100}px) rotate(1deg)`,
          background: PAPER, border: `6px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`,
          padding: '12px 34px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 40, color: INK,
        }}>
          {'\u20B9'}50L OUTSTANDING | 0.75–1% GAP
        </div>

        {/* Hero counter */}
        <div style={{
          position: 'absolute', top: 240, left: 0, right: 0, textAlign: 'center',
          opacity: sp(20), transform: `scale(${pulse * interpolate(sp(20), [0, 1], [0.7, 1])})`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 210, lineHeight: 1,
            color: YEL, textShadow: `12px 12px 0 ${INK}`,
          }}>
            {'\u20B9'}{counterStr}
          </div>
          <div style={{
            display: 'inline-block', marginTop: 18, background: INK, color: ORG,
            padding: '8px 30px', fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontWeight: 800, fontSize: 46, transform: 'rotate(-1deg)', border: `4px solid ${ORG}`,
          }}>
            हर साल की बचत (37,500–50,000)
          </div>
        </div>

        {/* 15-year payoff sticker */}
        <div style={{
          position: 'absolute', top: 300, right: 150,
          transform: `rotate(${8 + Math.sin(frame * 0.06) * 2}deg) scale(${interpolate(sp(120, { damping: 8, stiffness: 240 }), [0, 1], [2.6, 1])})`,
          opacity: sp(120), background: YEL, border: `8px solid ${INK}`,
          boxShadow: `12px 12px 0 ${INK}`, padding: '20px 34px', textAlign: 'center',
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 34, color: INK, letterSpacing: 2 }}>
            15 SAAL MEIN
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 80, color: INK, lineHeight: 1 }}>
            {'\u20B9'}5–7L
          </div>
        </div>

        {/* Timeline strip */}
        <div style={{ position: 'absolute', bottom: 200, left: 160, right: 160, opacity: sp(140) }}>
          <div style={{ position: 'relative', height: 12, background: 'rgba(255,243,226,0.25)' }}>
            <div style={{ position: 'absolute', left: 0, top: 0, height: 12, width: `${lineT * 100}%`, background: YEL }} />
          </div>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 22 }}>
            {[
              { t: 'TRANSFER', v: '4–6 WEEKS' },
              { t: 'COST RECOVER', v: '12–18 MONTHS' },
              { t: 'BAAKI SAB', v: 'PURE SAVINGS' },
            ].map((x, i) => (
              <div key={x.t} style={{
                background: i === 2 ? INK : PAPER, border: `5px solid ${INK}`,
                boxShadow: `8px 8px 0 ${INK}`, padding: '12px 24px',
                transform: `rotate(${i % 2 ? 1.5 : -1.5}deg) translateY(${(1 - sp(150 + i * 12)) * 120}px)`,
              }}>
                <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 26, color: i === 2 ? ORG : INK, letterSpacing: 2 }}>{x.t}</div>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 40, color: i === 2 ? YEL : INK }}>{x.v}</div>
              </div>
            ))}
          </div>
        </div>

        {/* Hidden costs teaser */}
        <div style={{
          position: 'absolute', bottom: 100, left: '50%',
          transform: `translateX(-50%) rotate(-2deg) translateY(${(1 - sp(200)) * 140}px)`,
          background: ORG, border: `6px dashed ${INK}`, padding: '10px 30px',
          fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 38, color: INK,
        }}>
          लेकिन... HIDDEN COSTS भी हैं
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(20,15,10,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene44;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene45',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#A6521B';
const PAPER = '#FFF3E2';

const Scene45: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow tilt-pan
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1], { easing: Easing.inOut(Easing.ease) });
  const camX = interpolate(frame, [0, durationInFrames], [-20, 20]);

  // Receipt unrolls from top
  const receiptDrop = sp(8, { damping: 14 });

  // Stamp slam + shake at frame 130
  const stampIn = sp(130, { damping: 8, stiffness: 260 });
  const shakeAmp = interpolate(frame, [130, 134, 142], [0, 10, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const items = [
    { label: 'PROCESSING FEE', d: 40 },
    { label: 'LEGAL CHARGES', d: 55 },
    { label: 'PROPERTY VALUATION', d: 70 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(140% 100% at 20% ${80 + Math.sin(frame * 0.012) * 6}%, #BC5F1E 0%, ${BG} 50%, #6E340C 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 640, height: 640, background: INK, opacity: 0.05,
        right: -220, top: -240, transform: `rotate(${frame * 0.06}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        border: `44px solid ${YEL}`, opacity: 0.04, left: -160, bottom: -200,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />

      {/* Motion texture: cross-hatch */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(-45deg, ${INK} 0 2px, transparent 2px 40px)`,
        backgroundPosition: `0px ${frame * 0.4}px`,
        opacity: 0.06,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 45);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: ((s * 1920) - frame * (0.4 + s * 0.8)) % 1980,
            top: seededRandom(i + 21) * 1080,
            width: 4 + s * 10, height: 4 + s * 10,
            background: i % 3 === 0 ? YEL : PAPER, opacity: 0.22,
            transform: `rotate(${frame * (0.8 + s)}deg)`,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${camX + Math.sin(frame * 3.4) * shakeAmp}px, ${Math.cos(frame * 2.9) * shakeAmp * 0.7}px)` }}>

        {/* Receipt paper - left */}
        <div style={{
          position: 'absolute', left: 170, top: 90, width: 620,
          transform: `translateY(${(1 - receiptDrop) * -800}px) rotate(-2deg)`,
          background: PAPER, border: `7px solid ${INK}`, boxShadow: `16px 16px 0 ${INK}`,
        }}>
          <div style={{ background: INK, padding: '18px 30px', fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 44, color: YEL, letterSpacing: 3 }}>
            HIDDEN COSTS — RECEIPT
          </div>
          <div style={{ padding: '26px 30px' }}>
            {items.map((it, i) => (
              <div key={it.label} style={{
                display: 'flex', justifyContent: 'space-between', alignItems: 'center',
                borderBottom: `4px dashed ${INK}`, padding: '18px 0',
                opacity: sp(it.d), transform: `translateX(${(1 - sp(it.d)) * -240}px)`,
              }}>
                <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 800, fontSize: 35, color: INK }}>{it.label}</div>
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 35, color: INK }}>+++</div>
              </div>
            ))}
            <div style={{
              display: 'flex', justifyContent: 'space-between', padding: '22px 0 8px',
              opacity: sp(92),
            }}>
              <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 40, color: INK }}>TOTAL</div>
              <div style={{
                fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 48, color: INK,
                background: YEL, padding: '2px 16px', border: `4px solid ${INK}`,
                transform: `scale(${1 + Math.sin(frame * 0.12) * 0.02})`,
              }}>
                {'\u20B9'}15K–30K
              </div>
            </div>
          </div>
          {/* Zigzag bottom edge */}
          <svg width="606" height="26" style={{ display: 'block' }}>
            {Array.from({ length: 21 }).map((_, i) => (
              <polygon key={i} points={`${i * 30},0 ${i * 30 + 15},24 ${i * 30 + 30},0`} fill={PAPER} stroke={INK} strokeWidth={3} />
            ))}
          </svg>
        </div>

        {/* Rule stamp - right */}
        <div style={{
          position: 'absolute', right: 150, top: 240, width: 660,
          transform: `rotate(${5 + Math.sin(frame * 0.05) * 1.5}deg) scale(${interpolate(stampIn, [0, 1], [3, 1])})`,
          opacity: stampIn,
          border: `12px solid ${YEL}`, padding: 36, background: 'rgba(37,52,63,0.92)',
          boxShadow: `18px 18px 0 rgba(37,52,63,0.5)`,
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 52, color: YEL, letterSpacing: 2, textAlign: 'center' }}>
            TRANSFER TABHI WORTH IT
          </div>
          <div style={{ display: 'flex', gap: 24, justifyContent: 'center', marginTop: 26 }}>
            <div style={{ background: YEL, color: INK, padding: '14px 26px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 52, border: `5px solid ${INK}` }}>
              GAP {'\u2265'} 0.35%
            </div>
            <div style={{ background: ORG, color: INK, padding: '14px 26px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 52, border: `5px solid ${INK}` }}>
              TENURE 7+ YRS
            </div>
          </div>
        </div>

        {/* Next strategy teaser - bottom */}
        <div style={{
          position: 'absolute', bottom: 110, right: 150,
          transform: `rotate(-2deg) translateY(${(1 - sp(190)) * 200}px)`,
          background: YEL, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`,
          padding: '18px 36px',
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 30, color: INK, letterSpacing: 3 }}>
            NEXT STRATEGY
          </div>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 48, color: INK }}>
            Salary Increment 7–12% का smart use
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(20,15,10,0.52) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene45;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene46',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#9E4E18';
const PAPER = '#FFF3E2';

const Scene46: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle zoom + vertical drift
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], { easing: Easing.inOut(Easing.ease) });
  const camY = Math.sin(frame * 0.009) * 8;

  // EMI number flip: 43,000 -> 45,000
  const flipT = interpolate(frame, [110, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const emiVal = Math.round(interpolate(flipT, [0, 1], [43000, 45000]));

  // Strike lines on lifestyle tags
  const tags = [
    { label: 'NAYI CAR', d: 20 },
    { label: 'BADA TV', d: 32 },
    { label: 'VACATION', d: 44 },
  ];

  // Arrow draw
  const arrowT = interpolate(sp(96), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${120 + Math.sin(frame * 0.011) * 10}deg, #B85C1F 0%, ${BG} 48%, #63300B 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 760, height: 760, borderRadius: '50%',
        border: `56px solid ${INK}`, opacity: 0.05, right: -260, bottom: -320,
        transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.017) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 420, height: 420, background: PAPER, opacity: 0.035,
        left: -140, top: -120, transform: `rotate(${18 + frame * 0.08}deg)`,
      }} />

      {/* Motion texture: vertical bars */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(90deg, ${INK} 0 3px, transparent 3px 70px)`,
        backgroundPosition: `${frame * 0.35}px 0`,
        opacity: 0.05,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 46);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 5) * 1080) - frame * (0.6 + s)) % 1140,
            width: 5 + s * 8, height: 5 + s * 8, borderRadius: i % 2 ? '50%' : 0,
            background: i % 3 === 0 ? YEL : i % 3 === 1 ? ORG : PAPER,
            opacity: 0.22,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>

        {/* Lifestyle inflation tags - crossed out, left column */}
        <div style={{ position: 'absolute', left: 140, top: 150 }}>
          <div style={{
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 46,
            color: PAPER, marginBottom: 28, opacity: sp(4),
          }}>
            Lifestyle inflation को रोको:
          </div>
          {tags.map((t, i) => {
            const strikeT = interpolate(sp(t.d + 30), [0, 1], [0, 1]);
            return (
              <div key={t.label} style={{
                position: 'relative', display: 'inline-block', marginBottom: 26, marginRight: 20,
                background: PAPER, border: `6px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`,
                padding: '16px 34px', transform: `rotate(${i % 2 ? 2 : -2.5}deg) translateX(${(1 - sp(t.d)) * -600}px)`,
              }}>
                <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 54, color: INK }}>
                  {t.label}
                </div>
                {/* Animated strike-through */}
                <div style={{
                  position: 'absolute', left: 8, top: '50%', height: 12,
                  width: `${strikeT * 94}%`, background: ORG,
                  transform: 'rotate(-4deg)', border: `2px solid ${INK}`,
                }} />
              </div>
            );
          })}
          <div style={{
            marginTop: 12, display: 'inline-block',
            background: YEL, border: `6px solid ${INK}`, padding: '12px 28px',
            transform: `rotate(-1.5deg) scale(${interpolate(sp(84, { damping: 9 }), [0, 1], [0, 1])})`,
            fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 44, color: INK,
          }}>
            30–40% REDIRECT {'\u2192'} EMI / PREPAY
          </div>
        </div>

        {/* Step-up machine - right side */}
        <div style={{
          position: 'absolute', right: 130, top: 180, width: 640,
          background: INK, border: `8px solid ${YEL}`, boxShadow: `16px 16px 0 rgba(37,52,63,0.5)`,
          padding: 40, transform: `rotate(1.5deg) translateX(${(1 - sp(60)) * 800}px)`,
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 38, color: ORG, letterSpacing: 3 }}>
            EMI STEP-UP MACHINE
          </div>

          <div style={{
            marginTop: 24, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800,
            fontSize: 120, color: YEL, lineHeight: 1,
            transform: `scale(${1 + (flipT > 0 && flipT < 1 ? Math.sin(flipT * Math.PI) * 0.08 : 0)})`,
            transformOrigin: 'left center',
          }}>
            {'\u20B9'}{emiVal.toLocaleString('en-IN')}
          </div>

          <div style={{ display: 'flex', alignItems: 'center', gap: 18, marginTop: 26 }}>
            <div style={{ background: PAPER, color: INK, padding: '10px 20px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 38, border: `4px solid ${YEL}`, opacity: sp(90) }}>
              INCREMENT +{'\u20B9'}5,000
            </div>
            {/* Arrow */}
            <svg width={110} height={54}>
              <line x1={0} y1={27} x2={interpolate(arrowT, [0, 1], [0, 78])} y2={27} stroke={YEL} strokeWidth={10} />
              <polygon points={`${interpolate(arrowT, [0, 1], [10, 88])},7 ${interpolate(arrowT, [0, 1], [30, 108])},27 ${interpolate(arrowT, [0, 1], [10, 88])},47`} fill={YEL} opacity={arrowT} />
            </svg>
            <div style={{
              background: YEL, color: INK, padding: '10px 20px', fontFamily: 'JetBrains Mono, monospace',
              fontWeight: 800, fontSize: 38, border: `4px solid ${INK}`,
              transform: `scale(${interpolate(sp(104, { damping: 8 }), [0, 1], [0, 1]) * (1 + Math.sin(frame * 0.14) * 0.02)})`,
            }}>
              SIRF {'\u20B9'}2,000
            </div>
          </div>

          <div style={{
            marginTop: 30, borderTop: `4px dashed ${ORG}`, paddingTop: 22,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 36, color: PAPER,
            opacity: sp(130),
          }}>
            Lifestyle impact: छोटा — Tenure impact: बड़ा
          </div>
        </div>

        {/* Bottom impact meter */}
        <div style={{ position: 'absolute', bottom: 120, left: 140, right: 140, opacity: sp(150) }}>
          <div style={{ display: 'flex', gap: 24, alignItems: 'flex-end' }}>
            <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 34, color: PAPER, letterSpacing: 2 }}>
              IMPACT METER
            </div>
            <div style={{ flex: 1, height: 46, background: 'rgba(255,243,226,0.18)', border: `5px solid ${INK}`, position: 'relative' }}>
              <div style={{
                position: 'absolute', left: 0, top: 0, bottom: 0,
                width: `${interpolate(sp(160), [0, 1], [0, 86])}%`,
                background: `repeating-linear-gradient(45deg, ${YEL} 0 26px, ${ORG} 26px 52px)`,
              }} />
            </div>
            <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 44, color: YEL }}>
              TENURE {'\u2193'}{'\u2193'}
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(20,15,10,0.5) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene46;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene47',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#8F4413';
const PAPER = '#FFF3E2';

const Scene47: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow zoom to stamp
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.1], { easing: Easing.inOut(Easing.ease) });

  // Stamp slam shake at frame 150
  const stampIn = sp(150, { damping: 7, stiffness: 280 });
  const shakeAmp = interpolate(frame, [150, 154, 164], [0, 12, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const bubbles = [
    { text: '"EMI RENT JITNI HAI"', d: 44, x: 180, y: 300, r: -3 },
    { text: '"EMI TO INVESTMENT HAI"', d: 74, x: 1000, y: 430, r: 2.5 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(110% 110% at ${30 + Math.sin(frame * 0.01) * 8}% 20%, #B85C1F 0%, ${BG} 52%, #5C2C09 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 860, height: 860,
        border: `64px solid ${YEL}`, borderRadius: '50%', opacity: 0.035,
        left: '50%', top: '55%', transform: `translate(-50%,-50%) rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.016) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 520, height: 520, background: INK, opacity: 0.05,
        right: -180, top: -180, transform: `rotate(${frame * 0.07}deg)`,
      }} />

      {/* Motion texture: torn paper strips */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(0deg, transparent 0 120px, ${INK} 120px 123px)`,
        backgroundPosition: `0 ${-frame * 0.3}px`,
        opacity: 0.05,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const s = seededRandom(i + 47);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: ((s * 1920) + frame * (0.5 + s)) % 1980 - 30,
            top: seededRandom(i + 17) * 1080,
            width: 6 + s * 8, height: 6 + s * 8,
            background: i % 3 === 0 ? YEL : i % 3 === 1 ? ORG : PAPER,
            opacity: 0.2, transform: `rotate(${frame * s * 2}deg)`,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${Math.sin(frame * 3.3) * shakeAmp}px, ${Math.cos(frame * 2.8) * shakeAmp * 0.7}px)` }}>

        {/* Top payoff bar from previous point */}
        <div style={{
          position: 'absolute', top: 80, left: 130, right: 130,
          transform: `translateY(${(1 - sp(2)) * -140}px)`,
          background: YEL, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`,
          padding: '16px 34px', display: 'flex', justifyContent: 'space-between', alignItems: 'center',
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 46, color: INK }}>
            ANNUAL EMI STEP-UP
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 46, color: INK }}>
            TENURE {'\u2193'} + INTEREST {'\u2193'}
          </div>
        </div>

        {/* Sales speech bubbles - popped and slashed */}
        {bubbles.map((b, i) => {
          const bIn = sp(b.d, { damping: 9 });
          const slashT = interpolate(sp(b.d + 34), [0, 1], [0, 1]);
          return (
            <div key={i} style={{
              position: 'absolute', left: b.x, top: b.y,
              transform: `rotate(${b.r}deg) scale(${interpolate(bIn, [0, 1], [0.3, 1])}) translateY(${Math.sin(frame * 0.06 + i * 2) * 5}px)`,
              opacity: bIn,
            }}>
              <div style={{
                position: 'relative',
                background: PAPER, border: `8px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`,
                padding: '28px 44px', fontFamily: 'Inter, Poppins, sans-serif',
                fontWeight: 900, fontSize: 60, color: INK, whiteSpace: 'nowrap',
              }}>
                {b.text}
                {/* Slash cross */}
                <div style={{
                  position: 'absolute', left: '4%', top: '46%', height: 14,
                  width: `${slashT * 92}%`, background: ORG, border: `3px solid ${INK}`,
                  transform: 'rotate(-6deg)',
                }} />
                <div style={{
                  position: 'absolute', left: '4%', top: '46%', height: 14,
                  width: `${Math.max(0, slashT - 0.3) / 0.7 * 92}%`, background: INK,
                  transform: 'rotate(5deg)',
                }} />
              </div>
              {/* Bubble tail */}
              <svg width={70} height={54} style={{ marginLeft: 60, marginTop: -6 }}>
                <polygon points="0,0 66,0 14,50" fill={PAPER} stroke={INK} strokeWidth={7} />
              </svg>
            </div>
          );
        })}

        {/* Sellers row */}
        <div style={{ position: 'absolute', bottom: 250, left: 0, right: 0, display: 'flex', justifyContent: 'center', gap: 34 }}>
          {['BANKS', 'BUILDERS', 'AGENTS'].map((sName, i) => (
            <div key={sName} style={{
              background: INK, color: ORG, border: `5px solid ${ORG}`,
              padding: '12px 34px', fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 42,
              transform: `rotate(${(i - 1) * 2}deg) translateY(${(1 - sp(110 + i * 12)) * 200}px)`,
              boxShadow: `8px 8px 0 rgba(37,52,63,0.5)`,
              letterSpacing: 3,
            }}>
              {sName}
            </div>
          ))}
        </div>

        {/* Diagonal slam stamp */}
        <div style={{
          position: 'absolute', left: '50%', top: '52%',
          transform: `translate(-50%,-50%) rotate(${-12 + Math.sin(frame * 0.05) * 1}deg) scale(${interpolate(stampIn, [0, 1], [4, 1])})`,
          opacity: stampIn * 0.96,
          border: `14px solid ${YEL}`, padding: '18px 60px',
          background: 'rgba(37,52,63,0.55)',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 110,
            color: YEL, letterSpacing: 8, textShadow: `8px 8px 0 ${INK}`,
          }}>
            EMOTIONAL SELLING
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 50%, rgba(20,15,10,0.58) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene47;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene48',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#A6521B';
const PAPER = '#FFF3E2';

const Scene48: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow pan right
  const camX = interpolate(frame, [0, durationInFrames], [15, -15]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.08], { easing: Easing.inOut(Easing.ease) });

  const cards = [
    { top: '20 SAAL MEIN', big: 'INTEREST > PRINCIPAL', d: 26, r: -2.5 },
    { top: 'SHURU KI EMI', big: '90%+ INTEREST', d: 48, r: 1.8 },
    { top: 'TENURE EXTENSION', big: 'LAKHON KA BURDEN', d: 70, r: -1.6 },
  ];

  const marqueeX = -((frame * 3) % 1400);
  const finalIn = sp(150, { damping: 9 });

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${180 + Math.sin(frame * 0.013) * 12}deg, #BC5F1E 0%, ${BG} 46%, #6E340C 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700, background: YEL, opacity: 0.035,
        left: -260, top: -280, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', width: 540, height: 540, border: `48px solid ${INK}`,
        opacity: 0.05, right: -200, bottom: -240, transform: `rotate(${frame * 0.08}deg)`,
      }} />

      {/* Motion texture: halftone big dots */}
      <AbsoluteFill style={{
        backgroundImage: `radial-gradient(${INK} 3px, transparent 3.8px)`,
        backgroundSize: '64px 64px',
        backgroundPosition: `${-frame * 0.4}px ${frame * 0.2}px`,
        opacity: 0.06,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 48);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 13) * 1080) + frame * (0.5 + s * 0.9)) % 1140 - 30,
            width: 5 + s * 9, height: 5 + s * 9,
            background: i % 3 === 0 ? YEL : i % 3 === 1 ? PAPER : ORG,
            opacity: 0.22, transform: `rotate(${frame * (1 + s)}deg)`,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)` }}>

        {/* Torn headline */}
        <div style={{
          position: 'absolute', top: 70, left: '50%',
          transform: `translateX(-50%) rotate(-1deg) translateY(${(1 - sp(0)) * -120}px)`,
          background: INK, border: `6px solid ${YEL}`, padding: '14px 46px',
          boxShadow: `12px 12px 0 rgba(37,52,63,0.5)`,
        }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 88, color: YEL }}>
            कोई नहीं बताता...
          </div>
        </div>

        {/* Three secret cards */}
        <div style={{ position: 'absolute', top: 280, left: 0, right: 0, display: 'flex', justifyContent: 'center', gap: 46 }}>
          {cards.map((c, i) => {
            const cIn = sp(c.d, { damping: 11 });
            return (
              <div key={i} style={{
                width: 500, background: i === 1 ? YEL : PAPER,
                border: `8px solid ${INK}`, boxShadow: `16px 16px 0 ${INK}`,
                padding: '34px 30px',
                transform: `rotate(${c.r}deg) scaleX(${interpolate(cIn, [0, 1], [0.1, 1])}) translateY(${Math.sin(frame * 0.05 + i * 2.1) * 5}px)`,
                opacity: cIn,
              }}>
                <div style={{
                  display: 'inline-block', background: INK, color: i === 1 ? YEL : ORG,
                  padding: '6px 18px', fontFamily: 'Inter, Poppins, sans-serif',
                  fontWeight: 900, fontSize: 30, letterSpacing: 3, marginBottom: 20,
                }}>
                  SECRET {String(i + 1).padStart(2, '0')}
                </div>
                <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 800, fontSize: 34, color: INK, letterSpacing: 1 }}>
                  {c.top}
                </div>
                <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 56, color: INK, lineHeight: 1.08, marginTop: 8 }}>
                  {c.big}
                </div>
              </div>
            );
          })}
        </div>

        {/* Whose interest marquee */}
        <div style={{
          position: 'absolute', bottom: 230, left: 0, right: 0, height: 76,
          background: INK, overflow: 'hidden', opacity: sp(110),
          borderTop: `5px solid ${ORG}`, borderBottom: `5px solid ${ORG}`,
        }}>
          <div style={{
            whiteSpace: 'nowrap', transform: `translateX(${marqueeX}px)`,
            fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 40,
            color: ORG, lineHeight: '66px', letterSpacing: 2,
          }}>
            BANK {'\u2192'} INTEREST /// BUILDER {'\u2192'} SALE /// AGENT {'\u2192'} COMMISSION /// BANK {'\u2192'} INTEREST /// BUILDER {'\u2192'} SALE /// AGENT {'\u2192'} COMMISSION ///
          </div>
        </div>

        {/* Final yellow truth bar */}
        <div style={{
          position: 'absolute', bottom: 110, left: '50%',
          transform: `translateX(-50%) rotate(1deg) scale(${interpolate(finalIn, [0, 1], [0.4, 1])})`,
          opacity: finalIn,
          background: YEL, border: `8px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`,
          padding: '16px 44px',
        }}>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 54, color: INK, whiteSpace: 'nowrap' }}>
            आपकी financial well-being = सिर्फ आपकी priority
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(20,15,10,0.52) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene48;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene49',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#994B16';
const PAPER = '#FFF3E2';

const Scene49: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: dashboard scan (pan down then settle)
  const camY = interpolate(frame, [0, 90, durationInFrames], [-26, 0, 10], { easing: Easing.inOut(Easing.ease) });
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1.01]);

  // Lock wiggle micro-animation
  const lockWiggle = Math.sin(frame * 0.2) * interpolate(frame, [60, 70, 100], [0, 4, 0.8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const tapeX = -((frame * 2.8) % 1300);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(120% 120% at 50% ${-10 + Math.sin(frame * 0.01) * 6}%, #BC5F1E 0%, ${BG} 55%, #63300B 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: `76px solid ${INK}`, opacity: 0.045, right: -360, top: -400,
        transform: `rotate(${frame * 0.04}deg) scale(${1 + Math.sin(frame * 0.015) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 460, height: 460, background: YEL, opacity: 0.03,
        left: -160, bottom: -200, transform: `rotate(${frame * 0.06}deg)`,
      }} />

      {/* Motion texture: blueprint grid */}
      <AbsoluteFill style={{
        backgroundImage: `linear-gradient(${PAPER} 1.5px, transparent 1.5px), linear-gradient(90deg, ${PAPER} 1.5px, transparent 1.5px)`,
        backgroundSize: '110px 110px',
        backgroundPosition: `${frame * 0.2}px ${-frame * 0.15}px`,
        opacity: 0.045,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 49);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: ((s * 1920) + frame * (0.3 + s * 0.7)) % 1980 - 30,
            top: seededRandom(i + 29) * 1080,
            width: 5 + s * 8, height: 5 + s * 8, borderRadius: i % 2 ? '50%' : 0,
            background: i % 3 === 0 ? YEL : PAPER, opacity: 0.2,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>

        {/* Mission strip */}
        <div style={{
          position: 'absolute', top: 70, left: 130,
          transform: `rotate(-1.4deg) translateX(${(1 - sp(2)) * -700}px)`,
          background: YEL, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`,
          padding: '14px 36px', fontFamily: "'Noto Sans Devanagari', sans-serif",
          fontWeight: 800, fontSize: 52, color: INK,
        }}>
          वो information जो decision-makers नहीं देंगे
        </div>

        {/* MACRO DASHBOARD label */}
        <div style={{
          position: 'absolute', top: 200, left: 130, opacity: sp(20),
          fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 36,
          letterSpacing: 8, color: ORG,
        }}>
          MACRO DASHBOARD — JULY 2026
        </div>

        {/* Panel 1: REPO with lock icon */}
        <div style={{
          position: 'absolute', left: 130, top: 280, width: 560,
          background: INK, border: `8px solid ${YEL}`, boxShadow: `14px 14px 0 rgba(37,52,63,0.5)`,
          padding: 34, transform: `rotate(-1.8deg) translateY(${(1 - sp(36)) * 500}px)`,
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 22 }}>
            {/* Lock SVG icon */}
            <svg width={74} height={90} style={{ transform: `rotate(${lockWiggle}deg)` }}>
              <rect x={8} y={38} width={58} height={46} fill={YEL} stroke={PAPER} strokeWidth={4} />
              <path d="M 18 38 L 18 24 A 19 19 0 0 1 56 24 L 56 38" fill="none" stroke={YEL} strokeWidth={10} />
              <circle cx={37} cy={58} r={8} fill={INK} />
              <rect x={33} y={58} width={8} height={16} fill={INK} />
            </svg>
            <div>
              <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 32, color: ORG, letterSpacing: 2 }}>REPO RATE</div>
              <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 100, color: YEL, lineHeight: 1 }}>5.25%</div>
            </div>
          </div>
          <div style={{ marginTop: 18, background: PAPER, color: INK, display: 'inline-block', padding: '6px 18px', fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 30 }}>
            UNCHANGED {'\u00D7'}3 MEETINGS
          </div>
        </div>

        {/* Panel 2: STANCE */}
        <div style={{
          position: 'absolute', left: 760, top: 300, width: 440,
          background: PAPER, border: `8px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`,
          padding: 34, transform: `rotate(1.6deg) translateY(${(1 - sp(56)) * 500}px)`,
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 32, color: INK, letterSpacing: 2 }}>STANCE</div>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 84, color: INK, lineHeight: 1.05,
            transform: `scale(${1 + Math.sin(frame * 0.1) * 0.012})`, transformOrigin: 'left',
          }}>
            NEUTRAL
          </div>
          <div style={{ marginTop: 14, height: 16, background: `repeating-linear-gradient(45deg, ${INK} 0 18px, ${YEL} 18px 36px)` }} />
        </div>

        {/* Panel 3: FY27 GROWTH */}
        <div style={{
          position: 'absolute', left: 1270, top: 280, width: 500,
          background: YEL, border: `8px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`,
          padding: 34, transform: `rotate(-1.2deg) translateY(${(1 - sp(76)) * 500}px)`,
        }}>
          <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 32, color: INK, letterSpacing: 2 }}>FY27 GROWTH (BMI)</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 110, color: INK, lineHeight: 1 }}>6.6%</div>
          {/* Mini sparkline */}
          <svg width={380} height={70}>
            <polyline
              points="0,18 70,20 140,26 210,34 280,42 350,50"
              fill="none" stroke={INK} strokeWidth={8}
              strokeDasharray={500} strokeDashoffset={interpolate(sp(96), [0, 1], [500, 0])}
            />
          </svg>
        </div>

        {/* Warning tape marquee */}
        <div style={{
          position: 'absolute', bottom: 150, left: 0, right: 0, height: 88,
          background: `repeating-linear-gradient(45deg, ${YEL} 0 60px, ${INK} 60px 120px)`,
          transform: 'rotate(-1deg)', opacity: sp(120),
          display: 'flex', alignItems: 'center', overflow: 'hidden',
        }}>
          <div style={{
            whiteSpace: 'nowrap', transform: `translateX(${tapeX}px)`,
            background: 'rgba(37,52,63,0.88)', padding: '10px 0',
            fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 42, color: YEL, letterSpacing: 3,
          }}>
            {'\u26A0'} MIDDLE EAST CONFLICT {'\u2192'} GROWTH DOWN + INFLATION UP {'\u26A0'} MIDDLE EAST CONFLICT {'\u2192'} GROWTH DOWN + INFLATION UP {'\u26A0'}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(20,15,10,0.52) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene49;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene50',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const ORG = '#FF9B51';
const BG = '#8F4413';
const PAPER = '#FFF3E2';

const Scene50: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: split reveal then push toward ACTIVE side
  const camScale = interpolate(frame, [0, 160, durationInFrames], [1, 1.03, 1.09], { easing: Easing.inOut(Easing.ease) });
  const camX = interpolate(frame, [160, durationInFrames], [0, -60], { extrapolateLeft: 'clamp', easing: Easing.inOut(Easing.ease) });

  const splitIn = sp(30, { damping: 14 });
  const tools = [
    { label: 'PREPAY', d: 90 },
    { label: 'BALANCE TRANSFER', d: 104 },
    { label: 'EMI STEP-UP', d: 118 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${90 + Math.sin(frame * 0.01) * 6}deg, #6E340C 0%, ${BG} 50%, #B85C1F 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 820, height: 820, borderRadius: '50%',
        border: `60px solid ${YEL}`, opacity: 0.035, right: -300, top: -320,
        transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.018) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 520, height: 520, background: INK, opacity: 0.05,
        left: -200, bottom: -260, transform: `rotate(${frame * 0.06}deg)`,
      }} />

      {/* Motion texture: noise dashes */}
      <AbsoluteFill style={{
        backgroundImage: `repeating-linear-gradient(135deg, ${PAPER} 0 2px, transparent 2px 52px)`,
        backgroundPosition: `${frame * 0.45}px 0`,
        opacity: 0.05,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 50);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: s * 1920,
            top: ((seededRandom(i + 31) * 1080) - frame * (0.4 + s * 1.1)) % 1140,
            width: 5 + s * 9, height: 5 + s * 9,
            background: i % 3 === 0 ? YEL : i % 3 === 1 ? ORG : PAPER,
            opacity: 0.22, transform: `rotate(${frame * s * 1.6}deg)`,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)` }}>

        {/* Top nuance strip */}
        <div style={{
          position: 'absolute', top: 70, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(0)) * -120}px) rotate(0.8deg)`,
          background: PAPER, border: `6px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`,
          padding: '12px 34px', display: 'flex', gap: 26, alignItems: 'center',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 42, color: INK }}>
            6.6% {'>'} 6.1% DECADE AVG
          </div>
          <div style={{ background: INK, color: YEL, padding: '6px 18px', fontFamily: "'Noto Sans Devanagari', sans-serif", fontWeight: 800, fontSize: 34 }}>
            SLOW, COLLAPSE नहीं
          </div>
        </div>

        {/* PASSIVE side - left */}
        <div style={{
          position: 'absolute', left: 110, top: 240, width: 760, height: 560,
          background: 'rgba(37,52,63,0.85)', border: `8px solid ${INK}`,
          transform: `translateX(${(1 - splitIn) * -900}px) rotate(-1.2deg)`,
          padding: 40, boxShadow: `16px 16px 0 rgba(20,15,10,0.4)`,
        }}>
          <div style={{
            display: 'inline-block', background: '#9E9E9E', color: INK,
            padding: '8px 26px', fontFamily: 'Inter, Poppins, sans-serif',
            fontWeight: 900, fontSize: 52, letterSpacing: 4,
          }}>
            PASSIVE
          </div>
          <div style={{
            marginTop: 34, fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontWeight: 800, fontSize: 46, color: '#C9C9C9', lineHeight: 1.5,
            opacity: 0.55 + Math.sin(frame * 0.07) * 0.08,
          }}>
            बस EMI भरो...<br />
            rate cut का इंतजार...<br />
            uncertainty का शिकार
          </div>
          {/* Slow spinning hourglass icon */}
          <svg width={90} height={110} style={{ position: 'absolute', right: 44, bottom: 44, transform: `rotate(${Math.sin(frame * 0.03) * 10}deg)`, opacity: 0.7 }}>
            <path d="M10 8 H80 L50 55 L80 102 H10 L40 55 Z" fill="none" stroke="#9E9E9E" strokeWidth={7} />
            <path d="M30 20 H60 L45 42 Z" fill="#9E9E9E" />
          </svg>
        </div>

        {/* ACTIVE side - right */}
        <div style={{
          position: 'absolute', right: 110, top: 220, width: 780, height: 600,
          background: YEL, border: `8px solid ${INK}`, boxShadow: `18px 18px 0 ${INK}`,
          transform: `translateX(${(1 - splitIn) * 900}px) rotate(1.4deg)`,
          padding: 40,
        }}>
          <div style={{
            display: 'inline-block', background: INK, color: YEL,
            padding: '8px 26px', fontFamily: 'Inter, Poppins, sans-serif',
            fontWeight: 900, fontSize: 52, letterSpacing: 4,
            transform: `scale(${1 + Math.sin(frame * 0.12) * 0.02})`, transformOrigin: 'left',
          }}>
            ACTIVE
          </div>
          {tools.map((t, i) => (
            <div key={t.label} style={{
              display: 'flex', alignItems: 'center', gap: 22, marginTop: 30,
              transform: `translateX(${(1 - sp(t.d)) * 500}px)`, opacity: sp(t.d),
            }}>
              {/* Bolt icon */}
              <svg width={44} height={56}>
                <polygon points="26,0 4,32 18,32 12,56 40,22 24,22" fill={INK} />
              </svg>
              <div style={{
                background: INK, color: i === 1 ? ORG : PAPER, padding: '12px 26px',
                fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 46,
                border: `4px solid ${INK}`, letterSpacing: 1,
              }}>
                {t.label}
              </div>
            </div>
          ))}
          <div style={{
            marginTop: 32, fontFamily: "'Noto Sans Devanagari', sans-serif",
            fontWeight: 800, fontSize: 40, color: INK, opacity: sp(140),
          }}>
            = uncertainty को navigate करता है
          </div>
        </div>

        {/* VS badge center */}
        <div style={{
          position: 'absolute', left: '50%', top: 470,
          transform: `translateX(-50%) rotate(${Math.sin(frame * 0.08) * 6}deg) scale(${interpolate(sp(70, { damping: 8, stiffness: 260 }), [0, 1], [0, 1])})`,
          width: 130, height: 130, borderRadius: '50%',
          background: ORG, border: `8px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`,
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 56, color: INK,
        }}>
          VS
        </div>

        {/* Bottom verdict */}
        <div style={{
          position: 'absolute', bottom: 90, left: '50%',
          transform: `translateX(-50%) rotate(-1deg) translateY(${(1 - sp(170)) * 160}px)`,
          background: INK, border: `6px solid ${YEL}`, padding: '12px 40px',
          fontFamily: 'Inter, Poppins, sans-serif', fontWeight: 900, fontSize: 44,
          color: YEL, letterSpacing: 3, whiteSpace: 'nowrap',
        }}>
          WAIT-AND-WATCH IS RBI'S JOB — NOT YOURS
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(20,15,10,0.56) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: interpolate(frame, [0, 60], [0.7, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene50;
```

