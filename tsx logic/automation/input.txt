```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'BuffettTapewormQuote',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BuffettTapewormQuote: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  // Quote mark animations
  const quoteIn = sp(5);
  const eyebrowIn = sp(12);
  const nameIn = sp(20);
  const yearIn = sp(28);
  const lineIn = interpolate(frame, [40, 75], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const wordReveal = (delay: number) => interpolate(frame, [delay, delay + 12], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const tapewormPulse = 1 + Math.sin(frame * 0.15) * 0.04;
  const tapewormGlow = 0.5 + Math.sin(frame * 0.12) * 0.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(6,32,43,0.08) 0%, rgba(238,238,238,0) 55%), radial-gradient(circle at 75% 70%, rgba(233,215,0,0.12) 0%, rgba(238,238,238,0) 50%)',
      }} />

      {/* Animated transparent shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '8%',
          left: '6%',
          width: 520,
          height: 520,
          borderRadius: '50%',
          border: '2px solid #06202B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute',
          bottom: '5%',
          right: '8%',
          width: 380,
          height: 380,
          background: '#06202B',
          opacity: 0.04,
          transform: `rotate(${-frame * 0.3}deg)`,
        }} />
        <div style={{
          position: 'absolute',
          top: '55%',
          left: '40%',
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '320px solid #06202B',
          opacity: 0.05,
          transform: `rotate(${frame * 0.15}deg)`,
        }} />
      </AbsoluteFill>

      {/* Grid texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(#06202B 1px, transparent 1px), linear-gradient(90deg, #06202B 1px, transparent 1px)',
        backgroundSize: '80px 80px',
        opacity: 0.04,
        transform: `translateY(${frame * 0.3}px)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r1 = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const speed = 0.4 + r1 * 0.8;
        const y = (r2 * 1080 - frame * speed + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r1 * 1920,
            top: y,
            width: 4 + r1 * 6,
            height: 4 + r1 * 6,
            borderRadius: '50%',
            background: i % 3 === 0 ? '#e9d700' : '#06202B',
            opacity: 0.18,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>

        {/* Top eyebrow tag */}
        <div style={{
          position: 'absolute',
          top: 110,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - eyebrowIn) * -30}px)`,
          opacity: eyebrowIn,
          display: 'flex',
          alignItems: 'center',
          gap: 18,
        }}>
          <div style={{ width: 80, height: 3, background: '#06202B' }} />
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            fontWeight: 700,
            letterSpacing: 6,
            color: '#06202B',
          }}>
            WISDOM // ORACLE OF OMAHA
          </div>
          <div style={{ width: 80, height: 3, background: '#06202B' }} />
        </div>

        {/* Giant decorative quotation mark */}
        <div style={{
          position: 'absolute',
          top: 140,
          left: 180,
          fontFamily: 'Mont Heavy, Impact, sans-serif',
          fontSize: 380,
          color: '#06202B',
          opacity: quoteIn * 0.18,
          transform: `scale(${quoteIn}) rotate(-5deg)`,
          lineHeight: 1,
        }}>
          “
        </div>

        {/* Main quote block */}
        <div style={{
          position: 'absolute',
          top: '32%',
          left: '50%',
          transform: 'translateX(-50%)',
          width: '78%',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 102,
            fontWeight: 900,
            lineHeight: 1.08,
            color: '#06202B',
            letterSpacing: -2,
          }}>
            <span style={{
              display: 'inline-block',
              opacity: wordReveal(45),
              transform: `translateY(${(1 - wordReveal(45)) * 25}px)`,
              marginRight: 22,
            }}>HE</span>
            <span style={{
              display: 'inline-block',
              opacity: wordReveal(52),
              transform: `translateY(${(1 - wordReveal(52)) * 25}px)`,
              marginRight: 22,
            }}>CALLED</span>
            <span style={{
              display: 'inline-block',
              opacity: wordReveal(59),
              transform: `translateY(${(1 - wordReveal(59)) * 25}px)`,
              marginRight: 22,
            }}>IT</span>
            <span style={{
              display: 'inline-block',
              opacity: wordReveal(66),
              transform: `translateY(${(1 - wordReveal(66)) * 25}px)`,
              marginRight: 22,
            }}>A</span>
          </div>

          {/* The big word: TAPEWORM */}
          <div style={{
            marginTop: 30,
            opacity: wordReveal(78),
            transform: `scale(${wordReveal(78) * tapewormPulse})`,
            position: 'relative',
            display: 'inline-block',
          }}>
            <div style={{
              position: 'absolute',
              inset: 0,
              background: '#e9d700',
              filter: `blur(${20 + tapewormGlow * 18}px)`,
              opacity: 0.55,
              transform: 'scale(1.15)',
            }} />
            <div style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 196,
              fontWeight: 900,
              lineHeight: 1,
              color: '#06202B',
              letterSpacing: -4,
              position: 'relative',
              padding: '0 40px',
              WebkitTextStroke: '0px',
            }}>
              TAPEWORM
              <div style={{
                position: 'absolute',
                left: 30,
                right: 30,
                bottom: 22,
                height: 18,
                background: '#e9d700',
                opacity: 0.6,
                zIndex: -1,
              }} />
            </div>
          </div>
        </div>

        {/* Underline accent line */}
        <div style={{
          position: 'absolute',
          bottom: 220,
          left: '50%',
          transform: 'translateX(-50%)',
          width: `${lineIn * 620}px`,
          height: 4,
          background: '#06202B',
        }} />

        {/* Attribution: Warren Buffett */}
        <div style={{
          position: 'absolute',
          bottom: 150,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - nameIn) * 20}px)`,
          opacity: nameIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 22,
            fontWeight: 600,
            letterSpacing: 8,
            color: '#06202B',
            opacity: 0.6,
            marginBottom: 8,
          }}>
            — ATTRIBUTED TO —
          </div>
          <div style={{
            fontFamily: 'Nexa Bold, Inter, sans-serif',
            fontSize: 58,
            fontWeight: 900,
            color: '#06202B',
            letterSpacing: -0.5,
          }}>
            WARREN BUFFETT
          </div>
        </div>

        {/* Year badge bottom left */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 80,
          opacity: yearIn,
          transform: `translateY(${(1 - yearIn) * 20}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 14,
        }}>
          <div style={{
            width: 14,
            height: 14,
            borderRadius: '50%',
            background: '#e9d700',
            boxShadow: '0 0 12px #e9d700',
          }} />
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#06202B',
            fontWeight: 700,
            letterSpacing: 3,
          }}>
            BERKSHIRE HATHAWAY · ANNUAL LETTER
          </div>
        </div>

        {/* SVG: stylized tapeworm/coil consuming dollar — bottom right corner illustration */}
        <div style={{
          position: 'absolute',
          bottom: 130,
          right: 90,
          opacity: nameIn * 0.85,
          transform: `rotate(${Math.sin(frame * 0.05) * 4}deg)`,
        }}>
          <svg width="220" height="180" viewBox="0 0 220 180">
            {/* Dollar bill */}
            <rect x="20" y="60" width="120" height="70" rx="6" fill="#06202B" opacity="0.15" />
            <rect x="20" y="60" width="120" height="70" rx="6" fill="none" stroke="#06202B" strokeWidth="3" />
            <circle cx="80" cy="95" r="22" fill="none" stroke="#06202B" strokeWidth="2.5" />
            <text x="80" y="104" textAnchor="middle" fontFamily="Mont Heavy, Impact" fontSize="28" fontWeight="900" fill="#06202B">$</text>
            {/* Tapeworm shape eating it */}
            <path
              d={`M 140 95 Q 170 ${70 + Math.sin(frame * 0.2) * 6} 180 100 Q 195 130 175 145 Q 155 155 145 135`}
              fill="none"
              stroke="#06202B"
              strokeWidth="6"
              strokeLinecap="round"
            />
            <circle cx={145} cy={135} r="8" fill="#e9d700" stroke="#06202B" strokeWidth="2.5" />
            {/* segments on worm */}
            {[0, 1, 2, 3].map(i => (
              <line key={i} x1={150 + i * 8} y1={90 + i * 3} x2={156 + i * 8} y2={102 + i * 3} stroke="#06202B" strokeWidth="2" />
            ))}
            {/* bite marks */}
            <path d="M 140 80 L 132 88 L 140 88 L 134 96" fill="none" stroke="#06202B" strokeWidth="2.5" strokeLinecap="round" />
          </svg>
        </div>

      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default BuffettTapewormQuote;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'ConsumesRegardless',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ConsumesRegardless: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-15, 15]);

  // Bar chart values — wage vs cost of living
  const barProgress = interpolate(frame, [25, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const headlineIn = sp(5);
  const subIn = sp(35);
  const taxStampIn = sp(95);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(6,32,43,0.05) 0%, rgba(238,238,238,0) 50%, rgba(233,215,0,0.08) 100%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: -100,
          right: -100,
          width: 600,
          height: 600,
          background: '#06202B',
          opacity: 0.05,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.08})`,
        }} />
        <div style={{
          position: 'absolute',
          bottom: -150,
          left: -150,
          width: 500,
          height: 500,
          border: '3px solid #06202B',
          opacity: 0.07,
          borderRadius: '50%',
          transform: `rotate(${frame * 0.4}deg)`,
        }} />
        <div style={{
          position: 'absolute',
          top: '40%',
          left: '20%',
          width: 300,
          height: 300,
          background: '#06202B',
          opacity: 0.03,
          transform: `rotate(${45 + frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Diagonal lines texture */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(45deg, transparent, transparent 60px, rgba(6,32,43,0.04) 60px, rgba(6,32,43,0.04) 62px)',
      }} />

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 10);
        const x = (r * 1920 + frame * 0.6) % 1920;
        const y = seededRandom(i + 200) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 6,
            height: 6,
            background: '#06202B',
            opacity: 0.15,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>

        {/* Top headline */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 100,
          opacity: headlineIn,
          transform: `translateY(${(1 - headlineIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#06202B',
            opacity: 0.55,
            letterSpacing: 5,
            marginBottom: 18,
          }}>
            ARITHMETIC // THE QUIET TAX
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 118,
            fontWeight: 900,
            color: '#06202B',
            lineHeight: 0.95,
            letterSpacing: -2,
          }}>
            CONSUMES
            <br />
            <span style={{ position: 'relative', display: 'inline-block' }}>
              REGARDLESS
              <div style={{
                position: 'absolute',
                left: 0,
                right: 0,
                bottom: 12,
                height: 18,
                background: '#e9d700',
                opacity: 0.65,
                zIndex: -1,
                transform: `scaleX(${interpolate(frame, [30, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
                transformOrigin: 'left',
              }} />
            </span>
          </div>
        </div>

        {/* Sub line */}
        <div style={{
          position: 'absolute',
          top: 410,
          left: 100,
          opacity: subIn,
          transform: `translateY(${(1 - subIn) * 20}px)`,
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 38,
          fontWeight: 500,
          color: '#06202B',
          opacity: subIn * 0.78,
          maxWidth: 800,
          lineHeight: 1.3,
        }}>
          The host organism doesn't matter.
          <br />
          The math eats anyway.
        </div>

        {/* Right side: animated comparison chart */}
        <div style={{
          position: 'absolute',
          top: 180,
          right: 130,
          width: 620,
          height: 620,
        }}>
          {/* Chart title */}
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 20,
            color: '#06202B',
            letterSpacing: 4,
            marginBottom: 24,
            opacity: 0.65,
          }}>
            PURCHASING POWER OF $1
          </div>

          {/* Y-axis labels & bars */}
          {[
            { year: '2014', value: 1.0, label: '$1.00' },
            { year: '2018', value: 0.91, label: '$0.91' },
            { year: '2022', value: 0.78, label: '$0.78' },
            { year: '2026', value: 0.66, label: '$0.66' },
          ].map((bar, i) => {
            const delay = 30 + i * 10;
            const localProgress = interpolate(frame, [delay, delay + 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
            const barWidth = bar.value * 460 * localProgress;
            return (
              <div key={i} style={{
                display: 'flex',
                alignItems: 'center',
                marginBottom: 22,
                opacity: localProgress,
              }}>
                <div style={{
                  width: 90,
                  fontFamily: 'Nexa Bold, Inter, sans-serif',
                  fontSize: 30,
                  fontWeight: 900,
                  color: '#06202B',
                }}>
                  {bar.year}
                </div>
                <div style={{
                  width: 460,
                  height: 52,
                  background: 'rgba(6,32,43,0.08)',
                  position: 'relative',
                }}>
                  <div style={{
                    width: barWidth,
                    height: '100%',
                    background: i === 3 ? '#06202B' : `rgba(6,32,43,${0.55 + i * 0.12})`,
                    position: 'relative',
                  }}>
                    {i === 3 && (
                      <div style={{
                        position: 'absolute',
                        inset: 0,
                        background: '#e9d700',
                        opacity: 0.35,
                        mixBlendMode: 'multiply',
                      }} />
                    )}
                  </div>
                </div>
                <div style={{
                  marginLeft: 18,
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 28,
                  fontWeight: 700,
                  color: i === 3 ? '#06202B' : 'rgba(6,32,43,0.7)',
                }}>
                  {bar.label}
                </div>
              </div>
            );
          })}

          {/* Footer note */}
          <div style={{
            marginTop: 30,
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 18,
            color: '#06202B',
            opacity: barProgress * 0.55,
            letterSpacing: 2,
          }}>
            * AT ~3% ANNUAL COMPOUNDING — BUFFETT'S MATH
          </div>
        </div>

        {/* "DEVASTATING TAX" stamp bottom left */}
        <div style={{
          position: 'absolute',
          bottom: 120,
          left: 110,
          opacity: taxStampIn,
          transform: `rotate(-6deg) scale(${taxStampIn})`,
        }}>
          <div style={{
            border: '5px solid #06202B',
            padding: '14px 32px',
            display: 'inline-block',
            background: 'rgba(238,238,238,0.6)',
          }}>
            <div style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 56,
              fontWeight: 900,
              color: '#06202B',
              letterSpacing: 2,
              lineHeight: 1,
            }}>
              A DEVASTATING TAX
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 16,
              color: '#06202B',
              opacity: 0.65,
              letterSpacing: 4,
              marginTop: 6,
            }}>
              UNLEGISLATED · UNAVOIDABLE · UNSEEN
            </div>
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default ConsumesRegardless;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'NotPoeticMathematical',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const NotPoeticMathematical: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);

  const poeticIn = sp(8);
  const strikeProgress = interpolate(frame, [35, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const mathIn = sp(70);
  const formulaIn = sp(95);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient bg */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top right, rgba(6,32,43,0.1) 0%, rgba(238,238,238,0) 50%)',
      }} />

      {/* Shape layer — triangles & circles */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '15%',
          left: '8%',
          width: 280,
          height: 280,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.1})`,
        }} />
        <div style={{
          position: 'absolute',
          bottom: '10%',
          right: '15%',
          width: 0,
          height: 0,
          borderLeft: '160px solid transparent',
          borderRight: '160px solid transparent',
          borderBottom: '280px solid #06202B',
          opacity: 0.05,
          transform: `rotate(${frame * 0.25}deg)`,
        }} />
        <div style={{
          position: 'absolute',
          top: '60%',
          left: '70%',
          width: 200,
          height: 200,
          border: '3px solid #06202B',
          opacity: 0.08,
          transform: `rotate(${45 + frame * 0.3}deg)`,
        }} />
      </AbsoluteFill>

      {/* Dotted texture */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle, rgba(6,32,43,0.15) 1.5px, transparent 1.5px)',
        backgroundSize: '50px 50px',
        opacity: 0.5,
      }} />

      {/* Floating math symbols particles */}
      {['+', '−', '×', '÷', '=', '%', '∑', 'π'].map((sym, i) => {
        const r = seededRandom(i + 30);
        const y = (r * 1080 - frame * 0.5 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: (i * 240 + 60) % 1920,
            top: y,
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 40,
            color: '#06202B',
            opacity: 0.08,
            transform: `rotate(${frame * 0.5 + i * 30}deg)`,
          }}>
            {sym}
          </div>
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>

        {/* Top label */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: poeticIn,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 24,
          color: '#06202B',
          letterSpacing: 8,
          opacity: poeticIn * 0.6,
        }}>
          THIS WASN'T A METAPHOR
        </div>

        {/* The struck-through word: POETIC */}
        <div style={{
          position: 'absolute',
          top: 200,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - poeticIn) * 30}px)`,
          opacity: poeticIn,
          display: 'inline-block',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 180,
            fontWeight: 900,
            color: 'rgba(6,32,43,0.45)',
            letterSpacing: -3,
            position: 'relative',
            fontStyle: 'italic',
            padding: '0 20px',
          }}>
            POETIC
            {/* Strikethrough line */}
            <div style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: `${strikeProgress * 100}%`,
              height: 12,
              background: '#ff0000',
              transform: 'translateY(-50%) rotate(-3deg)',
              transformOrigin: 'left',
            }} />
          </div>
        </div>

        {/* The replacement word: MATHEMATICAL */}
        <div style={{
          position: 'absolute',
          top: 430,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - mathIn) * 40}px) scale(${mathIn})`,
          opacity: mathIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#06202B',
            letterSpacing: 6,
            opacity: 0.6,
            marginBottom: 14,
          }}>
            HE WAS BEING →
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 200,
            fontWeight: 900,
            color: '#06202B',
            letterSpacing: -4,
            lineHeight: 1,
            position: 'relative',
            display: 'inline-block',
          }}>
            MATHEMATICAL
            <div style={{
              position: 'absolute',
              left: 0,
              right: 0,
              bottom: 22,
              height: 22,
              background: '#e9d700',
              opacity: 0.55,
              zIndex: -1,
            }} />
          </div>
        </div>

        {/* Formula card bottom */}
        <div style={{
          position: 'absolute',
          bottom: 100,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - formulaIn) * 20}px)`,
          opacity: formulaIn,
          background: '#06202B',
          color: '#EEEEEE',
          padding: '24px 50px',
          display: 'flex',
          alignItems: 'center',
          gap: 30,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            fontWeight: 700,
            letterSpacing: 4,
            opacity: 0.7,
          }}>
            FORMULA:
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 42,
            fontWeight: 700,
          }}>
            REAL VALUE = NOMINAL × (1 − i)ⁿ
          </div>
          <div style={{
            width: 4,
            height: 50,
            background: '#e9d700',
          }} />
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            opacity: 0.8,
          }}>
            n = years · i = inflation
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default NotPoeticMathematical;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'DollarToSeventyFour',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const DollarToSeventyFour: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom-in
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.05]);

  // Counter animation: $1.00 → $0.74
  const counter = interpolate(frame, [40, 130], [100, 74], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const counterValue = (counter / 100).toFixed(2);

  // Year ticker: 2026 → 2036
  const yearTicker = Math.floor(interpolate(frame, [40, 130], [2026, 2036], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) }));

  // Dollar shrink scale
  const dollarShrink = interpolate(frame, [40, 130], [1, 0.74], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  const labelIn = sp(8);
  const dollarIn = sp(20);
  const conclusionIn = sp(140);

  // Arrow progress
  const arrowProgress = interpolate(frame, [40, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(238,238,238,1) 0%, rgba(6,32,43,0.06) 100%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '10%',
          right: '5%',
          width: 450,
          height: 450,
          borderRadius: '50%',
          border: '4px solid #06202B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute',
          bottom: '8%',
          left: '8%',
          width: 350,
          height: 350,
          background: '#06202B',
          opacity: 0.04,
          transform: `rotate(${-frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Grid texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(6,32,43,0.06) 1px, transparent 1px), linear-gradient(90deg, rgba(6,32,43,0.06) 1px, transparent 1px)',
        backgroundSize: '100px 100px',
      }} />

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 60);
        const x = (r * 1920 + frame * 0.4) % 1920;
        const y = seededRandom(i + 300) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            background: '#e9d700',
            opacity: 0.25,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Top headline */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - labelIn) * 20}px)`,
          opacity: labelIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#06202B',
            letterSpacing: 6,
            opacity: 0.6,
            marginBottom: 12,
          }}>
            EVEN AT 3% ANNUAL INFLATION
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 78,
            fontWeight: 900,
            color: '#06202B',
            letterSpacing: -1,
            lineHeight: 1,
          }}>
            A DOLLAR LOSES ITS GRIP
          </div>
        </div>

        {/* Left: $1.00 today */}
        <div style={{
          position: 'absolute',
          top: 320,
          left: 160,
          width: 540,
          textAlign: 'center',
          opacity: dollarIn,
          transform: `translateY(${(1 - dollarIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#06202B',
            letterSpacing: 5,
            opacity: 0.6,
            marginBottom: 20,
          }}>
            TODAY · 2026
          </div>
          <div style={{
            width: 320,
            height: 320,
            margin: '0 auto',
            borderRadius: '50%',
            background: '#06202B',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            position: 'relative',
            boxShadow: '0 0 60px rgba(6,32,43,0.25)',
          }}>
            <div style={{
              position: 'absolute',
              inset: 16,
              borderRadius: '50%',
              border: '3px dashed #e9d700',
              opacity: 0.7,
            }} />
            <div style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 130,
              fontWeight: 900,
              color: '#e9d700',
              letterSpacing: -3,
              lineHeight: 1,
            }}>
              $1.00
            </div>
          </div>
          <div style={{
            marginTop: 22,
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 32,
            fontWeight: 700,
            color: '#06202B',
          }}>
            FULL PURCHASING POWER
          </div>
        </div>

        {/* Center: ARROW with decay */}
        <div style={{
          position: 'absolute',
          top: 470,
          left: '50%',
          transform: 'translateX(-50%)',
          width: 240,
          height: 120,
          opacity: dollarIn,
        }}>
          <svg width="240" height="120" viewBox="0 0 240 120">
            <defs>
              <marker id="arrowhead44" markerWidth="12" markerHeight="12" refX="6" refY="6" orient="auto">
                <polygon points="0 0, 12 6, 0 12" fill="#06202B" />
              </marker>
            </defs>
            <line
              x1="10"
              y1="60"
              x2={10 + 210 * arrowProgress}
              y2="60"
              stroke="#06202B"
              strokeWidth="6"
              strokeLinecap="round"
              markerEnd={arrowProgress > 0.95 ? "url(#arrowhead44)" : undefined}
            />
            {/* drop markers */}
            {[0.25, 0.5, 0.75].map((p, i) => (
              <circle
                key={i}
                cx={10 + 210 * p}
                cy={60}
                r={arrowProgress > p ? 8 : 0}
                fill="#ff0000"
              />
            ))}
            <text
              x="120"
              y="35"
              textAnchor="middle"
              fontFamily="JetBrains Mono"
              fontSize="20"
              fontWeight="700"
              fill="#06202B"
              opacity="0.7"
            >
              10 YEARS
            </text>
            <text
              x="120"
              y="100"
              textAnchor="middle"
              fontFamily="JetBrains Mono"
              fontSize="18"
              fill="#ff0000"
              opacity={arrowProgress * 0.85}
            >
              − 26¢ LOST
            </text>
          </svg>
        </div>

        {/* Right: shrinking dollar → $0.74 */}
        <div style={{
          position: 'absolute',
          top: 320,
          right: 160,
          width: 540,
          textAlign: 'center',
          opacity: dollarIn,
          transform: `translateY(${(1 - dollarIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#06202B',
            letterSpacing: 5,
            opacity: 0.6,
            marginBottom: 20,
          }}>
            DECADE LATER · {yearTicker}
          </div>
          <div style={{
            width: 320,
            height: 320,
            margin: '0 auto',
            position: 'relative',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
          }}>
            {/* Ghost outline of original */}
            <div style={{
              position: 'absolute',
              width: 320,
              height: 320,
              borderRadius: '50%',
              border: '3px dashed #06202B',
              opacity: 0.35,
            }} />
            {/* Shrunk coin */}
            <div style={{
              width: 320 * dollarShrink,
              height: 320 * dollarShrink,
              borderRadius: '50%',
              background: '#06202B',
              opacity: 0.85,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              position: 'relative',
            }}>
              <div style={{
                position: 'absolute',
                inset: 12,
                borderRadius: '50%',
                border: '3px dashed #ff0000',
                opacity: 0.55,
              }} />
              <div style={{
                fontFamily: 'Mont Heavy, Impact, sans-serif',
                fontSize: 130 * dollarShrink,
                fontWeight: 900,
                color: '#ff0000',
                letterSpacing: -3,
                lineHeight: 1,
              }}>
                ${counterValue}
              </div>
            </div>
          </div>
          <div style={{
            marginTop: 22,
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 32,
            fontWeight: 700,
            color: '#06202B',
          }}>
            SAME DOLLAR · LESS BREAD
          </div>
        </div>

        {/* Bottom conclusion bar */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - conclusionIn) * 20}px)`,
          opacity: conclusionIn,
          background: '#06202B',
          padding: '18px 44px',
          display: 'flex',
          alignItems: 'center',
          gap: 24,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#e9d700',
            letterSpacing: 4,
          }}>
            EROSION:
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 44,
            fontWeight: 900,
            color: '#EEEEEE',
            letterSpacing: -1,
          }}>
            26% PURCHASING POWER VANISHED — SILENTLY
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default DollarToSeventyFour;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'NotTapewormChainsaw',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const NotTapewormChainsaw: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 11, mass: 0.4, stiffness: 130, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on impact
  const shakeFrame = frame > 95 && frame < 115 ? Math.sin(frame * 2) * 8 : 0;
  const shakeY = frame > 95 && frame < 115 ? Math.cos(frame * 2.3) * 6 : 0;

  // Camera zoom-in to chainsaw
  const camScale = interpolate(frame, [60, 110], [1, 1.06], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const headerIn = sp(5);
  const workerLineIn = sp(15);
  const tapewormIn = sp(35);
  const notIn = sp(60);
  const chainsawIn = sp(90, { damping: 9, stiffness: 180 });
  const slashProgress = interpolate(frame, [105, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient bg */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 50%, rgba(255,0,0,0.06) 0%, rgba(238,238,238,0) 50%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '20%',
          left: '5%',
          width: 400,
          height: 400,
          background: '#06202B',
          opacity: 0.04,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
        }} />
        <div style={{
          position: 'absolute',
          bottom: '5%',
          right: '10%',
          width: 0,
          height: 0,
          borderLeft: '200px solid transparent',
          borderRight: '200px solid transparent',
          borderBottom: '350px solid #06202B',
          opacity: 0.05,
          transform: `rotate(${15 + frame * 0.15}deg)`,
        }} />
      </AbsoluteFill>

      {/* Diagonal warning stripes texture */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(45deg, transparent, transparent 80px, rgba(255,0,0,0.04) 80px, rgba(255,0,0,0.04) 82px)',
      }} />

      {/* Particles — sawdust effect after impact */}
      {Array.from({ length: 30 }).map((_, i) => {
        const r = seededRandom(i + 80);
        const startFrame = 105;
        const localFrame = Math.max(0, frame - startFrame);
        const x = 960 + r * 300 - 150 + Math.cos(i) * localFrame * 4;
        const y = 540 + Math.sin(i * 1.5) * localFrame * 5 - localFrame * 2;
        const opa = frame > 105 ? Math.max(0, 1 - localFrame * 0.04) : 0;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 4 + r * 6,
            height: 4 + r * 6,
            background: '#06202B',
            opacity: opa * 0.4,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shakeFrame}px, ${shakeY}px) scale(${camScale})` }}>

        {/* Header strip */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - headerIn) * 20}px)`,
          opacity: headerIn,
          display: 'flex',
          alignItems: 'center',
          gap: 18,
        }}>
          <div style={{ width: 60, height: 3, background: '#06202B' }} />
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#06202B',
            letterSpacing: 6,
            fontWeight: 700,
          }}>
            FOR THE WORKING AMERICAN
          </div>
          <div style={{ width: 60, height: 3, background: '#06202B' }} />
        </div>

        {/* Subtitle: fixed wage + retirement */}
        <div style={{
          position: 'absolute',
          top: 160,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - workerLineIn) * 20}px)`,
          opacity: workerLineIn,
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 38,
          fontWeight: 500,
          color: '#06202B',
          opacity: workerLineIn * 0.75,
          textAlign: 'center',
        }}>
          Fixed wage · Retirement savings · The math turns brutal
        </div>

        {/* Crossed out: TAPEWORM */}
        <div style={{
          position: 'absolute',
          top: 290,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - tapewormIn) * 30}px) scale(${tapewormIn})`,
          opacity: tapewormIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#06202B',
            letterSpacing: 6,
            opacity: 0.6,
            marginBottom: 14,
          }}>
            THIS ISN'T A —
          </div>
          <div style={{
            display: 'inline-block',
            position: 'relative',
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 140,
            fontWeight: 900,
            color: 'rgba(6,32,43,0.4)',
            letterSpacing: -2,
          }}>
            TAPEWORM
            <div style={{
              position: 'absolute',
              top: '50%',
              left: 0,
              width: '100%',
              height: 10,
              background: '#06202B',
              transform: 'translateY(-50%) rotate(-2deg)',
              opacity: 0.7,
            }} />
          </div>
        </div>

        {/* IT'S A → */}
        <div style={{
          position: 'absolute',
          top: 530,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - notIn) * 20}px)`,
          opacity: notIn,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 30,
          color: '#06202B',
          letterSpacing: 8,
          fontWeight: 700,
        }}>
          IT'S A →
        </div>

        {/* CHAINSAW — huge impactful */}
        <div style={{
          position: 'absolute',
          top: 600,
          left: '50%',
          transform: `translateX(-50%) scale(${chainsawIn})`,
          opacity: chainsawIn,
          textAlign: 'center',
        }}>
          <div style={{
            position: 'relative',
            display: 'inline-block',
          }}>
            {/* red glow */}
            <div style={{
              position: 'absolute',
              inset: -20,
              background: '#ff0000',
              filter: 'blur(40px)',
              opacity: 0.35,
            }} />
            <div style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 240,
              fontWeight: 900,
              color: '#06202B',
              letterSpacing: -6,
              lineHeight: 1,
              position: 'relative',
              padding: '0 40px',
            }}>
              CHAINSAW
              {/* Slash line cutting through */}
              <svg
                style={{
                  position: 'absolute',
                  top: '50%',
                  left: 0,
                  width: '100%',
                  height: '60%',
                  transform: 'translateY(-50%)',
                  pointerEvents: 'none',
                }}
                viewBox="0 0 1000 200"
              >
                <line
                  x1={-20}
                  y1={100}
                  x2={-20 + (1040 * slashProgress)}
                  y2={100}
                  stroke="#ff0000"
                  strokeWidth="14"
                  strokeLinecap="round"
                  opacity="0.9"
                />
                <line
                  x1={-20}
                  y1={100}
                  x2={-20 + (1040 * slashProgress)}
                  y2={100}
                  stroke="#e9d700"
                  strokeWidth="4"
                  strokeLinecap="round"
                />
              </svg>
            </div>
          </div>
        </div>

        {/* Chainsaw SVG icon top right */}
        <div style={{
          position: 'absolute',
          top: 240,
          right: 100,
          opacity: chainsawIn * 0.9,
          transform: `rotate(${-15 + Math.sin(frame * 0.3) * 3}deg)`,
        }}>
          <svg width="240" height="140" viewBox="0 0 240 140">
            {/* Handle */}
            <rect x="10" y="55" width="55" height="30" rx="6" fill="#06202B" />
            <rect x="20" y="35" width="35" height="22" rx="4" fill="#06202B" />
            {/* Engine body */}
            <rect x="60" y="40" width="60" height="55" rx="8" fill="#06202B" />
            <circle cx="90" cy="68" r="10" fill="#e9d700" />
            {/* Bar */}
            <rect x="115" y="60" width="115" height="18" rx="9" fill="#06202B" />
            {/* Teeth */}
            {Array.from({ length: 10 }).map((_, i) => (
              <polygon
                key={i}
                points={`${125 + i * 10},60 ${130 + i * 10},52 ${135 + i * 10},60`}
                fill="#06202B"
              />
            ))}
            {Array.from({ length: 10 }).map((_, i) => (
              <polygon
                key={`b${i}`}
                points={`${125 + i * 10},78 ${130 + i * 10},86 ${135 + i * 10},78`}
                fill="#06202B"
              />
            ))}
          </svg>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default NotTapewormChainsaw;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'NobodySaysOutLoud',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const NobodySaysOutLoud: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow pan
  const camX = interpolate(frame, [0, durationInFrames], [10, -10]);

  const labelIn = sp(5);
  const titleIn = sp(18);
  const partyLabelsIn = sp(50);
  const podiumIn = sp(80);
  const lipsealProgress = interpolate(frame, [110, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, rgba(238,238,238,1) 0%, rgba(6,32,43,0.08) 100%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '8%',
          right: '5%',
          width: 380,
          height: 380,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.07})`,
        }} />
        <div style={{
          position: 'absolute',
          bottom: '12%',
          left: '8%',
          width: 320,
          height: 320,
          border: '3px solid #06202B',
          opacity: 0.08,
          borderRadius: '50%',
          transform: `rotate(${frame * 0.3}deg)`,
        }} />
        <div style={{
          position: 'absolute',
          top: '45%',
          left: '50%',
          width: 250,
          height: 250,
          background: '#06202B',
          opacity: 0.03,
          transform: `translate(-50%, -50%) rotate(${frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Vertical lines texture */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(90deg, transparent, transparent 120px, rgba(6,32,43,0.04) 120px, rgba(6,32,43,0.04) 121px)',
      }} />

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 90);
        const x = (r * 1920 - frame * 0.3 + 1920) % 1920;
        const y = seededRandom(i + 400) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            background: '#06202B',
            opacity: 0.18,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>

        {/* Eyebrow tag */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: 100,
          opacity: labelIn,
          transform: `translateY(${(1 - labelIn) * 20}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 16,
        }}>
          <div style={{
            width: 10,
            height: 10,
            borderRadius: '50%',
            background: '#ff0000',
            boxShadow: `0 0 ${8 + Math.sin(frame * 0.2) * 6}px #ff0000`,
          }} />
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#06202B',
            letterSpacing: 6,
            fontWeight: 700,
          }}>
            THE QUIET PART · UNSAID
          </div>
        </div>

        {/* Main headline */}
        <div style={{
          position: 'absolute',
          top: 200,
          left: 100,
          width: 1000,
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 130,
            fontWeight: 900,
            color: '#06202B',
            lineHeight: 0.95,
            letterSpacing: -3,
          }}>
            HERE'S THE PART
            <br />
            <span style={{ position: 'relative', display: 'inline-block' }}>
              NOBODY
              <div style={{
                position: 'absolute',
                left: -8,
                right: -8,
                bottom: 14,
                height: 22,
                background: '#e9d700',
                opacity: 0.6,
                zIndex: -1,
                transform: `scaleX(${interpolate(frame, [30, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
                transformOrigin: 'left',
              }} />
            </span>
            <br />
            <span style={{ color: 'rgba(6,32,43,0.7)' }}>WILL SAY OUT LOUD.</span>
          </div>
        </div>

        {/* Bipartisan badge — two columns */}
        <div style={{
          position: 'absolute',
          bottom: 200,
          left: 100,
          opacity: partyLabelsIn,
          transform: `translateY(${(1 - partyLabelsIn) * 20}px)`,
          display: 'flex',
          alignItems: 'center',
          gap: 24,
        }}>
          <div style={{
            border: '3px solid #06202B',
            padding: '14px 28px',
            background: 'rgba(238,238,238,0.5)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 18,
              color: '#06202B',
              opacity: 0.55,
              letterSpacing: 3,
              marginBottom: 4,
            }}>
              PARTY A
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 42,
              fontWeight: 900,
              color: '#06202B',
            }}>
              SAME SCRIPT
            </div>
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 48,
            color: '#06202B',
            fontWeight: 900,
          }}>
            +
          </div>
          <div style={{
            border: '3px solid #06202B',
            padding: '14px 28px',
            background: 'rgba(238,238,238,0.5)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 18,
              color: '#06202B',
              opacity: 0.55,
              letterSpacing: 3,
              marginBottom: 4,
            }}>
              PARTY B
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 42,
              fontWeight: 900,
              color: '#06202B',
            }}>
              SAME OUTCOME
            </div>
          </div>
        </div>

        {/* Right side: Podium with sealed lips icon */}
        <div style={{
          position: 'absolute',
          right: 140,
          top: 320,
          opacity: podiumIn,
          transform: `translateY(${(1 - podiumIn) * 30}px) scale(${podiumIn})`,
        }}>
          <svg width="360" height="440" viewBox="0 0 360 440">
            {/* Podium */}
            <rect x="80" y="280" width="200" height="160" fill="#06202B" />
            <rect x="60" y="270" width="240" height="20" fill="#06202B" />
            {/* Seal on podium */}
            <circle cx="180" cy="360" r="36" fill="none" stroke="#e9d700" strokeWidth="4" />
            <text x="180" y="370" textAnchor="middle" fontFamily="Mont Heavy, Impact" fontSize="32" fontWeight="900" fill="#e9d700">$</text>

            {/* Microphone */}
            <line x1="180" y1="270" x2="180" y2="210" stroke="#06202B" strokeWidth="6" strokeLinecap="round" />
            <ellipse cx="180" cy="195" rx="22" ry="32" fill="#06202B" />
            <ellipse cx="180" cy="195" rx="16" ry="24" fill="#EEEEEE" opacity="0.15" />

            {/* Head silhouette behind */}
            <circle cx="180" cy="120" r="55" fill="#06202B" opacity="0.85" />
            {/* Mouth with seal/zip */}
            <line
              x1={155}
              y1={138}
              x2={155 + 50 * lipsealProgress}
              y2={138}
              stroke="#ff0000"
              strokeWidth="6"
              strokeLinecap="round"
            />
            {/* zipper teeth */}
            {Array.from({ length: 6 }).map((_, i) => {
              const tx = 158 + i * 8;
              const visible = lipsealProgress > i / 6;
              return visible ? (
                <line key={i} x1={tx} y1={134} x2={tx} y2={142} stroke="#ff0000" strokeWidth="2" />
              ) : null;
            })}

            {/* Label below */}
            <text x="180" y="430" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="16" fill="#06202B" opacity="0.6" letterSpacing="4">
              OFF THE RECORD
            </text>
          </svg>
        </div>

        {/* Bottom strip "LOVES INFLATION" stamp */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: 100,
          opacity: lipsealProgress,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          color: '#ff0000',
          fontWeight: 700,
          letterSpacing: 4,
        }}>
          QUOTE: "I LOVE THE INFLATION" ─── PRESIDENTIAL · BIPARTISAN PATTERN
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default NobodySaysOutLoud;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'FederalDebtTooHeavy',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const FederalDebtTooHeavy: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom-in on debt pile
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.07]);

  // Debt counter $0 → $36.5T
  const debtCounter = interpolate(frame, [25, 100], [0, 36.5], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Scale tilt
  const scaleTilt = interpolate(frame, [60, 130], [0, -14], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const headerIn = sp(5);
  const debtNumIn = sp(18);
  const scaleIn = sp(45);
  const exitLabelIn = sp(120);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at bottom center, rgba(6,32,43,0.12) 0%, rgba(238,238,238,0) 60%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '10%',
          left: '10%',
          width: 400,
          height: 400,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }} />
        <div style={{
          position: 'absolute',
          top: '20%',
          right: '8%',
          width: 0,
          height: 0,
          borderLeft: '150px solid transparent',
          borderRight: '150px solid transparent',
          borderBottom: '260px solid #06202B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Grid texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(6,32,43,0.05) 1px, transparent 1px)',
        backgroundSize: '60px 60px',
      }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 120);
        const x = r * 1920;
        const y = (r * 1080 + frame * 0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            background: i % 2 === 0 ? '#e9d700' : '#06202B',
            opacity: 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Top heading */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - headerIn) * 20}px)`,
          opacity: headerIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#06202B',
            letterSpacing: 6,
            opacity: 0.6,
            marginBottom: 12,
          }}>
            U.S. FEDERAL DEBT · LIVE COUNTER
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 72,
            fontWeight: 900,
            color: '#06202B',
            letterSpacing: -1,
          }}>
            TOO HEAVY TO CARRY
          </div>
        </div>

        {/* Giant debt counter */}
        <div style={{
          position: 'absolute',
          top: 260,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - debtNumIn) * 30}px)`,
          opacity: debtNumIn,
          textAlign: 'center',
        }}>
          <div style={{
            position: 'relative',
            display: 'inline-block',
          }}>
            <div style={{
              position: 'absolute',
              inset: -10,
              background: '#ff0000',
              filter: 'blur(40px)',
              opacity: 0.18,
            }} />
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 230,
              fontWeight: 900,
              color: '#06202B',
              letterSpacing: -6,
              lineHeight: 1,
              position: 'relative',
            }}>
              ${debtCounter.toFixed(2)}T
            </div>
          </div>
          <div style={{
            marginTop: 10,
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 30,
            color: '#06202B',
            opacity: 0.7,
            fontWeight: 600,
          }}>
            and climbing ─ interest alone now > $1T/year
          </div>
        </div>

        {/* Left side: Tilted scale visual */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 100,
          opacity: scaleIn,
          transform: `translateY(${(1 - scaleIn) * 30}px)`,
        }}>
          <svg width="500" height="280" viewBox="0 0 500 280">
            {/* Base */}
            <rect x="220" y="240" width="60" height="30" fill="#06202B" />
            <rect x="180" y="265" width="140" height="12" fill="#06202B" />
            {/* Center pole */}
            <line x1="250" y1="240" x2="250" y2="100" stroke="#06202B" strokeWidth="6" />
            {/* Beam — tilted */}
            <g transform={`rotate(${scaleTilt} 250 100)`}>
              <line x1="80" y1="100" x2="420" y2="100" stroke="#06202B" strokeWidth="8" strokeLinecap="round" />
              {/* Left plate — Revenue (light) */}
              <line x1="100" y1="100" x2="100" y2="135" stroke="#06202B" strokeWidth="3" />
              <ellipse cx="100" cy="145" rx="60" ry="10" fill="#06202B" />
              <text x="100" y="180" textAnchor="middle" fontFamily="Mont Heavy, Impact" fontSize="22" fontWeight="900" fill="#06202B">REVENUE</text>
              <text x="100" y="200" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="16" fill="#06202B" opacity="0.7">$5T</text>

              {/* Right plate — Debt (heavy) */}
              <line x1="400" y1="100" x2="400" y2="135" stroke="#06202B" strokeWidth="3" />
              <ellipse cx="400" cy="145" rx="70" ry="12" fill="#06202B" />
              <rect x="360" y="120" width="80" height="30" fill="#06202B" />
              <rect x="350" y="100" width="100" height="22" fill="#06202B" />
              <text x="400" y="180" textAnchor="middle" fontFamily="Mont Heavy, Impact" fontSize="22" fontWeight="900" fill="#ff0000">DEBT</text>
              <text x="400" y="200" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="16" fill="#06202B" opacity="0.7">$36.5T</text>
            </g>
          </svg>
        </div>

        {/* Right side: The only way out card */}
        <div style={{
          position: 'absolute',
          bottom: 100,
          right: 100,
          opacity: exitLabelIn,
          transform: `translateY(${(1 - exitLabelIn) * 20}px)`,
          background: '#06202B',
          padding: '28px 36px',
          maxWidth: 520,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 18,
            color: '#e9d700',
            letterSpacing: 5,
            marginBottom: 12,
          }}>
            THE ONLY MATHEMATICALLY
            <br />
            REALISTIC EXIT →
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 52,
            fontWeight: 900,
            color: '#EEEEEE',
            letterSpacing: -1,
            lineHeight: 1,
          }}>
            INFLATE
            <br />
            IT AWAY.
          </div>
          <div style={{
            marginTop: 14,
            fontFamily: 'Inter, sans-serif',
            fontSize: 18,
            color: '#EEEEEE',
            opacity: 0.7,
          }}>
            (slowly · quietly · over decades)
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default FederalDebtTooHeavy;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'WallStreetHeardIt',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const WallStreetHeardIt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt right
  const camRot = interpolate(frame, [0, durationInFrames], [0.3, -0.3]);

  const subtextIn = sp(5);
  const fourWordsIn = sp(20);
  const tickerIn = sp(50);
  const tradersIn = sp(80);

  // Yield chart wiggling line
  const chartProgress = interpolate(frame, [60, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(120deg, rgba(238,238,238,1) 0%, rgba(6,32,43,0.08) 70%, rgba(233,215,0,0.06) 100%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '50%',
          left: '50%',
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '3px solid #06202B',
          opacity: 0.05,
          transform: `translate(-50%, -50%) rotate(${frame * 0.2}deg)`,
        }} />
        <div style={{
          position: 'absolute',
          top: '15%',
          right: '20%',
          width: 260,
          height: 260,
          background: '#06202B',
          opacity: 0.04,
          transform: `rotate(${45 + frame * 0.3}deg)`,
        }} />
      </AbsoluteFill>

      {/* Ticker tape texture lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(0deg, transparent, transparent 100px, rgba(6,32,43,0.04) 100px, rgba(6,32,43,0.04) 101px)',
      }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 140);
        const x = (r * 1920 + frame * 0.7) % 1920;
        const y = seededRandom(i + 500) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 4,
            height: 4,
            background: i % 3 === 0 ? '#e9d700' : '#06202B',
            opacity: 0.22,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>

        {/* Top eyebrow */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 100,
          opacity: subtextIn,
          transform: `translateY(${(1 - subtextIn) * 20}px)`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 24,
          color: '#06202B',
          letterSpacing: 6,
          opacity: subtextIn * 0.6,
          fontWeight: 700,
        }}>
          THE UNSPOKEN SUBTEXT — DECODED
        </div>

        {/* Main: four words quote */}
        <div style={{
          position: 'absolute',
          top: 170,
          left: 100,
          width: 1100,
          opacity: fourWordsIn,
          transform: `translateY(${(1 - fourWordsIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 130,
            fontWeight: 900,
            color: '#06202B',
            lineHeight: 0.95,
            letterSpacing: -3,
          }}>
            EVERY BOND TRADER
          </div>
          <div style={{
            marginTop: 12,
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 130,
            fontWeight: 900,
            lineHeight: 0.95,
            letterSpacing: -3,
            color: '#06202B',
          }}>
            HEARD <span style={{
              position: 'relative',
              display: 'inline-block',
            }}>
              FOUR WORDS.
              <div style={{
                position: 'absolute',
                left: -6,
                right: -6,
                bottom: 16,
                height: 22,
                background: '#e9d700',
                opacity: 0.6,
                zIndex: -1,
                transform: `scaleX(${interpolate(frame, [35, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
                transformOrigin: 'left',
              }} />
            </span>
          </div>
        </div>

        {/* Ticker tape across middle */}
        <div style={{
          position: 'absolute',
          top: 530,
          left: 0,
          right: 0,
          height: 60,
          background: '#06202B',
          opacity: tickerIn,
          overflow: 'hidden',
          display: 'flex',
          alignItems: 'center',
        }}>
          <div style={{
            display: 'flex',
            gap: 60,
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#EEEEEE',
            whiteSpace: 'nowrap',
            transform: `translateX(${-(frame * 4) % 1920}px)`,
            fontWeight: 700,
          }}>
            {Array.from({ length: 3 }).map((_, k) => (
              <React.Fragment key={k}>
                <span>10Y YIELD <span style={{ color: '#ff0000' }}>▲ 4.62%</span></span>
                <span style={{ color: '#e9d700' }}>·</span>
                <span>30Y <span style={{ color: '#ff0000' }}>▲ 4.81%</span></span>
                <span style={{ color: '#e9d700' }}>·</span>
                <span>GOLD <span style={{ color: '#e9d700' }}>▲ $2,840</span></span>
                <span style={{ color: '#e9d700' }}>·</span>
                <span>DXY <span style={{ color: '#ff0000' }}>▼ 102.4</span></span>
                <span style={{ color: '#e9d700' }}>·</span>
                <span>TIPS BREAKEVEN <span style={{ color: '#ff0000' }}>▲ 2.7%</span></span>
                <span style={{ color: '#e9d700' }}>·</span>
              </React.Fragment>
            ))}
          </div>
        </div>

        {/* Bond yield chart bottom-left */}
        <div style={{
          position: 'absolute',
          bottom: 100,
          left: 100,
          width: 600,
          height: 220,
          opacity: tradersIn,
          transform: `translateY(${(1 - tradersIn) * 20}px)`,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 20,
            color: '#06202B',
            letterSpacing: 4,
            marginBottom: 8,
            opacity: 0.65,
          }}>
            10-YEAR TREASURY · REACTION
          </div>
          <svg width="600" height="180" viewBox="0 0 600 180">
            {/* Axis */}
            <line x1="0" y1="160" x2="600" y2="160" stroke="#06202B" strokeWidth="2" opacity="0.3" />
            <line x1="0" y1="0" x2="0" y2="160" stroke="#06202B" strokeWidth="2" opacity="0.3" />
            {/* Wavy line going up */}
            <path
              d={`M 0 130 ${Array.from({ length: 30 }).map((_, i) => {
                const x = (i / 29) * 600 * chartProgress;
                const baseY = 130 - (i / 29) * 80;
                const wiggle = Math.sin(i * 0.8 + frame * 0.1) * 6;
                return `L ${x} ${baseY + wiggle}`;
              }).join(' ')}`}
              fill="none"
              stroke="#ff0000"
              strokeWidth="4"
              strokeLinecap="round"
            />
            {/* End marker */}
            <circle cx={600 * chartProgress} cy={50} r="8" fill="#ff0000" opacity={chartProgress} />
            {/* Spike annotation */}
            <text x={600 * chartProgress - 110} y={35} fontFamily="JetBrains Mono" fontSize="18" fontWeight="700" fill="#ff0000" opacity={chartProgress}>
              YIELDS SPIKE
            </text>
          </svg>
        </div>

        {/* Right card: trader quote */}
        <div style={{
          position: 'absolute',
          bottom: 100,
          right: 100,
          opacity: tradersIn,
          transform: `translateY(${(1 - tradersIn) * 20}px)`,
          background: 'rgba(6,32,43,0.06)',
          border: '3px solid #06202B',
          padding: '24px 30px',
          maxWidth: 500,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 18,
            color: '#06202B',
            opacity: 0.6,
            letterSpacing: 4,
            marginBottom: 10,
          }}>
            WALL STREET TRANSLATION
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 38,
            fontWeight: 900,
            color: '#06202B',
            letterSpacing: -1,
            lineHeight: 1.05,
          }}>
            "He just said
            <br />
            <span style={{ color: '#ff0000' }}>he'll inflate</span>
            <br />
            our holdings away."
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default WallStreetHeardIt;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'NotBuyNotSell',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const NotBuyNotSell: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.04]);

  const labelIn = sp(5);
  const clearIn = sp(15);
  const buyIn = sp(40);
  const sellIn = sp(55);
  const buyStrike = interpolate(frame, [70, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const sellStrike = interpolate(frame, [85, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const missionIn = sp(125);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, rgba(233,215,0,0.08) 0%, rgba(238,238,238,0) 60%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '15%',
          left: '12%',
          width: 350,
          height: 350,
          borderRadius: '50%',
          background: '#06202B',
          opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.07})`,
        }} />
        <div style={{
          position: 'absolute',
          bottom: '12%',
          right: '12%',
          width: 280,
          height: 280,
          border: '3px solid #06202B',
          opacity: 0.08,
          transform: `rotate(${frame * 0.25}deg)`,
        }} />
        <div style={{
          position: 'absolute',
          top: '50%',
          right: '5%',
          width: 0,
          height: 0,
          borderLeft: '100px solid transparent',
          borderRight: '100px solid transparent',
          borderBottom: '180px solid #06202B',
          opacity: 0.05,
          transform: `rotate(${-15 + frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Dot grid */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle, rgba(6,32,43,0.1) 1.5px, transparent 1.5px)',
        backgroundSize: '70px 70px',
        opacity: 0.5,
      }} />

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 160);
        const x = r * 1920;
        const y = (r * 1080 - frame * 0.4 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 5,
            height: 5,
            background: '#e9d700',
            opacity: 0.25,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Top label */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - labelIn) * 20}px)`,
          opacity: labelIn,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 24,
          color: '#06202B',
          letterSpacing: 8,
          opacity: labelIn * 0.65,
          fontWeight: 700,
        }}>
          A NOTE FROM THE STUDIO
        </div>

        {/* Headline */}
        <div style={{
          position: 'absolute',
          top: 170,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - clearIn) * 30}px)`,
          opacity: clearIn,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 112,
            fontWeight: 900,
            color: '#06202B',
            letterSpacing: -2,
            lineHeight: 1,
          }}>
            FAILED FINANCE
            <br />
            <span style={{ position: 'relative', display: 'inline-block' }}>
              IS NOT HERE TO TELL YOU…
              <div style={{
                position: 'absolute',
                left: 0,
                right: 0,
                bottom: 8,
                height: 16,
                background: '#e9d700',
                opacity: 0.55,
                zIndex: -1,
                transform: `scaleX(${interpolate(frame, [25, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
                transformOrigin: 'left',
              }} />
            </span>
          </div>
        </div>

        {/* Two crossed-out cards: BUY and SELL */}
        <div style={{
          position: 'absolute',
          top: 480,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 60,
          alignItems: 'center',
        }}>
          {/* BUY card */}
          <div style={{
            opacity: buyIn,
            transform: `translateY(${(1 - buyIn) * 30}px) rotate(-3deg) scale(${buyIn})`,
            background: 'rgba(238,238,238,0.6)',
            border: '5px solid #06202B',
            padding: '36px 70px',
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 18,
              color: '#06202B',
              opacity: 0.55,
              letterSpacing: 4,
              marginBottom: 8,
            }}>
              WHAT TO
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 130,
              fontWeight: 900,
              color: '#06202B',
              letterSpacing: -3,
              lineHeight: 1,
            }}>
              BUY
            </div>
            {/* Strike through X */}
            <svg
              style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', pointerEvents: 'none' }}
              viewBox="0 0 300 220"
              preserveAspectRatio="none"
            >
              <line x1="20" y1="20" x2={20 + 260 * buyStrike} y2={20 + 180 * buyStrike} stroke="#ff0000" strokeWidth="10" strokeLinecap="round" />
              <line x1="280" y1="20" x2={280 - 260 * buyStrike} y2={20 + 180 * buyStrike} stroke="#ff0000" strokeWidth="10" strokeLinecap="round" />
            </svg>
          </div>

          {/* OR */}
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 64,
            fontWeight: 900,
            color: '#06202B',
            opacity: buyIn * 0.6,
          }}>
            OR
          </div>

          {/* SELL card */}
          <div style={{
            opacity: sellIn,
            transform: `translateY(${(1 - sellIn) * 30}px) rotate(3deg) scale(${sellIn})`,
            background: 'rgba(238,238,238,0.6)',
            border: '5px solid #06202B',
            padding: '36px 70px',
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 18,
              color: '#06202B',
              opacity: 0.55,
              letterSpacing: 4,
              marginBottom: 8,
            }}>
              WHAT TO
            </div>
            <div style={{
              fontFamily: 'Mont Heavy, Impact, sans-serif',
              fontSize: 130,
              fontWeight: 900,
              color: '#06202B',
              letterSpacing: -3,
              lineHeight: 1,
            }}>
              SELL
            </div>
            <svg
              style={{ position: 'absolute', inset: 0, width: '100%', height: '100%', pointerEvents: 'none' }}
              viewBox="0 0 300 220"
              preserveAspectRatio="none"
            >
              <line x1="20" y1="20" x2={20 + 260 * sellStrike} y2={20 + 180 * sellStrike} stroke="#ff0000" strokeWidth="10" strokeLinecap="round" />
              <line x1="280" y1="20" x2={280 - 260 * sellStrike} y2={20 + 180 * sellStrike} stroke="#ff0000" strokeWidth="10" strokeLinecap="round" />
            </svg>
          </div>
        </div>

        {/* Mission statement bottom */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - missionIn) * 20}px)`,
          opacity: missionIn,
          background: '#06202B',
          padding: '20px 50px',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 42,
            fontWeight: 900,
            color: '#EEEEEE',
            letterSpacing: -0.5,
          }}>
            WE'RE HERE TO HELP YOU <span style={{ color: '#e9d700' }}>SEE THE BOARD.</span>
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default NotBuyNotSell;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SeeTheBoardChess',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SeeTheBoardChess: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera orbit (slow rotation + scale)
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.05]);
  const camRot = Math.sin(frame * 0.015) * 0.8;

  const labelIn = sp(5);
  const titleIn = sp(15);
  const boardIn = sp(40);
  const piecesIn = (i: number) => sp(60 + i * 6);
  const disclaimerIn = sp(140);

  // Chess piece move animation
  const moveProgress = interpolate(frame, [110, 145], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 50%, rgba(6,32,43,0.1) 0%, rgba(238,238,238,0) 60%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute',
          top: '5%',
          right: '5%',
          width: 480,
          height: 480,
          borderRadius: '50%',
          border: '3px solid #06202B',
          opacity: 0.06,
          transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute',
          bottom: '10%',
          left: '8%',
          width: 300,
          height: 300,
          background: '#06202B',
          opacity: 0.04,
          transform: `rotate(${-frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Subtle chess pattern texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(45deg, rgba(6,32,43,0.04) 25%, transparent 25%, transparent 75%, rgba(6,32,43,0.04) 75%), linear-gradient(45deg, rgba(6,32,43,0.04) 25%, transparent 25%, transparent 75%, rgba(6,32,43,0.04) 75%)',
        backgroundSize: '60px 60px',
        backgroundPosition: '0 0, 30px 30px',
        opacity: 0.5,
      }} />

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 180);
        const x = r * 1920;
        const y = (r * 1080 + frame * 0.6) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 4,
            height: 4,
            background: i % 2 === 0 ? '#e9d700' : '#06202B',
            opacity: 0.22,
            borderRadius: '50%',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        {/* Top eyebrow */}
        <div style={{
          position: 'absolute',
          top: 90,
          left: 100,
          opacity: labelIn,
          transform: `translateY(${(1 - labelIn) * 20}px)`,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 24,
          color: '#06202B',
          letterSpacing: 6,
          opacity: labelIn * 0.6,
          fontWeight: 700,
        }}>
          THIS CHANNEL'S MISSION →
        </div>

        {/* Main headline left side */}
        <div style={{
          position: 'absolute',
          top: 180,
          left: 100,
          width: 900,
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Impact, sans-serif',
            fontSize: 116,
            fontWeight: 900,
            color: '#06202B',
            lineHeight: 0.95,
            letterSpacing: -3,
          }}>
            SEE THE
            <br />
            <span style={{ position: 'relative', display: 'inline-block' }}>
              BOARD
              <div style={{
                position: 'absolute',
                left: -8,
                right: -8,
                bottom: 14,
                height: 22,
                background: '#e9d700',
                opacity: 0.6,
                zIndex: -1,
                transform: `scaleX(${interpolate(frame, [25, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
                transformOrigin: 'left',
              }} />
            </span>
          </div>
          <div style={{
            marginTop: 24,
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 36,
            fontWeight: 500,
            color: '#06202B',
            opacity: 0.78,
            lineHeight: 1.3,
            maxWidth: 720,
          }}>
            the way the people moving the pieces
            <br />
            already see it.
          </div>
        </div>

        {/* Right side: Chess board */}
        <div style={{
          position: 'absolute',
          top: 200,
          right: 130,
          opacity: boardIn,
          transform: `translateY(${(1 - boardIn) * 30}px) scale(${boardIn})`,
        }}>
          <svg width="520" height="520" viewBox="0 0 520 520">
            {/* Board */}
            {Array.from({ length: 8 }).map((_, row) =>
              Array.from({ length: 8 }).map((_, col) => {
                const isDark = (row + col) % 2 === 1;
                return (
                  <rect
                    key={`${row}-${col}`}
                    x={col * 60 + 20}
                    y={row * 60 + 20}
                    width="60"
                    height="60"
                    fill={isDark ? '#06202B' : '#EEEEEE'}
                    stroke="#06202B"
                    strokeWidth="1"
                    opacity={isDark ? 0.85 : 1}
                  />
                );
              })
            )}
            {/* Board border */}
            <rect x="20" y="20" width="480" height="480" fill="none" stroke="#06202B" strokeWidth="6" />

            {/* Highlighted move squares */}
            <rect x={20 + 4 * 60} y={20 + 6 * 60} width="60" height="60" fill="#e9d700" opacity={piecesIn(0) * 0.4} />
            <rect
              x={20 + 4 * 60}
              y={20 + (6 - 4 * moveProgress) * 60}
              width="60"
              height="60"
              fill="#e9d700"
              opacity={moveProgress * 0.5}
            />

            {/* King (top center) */}
            <g opacity={piecesIn(1)} transform={`translate(${20 + 3.5 * 60 + 30}, ${20 + 30})`}>
              <circle r="20" fill="#e9d700" />
              <text textAnchor="middle" y="8" fontFamily="Mont Heavy, Impact" fontSize="28" fontWeight="900" fill="#06202B">♚</text>
            </g>

            {/* Queen */}
            <g opacity={piecesIn(2)} transform={`translate(${20 + 4.5 * 60}, ${20 + 1.5 * 60})`}>
              <circle r="18" fill="#06202B" />
              <text textAnchor="middle" y="7" fontFamily="Mont Heavy, Impact" fontSize="24" fontWeight="900" fill="#e9d700">♛</text>
            </g>

            {/* Knight (player piece — animates) */}
            <g
              opacity={piecesIn(3)}
              transform={`translate(${20 + 4.5 * 60}, ${20 + (6.5 - 4 * moveProgress) * 60})`}
            >
              <circle r="22" fill="#06202B" stroke="#e9d700" strokeWidth="3" />
              <text textAnchor="middle" y="8" fontFamily="Mont Heavy, Impact" fontSize="26" fontWeight="900" fill="#EEEEEE">♞</text>
            </g>

            {/* Pawns */}
            {[0, 2, 5, 7].map((c, i) => (
              <g key={i} opacity={piecesIn(4 + i)} transform={`translate(${20 + c * 60 + 30}, ${20 + 5 * 60 + 30})`}>
                <circle r="14" fill="#06202B" opacity="0.85" />
                <text textAnchor="middle" y="6" fontFamily="Mont Heavy, Impact" fontSize="18" fontWeight="900" fill="#EEEEEE">♟</text>
              </g>
            ))}

            {/* Move arrow */}
            <line
              x1={20 + 4.5 * 60}
              y1={20 + 6.5 * 60}
              x2={20 + 4.5 * 60}
              y2={20 + (6.5 - 4 * moveProgress) * 60 + 30}
              stroke="#e9d700"
              strokeWidth="4"
              strokeDasharray="8 6"
              opacity={moveProgress}
            />

            {/* Board label */}
            <text x="270" y="510" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="14" fill="#06202B" letterSpacing="3" opacity="0.6">
              THE PIECES ARE ALREADY MOVING
            </text>
          </svg>
        </div>

        {/* Bottom disclaimer strip */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - disclaimerIn) * 20}px)`,
          opacity: disclaimerIn,
          background: '#06202B',
          padding: '14px 36px',
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <div style={{
            width: 12,
            height: 12,
            background: '#ff0000',
            borderRadius: '50%',
            boxShadow: `0 0 ${8 + Math.sin(frame * 0.25) * 6}px #ff0000`,
          }} />
          <div style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#EEEEEE',
            fontWeight: 700,
            letterSpacing: 3,
          }}>
            DISCLAIMER · NOT PERSONALIZED FINANCIAL ADVICE · CONSULT A LICENSED ADVISOR
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default SeeTheBoardChess;
```

