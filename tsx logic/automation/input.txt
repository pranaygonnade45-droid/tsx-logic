```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01FeuRouge',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01FeuRouge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom-in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  const redPulse = 0.55 + Math.sin(frame * 0.15) * 0.45;

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 20%, #D8FFB8 0%, #C3FF99 45%, #A8E87D 100%)' }} />

      {/* Animated shape layers — drifting transparent forms */}
      <div style={{ position: 'absolute', width: 620, height: 620, borderRadius: '50%', border: '3px solid #000000',
        opacity: 0.06, left: -160 + Math.sin(frame * 0.008) * 30, top: -180 + Math.cos(frame * 0.006) * 20,
        transform: `scale(${1 + Math.sin(frame * 0.01) * 0.04})` }} />
      <div style={{ position: 'absolute', width: 420, height: 420, backgroundColor: '#000000', opacity: 0.04,
        right: -120, bottom: -140, transform: `rotate(${frame * 0.05}deg)` }} />

      {/* Motion texture — punk collage diagonal stripes */}
      <AbsoluteFill style={{ opacity: 0.05,
        backgroundImage: 'repeating-linear-gradient(-35deg, #000000 0px, #000000 3px, transparent 3px, transparent 46px)',
        backgroundPosition: `${frame * 0.4}px 0px` }} />

      {/* Punk collage torn strips */}
      <div style={{ position: 'absolute', top: 70, left: -60, width: 560, height: 44, backgroundColor: '#000000',
        transform: 'rotate(-4deg)', opacity: 0.9 }} />
      <div style={{ position: 'absolute', bottom: 120, right: -50, width: 480, height: 38, backgroundColor: '#F4F2F2',
        transform: 'rotate(3deg)', border: '3px solid #000000' }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 1) * 1920;
        const drift = ((frame * (0.3 + seededRandom(i + 40) * 0.6)) % 1180) - 50;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift,
            width: 5 + seededRandom(i + 9) * 6, height: 5 + seededRandom(i + 9) * 6,
            backgroundColor: i % 3 === 0 ? '#000000' : '#F4F2F2', opacity: 0.28,
            transform: `rotate(${frame * 2 + i * 30}deg)` }} />
        );
      })}

      {/* Main content with camera movement */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Traffic light SVG — red pulsing */}
        <div style={{ position: 'absolute', left: 130, top: 250, transform: `translateY(${(1 - sp(5)) * -300}px) rotate(-3deg)` }}>
          <svg width="150" height="400" viewBox="0 0 150 400">
            <rect x="20" y="10" width="110" height="330" rx="18" fill="#000000" />
            <circle cx="75" cy="80" r="38" fill="#ff0000" opacity={redPulse} />
            <circle cx="75" cy="175" r="38" fill="#2D2D2D" />
            <circle cx="75" cy="270" r="38" fill="#2D2D2D" />
            <rect x="62" y="340" width="26" height="60" fill="#000000" />
          </svg>
        </div>

        {/* Headline block */}
        <div style={{ position: 'absolute', top: 130, left: 380, transform: `translateX(${(1 - sp(15)) * 600}px)`, opacity: sp(15) }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 110, color: '#000000', lineHeight: 0.95, letterSpacing: 2 }}>
            UN FEU ROUGE.
          </div>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 38, color: '#000000', marginTop: 14,
            backgroundColor: '#F4F2F2', display: 'inline-block', padding: '6px 20px', border: '3px solid #000000',
            boxShadow: '8px 8px 0 #000000', transform: 'rotate(-1.5deg)' }}>
            BOULEVARD HAUSSMANN — MERCREDI, 9H
          </div>
        </div>

        {/* Card gauche — SUV */}
        <div style={{ position: 'absolute', left: 380, top: 430, width: 620, backgroundColor: '#000000',
          border: '4px solid #000000', boxShadow: '12px 12px 0 rgba(0,0,0,0.25)', padding: '34px 40px',
          transform: `rotate(-2deg) scale(${sp(45)})`, opacity: interpolate(frame, [45, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <svg width="120" height="52" viewBox="0 0 120 52">
            <path d="M8 38 L18 20 Q22 12 32 12 L78 12 Q88 12 96 22 L112 38 Z" fill="#F4F2F2" />
            <circle cx="32" cy="42" r="9" fill="#C3FF99" />
            <circle cx="90" cy="42" r="9" fill="#C3FF99" />
          </svg>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 56, color: '#F4F2F2', marginTop: 10 }}>SUV ALLEMAND</div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 30, fontWeight: 600, color: '#C3FF99' }}>
            Neuf. Jantes brillantes. Odeur du neuf.
          </div>
        </div>

        {/* Card droite — Clio */}
        <div style={{ position: 'absolute', left: 1080, top: 470, width: 580, backgroundColor: '#F4F2F2',
          border: '4px solid #000000', boxShadow: '12px 12px 0 #000000', padding: '34px 40px',
          transform: `rotate(2deg) scale(${sp(65)})`, opacity: interpolate(frame, [65, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <svg width="110" height="50" viewBox="0 0 110 50">
            <path d="M6 36 L16 20 Q20 12 30 12 L70 14 Q80 14 88 24 L102 36 Z" fill="#000000" />
            <circle cx="28" cy="40" r="8" fill="#C3FF99" stroke="#000000" strokeWidth="2" />
            <circle cx="82" cy="40" r="8" fill="#C3FF99" stroke="#000000" strokeWidth="2" />
            <path d="M88 24 L96 18" stroke="#000000" strokeWidth="3" />
          </svg>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 56, color: '#000000', marginTop: 10 }}>CLIO 2012</div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 30, fontWeight: 600, color: '#000000' }}>
            Cabossée. Gobelet sur le tableau de bord.
          </div>
        </div>

        {/* VS badge */}
        <div style={{ position: 'absolute', left: 985, top: 560, width: 110, height: 110, borderRadius: '50%',
          backgroundColor: '#C3FF99', border: '5px solid #000000', display: 'flex', justifyContent: 'center',
          alignItems: 'center', transform: `scale(${sp(90, { stiffness: 200 })}) rotate(${Math.sin(frame * 0.06) * 6}deg)`,
          fontFamily: 'Anton, sans-serif', fontSize: 46, color: '#000000', zIndex: 5 }}>
          VS
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene01FeuRouge;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02LaVieille',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02LaVieille: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Impact frame for the stamp
  const stampAt = 420;
  const shake = frame > stampAt && frame < stampAt + 20
    ? Math.sin((frame - stampAt) * 2.2) * (20 - (frame - stampAt)) * 0.9 : 0;

  // Camera pan left → right
  const camX = interpolate(frame, [0, durationInFrames], [30, -30], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #CFFFAB 0%, #C3FF99 50%, #9FDD75 100%)' }} />

      {/* Motion texture — halftone dots */}
      <AbsoluteFill style={{ opacity: 0.07,
        backgroundImage: 'radial-gradient(#000000 2.5px, transparent 2.5px)',
        backgroundSize: '38px 38px', backgroundPosition: `0px ${frame * 0.5}px` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 700, height: 700, border: '4px dashed #000000', borderRadius: '50%',
        opacity: 0.06, left: 1350, top: -220, transform: `rotate(${frame * 0.15}deg)` }} />
      <div style={{ position: 'absolute', width: 0, height: 0, borderLeft: '190px solid transparent',
        borderRight: '190px solid transparent', borderBottom: '330px solid #000000', opacity: 0.045,
        left: -100, bottom: -80, transform: `rotate(${Math.sin(frame * 0.01) * 8}deg)` }} />

      {/* Punk collage tape strips */}
      <div style={{ position: 'absolute', top: 150, right: 200, width: 220, height: 46, backgroundColor: '#F4F2F2',
        opacity: 0.85, transform: 'rotate(12deg)', border: '2px solid #000000' }} />
      <div style={{ position: 'absolute', bottom: 200, left: 160, width: 200, height: 42, backgroundColor: '#F4F2F2',
        opacity: 0.85, transform: 'rotate(-9deg)', border: '2px solid #000000' }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const x = seededRandom(i + 3) * 1920;
        const drift = ((frame * (0.4 + seededRandom(i + 21) * 0.5)) % 1160) - 40;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift, width: 8, height: 8,
            borderRadius: i % 2 === 0 ? '50%' : 0, backgroundColor: '#000000', opacity: 0.2,
            transform: `rotate(${frame * 3}deg)` }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px)`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Big question typography */}
        <div style={{ textAlign: 'center', transform: `translateY(${(1 - sp(10)) * -200}px)`, opacity: sp(10) }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 150, color: '#000000', lineHeight: 0.92 }}>
            LEQUEL EST
          </div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 190, color: '#F4F2F2', lineHeight: 0.95,
            WebkitTextStroke: '5px #000000', textShadow: '12px 12px 0 rgba(0,0,0,0.9)' }}>
            RICHE ?
          </div>
        </div>

        {/* Context strip */}
        <div style={{ marginTop: 40, fontFamily: 'Space Mono, monospace', fontSize: 36, color: '#F4F2F2',
          backgroundColor: '#000000', padding: '10px 34px', transform: `rotate(-1deg) scale(${sp(60)})` }}>
          FRANCE — 2026 — LE PARI STATISTIQUE
        </div>

        {/* Punk stamp answer */}
        <div style={{ position: 'absolute', top: 340, left: '50%',
          transform: `translateX(-50%) rotate(-9deg) scale(${interpolate(sp(stampAt, { stiffness: 300, damping: 14 }), [0, 1], [3.2, 1])})`,
          opacity: interpolate(frame, [stampAt, stampAt + 6], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          border: '8px solid #ff0000', padding: '18px 50px', backgroundColor: 'rgba(244,242,242,0.92)' }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 120, color: '#ff0000', letterSpacing: 4 }}>
            LA VIEILLE.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene02LaVieille;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03Fragiles',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03Fragiles: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow tilt + zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });
  const camRot = Math.sin(frame * 0.005) * 0.6;

  const debts = [
    { label: 'MENSUALITÉ QUI ÉTRANGLE', delay: 40, rot: -2.5 },
    { label: 'LOA — 48 MOIS', delay: 60, rot: 1.8 },
    { label: 'ASSURANCE À 4 CHIFFRES', delay: 80, rot: -1.4 },
    { label: 'DÉCOUVERT PERMANENT', delay: 100, rot: 2.2 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 75% 80%, #D6FFB5 0%, #C3FF99 40%, #A3E278 100%)' }} />

      {/* Motion texture — vertical scan lines */}
      <AbsoluteFill style={{ opacity: 0.045,
        backgroundImage: 'repeating-linear-gradient(90deg, #000000 0px, #000000 2px, transparent 2px, transparent 60px)',
        backgroundPosition: `${Math.sin(frame * 0.02) * 20}px 0px` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 520, height: 520, backgroundColor: '#F4F2F2', opacity: 0.25,
        borderRadius: '50%', right: -140, top: -160, transform: `scale(${1 + Math.sin(frame * 0.012) * 0.05})` }} />
      <div style={{ position: 'absolute', width: 300, height: 300, border: '4px solid #000000', opacity: 0.05,
        left: 100, bottom: 60, transform: `rotate(${45 + frame * 0.08}deg)` }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const x = seededRandom(i + 7) * 1920;
        const drift = ((frame * (0.35 + seededRandom(i + 30) * 0.55)) % 1170) - 45;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift, width: 6, height: 6,
            backgroundColor: i % 4 === 0 ? '#ff0000' : '#000000', opacity: 0.22 }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        {/* Left — headline */}
        <div style={{ position: 'absolute', left: 120, top: 160, width: 780, opacity: sp(8),
          transform: `translateY(${(1 - sp(8)) * 120}px)` }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 96, color: '#000000', lineHeight: 1.02 }}>
            L'AIR RICHE.
          </div>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 96, color: '#F4F2F2', lineHeight: 1.02,
            WebkitTextStroke: '4px #000000' }}>
            SOUVENT FRAGILE.
          </div>
          <div style={{ width: interpolate(sp(30), [0, 1], [0, 420]), height: 12, backgroundColor: '#ff0000',
            marginTop: 24 }} />

          {/* Signature card */}
          <div style={{ marginTop: 70, display: 'inline-block', backgroundColor: '#000000', padding: '22px 36px',
            transform: `rotate(-1.5deg) scale(${sp(150)})`, boxShadow: '10px 10px 0 rgba(0,0,0,0.25)' }}>
            <div style={{ fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 44, color: '#F4F2F2', fontWeight: 700 }}>
              Lucas — Matures Finance
            </div>
            <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 26, color: '#C3FF99', fontWeight: 600, marginTop: 6 }}>
              Luxe visible ≠ richesse réelle
            </div>
          </div>
        </div>

        {/* Right — falling debt cards (animated cards, staggered) */}
        <div style={{ position: 'absolute', right: 120, top: 190, width: 640 }}>
          {debts.map((d, i) => {
            const s = sp(d.delay, { stiffness: 160, damping: 13 });
            return (
              <div key={i} style={{ marginBottom: 26, backgroundColor: i % 2 === 0 ? '#000000' : '#F4F2F2',
                border: '4px solid #000000', boxShadow: '10px 10px 0 rgba(0,0,0,0.85)',
                padding: '24px 32px', display: 'flex', alignItems: 'center', gap: 22,
                transform: `rotate(${d.rot + Math.sin(frame * 0.03 + i) * 0.6}deg) translateY(${(1 - s) * -420}px)`,
                opacity: interpolate(frame, [d.delay, d.delay + 10], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
                {/* Warning icon */}
                <svg width="52" height="52" viewBox="0 0 52 52">
                  <polygon points="26,4 50,46 2,46" fill="#ff0000" stroke={i % 2 === 0 ? '#F4F2F2' : '#000000'} strokeWidth="3" />
                  <rect x="23.5" y="18" width="5" height="15" fill="#F4F2F2" />
                  <rect x="23.5" y="37" width="5" height="5" fill="#F4F2F2" />
                </svg>
                <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 40,
                  color: i % 2 === 0 ? '#F4F2F2' : '#000000' }}>
                  {d.label}
                </div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene03Fragiles;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04TauxEpargne',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04TauxEpargne: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big counter 0 → 17.4
  const counter = interpolate(frame, [30, 120], [0, 17.4], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // Camera slow orbit feel
  const camX = Math.sin(frame * 0.006) * 18;
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, #CFFFAE 0%, #C3FF99 55%, #ABE981 100%)' }} />

      {/* Motion texture — grid */}
      <AbsoluteFill style={{ opacity: 0.06,
        backgroundImage: 'linear-gradient(#000000 1.5px, transparent 1.5px), linear-gradient(90deg, #000000 1.5px, transparent 1.5px)',
        backgroundSize: '90px 90px', backgroundPosition: `${frame * 0.25}px ${frame * 0.15}px` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        border: '60px solid #000000', opacity: 0.035, left: -260, top: 180,
        transform: `scale(${1 + Math.sin(frame * 0.009) * 0.04}) rotate(${frame * 0.03}deg)` }} />

      {/* Punk collage corner blocks */}
      <div style={{ position: 'absolute', top: -20, right: 120, width: 300, height: 60, backgroundColor: '#000000',
        transform: 'rotate(2deg)' }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const x = seededRandom(i + 11) * 1920;
        const drift = ((frame * (0.3 + seededRandom(i + 50) * 0.5)) % 1150) - 40;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift, width: 7, height: 7,
            borderRadius: '50%', border: '2px solid #000000', opacity: 0.22 }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Label */}
        <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 40, color: '#F4F2F2', backgroundColor: '#000000',
          padding: '8px 30px', transform: `rotate(-1deg) translateY(${(1 - sp(10)) * -120}px)`, opacity: sp(10), marginBottom: 26 }}>
          TAUX D'ÉPARGNE DES MÉNAGES — 2026
        </div>

        {/* Giant animated number */}
        <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 300, color: '#000000', lineHeight: 0.95,
          transform: `scale(${sp(30, { stiffness: 90 })})`,
          textShadow: '14px 14px 0 #F4F2F2' }}>
          {counter.toFixed(1).replace('.', ',')}<span style={{ fontSize: 170 }}>%</span>
        </div>

        {/* Comparison row — animated cards */}
        <div style={{ display: 'flex', gap: 34, marginTop: 50 }}>
          <div style={{ backgroundColor: '#F4F2F2', border: '4px solid #000000', boxShadow: '9px 9px 0 #000000',
            padding: '20px 34px', transform: `rotate(-1.6deg) scale(${sp(120)})`, display: 'flex', alignItems: 'center', gap: 18 }}>
            {/* Down arrow icon */}
            <svg width="44" height="44" viewBox="0 0 44 44">
              <path d="M22 4 L22 30 M10 20 L22 34 L34 20" stroke="#ff0000" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 34, color: '#000000', fontWeight: 700 }}>
              18,9% en 2025 → baisse
            </div>
          </div>
          <div style={{ backgroundColor: '#000000', padding: '20px 34px', boxShadow: '9px 9px 0 rgba(0,0,0,0.3)',
            transform: `rotate(1.4deg) scale(${sp(145)})`, display: 'flex', alignItems: 'center', gap: 18 }}>
            {/* Coin icon */}
            <svg width="44" height="44" viewBox="0 0 44 44">
              <circle cx="22" cy="22" r="18" fill="#C3FF99" stroke="#F4F2F2" strokeWidth="3" />
              <text x="22" y="30" textAnchor="middle" fontFamily="Anton" fontSize="22" fill="#000000">€</text>
            </svg>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 34, color: '#F4F2F2', fontWeight: 700 }}>
              ≈ 240€ / mois de côté
            </div>
          </div>
        </div>

        {/* Source tag */}
        <div style={{ marginTop: 40, fontFamily: 'Montserrat, sans-serif', fontSize: 26, fontWeight: 600,
          color: '#000000', opacity: interpolate(frame, [170, 190], [0, 0.75], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          Source : INSEE
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene04TauxEpargne;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05EcartAges',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05EcartAges: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bar growth (multi-beat: labels → bars → punchline)
  const barA = interpolate(frame, [70, 150], [0, 560], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const barB = interpolate(frame, [90, 170], [0, 222], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const valA = Math.round(interpolate(frame, [70, 150], [0, 491], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const valB = Math.round(interpolate(frame, [90, 170], [0, 195], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 20% 85%, #D9FFBC 0%, #C3FF99 45%, #A5E47B 100%)' }} />

      {/* Motion texture — cross marks */}
      {Array.from({ length: 8 }).map((_, i) => (
        <div key={i} style={{ position: 'absolute', left: seededRandom(i + 60) * 1800, top: seededRandom(i + 70) * 1000,
          fontFamily: 'Space Mono, monospace', fontSize: 34, color: '#000000', opacity: 0.06,
          transform: `rotate(${frame * 0.3 + i * 45}deg)` }}>+</div>
      ))}

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 480, height: 480, backgroundColor: '#000000', opacity: 0.04,
        borderRadius: '50%', right: -100, bottom: -160, transform: `scale(${1 + Math.sin(frame * 0.011) * 0.06})` }} />
      <div style={{ position: 'absolute', width: 340, height: 340, border: '3px solid #000000', opacity: 0.06,
        left: -80, top: -90, transform: `rotate(${frame * 0.1}deg)` }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const x = seededRandom(i + 17) * 1920;
        const drift = ((frame * (0.35 + seededRandom(i + 80) * 0.5)) % 1160) - 40;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift, width: 6, height: 6,
            backgroundColor: '#000000', opacity: 0.2, transform: `rotate(45deg)` }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Headline */}
        <div style={{ position: 'absolute', top: 100, width: '100%', textAlign: 'center',
          transform: `translateY(${(1 - sp(8)) * -140}px)`, opacity: sp(8) }}>
          <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 92, color: '#000000' }}>QUI ÉPARGNE </span>
          <span style={{ fontFamily: 'Anton, sans-serif', fontSize: 92, color: '#F4F2F2', WebkitTextStroke: '4px #000000' }}>VRAIMENT ?</span>
        </div>

        {/* Chart zone — two typographic bar cards */}
        <div style={{ position: 'absolute', bottom: 180, left: 0, width: '100%', display: 'flex',
          justifyContent: 'center', alignItems: 'flex-end', gap: 170 }}>

          {/* Bar 50-59 */}
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 60, fontWeight: 700, color: '#000000', marginBottom: 14 }}>
              {valA}€
            </div>
            <div style={{ width: 250, height: barA, backgroundColor: '#000000', border: '4px solid #000000',
              boxShadow: '12px 0 0 rgba(0,0,0,0.2)' }} />
            <div style={{ marginTop: 18, fontFamily: 'Anton, sans-serif', fontSize: 44, color: '#F4F2F2',
              backgroundColor: '#000000', padding: '6px 22px', display: 'inline-block', transform: 'rotate(-1.5deg)' }}>
              50-59 ANS
            </div>
          </div>

          {/* Bar 30-39 */}
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 60, fontWeight: 700, color: '#000000', marginBottom: 14 }}>
              {valB}€
            </div>
            <div style={{ width: 250, height: barB, backgroundColor: '#F4F2F2', border: '4px solid #000000',
              boxShadow: '12px 0 0 rgba(0,0,0,0.2)' }} />
            <div style={{ marginTop: 18, fontFamily: 'Anton, sans-serif', fontSize: 44, color: '#000000',
              backgroundColor: '#F4F2F2', border: '3px solid #000000', padding: '6px 22px', display: 'inline-block',
              transform: 'rotate(1.5deg)' }}>
              30-39 ANS
            </div>
          </div>
        </div>

        {/* Punchline sticker */}
        <div style={{ position: 'absolute', right: 170, top: 330, backgroundColor: '#ff0000', padding: '20px 34px',
          transform: `rotate(6deg) scale(${sp(200, { stiffness: 220 })})`, boxShadow: '10px 10px 0 #000000' }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 48, color: '#F4F2F2' }}>
            ÉCART × 2,5
          </div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 26, fontWeight: 600, color: '#F4F2F2' }}>
            à peine une panne de chaudière
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene05EcartAges;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06Mediane',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PersonIcon: React.FC<{ filled: boolean; size: number; stroke: string; fill: string }> = ({ filled, size, stroke, fill }) => (
  <svg width={size} height={size * 1.4} viewBox="0 0 60 84">
    <circle cx="30" cy="16" r="13" fill={filled ? fill : 'none'} stroke={stroke} strokeWidth="4" />
    <path d="M10 80 L10 52 Q10 36 30 36 Q50 36 50 52 L50 80 Z" fill={filled ? fill : 'none'} stroke={stroke} strokeWidth="4" />
  </svg>
);

const Scene06Mediane: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camY = interpolate(frame, [0, durationInFrames], [15, -15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, #D2FFB0 0%, #C3FF99 50%, #A0E076 100%)' }} />

      {/* Motion texture — concentric circles */}
      <div style={{ position: 'absolute', left: '50%', top: '50%', transform: 'translate(-50%, -50%)' }}>
        {[300, 520, 740, 960].map((d, i) => (
          <div key={i} style={{ position: 'absolute', width: d, height: d, left: -d / 2, top: -d / 2,
            borderRadius: '50%', border: '2px solid #000000', opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.03})` }} />
        ))}
      </div>

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 380, height: 380, backgroundColor: '#F4F2F2', opacity: 0.3,
        left: -110, top: 300, borderRadius: '50%', transform: `translateY(${Math.sin(frame * 0.015) * 20}px)` }} />

      {/* Punk collage strip */}
      <div style={{ position: 'absolute', top: 40, left: -40, width: 700, height: 50, backgroundColor: '#000000',
        transform: 'rotate(-2deg)' }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const x = seededRandom(i + 23) * 1920;
        const drift = ((frame * (0.3 + seededRandom(i + 90) * 0.6)) % 1150) - 40;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift, width: 7, height: 7,
            borderRadius: '50%', backgroundColor: '#000000', opacity: 0.18 }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px)`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Big number card */}
        <div style={{ backgroundColor: '#000000', padding: '28px 60px', transform: `rotate(-1.5deg) scale(${sp(10)})`,
          boxShadow: '14px 14px 0 rgba(0,0,0,0.25)', marginBottom: 44 }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 30, fontWeight: 700, color: '#C3FF99', letterSpacing: 3 }}>
            PATRIMOINE MÉDIAN — FRANCE
          </div>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 130, fontWeight: 700, color: '#F4F2F2', lineHeight: 1.05 }}>
            205 000€
          </div>
        </div>

        {/* 10 person icons — half fill with stagger (1 sur 2) */}
        <div style={{ display: 'flex', gap: 26 }}>
          {Array.from({ length: 10 }).map((_, i) => {
            const s = sp(70 + i * 8, { stiffness: 180 });
            const filled = i < 5;
            return (
              <div key={i} style={{ transform: `translateY(${(1 - s) * 160}px) rotate(${(seededRandom(i + 5) - 0.5) * 8}deg)`,
                opacity: interpolate(frame, [70 + i * 8, 80 + i * 8], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
                <PersonIcon filled={filled} size={62} stroke="#000000" fill="#000000" />
              </div>
            );
          })}
        </div>

        {/* Median cut line */}
        <div style={{ position: 'absolute', top: 490, left: '50%', width: 6,
          height: interpolate(sp(160), [0, 1], [0, 300]), backgroundColor: '#ff0000',
          transform: 'translateX(-50%) rotate(3deg)' }} />

        {/* Punchline */}
        <div style={{ marginTop: 56, fontFamily: 'Anton, sans-serif', fontSize: 74, color: '#000000', textAlign: 'center',
          transform: `scale(${sp(190)})` }}>
          1 FRANÇAIS SUR 2 <span style={{ color: '#F4F2F2', WebkitTextStroke: '3px #000000' }}>POSSÈDE MOINS.</span>
        </div>

        <div style={{ marginTop: 22, fontFamily: 'Montserrat, sans-serif', fontSize: 28, fontWeight: 600, color: '#000000',
          opacity: interpolate(frame, [220, 240], [0, 0.8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          La médiane, pas la moyenne — le vrai visage du pays. (INSEE)
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene06Mediane;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07Credit84',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07Credit84: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Price ticker 60 000 → 90 000
  const price = Math.round(interpolate(frame, [40, 130], [60000, 90000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  }) / 1000) * 1000;

  // Timeline fill 0 → 84 months
  const months = Math.round(interpolate(frame, [180, 300], [0, 84], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const timelineW = interpolate(frame, [180, 300], [0, 1400], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.quad),
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 85% 15%, #DAFFBE 0%, #C3FF99 50%, #9EDC74 100%)' }} />

      {/* Motion texture — horizontal speed lines */}
      <AbsoluteFill style={{ opacity: 0.05,
        backgroundImage: 'repeating-linear-gradient(0deg, #000000 0px, #000000 2px, transparent 2px, transparent 70px)',
        backgroundPosition: `0px ${frame * -0.3}px` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 560, height: 560, border: '5px solid #000000', borderRadius: '50%',
        opacity: 0.05, right: -180, top: -170, transform: `rotate(${frame * 0.07}deg)` }} />
      <div style={{ position: 'absolute', width: 260, height: 260, backgroundColor: '#000000', opacity: 0.045,
        left: 60, bottom: -70, transform: `rotate(${20 + Math.sin(frame * 0.012) * 10}deg)` }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const y = seededRandom(i + 31) * 1080;
        const driftX = ((frame * (0.8 + seededRandom(i + 100) * 1.2)) % 2040) - 60;
        return (
          <div key={i} style={{ position: 'absolute', left: driftX, top: y, width: 26, height: 4,
            backgroundColor: '#000000', opacity: 0.15 }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Headline */}
        <div style={{ position: 'absolute', top: 110, left: 130, transform: `translateX(${(1 - sp(8)) * -500}px)`, opacity: sp(8) }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 100, color: '#000000', lineHeight: 1 }}>
            SUV PREMIUM :
          </div>
        </div>

        {/* Price ticker card */}
        <div style={{ position: 'absolute', top: 240, left: 130, backgroundColor: '#000000', padding: '24px 50px',
          transform: `rotate(-1.8deg) scale(${sp(35)})`, boxShadow: '12px 12px 0 rgba(0,0,0,0.25)' }}>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 120, fontWeight: 700, color: '#F4F2F2' }}>
            {price.toLocaleString('fr-FR')}€
          </div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 28, fontWeight: 600, color: '#C3FF99' }}>
            neuf, avant options — de 60 000 à 90 000€
          </div>
        </div>

        {/* Calendar icon sticker */}
        <div style={{ position: 'absolute', right: 200, top: 190, transform: `rotate(5deg) scale(${sp(100, { stiffness: 200 })})` }}>
          <svg width="180" height="180" viewBox="0 0 100 100">
            <rect x="8" y="16" width="84" height="76" rx="6" fill="#F4F2F2" stroke="#000000" strokeWidth="5" />
            <rect x="8" y="16" width="84" height="20" fill="#ff0000" stroke="#000000" strokeWidth="5" />
            <rect x="24" y="4" width="8" height="20" rx="3" fill="#000000" />
            <rect x="68" y="4" width="8" height="20" rx="3" fill="#000000" />
            <text x="50" y="76" textAnchor="middle" fontFamily="Anton" fontSize="34" fill="#000000">84</text>
          </svg>
        </div>

        {/* Timeline 84 mois — drawing ruler */}
        <div style={{ position: 'absolute', top: 620, left: 130, width: 1400 }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 66, color: '#000000', marginBottom: 20,
            opacity: interpolate(frame, [165, 185], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
            CRÉDIT ÉTALÉ SUR <span style={{ color: '#ff0000' }}>{months} MOIS</span>
          </div>
          <div style={{ position: 'relative', height: 70, border: '4px solid #000000', backgroundColor: '#F4F2F2',
            boxShadow: '10px 10px 0 #000000' }}>
            <div style={{ position: 'absolute', left: 0, top: 0, height: '100%', width: timelineW, maxWidth: '100%',
              backgroundColor: '#000000' }} />
            {/* Year ticks */}
            {Array.from({ length: 7 }).map((_, i) => (
              <div key={i} style={{ position: 'absolute', left: `${((i + 1) / 7) * 100}%`, top: 0, height: '100%',
                width: 3, backgroundColor: '#ff0000', opacity: 0.85 }} />
            ))}
          </div>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginTop: 12,
            fontFamily: 'Space Mono, monospace', fontSize: 26, color: '#000000', fontWeight: 700 }}>
            <span>AN 1</span><span>AN 2</span><span>AN 3</span><span>AN 4</span><span>AN 5</span><span>AN 6</span><span>AN 7</span>
          </div>
        </div>

        {/* Bottom note */}
        <div style={{ position: 'absolute', bottom: 90, left: 130, fontFamily: 'Bricolage Grotesque, sans-serif',
          fontSize: 34, fontWeight: 700, color: '#000000',
          opacity: interpolate(frame, [320, 345], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          Prix moyen d'une voiture neuve en France : ≈ 38 000€ (2025)
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene07Credit84;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08Decote',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08Decote: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // SVG curve draw (value falling)
  const pathLen = 1600;
  const dash = interpolate(frame, [80, 260], [pathLen, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  // Car icon position along the fall
  const carProg = interpolate(frame, [80, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const carX = 180 + carProg * 1100;
  const carY = 240 + Math.pow(carProg, 1.6) * 380;

  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #D4FFB2 0%, #C3FF99 45%, #A2E077 100%)' }} />

      {/* Motion texture — halftone corner */}
      <div style={{ position: 'absolute', right: 0, top: 0, width: 700, height: 700, opacity: 0.07,
        backgroundImage: 'radial-gradient(#000000 3px, transparent 3px)', backgroundSize: '30px 30px',
        maskImage: 'radial-gradient(circle at 100% 0%, black 0%, transparent 70%)',
        WebkitMaskImage: 'radial-gradient(circle at 100% 0%, black 0%, transparent 70%)',
        backgroundPosition: `${frame * 0.2}px 0px` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 460, height: 460, borderRadius: '50%', backgroundColor: '#F4F2F2',
        opacity: 0.28, left: -140, bottom: -160, transform: `scale(${1 + Math.sin(frame * 0.01) * 0.05})` }} />
      <div style={{ position: 'absolute', width: 240, height: 240, border: '4px solid #000000', opacity: 0.06,
        right: 140, bottom: 120, transform: `rotate(${frame * 0.12}deg)` }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const x = seededRandom(i + 41) * 1920;
        const drift = ((frame * (0.3 + seededRandom(i + 110) * 0.55)) % 1150) - 40;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift, width: 6, height: 6,
            backgroundColor: '#000000', opacity: 0.18, borderRadius: '50%' }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Headline sticker */}
        <div style={{ position: 'absolute', top: 90, left: 140, transform: `rotate(-2deg) scale(${sp(8)})`,
          backgroundColor: '#000000', padding: '18px 40px', boxShadow: '12px 12px 0 rgba(0,0,0,0.25)' }}>
          <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 88, color: '#F4F2F2' }}>
            7 ANS DE CRÉDIT.
          </div>
        </div>
        <div style={{ position: 'absolute', top: 235, left: 180, transform: `rotate(1deg) scale(${sp(40)})`,
          backgroundColor: '#ff0000', padding: '10px 28px' }}>
          <div style={{ fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 40, fontWeight: 800, color: '#F4F2F2' }}>
            La valeur chute dès la concession
          </div>
        </div>

        {/* Depreciation curve — SVG path draw */}
        <svg width="1920" height="1080" style={{ position: 'absolute', left: 0, top: 0 }}>
          {/* Axis */}
          <line x1="160" y1="230" x2="160" y2="700" stroke="#000000" strokeWidth="5"
            opacity={interpolate(frame, [60, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} />
          <line x1="160" y1="700" x2="1400" y2="700" stroke="#000000" strokeWidth="5"
            opacity={interpolate(frame, [60, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} />
          {/* Falling value curve */}
          <path d="M 180 250 C 500 280, 750 420, 1000 520 S 1250 610, 1290 625"
            fill="none" stroke="#ff0000" strokeWidth="12" strokeLinecap="round"
            strokeDasharray={pathLen} strokeDashoffset={dash} />
        </svg>

        {/* Car icon sliding down the curve */}
        <div style={{ position: 'absolute', left: carX - 55, top: carY - 60, transform: `rotate(${carProg * 14}deg)`,
          opacity: interpolate(frame, [80, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <svg width="110" height="50" viewBox="0 0 120 52">
            <path d="M8 38 L18 20 Q22 12 32 12 L78 12 Q88 12 96 22 L112 38 Z" fill="#000000" />
            <circle cx="32" cy="42" r="9" fill="#C3FF99" stroke="#000000" strokeWidth="3" />
            <circle cx="90" cy="42" r="9" fill="#C3FF99" stroke="#000000" strokeWidth="3" />
          </svg>
        </div>

        {/* Value labels */}
        <div style={{ position: 'absolute', left: 210, top: 175, fontFamily: 'Space Mono, monospace', fontSize: 34,
          fontWeight: 700, color: '#000000', opacity: interpolate(frame, [90, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          100% — jour de l'achat
        </div>
        <div style={{ position: 'absolute', left: 1330, top: 640, fontFamily: 'Space Mono, monospace', fontSize: 40,
          fontWeight: 700, color: '#F4F2F2', backgroundColor: '#000000', padding: '8px 20px',
          transform: `rotate(2deg) scale(${sp(255, { stiffness: 220 })})` }}>
          -40% en 3-5 ans
        </div>

        {/* Bottom bar */}
        <div style={{ position: 'absolute', bottom: 100, left: 140, right: 300, height: 4, backgroundColor: '#000000',
          opacity: 0.35 }} />
        <div style={{ position: 'absolute', bottom: 120, left: 140, fontFamily: 'Montserrat, sans-serif', fontSize: 30,
          fontWeight: 700, color: '#000000',
          opacity: interpolate(frame, [300, 325], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          Un objet financé sur 84 mois. Une valeur qui fond en silence.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene08Decote;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09CoutLOA',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09CoutLOA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Strike-through of marketing card
  const strikeW = interpolate(frame, [150, 175], [0, 105], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Impact shake when real total slams
  const slamAt = 200;
  const shake = frame > slamAt && frame < slamAt + 18
    ? Math.sin((frame - slamAt) * 2.4) * (18 - (frame - slamAt)) * 1.1 : 0;

  // Real total counter
  const total = Math.round(interpolate(frame, [slamAt, slamAt + 70], [0, 18000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }) / 100) * 100;

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 110%, #D7FFB7 0%, #C3FF99 45%, #A6E57C 100%)' }} />

      {/* Motion texture — punk zigzag lines */}
      <AbsoluteFill style={{ opacity: 0.05,
        backgroundImage: 'repeating-linear-gradient(45deg, #000000 0px, #000000 4px, transparent 4px, transparent 52px)',
        backgroundPosition: `${frame * -0.35}px 0px` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 540, height: 540, border: '4px solid #000000', opacity: 0.05,
        borderRadius: '50%', left: -170, top: -140, transform: `scale(${1 + Math.sin(frame * 0.013) * 0.05})` }} />
      <div style={{ position: 'absolute', width: 330, height: 330, backgroundColor: '#F4F2F2', opacity: 0.3,
        right: -90, bottom: -110, transform: `rotate(${frame * 0.06}deg)` }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const x = seededRandom(i + 55) * 1920;
        const drift = ((frame * (0.35 + seededRandom(i + 120) * 0.5)) % 1160) - 45;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift, width: 6, height: 6,
            backgroundColor: i % 3 === 0 ? '#ff0000' : '#000000', opacity: 0.2 }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Top label */}
        <div style={{ position: 'absolute', top: 90, fontFamily: 'Anton, sans-serif', fontSize: 80, color: '#000000',
          transform: `translateY(${(1 - sp(8)) * -160}px)`, opacity: sp(8) }}>
          LE VERNIS MARKETING :
        </div>

        {/* Marketing card — gets crossed out */}
        <div style={{ position: 'absolute', top: 230, left: 260, width: 620, backgroundColor: '#F4F2F2',
          border: '4px solid #000000', boxShadow: '12px 12px 0 #000000', padding: '32px 44px',
          transform: `rotate(-2.5deg) scale(${sp(35)})` }}>
          {/* Price tag icon */}
          <svg width="56" height="56" viewBox="0 0 56 56" style={{ marginBottom: 8 }}>
            <path d="M6 24 L24 6 L50 6 L50 32 L32 50 Z" fill="#C3FF99" stroke="#000000" strokeWidth="4" />
            <circle cx="40" cy="16" r="5" fill="#000000" />
          </svg>
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 76, fontWeight: 700, color: '#000000' }}>
              « 299€ / mois »
            </div>
            {/* Red strike-through */}
            <div style={{ position: 'absolute', left: '-2%', top: '48%', width: `${strikeW}%`, height: 12,
              backgroundColor: '#ff0000', transform: 'rotate(-4deg)' }} />
          </div>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 28, fontWeight: 600, color: '#000000', marginTop: 8 }}>
            Le cerveau lit 299. Pas 299 × 48.
          </div>
        </div>

        {/* Real cost card — slams in */}
        <div style={{ position: 'absolute', top: 420, right: 220, width: 660, backgroundColor: '#000000',
          padding: '36px 48px', boxShadow: '14px 14px 0 rgba(0,0,0,0.3)',
          transform: `rotate(2deg) scale(${interpolate(sp(slamAt, { stiffness: 260, damping: 14 }), [0, 1], [2.6, 1])})`,
          opacity: interpolate(frame, [slamAt, slamAt + 5], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <div style={{ fontFamily: 'Montserrat, sans-serif', fontSize: 30, fontWeight: 700, color: '#C3FF99', letterSpacing: 3 }}>
            COÛT RÉEL TOTAL LOA
          </div>
          <div style={{ fontFamily: 'Space Mono, monospace', fontSize: 120, fontWeight: 700, color: '#F4F2F2', lineHeight: 1.05 }}>
            {total.toLocaleString('fr-FR')}€
          </div>
          <div style={{ fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 34, fontWeight: 800, color: '#ff0000' }}>
            20 000€ pour un SUV
          </div>
        </div>

        {/* Bottom equivalence strip */}
        <div style={{ position: 'absolute', bottom: 110, fontFamily: 'Anton, sans-serif', fontSize: 46, color: '#000000',
          backgroundColor: '#F4F2F2', border: '3px solid #000000', padding: '10px 34px', transform: `rotate(-1deg) scale(${sp(320)})` }}>
          = 7 À 8 ANS DE PLAFOND DE LIVRET A, ENVOLÉS.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene09CoutLOA;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10Moins40',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10Moins40: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Giant -40% slam + shake
  const slamAt = 50;
  const shake = frame > slamAt && frame < slamAt + 22
    ? Math.sin((frame - slamAt) * 2) * (22 - (frame - slamAt)) * 1.2 : 0;

  // Melting money bar (value draining)
  const drain = interpolate(frame, [130, 300], [100, 60], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#C3FF99', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(220deg, #D1FFAF 0%, #C3FF99 40%, #99D96F 100%)' }} />

      {/* Motion texture — big halftone field */}
      <AbsoluteFill style={{ opacity: 0.06,
        backgroundImage: 'radial-gradient(#000000 2px, transparent 2px)',
        backgroundSize: '46px 46px', backgroundPosition: `${frame * -0.3}px ${frame * 0.2}px` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 900, height: 900, borderRadius: '50%', border: '3px solid #000000',
        opacity: 0.05, left: '50%', top: '50%',
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.008) * 0.05})` }} />
      <div style={{ position: 'absolute', width: 300, height: 300, backgroundColor: '#000000', opacity: 0.04,
        left: -80, top: 120, transform: `rotate(${frame * 0.09}deg)` }} />

      {/* Punk collage torn strips */}
      <div style={{ position: 'absolute', top: -14, left: 300, width: 600, height: 46, backgroundColor: '#000000',
        transform: 'rotate(-1.5deg)' }} />
      <div style={{ position: 'absolute', bottom: 60, left: -50, width: 460, height: 40, backgroundColor: '#F4F2F2',
        border: '3px solid #000000', transform: 'rotate(2.5deg)' }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const x = seededRandom(i + 61) * 1920;
        const drift = ((frame * (0.4 + seededRandom(i + 130) * 0.6)) % 1180) - 50;
        return (
          <div key={i} style={{ position: 'absolute', left: x, top: 1080 - drift, width: 7, height: 7,
            backgroundColor: i % 3 === 0 ? '#ff0000' : '#000000', opacity: 0.22,
            transform: `rotate(${frame * 2.5 + i * 20}deg)` }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${shake}px) scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Giant number */}
        <div style={{ fontFamily: 'Anton, sans-serif', fontSize: 380, color: '#ff0000', lineHeight: 0.9,
          WebkitTextStroke: '6px #000000', textShadow: '18px 18px 0 rgba(0,0,0,0.85)',
          transform: `rotate(-3deg) scale(${interpolate(sp(slamAt, { stiffness: 240, damping: 13 }), [0, 1], [2.8, 1])})`,
          opacity: interpolate(frame, [slamAt, slamAt + 5], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          -40%
        </div>

        <div style={{ marginTop: 24, fontFamily: 'Space Mono, monospace', fontSize: 40, fontWeight: 700, color: '#F4F2F2',
          backgroundColor: '#000000', padding: '8px 30px', transform: `rotate(1deg) scale(${sp(110)})` }}>
          DE VALEUR EN 3 À 5 ANS
        </div>

        {/* Draining value bar */}
        <div style={{ marginTop: 50, width: 1000, height: 60, border: '4px solid #000000', backgroundColor: '#F4F2F2',
          boxShadow: '10px 10px 0 #000000', position: 'relative',
          opacity: interpolate(frame, [125, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <div style={{ position: 'absolute', left: 0, top: 0, height: '100%', width: `${drain}%`, backgroundColor: '#000000' }} />
          <div style={{ position: 'absolute', right: 14, top: 8, fontFamily: 'Space Mono, monospace', fontSize: 32,
            fontWeight: 700, color: '#ff0000' }}>
            {Math.round(100 - drain)}% perdu
          </div>
        </div>

        {/* Final punchline */}
        <div style={{ marginTop: 56, fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 58, fontWeight: 800,
          color: '#000000', textAlign: 'center', maxWidth: 1400,
          transform: `translateY(${(1 - sp(330)) * 100}px)`, opacity: sp(330) }}>
          Vous payez 40% d'un bien…
          <span style={{ color: '#F4F2F2', WebkitTextStroke: '2.5px #000000' }}> pour avoir le droit de le rendre.</span>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Montserrat, sans-serif', fontSize: 22,
        fontWeight: 600, color: '#000000', opacity: interpolate(frame, [0, 60], [0.6, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene10Moins40;
```

