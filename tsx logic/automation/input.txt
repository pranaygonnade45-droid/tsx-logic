```tsx
import React from 'react';
Audio: "If a caller says they are from the IRS, but you have not received any of these numbered notices in the mail recently, it is a scam. When you get the call, ask them for the exact notice number they are calling about."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene41NoticeCheck',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene41NoticeCheck: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, 300], [1, 1.04], { extrapolateRight: 'clamp' });

  // Headline reveal
  const headlineOp = sp(8);
  const headlineY = interpolate(sp(8), [0, 1], [60, 0]);

  // Mailbox appears empty
  const mailboxOp = sp(35);
  const mailboxScale = sp(35);

  // Cross over mailbox
  const crossOp = interpolate(sp(60), [0, 1], [0, 1]);

  // SCAM stamp
  const stampScale = sp(80, { damping: 8, stiffness: 180 });
  const stampRot = interpolate(sp(80), [0, 1], [-30, -12]);

  // Question list reveal
  const q1Op = sp(110);
  const q2Op = sp(135);
  const q3Op = sp(160);

  // Phone shake
  const phoneShake = Math.sin(frame * 0.4) * (frame > 100 && frame < 200 ? 3 : 0);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 20%, rgba(254,236,65,0.18) 0%, rgba(238,227,203,0) 55%)',
        }}
      />

      {/* Shape Layers - drifting geometric forms */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '3px solid #000000',
          opacity: 0.05,
          top: -200,
          right: -180,
          transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 480,
          height: 480,
          backgroundColor: '#FEEC41',
          opacity: 0.07,
          bottom: -160,
          left: -120,
          transform: `rotate(${-frame * 0.1}deg)`,
          borderRadius: 40,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '180px solid transparent',
          borderRight: '180px solid transparent',
          borderBottom: '300px solid #000000',
          opacity: 0.04,
          top: '40%',
          left: '8%',
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="g41" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g41)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 41;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 100) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 18;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x + drift,
              top: y,
              width: 6,
              height: 6,
              backgroundColor: '#000000',
              opacity: 0.18,
              borderRadius: '50%',
            }}
          />
        );
      })}

      {/* Main content wrapper with camera scale */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top Headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headlineOp,
            transform: `translateY(${headlineY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              opacity: 0.55,
              marginBottom: 12,
            }}
          >
            NO MAIL? NO IRS.
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.02,
              letterSpacing: -2,
            }}
          >
            IF NO <span style={{ backgroundColor: '#FEEC41', padding: '0 18px' }}>NOTICE</span> CAME—
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.02,
              letterSpacing: -2,
              marginTop: 6,
            }}
          >
            IT'S A <span style={{ color: '#FEEC41', WebkitTextStroke: '3px #000000' }}>SCAM.</span>
          </div>
        </div>

        {/* Mailbox illustration - LEFT */}
        <div
          style={{
            position: 'absolute',
            left: 180,
            top: 460,
            opacity: mailboxOp,
            transform: `scale(${mailboxScale}) translateY(${Math.sin(frame * 0.05) * 4}px)`,
          }}
        >
          <svg width="280" height="340" viewBox="0 0 280 340">
            {/* Post */}
            <rect x="125" y="220" width="30" height="120" fill="#000000" />
            {/* Mailbox body */}
            <rect x="40" y="100" width="200" height="130" rx="8" fill="#000000" />
            <rect x="50" y="110" width="180" height="110" rx="4" fill="#EEE3CB" />
            {/* Flag */}
            <rect x="232" y="120" width="40" height="20" fill="#FEEC41" stroke="#000000" strokeWidth="3" />
            <rect x="226" y="115" width="6" height="35" fill="#000000" />
            {/* Door opening */}
            <rect x="60" y="125" width="160" height="80" rx="4" fill="#000000" opacity="0.85" />
            {/* Empty interior text */}
            <text x="140" y="172" fontFamily="Inter" fontSize="32" fontWeight="900" fill="#FEEC41" textAnchor="middle">
              EMPTY
            </text>
          </svg>

          {/* Big X over mailbox */}
          <svg
            width="280"
            height="340"
            style={{
              position: 'absolute',
              top: 0,
              left: 0,
              opacity: crossOp,
            }}
          >
            <line x1="40" y1="100" x2="240" y2="230" stroke="#FF0000" strokeWidth="14" strokeLinecap="round" />
            <line x1="240" y1="100" x2="40" y2="230" stroke="#FF0000" strokeWidth="14" strokeLinecap="round" />
          </svg>

          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 800,
              color: '#000000',
              textAlign: 'center',
              marginTop: 16,
              letterSpacing: 1,
            }}
          >
            NO LETTER
          </div>
        </div>

        {/* Arrow connector */}
        <div
          style={{
            position: 'absolute',
            left: 510,
            top: 580,
            opacity: interpolate(sp(70), [0, 1], [0, 1]),
          }}
        >
          <svg width="120" height="60" viewBox="0 0 120 60">
            <line x1="5" y1="30" x2="105" y2="30" stroke="#000000" strokeWidth="6" strokeDasharray="10 8" />
            <polygon points="100,15 120,30 100,45" fill="#000000" />
          </svg>
        </div>

        {/* SCAM Stamp - CENTER */}
        <div
          style={{
            position: 'absolute',
            left: 640,
            top: 480,
            transform: `scale(${stampScale}) rotate(${stampRot}deg) translateX(${phoneShake}px)`,
            opacity: sp(80),
          }}
        >
          <div
            style={{
              border: '8px solid #FF0000',
              padding: '20px 50px',
              fontFamily: 'Inter',
              fontSize: 92,
              fontWeight: 900,
              color: '#FF0000',
              backgroundColor: 'rgba(255,255,255,0.1)',
              letterSpacing: 6,
            }}
          >
            SCAM
          </div>
          <div
            style={{
              border: '4px solid #FF0000',
              marginTop: 8,
              padding: '6px 0',
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              fontWeight: 700,
              color: '#FF0000',
              textAlign: 'center',
              letterSpacing: 4,
            }}
          >
            VERIFIED FRAUD
          </div>
        </div>

        {/* Question Checklist - RIGHT */}
        <div
          style={{
            position: 'absolute',
            right: 120,
            top: 470,
            width: 540,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 3,
              marginBottom: 22,
              borderBottom: '4px solid #000000',
              paddingBottom: 10,
              opacity: sp(95),
            }}
          >
            ASK THE CALLER:
          </div>

          {[
            { txt: 'WHAT NOTICE NUMBER?', op: q1Op, delay: 110 },
            { txt: 'YOUR BADGE ID?', op: q2Op, delay: 135 },
            { txt: 'CALLBACK NUMBER?', op: q3Op, delay: 160 },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 16,
                marginBottom: 18,
                opacity: item.op,
                transform: `translateX(${interpolate(item.op, [0, 1], [40, 0])}px)`,
              }}
            >
              <div
                style={{
                  width: 44,
                  height: 44,
                  backgroundColor: '#FEEC41',
                  border: '3px solid #000000',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Inter',
                  fontSize: 26,
                  fontWeight: 900,
                  color: '#000000',
                  flexShrink: 0,
                }}
              >
                {i + 1}
              </div>
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 36,
                  fontWeight: 800,
                  color: '#000000',
                  letterSpacing: 1,
                }}
              >
                {item.txt}
              </div>
            </div>
          ))}
        </div>

        {/* Bottom ticker */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(200),
          }}
        >
          <div
            style={{
              display: 'inline-block',
              backgroundColor: '#000000',
              color: '#FEEC41',
              padding: '14px 40px',
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 700,
              letterSpacing: 3,
            }}
          >
            NO PAPER TRAIL = NO IRS
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.35) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene41NoticeCheck;
```

```tsx
import React from 'react';
Audio: "Ask them for their badge number and a direct callback number. A real agent will give it to you. A scammer will get angry, hang up, or start screaming at you."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene42BadgeTest',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene42BadgeTest: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow pan
  const camX = interpolate(frame, [0, 330], [-15, 15], { extrapolateRight: 'clamp' });

  const titleOp = sp(8);
  const titleY = interpolate(sp(8), [0, 1], [-50, 0]);

  // Left side - REAL AGENT
  const leftCardOp = sp(40);
  const leftCardX = interpolate(sp(40), [0, 1], [-100, 0]);

  // Right side - SCAMMER
  const rightCardOp = sp(80);
  const rightCardX = interpolate(sp(80), [0, 1], [100, 0]);

  // VS divider pulse
  const vsScale = 1 + Math.sin(frame * 0.12) * 0.05;

  // Check mark draw
  const checkLen = interpolate(sp(120), [0, 1], [0, 100]);

  // Red X draw
  const xOp = sp(160);

  // Scammer shake
  const scamShake = frame > 180 ? Math.sin(frame * 0.8) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Background gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(254,236,65,0.12) 0%, rgba(238,227,203,0) 50%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 520,
          height: 520,
          backgroundColor: '#FEEC41',
          opacity: 0.06,
          top: '20%',
          left: '15%',
          borderRadius: 30,
          transform: `rotate(${15 + frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          border: '4px solid #000000',
          opacity: 0.05,
          borderRadius: '50%',
          bottom: '-20%',
          right: '-10%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="d42" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#000000" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#d42)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const seed = i + 142;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 50) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y + Math.sin(frame * 0.025 + i) * 25,
              width: 5,
              height: 5,
              backgroundColor: '#000000',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${titleY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              opacity: 0.6,
              marginBottom: 10,
            }}
          >
            — THE 2-QUESTION TEST —
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 88,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -1.5,
            }}
          >
            BADGE NUMBER <span style={{ color: '#FEEC41', WebkitTextStroke: '2px #000' }}>+</span> CALLBACK
          </div>
        </div>

        {/* VS divider in middle */}
        <div
          style={{
            position: 'absolute',
            top: 480,
            left: '50%',
            transform: `translateX(-50%) scale(${vsScale})`,
            zIndex: 5,
            opacity: sp(100),
          }}
        >
          <div
            style={{
              width: 100,
              height: 100,
              backgroundColor: '#000000',
              borderRadius: '50%',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Inter',
              fontSize: 38,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: 1,
              border: '5px solid #FEEC41',
            }}
          >
            VS
          </div>
        </div>

        {/* LEFT: REAL AGENT */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 100,
            width: 740,
            opacity: leftCardOp,
            transform: `translateX(${leftCardX}px)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#FEEC41',
              border: '5px solid #000000',
              padding: 30,
              transform: 'rotate(-1deg)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#000000',
                letterSpacing: 3,
                marginBottom: 10,
              }}
            >
              [ SCENARIO A ]
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 58,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1,
                marginBottom: 24,
              }}
            >
              REAL AGENT
            </div>

            {/* ID badge graphic */}
            <div
              style={{
                backgroundColor: '#EEE3CB',
                border: '3px solid #000000',
                padding: 18,
                marginBottom: 20,
              }}
            >
              <div style={{ display: 'flex', gap: 16, alignItems: 'center' }}>
                <svg width="80" height="80" viewBox="0 0 80 80">
                  <rect x="5" y="5" width="70" height="70" fill="#000000" />
                  <circle cx="40" cy="32" r="14" fill="#FEEC41" />
                  <rect x="22" y="48" width="36" height="20" rx="3" fill="#FEEC41" />
                </svg>
                <div>
                  <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#000000', opacity: 0.7 }}>
                    BADGE #
                  </div>
                  <div style={{ fontFamily: 'JetBrains Mono', fontSize: 38, fontWeight: 800, color: '#000000', letterSpacing: 3 }}>
                    1000827453
                  </div>
                </div>
              </div>
            </div>

            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 36,
                fontWeight: 700,
                color: '#000000',
                lineHeight: 1.2,
              }}
            >
              "Yes, my badge is —"
            </div>

            {/* Check mark */}
            <div style={{ marginTop: 20, display: 'flex', justifyContent: 'flex-end' }}>
              <svg width="120" height="120" viewBox="0 0 120 120">
                <circle cx="60" cy="60" r="50" fill="#000000" />
                <path
                  d="M 35 60 L 55 80 L 90 40"
                  stroke="#FEEC41"
                  strokeWidth="10"
                  fill="none"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  strokeDasharray="100"
                  strokeDashoffset={100 - checkLen}
                />
              </svg>
            </div>
          </div>
        </div>

        {/* RIGHT: SCAMMER */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            right: 100,
            width: 740,
            opacity: rightCardOp,
            transform: `translateX(${rightCardX}px) translateX(${scamShake}px)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#000000',
              border: '5px solid #FF0000',
              padding: 30,
              transform: 'rotate(1deg)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#FF0000',
                letterSpacing: 3,
                marginBottom: 10,
              }}
            >
              [ SCENARIO B ]
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 58,
                fontWeight: 900,
                color: '#FEEC41',
                lineHeight: 1,
                marginBottom: 24,
              }}
            >
              SCAMMER
            </div>

            {/* Reaction graph */}
            <div
              style={{
                backgroundColor: '#1C1C1C',
                border: '3px solid #FF0000',
                padding: 18,
                marginBottom: 20,
              }}
            >
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 16, color: '#FEEC41', opacity: 0.7, marginBottom: 8 }}>
                ANGER LEVEL ↑
              </div>
              <svg width="100%" height="80" viewBox="0 0 400 80">
                <polyline
                  points="0,70 50,65 100,55 150,40 200,30 250,20 300,12 350,8 400,5"
                  fill="none"
                  stroke="#FF0000"
                  strokeWidth="4"
                />
                <polyline
                  points="0,70 50,65 100,55 150,40 200,30 250,20 300,12 350,8 400,5 400,80 0,80"
                  fill="#FF0000"
                  opacity="0.3"
                />
              </svg>
            </div>

            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 36,
                fontWeight: 700,
                color: '#FEEC41',
                lineHeight: 1.2,
              }}
            >
              "HOW DARE YOU—!!!"
            </div>

            {/* Red X */}
            <div style={{ marginTop: 20, display: 'flex', justifyContent: 'flex-end', opacity: xOp }}>
              <svg width="120" height="120" viewBox="0 0 120 120">
                <circle cx="60" cy="60" r="50" fill="#FF0000" />
                <line x1="40" y1="40" x2="80" y2="80" stroke="#FFFFFF" strokeWidth="10" strokeLinecap="round" />
                <line x1="80" y1="40" x2="40" y2="80" stroke="#FFFFFF" strokeWidth="10" strokeLinecap="round" />
              </svg>
            </div>
          </div>
        </div>

        {/* Bottom strip */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(220),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 2,
            }}
          >
            REAL = COMPLIANT  •  FAKE = HOSTILE
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene42BadgeTest;
```

```tsx
import React from 'react';
Audio: "And rule number five. This is the one almost nobody talks about. And it is the ultimate legal trap door that proves you are talking to a fake agent in less than 10 seconds."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene43Rule5Reveal',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene43Rule5Reveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom in
  const camScale = interpolate(frame, [0, 270], [0.95, 1.05], { extrapolateRight: 'clamp' });

  // Massive 5 reveal
  const fiveScale = sp(15, { damping: 8, stiffness: 100 });
  const fiveRot = interpolate(sp(15), [0, 1], [-15, 0]);

  // RULE label
  const ruleOp = sp(40);

  // Trap door animation
  const doorRotate = interpolate(sp(90), [0, 1], [0, -85]);
  const doorOp = sp(60);

  // 10 second countdown
  const countNum = Math.max(0, 10 - Math.floor((frame - 130) / 12));
  const countOp = sp(130);

  // Bottom text
  const bottomOp = sp(180);

  // Pulse on 5
  const fivePulse = 1 + Math.sin(frame * 0.15) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dramatic radial gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(254,236,65,0.22) 0%, rgba(238,227,203,0) 60%)',
        }}
      />

      {/* Radial burst lines */}
      <svg
        width="1920"
        height="1080"
        style={{
          position: 'absolute',
          opacity: 0.08,
          transform: `rotate(${frame * 0.3}deg)`,
          transformOrigin: 'center',
        }}
      >
        {Array.from({ length: 24 }).map((_, i) => {
          const angle = (i / 24) * Math.PI * 2;
          const x1 = 960 + Math.cos(angle) * 200;
          const y1 = 540 + Math.sin(angle) * 200;
          const x2 = 960 + Math.cos(angle) * 1200;
          const y2 = 540 + Math.sin(angle) * 1200;
          return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth="2" />;
        })}
      </svg>

      {/* Shape layers */}
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          border: '5px solid #000000',
          opacity: 0.06,
          borderRadius: '50%',
          top: '50%',
          left: '50%',
          transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          backgroundColor: '#FEEC41',
          opacity: 0.05,
          top: '60%',
          right: '5%',
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i + 243;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 70) * 1080;
        const drift = Math.sin(frame * 0.03 + i * 0.5) * 30;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y + drift,
              width: 7,
              height: 7,
              backgroundColor: i % 3 === 0 ? '#FEEC41' : '#000000',
              opacity: 0.25,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* RULE # label top */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: ruleOp,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              backgroundColor: '#000000',
              color: '#FEEC41',
              padding: '12px 36px',
              fontFamily: 'JetBrains Mono',
              fontSize: 32,
              fontWeight: 800,
              letterSpacing: 6,
            }}
          >
            FINAL RULE — #5
          </div>
        </div>

        {/* MASSIVE 5 */}
        <div
          style={{
            position: 'absolute',
            top: 170,
            left: 0,
            right: 0,
            textAlign: 'center',
            transform: `scale(${fiveScale * fivePulse}) rotate(${fiveRot}deg)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 580,
              fontWeight: 900,
              color: '#FEEC41',
              WebkitTextStroke: '8px #000000',
              lineHeight: 1,
              letterSpacing: -20,
              textShadow: '12px 12px 0 #000000',
            }}
          >
            5
          </div>
        </div>

        {/* Trap door illustration - bottom left */}
        <div
          style={{
            position: 'absolute',
            bottom: 140,
            left: 140,
            opacity: doorOp,
          }}
        >
          <svg width="320" height="220" viewBox="0 0 320 220">
            {/* Frame */}
            <rect x="20" y="180" width="280" height="20" fill="#000000" />
            <rect x="20" y="20" width="280" height="180" fill="none" stroke="#000000" strokeWidth="5" strokeDasharray="8 6" />
            {/* Door panel rotating */}
            <g style={{ transform: `rotate(${doorRotate}deg)`, transformOrigin: '20px 100px' }}>
              <rect x="20" y="20" width="280" height="160" fill="#000000" />
              <rect x="30" y="30" width="260" height="140" fill="none" stroke="#FEEC41" strokeWidth="3" />
              <text x="160" y="115" fontFamily="Inter" fontSize="48" fontWeight="900" fill="#FEEC41" textAnchor="middle">
                TRAP
              </text>
            </g>
            {/* Falling icon */}
            <circle
              cx={160 + Math.sin(frame * 0.1) * 20}
              cy={50 + (frame > 100 ? (frame - 100) * 1.2 : 0)}
              r="14"
              fill="#FF0000"
              opacity={frame > 100 && frame < 250 ? 1 : 0}
            />
          </svg>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 800,
              color: '#000000',
              marginTop: 8,
              letterSpacing: 1,
            }}
          >
            LEGAL TRAPDOOR
          </div>
        </div>

        {/* 10 second timer - bottom right */}
        <div
          style={{
            position: 'absolute',
            bottom: 160,
            right: 160,
            opacity: countOp,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 3,
              marginBottom: 10,
              textAlign: 'right',
            }}
          >
            EXPOSURE TIME →
          </div>
          <div style={{ display: 'flex', alignItems: 'baseline', justifyContent: 'flex-end', gap: 10 }}>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 240,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1,
                fontVariantNumeric: 'tabular-nums',
              }}
            >
              {countNum}
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 60,
                fontWeight: 800,
                color: '#000000',
              }}
            >
              SEC
            </div>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 700,
              color: '#FF0000',
              textAlign: 'right',
              marginTop: -6,
              letterSpacing: 2,
            }}
          >
            ━━━ TO EXPOSE A FAKE ━━━
          </div>
        </div>

        {/* Bottom banner */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: bottomOp,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '14px 50px',
              backgroundColor: '#FEEC41',
              border: '4px solid #000000',
              fontFamily: 'Inter',
              fontSize: 38,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 2,
            }}
          >
            NOBODY TALKS ABOUT THIS ONE
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene43Rule5Reveal;
```

```tsx
import React from 'react';
Audio: "It is the right to appeal. The tax code is complex, and the IRS is legally required to give you due process. If you disagree with the amount they say you owe..."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene44RightToAppeal',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene44RightToAppeal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Tilt camera
  const camRot = interpolate(frame, [0, 300], [-0.5, 0.5], { extrapolateRight: 'clamp' });

  // Title reveal
  const titleOp = sp(8);
  const titleScale = sp(8, { damping: 10, stiffness: 100 });

  // Gavel drop
  const gavelY = interpolate(sp(50), [0, 1], [-200, 0]);
  const gavelRot = interpolate(sp(50), [0, 1], [-25, 0]);
  const gavelImpact = sp(75) > 0.5 ? Math.sin((frame - 75) * 0.5) * 3 : 0;

  // Scale of justice
  const scaleOp = sp(90);
  const scaleTilt = Math.sin(frame * 0.08) * 4;

  // Steps reveal
  const step1Op = sp(120);
  const step2Op = sp(150);
  const step3Op = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(254,236,65,0.10) 0%, rgba(238,227,203,0) 50%, rgba(0,0,0,0.10) 100%)',
        }}
      />

      {/* Background shapes */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          border: '3px solid #000000',
          opacity: 0.06,
          borderRadius: 30,
          top: '-15%',
          left: '-10%',
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '250px solid transparent',
          borderRight: '250px solid transparent',
          borderBottom: '400px solid #FEEC41',
          opacity: 0.08,
          bottom: '-10%',
          right: '5%',
          transform: `rotate(${-frame * 0.15}deg)`,
        }}
      />

      {/* Dots grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="dt44" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dt44)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = i + 344;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 80) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + i) * 15,
              top: y + Math.cos(frame * 0.03 + i) * 12,
              width: 6,
              height: 6,
              backgroundColor: '#000000',
              opacity: 0.18,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `scale(${titleScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 8,
              opacity: 0.6,
              marginBottom: 12,
            }}
          >
            YOUR LEGAL WEAPON
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 140,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            THE RIGHT TO
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 180,
              fontWeight: 900,
              color: '#000000',
              backgroundColor: '#FEEC41',
              display: 'inline-block',
              padding: '0 40px',
              lineHeight: 0.95,
              letterSpacing: -4,
              marginTop: 8,
              transform: 'rotate(-2deg)',
              border: '6px solid #000000',
            }}
          >
            APPEAL.
          </div>
        </div>

        {/* Gavel - left */}
        <div
          style={{
            position: 'absolute',
            bottom: 140,
            left: 180,
            transform: `translateY(${gavelY}px) rotate(${gavelRot + gavelImpact}deg)`,
          }}
        >
          <svg width="280" height="280" viewBox="0 0 280 280">
            {/* Block */}
            <rect x="40" y="220" width="200" height="35" fill="#000000" />
            <rect x="50" y="235" width="180" height="20" fill="#FEEC41" />
            {/* Handle */}
            <rect x="60" y="100" width="160" height="22" rx="11" fill="#000000" transform="rotate(-15 140 111)" />
            {/* Head */}
            <rect x="40" y="60" width="100" height="60" rx="6" fill="#000000" transform="rotate(-15 90 90)" />
            <rect x="50" y="68" width="80" height="44" rx="4" fill="#FEEC41" transform="rotate(-15 90 90)" opacity="0.3" />
            {/* Impact lines */}
            {frame > 78 && frame < 120 && (
              <>
                <line x1="100" y1="240" x2="60" y2="220" stroke="#000000" strokeWidth="4" />
                <line x1="180" y1="240" x2="220" y2="220" stroke="#000000" strokeWidth="4" />
                <line x1="140" y1="265" x2="140" y2="285" stroke="#000000" strokeWidth="4" />
              </>
            )}
          </svg>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 800,
              color: '#000000',
              textAlign: 'center',
              marginTop: 10,
              letterSpacing: 2,
            }}
          >
            DUE PROCESS
          </div>
        </div>

        {/* Scale of justice - right */}
        <div
          style={{
            position: 'absolute',
            bottom: 180,
            right: 220,
            opacity: scaleOp,
            transform: `rotate(${scaleTilt}deg)`,
            transformOrigin: 'center top',
          }}
        >
          <svg width="280" height="280" viewBox="0 0 280 280">
            {/* Base */}
            <rect x="100" y="240" width="80" height="20" fill="#000000" />
            <rect x="130" y="80" width="20" height="160" fill="#000000" />
            {/* Beam */}
            <rect x="40" y="78" width="200" height="10" fill="#000000" />
            {/* Left plate */}
            <line x1="65" y1="88" x2="65" y2="130" stroke="#000000" strokeWidth="3" />
            <path d="M 30 130 Q 65 155 100 130 Z" fill="#FEEC41" stroke="#000000" strokeWidth="3" />
            {/* Right plate */}
            <line x1="215" y1="88" x2="215" y2="130" stroke="#000000" strokeWidth="3" />
            <path d="M 180 130 Q 215 155 250 130 Z" fill="#FEEC41" stroke="#000000" strokeWidth="3" />
            {/* Top ornament */}
            <circle cx="140" cy="75" r="8" fill="#FEEC41" stroke="#000000" strokeWidth="3" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 800,
              color: '#000000',
              textAlign: 'center',
              marginTop: 10,
              letterSpacing: 2,
            }}
          >
            FAIR HEARING
          </div>
        </div>

        {/* Center process list */}
        <div
          style={{
            position: 'absolute',
            bottom: 280,
            left: '50%',
            transform: 'translateX(-50%)',
            width: 580,
          }}
        >
          {[
            { lbl: 'DISAGREE', op: step1Op },
            { lbl: 'FILE APPEAL', op: step2Op },
            { lbl: 'TAX COURT', op: step3Op },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 14,
                marginBottom: 14,
                opacity: item.op,
                transform: `translateY(${interpolate(item.op, [0, 1], [20, 0])}px)`,
              }}
            >
              <div
                style={{
                  width: 16,
                  height: 16,
                  backgroundColor: '#000000',
                  borderRadius: '50%',
                  flexShrink: 0,
                }}
              />
              <div style={{ flex: 1, height: 3, backgroundColor: '#000000', opacity: 0.3 }} />
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 40,
                  fontWeight: 900,
                  color: '#000000',
                  letterSpacing: 2,
                  backgroundColor: i === 2 ? '#FEEC41' : 'transparent',
                  padding: i === 2 ? '4px 16px' : '0',
                  border: i === 2 ? '3px solid #000000' : 'none',
                }}
              >
                {item.lbl}
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene44RightToAppeal;
```

```tsx
import React from 'react';
Audio: "...you have the absolute legal right to appeal that decision in tax court before you pay a single dime. The real IRS will always inform you of your appeal rights in writing."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene45BeforeYouPay',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene45BeforeYouPay: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camY = interpolate(frame, [0, 300], [10, -10], { extrapolateRight: 'clamp' });

  // Headline
  const headOp = sp(8);
  const headY = interpolate(sp(8), [0, 1], [-60, 0]);

  // Dollar bill
  const dollarOp = sp(40);
  const dollarRot = Math.sin(frame * 0.06) * 3;

  // Stop hand
  const stopScale = sp(70, { damping: 8, stiffness: 150 });

  // Letter/document
  const letterOp = sp(110);
  const letterX = interpolate(sp(110), [0, 1], [100, 0]);

  // Underline draw
  const underlineW = interpolate(sp(150), [0, 1], [0, 100]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 75% 30%, rgba(254,236,65,0.18) 0%, rgba(238,227,203,0) 55%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          backgroundColor: '#FEEC41',
          opacity: 0.06,
          top: '-30%',
          right: '-15%',
          borderRadius: 50,
          transform: `rotate(${20 + frame * 0.08}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          border: '4px solid #000000',
          opacity: 0.05,
          borderRadius: '50%',
          bottom: '-15%',
          left: '-5%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />

      {/* Lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="hl45" width="100" height="6" patternUnits="userSpaceOnUse">
            <line x1="0" y1="3" x2="100" y2="3" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hl45)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 445;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 90) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.025 + i) * 20,
              top: y,
              width: 5,
              height: 5,
              backgroundColor: i % 4 === 0 ? '#FEEC41' : '#000000',
              opacity: 0.22,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Top headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headOp,
            transform: `translateY(${headY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              opacity: 0.6,
              marginBottom: 14,
            }}
          >
            APPEAL FIRST — PAY LATER
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 130,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            BEFORE YOU PAY
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 130,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.95,
              letterSpacing: -3,
              marginTop: 6,
            }}
          >
            A <span style={{ color: '#FEEC41', WebkitTextStroke: '3px #000000', fontSize: 160 }}>SINGLE</span> DIME.
          </div>
          {/* Animated underline */}
          <div style={{ width: 600, margin: '14px auto 0', height: 8, backgroundColor: '#000000', transform: `scaleX(${underlineW / 100})`, transformOrigin: 'left' }} />
        </div>

        {/* Dollar bill - left bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 180,
            left: 160,
            opacity: dollarOp,
            transform: `rotate(${dollarRot}deg)`,
          }}
        >
          <svg width="340" height="180" viewBox="0 0 340 180">
            <rect x="5" y="5" width="330" height="170" rx="6" fill="#000000" />
            <rect x="14" y="14" width="312" height="152" rx="4" fill="#EEE3CB" stroke="#000000" strokeWidth="3" />
            {/* Center circle */}
            <circle cx="170" cy="90" r="50" fill="none" stroke="#000000" strokeWidth="3" />
            <text x="170" y="105" fontFamily="Inter" fontSize="56" fontWeight="900" fill="#000000" textAnchor="middle">
              $
            </text>
            {/* Corner numbers */}
            <text x="40" y="50" fontFamily="JetBrains Mono" fontSize="28" fontWeight="800" fill="#000000">1</text>
            <text x="310" y="155" fontFamily="JetBrains Mono" fontSize="28" fontWeight="800" fill="#000000" textAnchor="end">1</text>
            <text x="40" y="155" fontFamily="JetBrains Mono" fontSize="14" fontWeight="600" fill="#000000">ONE</text>
            <text x="310" y="50" fontFamily="JetBrains Mono" fontSize="14" fontWeight="600" fill="#000000" textAnchor="end">ONE</text>
          </svg>

          {/* STOP hand over it */}
          <div
            style={{
              position: 'absolute',
              top: -20,
              right: -40,
              transform: `scale(${stopScale})`,
            }}
          >
            <svg width="180" height="180" viewBox="0 0 180 180">
              <circle cx="90" cy="90" r="80" fill="#FF0000" stroke="#000000" strokeWidth="5" />
              <text x="90" y="105" fontFamily="Inter" fontSize="42" fontWeight="900" fill="#FFFFFF" textAnchor="middle" letterSpacing="2">
                STOP
              </text>
            </svg>
          </div>
        </div>

        {/* Letter/Document - right bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            right: 160,
            opacity: letterOp,
            transform: `translateX(${letterX}px) rotate(3deg)`,
          }}
        >
          <div
            style={{
              width: 380,
              backgroundColor: '#FFFFFF',
              border: '5px solid #000000',
              padding: 24,
              boxShadow: '12px 12px 0 #000000',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 16,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.6,
                marginBottom: 6,
                letterSpacing: 2,
              }}
            >
              INTERNAL REVENUE SERVICE
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 30,
                fontWeight: 900,
                color: '#000000',
                marginBottom: 12,
                lineHeight: 1.1,
              }}
            >
              YOUR APPEAL RIGHTS
            </div>
            <div style={{ height: 3, backgroundColor: '#FEEC41', marginBottom: 14 }} />
            <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#000000', lineHeight: 1.4, marginBottom: 6 }}>
              ▸ Form 12203 — Request for Appeals
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#000000', lineHeight: 1.4, marginBottom: 6 }}>
              ▸ Pub 5 — Your Appeal Rights
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, color: '#000000', lineHeight: 1.4, marginBottom: 14 }}>
              ▸ Office of Appeals contact info
            </div>
            <div
              style={{
                backgroundColor: '#FEEC41',
                padding: '8px 12px',
                fontFamily: 'JetBrains Mono',
                fontSize: 18,
                fontWeight: 800,
                color: '#000000',
                letterSpacing: 1,
                border: '2px solid #000000',
              }}
            >
              IN WRITING ✓ ALWAYS
            </div>
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene45BeforeYouPay;
```

```tsx
import React from 'react';
Audio: "They will explain the process, give you the forms to file an appeal, and tell you how to contact the independent office of appeals. A scammer will never, ever mention your right to appeal."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene46FormsVsSilence',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene46FormsVsSilence: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, 300], [1, 1.03], { extrapolateRight: 'clamp' });

  const titleOp = sp(8);

  // Forms fan out
  const form1 = sp(30);
  const form2 = sp(50);
  const form3 = sp(70);

  // Phone with X
  const phoneOp = sp(110);
  const phoneScale = sp(110, { damping: 9, stiffness: 130 });

  // NEVER stamp
  const neverScale = sp(150, { damping: 7, stiffness: 180 });
  const neverRot = interpolate(sp(150), [0, 1], [20, -8]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(120deg, rgba(254,236,65,0.10) 0%, rgba(238,227,203,0) 50%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 550,
          height: 550,
          backgroundColor: '#000000',
          opacity: 0.04,
          top: '-15%',
          left: '40%',
          borderRadius: 50,
          transform: `rotate(${frame * 0.12}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          border: '4px solid #FEEC41',
          opacity: 0.15,
          borderRadius: '50%',
          top: '50%',
          right: '8%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }}
      />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="cr46" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="30" cy="30" r="20" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cr46)" />
      </svg>

      {Array.from({ length: 20 }).map((_, i) => {
        const seed = i + 546;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 60) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y + Math.sin(frame * 0.03 + i) * 20,
              width: 6,
              height: 6,
              backgroundColor: '#000000',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 92,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -2,
              lineHeight: 1,
            }}
          >
            REAL IRS GIVES YOU
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 92,
              fontWeight: 900,
              color: '#000000',
              backgroundColor: '#FEEC41',
              display: 'inline-block',
              padding: '4px 26px',
              marginTop: 8,
              letterSpacing: -2,
              border: '5px solid #000000',
            }}
          >
            FORMS. PROCESS. CONTACTS.
          </div>
        </div>

        {/* Left: Forms fanned out */}
        <div
          style={{
            position: 'absolute',
            bottom: 220,
            left: 200,
            width: 500,
            height: 360,
          }}
        >
          {[
            { lbl: 'FORM 12203', sub: 'Request for Appeals', rot: -12, x: 0, y: 0, op: form1, color: '#FFFFFF' },
            { lbl: 'PUB 5', sub: 'Your Appeal Rights', rot: 0, x: 70, y: -20, op: form2, color: '#FEEC41' },
            { lbl: 'PUB 1660', sub: 'Collection Appeal', rot: 12, x: 140, y: 0, op: form3, color: '#FFFFFF' },
          ].map((f, i) => (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: f.x,
                top: f.y,
                width: 260,
                height: 340,
                backgroundColor: f.color,
                border: '5px solid #000000',
                padding: 22,
                opacity: f.op,
                transform: `rotate(${f.rot * f.op}deg) translateY(${interpolate(f.op, [0, 1], [40, 0])}px)`,
                boxShadow: '8px 8px 0 rgba(0,0,0,0.2)',
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 16,
                  fontWeight: 700,
                  color: '#000000',
                  opacity: 0.6,
                  letterSpacing: 2,
                  marginBottom: 8,
                }}
              >
                IRS.GOV
              </div>
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 42,
                  fontWeight: 900,
                  color: '#000000',
                  lineHeight: 1,
                  marginBottom: 10,
                  letterSpacing: -1,
                }}
              >
                {f.lbl}
              </div>
              <div style={{ height: 3, backgroundColor: '#000000', marginBottom: 14 }} />
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 22,
                  fontWeight: 700,
                  color: '#000000',
                  lineHeight: 1.3,
                  marginBottom: 18,
                }}
              >
                {f.sub}
              </div>
              {/* Form lines */}
              {[0, 1, 2, 3].map((l) => (
                <div
                  key={l}
                  style={{
                    height: 8,
                    backgroundColor: '#000000',
                    opacity: 0.15,
                    marginBottom: 10,
                    width: `${90 - l * 8}%`,
                  }}
                />
              ))}
              <div
                style={{
                  marginTop: 16,
                  display: 'inline-block',
                  padding: '4px 10px',
                  border: '2px solid #000000',
                  fontFamily: 'JetBrains Mono',
                  fontSize: 14,
                  fontWeight: 700,
                  color: '#000000',
                }}
              >
                OFFICIAL ✓
              </div>
            </div>
          ))}
        </div>

        {/* Right: Scammer Phone with NEVER */}
        <div
          style={{
            position: 'absolute',
            bottom: 240,
            right: 200,
            opacity: phoneOp,
            transform: `scale(${phoneScale})`,
          }}
        >
          <svg width="280" height="380" viewBox="0 0 280 380">
            {/* Phone body */}
            <rect x="20" y="20" width="240" height="340" rx="30" fill="#000000" />
            {/* Screen */}
            <rect x="35" y="50" width="210" height="280" rx="6" fill="#1C1C1C" />
            <rect x="35" y="50" width="210" height="280" rx="6" fill="#FF0000" opacity="0.15" />
            {/* Speaker dot */}
            <circle cx="140" cy="345" r="8" fill="#FEEC41" />
            {/* Incoming call icon */}
            <text x="140" y="180" fontFamily="Inter" fontSize="22" fontWeight="800" fill="#FF0000" textAnchor="middle">
              SCAMMER
            </text>
            <text x="140" y="220" fontFamily="JetBrains Mono" fontSize="14" fontWeight="700" fill="#FEEC41" textAnchor="middle">
              talking about:
            </text>
            <text x="140" y="260" fontFamily="Inter" fontSize="38" fontWeight="900" fill="#FFFFFF" textAnchor="middle">
              ZERO
            </text>
            <text x="140" y="288" fontFamily="Inter" fontSize="18" fontWeight="700" fill="#FFFFFF" textAnchor="middle">
              rights mentioned
            </text>
          </svg>

          {/* NEVER stamp */}
          <div
            style={{
              position: 'absolute',
              top: 130,
              left: -100,
              transform: `scale(${neverScale}) rotate(${neverRot}deg)`,
              transformOrigin: 'center',
            }}
          >
            <div
              style={{
                border: '7px solid #FF0000',
                padding: '14px 32px',
                fontFamily: 'Inter',
                fontSize: 78,
                fontWeight: 900,
                color: '#FF0000',
                backgroundColor: 'rgba(255,255,255,0.05)',
                letterSpacing: 4,
              }}
            >
              NEVER
            </div>
          </div>
        </div>

        {/* Bottom label */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(200),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 36,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 2,
            }}
          >
            FORMS = REAL  /  SILENCE ON RIGHTS = FAKE
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene46FormsVsSilence;
```

```tsx
import React from 'react';
Audio: "Why? Because they don't want you to have rights. They don't want you to involve lawyers or judges. They want you to be a terrified victim who just hands over their credit card."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene47ScammerMotive',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene47ScammerMotive: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera subtle zoom
  const camScale = interpolate(frame, [0, 300], [1.02, 1.0], { extrapolateRight: 'clamp' });

  // WHY? huge text
  const whyScale = sp(8, { damping: 8, stiffness: 100 });

  // Three crossed-out items
  const item1Op = sp(50);
  const item2Op = sp(85);
  const item3Op = sp(120);

  // Strike line widths
  const strike1 = interpolate(sp(65), [0, 1], [0, 100]);
  const strike2 = interpolate(sp(100), [0, 1], [0, 100]);
  const strike3 = interpolate(sp(135), [0, 1], [0, 100]);

  // Credit card slide
  const cardOp = sp(170);
  const cardY = interpolate(sp(170), [0, 1], [80, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 100%, rgba(255,0,0,0.10) 0%, rgba(238,227,203,0) 50%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          backgroundColor: '#000000',
          opacity: 0.05,
          top: '-25%',
          left: '-15%',
          borderRadius: 50,
          transform: `rotate(${-15 + frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          border: '5px solid #FEEC41',
          opacity: 0.18,
          top: '20%',
          right: '-10%',
          transform: `rotate(${45 + frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="x47" width="50" height="50" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="50" y2="50" stroke="#000000" strokeWidth="1" />
            <line x1="50" y1="0" x2="0" y2="50" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#x47)" />
      </svg>

      {Array.from({ length: 18 }).map((_, i) => {
        const seed = i + 647;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 70) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.03 + i) * 18,
              top: y,
              width: 6,
              height: 6,
              backgroundColor: '#000000',
              opacity: 0.18,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* WHY? massive */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 130,
            transform: `scale(${whyScale})`,
            transformOrigin: 'left top',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 320,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.85,
              letterSpacing: -10,
            }}
          >
            WHY?
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              opacity: 0.7,
              marginTop: -10,
            }}
          >
            ━━━ THEY DON'T WANT YOU TO HAVE...
          </div>
        </div>

        {/* Three crossed items - right side */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            right: 130,
            width: 720,
          }}
        >
          {[
            { lbl: 'RIGHTS', op: item1Op, strike: strike1 },
            { lbl: 'LAWYERS', op: item2Op, strike: strike2 },
            { lbl: 'JUDGES', op: item3Op, strike: strike3 },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                marginBottom: 30,
                opacity: item.op,
                transform: `translateX(${interpolate(item.op, [0, 1], [60, 0])}px)`,
                position: 'relative',
              }}
            >
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 130,
                  fontWeight: 900,
                  color: '#000000',
                  lineHeight: 1,
                  letterSpacing: -2,
                  textAlign: 'right',
                }}
              >
                {item.lbl}
              </div>
              {/* Strike through */}
              <div
                style={{
                  position: 'absolute',
                  top: '50%',
                  right: 0,
                  height: 10,
                  width: `${item.strike}%`,
                  backgroundColor: '#FF0000',
                  transformOrigin: 'right',
                }}
              />
            </div>
          ))}
        </div>

        {/* Bottom credit card grab */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: cardOp,
            transform: `translateY(${cardY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#FF0000',
              letterSpacing: 4,
              marginBottom: 16,
            }}
          >
            THEY ONLY WANT →
          </div>
          <div style={{ display: 'inline-flex', alignItems: 'center', gap: 30 }}>
            {/* Credit card */}
            <svg width="280" height="180" viewBox="0 0 280 180">
              <rect x="5" y="5" width="270" height="170" rx="14" fill="#000000" />
              <rect x="15" y="15" width="250" height="40" fill="#FEEC41" />
              <text x="30" y="100" fontFamily="JetBrains Mono" fontSize="22" fontWeight="700" fill="#FEEC41" letterSpacing="3">
                •••• •••• ••••
              </text>
              <text x="30" y="135" fontFamily="JetBrains Mono" fontSize="28" fontWeight="800" fill="#FEEC41" letterSpacing="3">
                4 2 9 1
              </text>
              <text x="30" y="165" fontFamily="Inter" fontSize="14" fontWeight="700" fill="#FEEC41">
                YOUR CARD
              </text>
            </svg>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 70,
                fontWeight: 900,
                color: '#000000',
                backgroundColor: '#FEEC41',
                padding: '6px 26px',
                border: '5px solid #000000',
                letterSpacing: -1,
              }}
            >
              YOUR MONEY.
            </div>
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene47ScammerMotive;
```

```tsx
import React from 'react';
Audio: "If the caller tells you that you owe money, and you have no right to question it, or that you must pay immediately to avoid arrest, you have absolute, undeniable proof that you are talking to a criminal."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene48UndeniableProof',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene48UndeniableProof: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera shake at impact
  const shakeX = frame > 130 && frame < 160 ? Math.sin(frame * 1.2) * 4 : 0;
  const shakeY = frame > 130 && frame < 160 ? Math.cos(frame * 1.1) * 3 : 0;

  // Header
  const headOp = sp(8);

  // Quote bubbles
  const q1Op = sp(35);
  const q2Op = sp(70);

  // CONFIRMED stamp
  const confirmedScale = sp(130, { damping: 7, stiffness: 200 });
  const confirmedRot = interpolate(sp(130), [0, 1], [25, -10]);

  // CRIMINAL label
  const criminalOp = sp(170);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(255,0,0,0.10) 0%, rgba(238,227,203,0) 60%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          border: '6px solid #000000',
          opacity: 0.06,
          borderRadius: '50%',
          top: '50%',
          left: '50%',
          transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.05) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          backgroundColor: '#FF0000',
          opacity: 0.04,
          top: '10%',
          right: '5%',
          borderRadius: 30,
          transform: `rotate(${frame * 0.12}deg)`,
        }}
      />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="tr48" width="60" height="60" patternUnits="userSpaceOnUse">
            <polygon points="30,10 50,50 10,50" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#tr48)" />
      </svg>

      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i + 748;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 50) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.025 + i) * 16,
              top: y,
              width: 5,
              height: 5,
              backgroundColor: i % 3 === 0 ? '#FF0000' : '#000000',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shakeX}px, ${shakeY}px)` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headOp,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              opacity: 0.6,
              marginBottom: 14,
            }}
          >
            IF YOU HEAR THESE WORDS...
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 110,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.95,
              letterSpacing: -2.5,
            }}
          >
            UNDENIABLE
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 110,
              fontWeight: 900,
              color: '#FF0000',
              lineHeight: 0.95,
              letterSpacing: -2.5,
              marginTop: 4,
            }}
          >
            PROOF.
          </div>
        </div>

        {/* Two scammer quote bubbles */}
        <div
          style={{
            position: 'absolute',
            top: 460,
            left: 140,
            width: 700,
            opacity: q1Op,
            transform: `translateX(${interpolate(q1Op, [0, 1], [-60, 0])}px) rotate(-2deg)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#000000',
              padding: 30,
              border: '5px solid #FF0000',
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 18,
                fontWeight: 700,
                color: '#FF0000',
                letterSpacing: 3,
                marginBottom: 10,
              }}
            >
              SCAMMER SAYS:
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 48,
                fontWeight: 900,
                color: '#FEEC41',
                lineHeight: 1.1,
                letterSpacing: -1,
              }}
            >
              "You have NO right
              <br />
              to question this."
            </div>
            {/* Tail */}
            <div
              style={{
                position: 'absolute',
                bottom: -30,
                left: 80,
                width: 0,
                height: 0,
                borderLeft: '30px solid transparent',
                borderRight: '0 solid transparent',
                borderTop: '30px solid #000000',
              }}
            />
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            top: 460,
            right: 140,
            width: 700,
            opacity: q2Op,
            transform: `translateX(${interpolate(q2Op, [0, 1], [60, 0])}px) rotate(2deg)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#000000',
              padding: 30,
              border: '5px solid #FF0000',
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 18,
                fontWeight: 700,
                color: '#FF0000',
                letterSpacing: 3,
                marginBottom: 10,
              }}
            >
              SCAMMER SAYS:
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 48,
                fontWeight: 900,
                color: '#FEEC41',
                lineHeight: 1.1,
                letterSpacing: -1,
              }}
            >
              "Pay NOW or face
              <br />
              ARREST."
            </div>
            <div
              style={{
                position: 'absolute',
                bottom: -30,
                right: 80,
                width: 0,
                height: 0,
                borderLeft: '0 solid transparent',
                borderRight: '30px solid transparent',
                borderTop: '30px solid #000000',
              }}
            />
          </div>
        </div>

        {/* CRIMINAL CONFIRMED stamp */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: '50%',
            transform: `translateX(-50%) scale(${confirmedScale}) rotate(${confirmedRot}deg)`,
            opacity: criminalOp,
          }}
        >
          <div
            style={{
              border: '10px double #FF0000',
              padding: '20px 50px',
              backgroundColor: 'rgba(238,227,203,0.4)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 28,
                fontWeight: 700,
                color: '#FF0000',
                letterSpacing: 6,
                textAlign: 'center',
                marginBottom: 6,
              }}
            >
              ━━━ VERDICT ━━━
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 110,
                fontWeight: 900,
                color: '#FF0000',
                letterSpacing: 4,
                lineHeight: 1,
                textAlign: 'center',
              }}
            >
              CRIMINAL.
            </div>
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene48UndeniableProof;
```

```tsx
import React from 'react';
Audio: "So, what do you do when you get this call? First, do not engage. Do not try to outsmart them or give them fake information. Just hang up the phone."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene49JustHangUp',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene49JustHangUp: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, 270], [0, -0.8], { extrapolateRight: 'clamp' });

  // Question header
  const qOp = sp(8);

  // Don'ts list
  const dont1 = sp(40);
  const dont2 = sp(65);
  const dont3 = sp(90);

  // Phone hang up animation
  const phoneOp = sp(130);
  const phoneRot = interpolate(sp(150), [0, 1], [0, 75]);
  const phoneY = interpolate(sp(180), [0, 1], [0, 120]);

  // HANG UP text pop
  const hangScale = sp(200, { damping: 7, stiffness: 200 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(254,236,65,0.14) 0%, rgba(238,227,203,0) 60%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          backgroundColor: '#FEEC41',
          opacity: 0.07,
          top: '-15%',
          left: '-10%',
          borderRadius: 50,
          transform: `rotate(${frame * 0.1}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 450,
          height: 450,
          border: '4px solid #000000',
          opacity: 0.06,
          borderRadius: '50%',
          bottom: '5%',
          right: '5%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="ln49" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform="rotate(30)">
            <line x1="0" y1="30" x2="60" y2="30" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#ln49)" />
      </svg>

      {Array.from({ length: 20 }).map((_, i) => {
        const seed = i + 849;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 60) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y + Math.sin(frame * 0.03 + i) * 20,
              width: 6,
              height: 6,
              backgroundColor: '#000000',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)` }}>
        {/* Top question */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: qOp,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              opacity: 0.6,
              marginBottom: 10,
            }}
          >
            WHAT DO YOU DO?
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 100,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            DO. NOT. ENGAGE.
          </div>
        </div>

        {/* Don'ts list - left */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 160,
            width: 720,
          }}
        >
          {[
            { lbl: "DON'T TALK", sub: 'No conversation, no questions back', op: dont1 },
            { lbl: "DON'T OUTSMART", sub: "Don't try to play the scammer", op: dont2 },
            { lbl: "DON'T LIE BACK", sub: 'No fake info — just disconnect', op: dont3 },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 18,
                marginBottom: 32,
                opacity: item.op,
                transform: `translateX(${interpolate(item.op, [0, 1], [-50, 0])}px)`,
              }}
            >
              <div
                style={{
                  width: 70,
                  height: 70,
                  backgroundColor: '#000000',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  flexShrink: 0,
                }}
              >
                <svg width="40" height="40" viewBox="0 0 40 40">
                  <circle cx="20" cy="20" r="16" fill="none" stroke="#FF0000" strokeWidth="5" />
                  <line x1="9" y1="9" x2="31" y2="31" stroke="#FF0000" strokeWidth="5" strokeLinecap="round" />
                </svg>
              </div>
              <div>
                <div
                  style={{
                    fontFamily: 'Inter',
                    fontSize: 56,
                    fontWeight: 900,
                    color: '#000000',
                    lineHeight: 1,
                    letterSpacing: -1,
                  }}
                >
                  {item.lbl}
                </div>
                <div
                  style={{
                    fontFamily: 'Inter',
                    fontSize: 22,
                    fontWeight: 600,
                    color: '#000000',
                    opacity: 0.7,
                    marginTop: 4,
                  }}
                >
                  {item.sub}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Phone hang up - right */}
        <div
          style={{
            position: 'absolute',
            top: 400,
            right: 180,
            opacity: phoneOp,
            transform: `translateY(${phoneY}px) rotate(${phoneRot}deg)`,
            transformOrigin: 'center bottom',
          }}
        >
          <svg width="320" height="320" viewBox="0 0 320 320">
            {/* Old telephone receiver */}
            <g transform="rotate(-30 160 160)">
              <rect x="80" y="140" width="160" height="40" rx="20" fill="#000000" />
              <circle cx="100" cy="160" r="32" fill="#000000" />
              <circle cx="220" cy="160" r="32" fill="#000000" />
              <circle cx="100" cy="160" r="18" fill="#FEEC41" />
              <circle cx="220" cy="160" r="18" fill="#FEEC41" />
              <circle cx="100" cy="160" r="6" fill="#000000" />
              <circle cx="220" cy="160" r="6" fill="#000000" />
            </g>
            {/* Drop lines */}
            {frame > 170 && (
              <>
                <line x1="160" y1="50" x2="160" y2="80" stroke="#000000" strokeWidth="4" strokeLinecap="round" />
                <line x1="120" y1="55" x2="135" y2="78" stroke="#000000" strokeWidth="4" strokeLinecap="round" />
                <line x1="200" y1="55" x2="185" y2="78" stroke="#000000" strokeWidth="4" strokeLinecap="round" />
              </>
            )}
          </svg>

          {/* HANG UP big text */}
          <div
            style={{
              transform: `scale(${hangScale}) rotate(-4deg)`,
              transformOrigin: 'center',
              marginTop: -30,
              textAlign: 'center',
            }}
          >
            <div
              style={{
                display: 'inline-block',
                fontFamily: 'Inter',
                fontSize: 90,
                fontWeight: 900,
                color: '#000000',
                backgroundColor: '#FEEC41',
                padding: '6px 24px',
                border: '6px solid #000000',
                letterSpacing: -2,
                boxShadow: '8px 8px 0 #000000',
              }}
            >
              HANG UP.
            </div>
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene49JustHangUp;
```

```tsx
import React from 'react';
Audio: "If they leave a voicemail, delete it. Do not call the number they left, because it connects right back to the scam center."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene50DeleteVoicemail',
  durationInSeconds: 6,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene50DeleteVoicemail: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, 180], [1, 1.04], { extrapolateRight: 'clamp' });

  // Title
  const titleOp = sp(6);

  // Voicemail interface
  const vmOp = sp(25);
  const vmScale = sp(25, { damping: 9, stiffness: 130 });

  // Delete button press
  const deletePress = frame > 70 && frame < 90 ? 0.92 : 1;

  // Trash bin animation
  const trashOp = sp(85);
  const trashScale = sp(85, { damping: 8, stiffness: 180 });

  // Trap warning at bottom
  const trapOp = sp(120);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEE3CB', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 60% 50%, rgba(254,236,65,0.16) 0%, rgba(238,227,203,0) 55%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          border: '5px solid #000000',
          opacity: 0.06,
          borderRadius: 40,
          top: '10%',
          left: '-10%',
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          backgroundColor: '#FEEC41',
          opacity: 0.05,
          bottom: '-15%',
          right: '-5%',
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.05) * 0.06})`,
        }}
      />

      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="gr50" width="70" height="70" patternUnits="userSpaceOnUse">
            <path d="M 70 0 L 0 0 0 70" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gr50)" />
      </svg>

      {Array.from({ length: 16 }).map((_, i) => {
        const seed = i + 950;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 40) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.03 + i) * 20,
              top: y,
              width: 6,
              height: 6,
              backgroundColor: '#000000',
              opacity: 0.2,
              borderRadius: '50%',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${interpolate(titleOp, [0, 1], [-40, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 6,
              opacity: 0.6,
              marginBottom: 10,
            }}
          >
            VOICEMAIL LEFT? →
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 130,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1,
              letterSpacing: -2.5,
            }}
          >
            DELETE.{' '}
            <span style={{ color: '#FF0000', WebkitTextStroke: '3px #000000' }}>DON'T</span> CALL.
          </div>
        </div>

        {/* Voicemail interface - left */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 200,
            opacity: vmOp,
            transform: `scale(${vmScale})`,
          }}
        >
          <div
            style={{
              width: 540,
              backgroundColor: '#000000',
              border: '5px solid #FEEC41',
              padding: 24,
              borderRadius: 18,
            }}
          >
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 18 }}>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 18,
                  fontWeight: 700,
                  color: '#FEEC41',
                  letterSpacing: 3,
                }}
              >
                VOICEMAIL — INBOX
              </div>
              <div
                style={{
                  width: 14,
                  height: 14,
                  backgroundColor: '#FF0000',
                  borderRadius: '50%',
                  opacity: 0.5 + Math.sin(frame * 0.3) * 0.5,
                }}
              />
            </div>

            {/* Voicemail entry */}
            <div
              style={{
                backgroundColor: '#1C1C1C',
                border: '2px solid #FF0000',
                padding: 16,
                marginBottom: 14,
              }}
            >
              <div
                style={{
                  fontFamily: 'Inter',
                  fontSize: 26,
                  fontWeight: 800,
                  color: '#FEEC41',
                  marginBottom: 6,
                }}
              >
                "IRS AGENT" — Unknown
              </div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 16, color: '#9E9E9E', marginBottom: 10 }}>
                +1 (555) 0XX-XXXX  •  0:47
              </div>
              {/* Waveform */}
              <svg width="100%" height="36" viewBox="0 0 480 36">
                {Array.from({ length: 30 }).map((_, i) => {
                  const h = 6 + seededRandom(i + 500) * 28;
                  return (
                    <rect
                      key={i}
                      x={i * 16}
                      y={18 - h / 2}
                      width="8"
                      height={h}
                      fill="#FF0000"
                      opacity={frame > 40 + i * 2 ? 0.8 : 0.2}
                    />
                  );
                })}
              </svg>
            </div>

            {/* Delete button */}
            <div
              style={{
                display: 'flex',
                justifyContent: 'center',
                marginTop: 8,
                transform: `scale(${deletePress})`,
              }}
            >
              <div
                style={{
                  backgroundColor: '#FF0000',
                  border: '4px solid #FEEC41',
                  padding: '14px 38px',
                  fontFamily: 'Inter',
                  fontSize: 32,
                  fontWeight: 900,
                  color: '#FFFFFF',
                  letterSpacing: 3,
                  borderRadius: 8,
                }}
              >
                DELETE
              </div>
            </div>
          </div>
        </div>

        {/* Trash bin - right */}
        <div
          style={{
            position: 'absolute',
            bottom: 220,
            right: 250,
            opacity: trashOp,
            transform: `scale(${trashScale})`,
          }}
        >
          <svg width="240" height="280" viewBox="0 0 240 280">
            {/* Lid */}
            <rect
              x="20"
              y="50"
              width="200"
              height="22"
              fill="#000000"
              style={{ transform: `rotate(${interpolate(sp(100), [0, 1], [0, -15])}deg)`, transformOrigin: '20px 60px' }}
            />
            <rect x="90" y="40" width="60" height="14" rx="6" fill="#000000" />
            {/* Bin body */}
            <path d="M 30 80 L 50 270 L 190 270 L 210 80 Z" fill="#000000" />
            <line x1="80" y1="100" x2="90" y2="250" stroke="#FEEC41" strokeWidth="3" />
            <line x1="120" y1="100" x2="120" y2="250" stroke="#FEEC41" strokeWidth="3" />
            <line x1="160" y1="100" x2="150" y2="250" stroke="#FEEC41" strokeWidth="3" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 800,
              color: '#000000',
              textAlign: 'center',
              marginTop: 4,
              letterSpacing: 1,
            }}
          >
            STRAIGHT TO TRASH
          </div>
        </div>

        {/* Bottom warning */}
        <div
          style={{
            position: 'absolute',
            bottom: 50,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: trapOp,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '12px 32px',
              backgroundColor: '#000000',
              color: '#FEEC41',
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 800,
              letterSpacing: 2,
              border: '3px solid #FEEC41',
            }}
          >
            CALLBACK = SCAM CENTER TRAP
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.5,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene50DeleteVoicemail;
```

