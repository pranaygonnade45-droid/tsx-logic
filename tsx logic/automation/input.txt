```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SubscriptionTrap',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SubscriptionTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera zoom
  const cameraScale = interpolate(frame, [0, 180], [1.2, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Subscription icons data
  const subscriptions = [
    { name: 'Netflix', color: '#E50914', x: 200, y: 150 },
    { name: 'Prime', color: '#00A8E1', x: 500, y: 200 },
    { name: 'Hotstar', color: '#0F1014', x: 800, y: 150 },
    { name: 'Spotify', color: '#1DB954', x: 1100, y: 250 },
    { name: 'Fitness', color: '#FF6B35', x: 1400, y: 180 },
    { name: 'Meditation', color: '#6C63FF', x: 300, y: 450 },
    { name: 'News', color: '#FFD700', x: 700, y: 500 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle at 30% 40%, rgba(233, 215, 0, 0.12), transparent 60%)',
        }}
      />

      {/* Animated Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: '600px',
          height: '600px',
          left: '10%',
          top: '5%',
          borderRadius: '50%',
          border: '2px solid rgba(233, 215, 0, 0.06)',
          transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          opacity: 0.4,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: '400px',
          height: '400px',
          right: '15%',
          bottom: '10%',
          borderRadius: '30%',
          border: '2px solid rgba(255, 193, 7, 0.05)',
          transform: `rotate(${-frame * 0.2}deg)`,
          opacity: 0.3,
        }}
      />

      {/* Main Content Container with Camera */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          transform: `scale(${cameraScale})`,
        }}
      >
        {/* Title */}
        <div
          style={{
            transform: `translateY(${(1 - sp(0)) * -80}px)`,
            opacity: sp(0),
            marginBottom: '60px',
          }}
        >
          <h1
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '140px',
              fontWeight: 900,
              color: '#e9d700',
              margin: 0,
              textAlign: 'center',
              textShadow: '0 0 40px rgba(233, 215, 0, 0.4)',
            }}
          >
            Subscription का जाल
          </h1>
        </div>

        {/* Subscription Cards Container */}
        <div
          style={{
            position: 'relative',
            width: '1600px',
            height: '600px',
          }}
        >
          {subscriptions.map((sub, i) => {
            const cardSpring = sp(20 + i * 8);
            const floatY = Math.sin((frame + i * 30) * 0.04) * 8;
            const rotateVal = Math.sin((frame + i * 20) * 0.03) * 2;

            return (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  left: sub.x,
                  top: sub.y,
                  transform: `translateY(${(1 - cardSpring) * 100 + floatY}px) rotate(${rotateVal}deg)`,
                  opacity: cardSpring,
                }}
              >
                <div
                  style={{
                    width: '180px',
                    height: '100px',
                    backgroundColor: '#2D2D2D',
                    borderRadius: '16px',
                    border: `3px solid ${sub.color}`,
                    display: 'flex',
                    flexDirection: 'column',
                    justifyContent: 'center',
                    alignItems: 'center',
                    boxShadow: `0 8px 30px ${sub.color}40`,
                    position: 'relative',
                    overflow: 'hidden',
                  }}
                >
                  {/* Card glow */}
                  <div
                    style={{
                      position: 'absolute',
                      top: 0,
                      left: `-${frame % 200}px`,
                      width: '50px',
                      height: '100%',
                      background: `linear-gradient(90deg, transparent, ${sub.color}30, transparent)`,
                    }}
                  />

                  <span
                    style={{
                      fontFamily: 'Inter, sans-serif',
                      fontSize: '28px',
                      fontWeight: 700,
                      color: sub.color,
                      marginBottom: '4px',
                    }}
                  >
                    {sub.name}
                  </span>
                  <span
                    style={{
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: '20px',
                      fontWeight: 600,
                      color: '#9E9E9E',
                    }}
                  >
                    ₹{99 + i * 50}/माह
                  </span>
                </div>
              </div>
            );
          })}
        </div>

        {/* Connecting Lines - representing the web/trap */}
        <svg
          width="1600"
          height="600"
          style={{
            position: 'absolute',
            top: '50%',
            left: '50%',
            transform: 'translate(-50%, -50%)',
            opacity: interpolate(frame, [40, 80], [0, 0.15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            pointerEvents: 'none',
          }}
        >
          {subscriptions.map((sub, i) =>
            subscriptions.slice(i + 1).map((target, j) => (
              <line
                key={`${i}-${j}`}
                x1={sub.x + 90}
                y1={sub.y + 50}
                x2={target.x + 90}
                y2={target.y + 50}
                stroke="#e9d700"
                strokeWidth="2"
                strokeDasharray="8,8"
                strokeDashoffset={-frame * 2}
              />
            ))
          )}
        </svg>
      </div>

      {/* Stick Figure Character */}
      <div
        style={{
          position: 'absolute',
          bottom: '120px',
          right: '180px',
          transform: `translateY(${(1 - sp(60)) * 150}px)`,
          opacity: sp(60),
        }}
      >
        <svg width="140" height="200" viewBox="0 0 140 200">
          {/* Golden glow */}
          <circle cx="70" cy="30" r="35" fill="rgba(255, 193, 7, 0.15)" />

          {/* Head */}
          <circle
            cx="70"
            cy="30"
            r="22"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            transform={`rotate(${Math.sin(frame * 0.04) * 3} 70 30)`}
          />

          {/* Glasses */}
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="62" cy="30" r="7" />
            <circle cx="78" cy="30" r="7" />
            <line x1="69" y1="30" x2="71" y2="30" />
          </g>

          {/* Body */}
          <line x1="70" y1="52" x2="70" y2="120" stroke="#e9d700" strokeWidth="4" />

          {/* Arms - stressed position */}
          <line
            x1="70"
            y1="70"
            x2={40 + Math.sin(frame * 0.08) * 5}
            y2="95"
            stroke="#e9d700"
            strokeWidth="4"
          />
          <line
            x1="70"
            y1="70"
            x2={100 - Math.sin(frame * 0.08) * 5}
            y2="95"
            stroke="#e9d700"
            strokeWidth="4"
          />

          {/* Legs */}
          <line x1="70" y1="120" x2="50" y2="170" stroke="#e9d700" strokeWidth="4" />
          <line x1="70" y1="120" x2="90" y2="170" stroke="#e9d700" strokeWidth="4" />

          {/* Breathing animation */}
          <circle
            cx="70"
            cy="30"
            r="22"
            fill="none"
            stroke="rgba(233, 215, 0, 0.3)"
            strokeWidth="2"
            transform={`scale(${1 + Math.sin(frame * 0.05) * 0.05})`}
            style={{ transformOrigin: '70px 30px' }}
          />
        </svg>
      </div>

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 40%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default SubscriptionTrap;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SubscriptionMath',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SubscriptionMath: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Counter animation for total
  const totalAmount = Math.floor(interpolate(frame, [60, 120], [0, 3000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  const subscriptionAmounts = [
    { name: 'Netflix', amount: 199 },
    { name: 'Prime', amount: 299 },
    { name: 'Hotstar', amount: 399 },
    { name: 'Spotify', amount: 119 },
    { name: 'Fitness App', amount: 499 },
    { name: 'Meditation', amount: 249 },
    { name: 'News App', amount: 99 },
    { name: 'Others', amount: 339 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'linear-gradient(135deg, rgba(255, 0, 0, 0.08) 0%, rgba(233, 215, 0, 0.08) 100%)',
        }}
      />

      {/* Animated Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: '700px',
          height: '700px',
          left: '-100px',
          top: '200px',
          borderRadius: '40%',
          border: '3px solid rgba(255, 0, 0, 0.04)',
          transform: `rotate(${frame * 0.2}deg)`,
          opacity: 0.6,
        }}
      />

      {/* Floating rupee symbols */}
      {[...Array(8)].map((_, i) => {
        const yOffset = interpolate((frame + i * 30) % 180, [0, 180], [0, -1080], { extrapolateRight: 'clamp' });
        const xPos = 200 + i * 200;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: xPos,
              top: 1080,
              transform: `translateY(${yOffset}px)`,
              opacity: 0.1,
              fontSize: '120px',
              color: '#e9d700',
              fontFamily: 'JetBrains Mono, monospace',
            }}
          >
            ₹
          </div>
        );
      })}

      {/* Main Content */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
        }}
      >
        {/* Title */}
        <div
          style={{
            transform: `translateY(${(1 - sp(0)) * -60}px)`,
            opacity: sp(0),
            marginBottom: '50px',
          }}
        >
          <h2
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '90px',
              fontWeight: 800,
              color: '#F5F5F5',
              margin: 0,
              textAlign: 'center',
            }}
          >
            महीने का हिसाब
          </h2>
        </div>

        {/* Subscription List */}
        <div
          style={{
            display: 'flex',
            flexDirection: 'column',
            gap: '18px',
            marginBottom: '60px',
          }}
        >
          {subscriptionAmounts.map((sub, i) => {
            const itemSpring = sp(15 + i * 6);
            return (
              <div
                key={i}
                style={{
                  transform: `translateX(${(1 - itemSpring) * -100}px)`,
                  opacity: itemSpring,
                  display: 'flex',
                  alignItems: 'center',
                  gap: '30px',
                }}
              >
                <div
                  style={{
                    width: '18px',
                    height: '18px',
                    borderRadius: '50%',
                    backgroundColor: '#e9d700',
                    boxShadow: '0 0 15px rgba(233, 215, 0, 0.5)',
                  }}
                />
                <span
                  style={{
                    fontFamily: 'Noto Sans Devanagari, sans-serif',
                    fontSize: '42px',
                    fontWeight: 600,
                    color: '#F5F5F5',
                    width: '300px',
                  }}
                >
                  {sub.name}
                </span>
                <span
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: '48px',
                    fontWeight: 700,
                    color: '#ff0000',
                  }}
                >
                  ₹{sub.amount}
                </span>
              </div>
            );
          })}
        </div>

        {/* Divider Line */}
        <div
          style={{
            width: interpolate(frame, [100, 140], [0, 800], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            height: '4px',
            backgroundColor: '#e9d700',
            marginBottom: '40px',
            boxShadow: '0 0 20px rgba(233, 215, 0, 0.6)',
          }}
        />

        {/* Total Amount */}
        <div
          style={{
            transform: `scale(${sp(110, { damping: 8, stiffness: 100 })})`,
            opacity: sp(110),
          }}
        >
          <div
            style={{
              backgroundColor: '#2D2D2D',
              padding: '35px 80px',
              borderRadius: '20px',
              border: '4px solid #ff0000',
              boxShadow: '0 10px 50px rgba(255, 0, 0, 0.4)',
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            {/* Animated background glow */}
            <div
              style={{
                position: 'absolute',
                top: 0,
                left: `-${frame % 300}px`,
                width: '100px',
                height: '100%',
                background: 'linear-gradient(90deg, transparent, rgba(255, 0, 0, 0.2), transparent)',
              }}
            />

            <div style={{ display: 'flex', alignItems: 'baseline', gap: '25px', position: 'relative' }}>
              <span
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: '60px',
                  fontWeight: 700,
                  color: '#F5F5F5',
                }}
              >
                कुल खर्च:
              </span>
              <span
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: '110px',
                  fontWeight: 900,
                  color: '#ff0000',
                  textShadow: '0 0 30px rgba(255, 0, 0, 0.6)',
                }}
              >
                ₹{totalAmount}
              </span>
              <span
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: '50px',
                  fontWeight: 600,
                  color: '#9E9E9E',
                }}
              >
                /माह
              </span>
            </div>
          </div>
        </div>

        {/* Warning Text */}
        <div
          style={{
            transform: `translateY(${(1 - sp(150)) * 50}px)`,
            opacity: sp(150),
            marginTop: '50px',
          }}
        >
          <p
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '48px',
              fontWeight: 700,
              color: '#e9d700',
              margin: 0,
              textAlign: 'center',
            }}
          >
            साल में ₹36,000+ !
          </p>
        </div>
      </div>

      {/* Stick Figure Character - Shocked */}
      <div
        style={{
          position: 'absolute',
          bottom: '100px',
          right: '150px',
          transform: `translateY(${(1 - sp(70)) * 150}px)`,
          opacity: sp(70),
        }}
      >
        <svg width="150" height="210" viewBox="0 0 150 210">
          {/* Golden glow */}
          <circle cx="75" cy="35" r="40" fill="rgba(255, 193, 7, 0.12)" />

          {/* Head */}
          <circle cx="75" cy="35" r="24" fill="none" stroke="#e9d700" strokeWidth="4" />

          {/* Glasses */}
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="66" cy="35" r="8" />
            <circle cx="84" cy="35" r="8" />
            <line x1="74" y1="35" x2="76" y2="35" />
          </g>

          {/* Shocked expression - open mouth */}
          <circle cx="75" cy="45" r="4" fill="#ff0000" />

          {/* Body */}
          <line x1="75" y1="59" x2="75" y2="130" stroke="#e9d700" strokeWidth="4" />

          {/* Arms raised in shock */}
          <line
            x1="75"
            y1="75"
            x2={45 + Math.sin(frame * 0.15) * 3}
            y2={50 + Math.sin(frame * 0.15) * 2}
            stroke="#e9d700"
            strokeWidth="4"
          />
          <line
            x1="75"
            y1="75"
            x2={105 - Math.sin(frame * 0.15) * 3}
            y2={50 - Math.sin(frame * 0.15) * 2}
            stroke="#e9d700"
            strokeWidth="4"
          />

          {/* Legs */}
          <line x1="75" y1="130" x2="55" y2="180" stroke="#e9d700" strokeWidth="4" />
          <line x1="75" y1="130" x2="95" y2="180" stroke="#e9d700" strokeWidth="4" />

          {/* Exclamation marks */}
          <text x="110" y="30" fill="#ff0000" fontSize="40" fontWeight="900">
            !
          </text>
          <text x="25" y="30" fill="#ff0000" fontSize="40" fontWeight="900">
            !
          </text>
        </svg>
      </div>

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.7) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default SubscriptionMath;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'UnusedSubscriptions',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const UnusedSubscriptions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera pan
  const cameraPanX = interpolate(frame, [0, 240], [0, -50], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const apps = [
    { name: 'App 1', used: 0, color: '#ff0000' },
    { name: 'App 2', used: 2, color: '#e9d700' },
    { name: 'App 3', used: 0, color: '#ff0000' },
    { name: 'App 4', used: 15, color: '#FFC107' },
    { name: 'App 5', used: 0, color: '#ff0000' },
    { name: 'App 6', used: 1, color: '#ff0000' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle at 70% 30%, rgba(255, 0, 0, 0.1), transparent 65%)',
        }}
      />

      {/* Animated Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: '500px',
          height: '500px',
          right: '100px',
          top: '100px',
          borderRadius: '50%',
          border: '2px solid rgba(233, 215, 0, 0.05)',
          transform: `rotate(${frame * 0.25}deg) scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
          opacity: 0.5,
        }}
      />

      {/* Dust particle effect */}
      {[...Array(20)].map((_, i) => {
        const x = seededRandom(i * 123) * 1920;
        const y = seededRandom(i * 456) * 1080;
        const delay = seededRandom(i * 789) * 100;
        const particleOp = interpolate((frame + delay) % 200, [0, 100, 200], [0, 0.3, 0], {
          extrapolateLeft: 'clamp',
          extrapolateRight: 'clamp',
        });
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: '4px',
              height: '4px',
              borderRadius: '50%',
              backgroundColor: '#9E9E9E',
              opacity: particleOp,
            }}
          />
        );
      })}

      {/* Main Content with Camera */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          transform: `translateX(${cameraPanX}px)`,
        }}
      >
        {/* Title */}
        <div
          style={{
            transform: `translateY(${(1 - sp(0)) * -70}px)`,
            opacity: sp(0),
            marginBottom: '80px',
          }}
        >
          <h1
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '120px',
              fontWeight: 900,
              color: '#F5F5F5',
              margin: 0,
              textAlign: 'center',
              lineHeight: 1.2,
            }}
          >
            पिछले 30 दिनों में<br />
            <span style={{ color: '#ff0000' }}>कितनी बार यूज़ किया?</span>
          </h1>
        </div>

        {/* Apps Usage Grid */}
        <div
          style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(3, 1fr)',
            gap: '40px',
          }}
        >
          {apps.map((app, i) => {
            const cardSpring = sp(25 + i * 10);
            const shakeIntensity = app.used === 0 ? Math.sin(frame * 0.2) * 3 : 0;

            return (
              <div
                key={i}
                style={{
                  transform: `translateY(${(1 - cardSpring) * 120}px) rotate(${shakeIntensity}deg)`,
                  opacity: cardSpring,
                }}
              >
                <div
                  style={{
                    width: '340px',
                    height: '200px',
                    backgroundColor: '#2D2D2D',
                    borderRadius: '20px',
                    border: `4px solid ${app.color}`,
                    display: 'flex',
                    flexDirection: 'column',
                    justifyContent: 'center',
                    alignItems: 'center',
                    gap: '20px',
                    boxShadow: `0 10px 40px ${app.color}30`,
                    position: 'relative',
                    overflow: 'hidden',
                  }}
                >
                  {/* Warning glow for unused apps */}
                  {app.used === 0 && (
                    <div
                      style={{
                        position: 'absolute',
                        width: '100%',
                        height: '100%',
                        background: `radial-gradient(circle, rgba(255,0,0,${0.1 + Math.sin(frame * 0.1) * 0.05}), transparent 70%)`,
                      }}
                    />
                  )}

                  <span
                    style={{
                      fontFamily: 'Noto Sans Devanagari, sans-serif',
                      fontSize: '48px',
                      fontWeight: 700,
                      color: '#F5F5F5',
                    }}
                  >
                    Subscription {i + 1}
                  </span>

                  <div
                    style={{
                      fontSize: '90px',
                      fontWeight: 900,
                      fontFamily: 'JetBrains Mono, monospace',
                      color: app.color,
                      textShadow: `0 0 20px ${app.color}80`,
                    }}
                  >
                    {app.used}
                  </div>

                  <span
                    style={{
                      fontFamily: 'Noto Sans Devanagari, sans-serif',
                      fontSize: '32px',
                      fontWeight: 600,
                      color: '#9E9E9E',
                    }}
                  >
                    बार
                  </span>

                  {/* Cross mark for unused */}
                  {app.used === 0 && (
                    <div
                      style={{
                        position: 'absolute',
                        top: '15px',
                        right: '15px',
                        width: '50px',
                        height: '50px',
                        opacity: interpolate(frame, [100 + i * 10, 140 + i * 10], [0, 1], {
                          extrapolateLeft: 'clamp',
                          extrapolateRight: 'clamp',
                        }),
                      }}
                    >
                      <svg width="50" height="50" viewBox="0 0 50 50">
                        <line x1="10" y1="10" x2="40" y2="40" stroke="#ff0000" strokeWidth="6" strokeLinecap="round" />
                        <line x1="40" y1="10" x2="10" y2="40" stroke="#ff0000" strokeWidth="6" strokeLinecap="round" />
                      </svg>
                    </div>
                  )}
                </div>
              </div>
            );
          })}
        </div>

        {/* Action CTA */}
        <div
          style={{
            transform: `scale(${sp(120, { damping: 10, stiffness: 100 })})`,
            opacity: sp(120),
            marginTop: '70px',
          }}
        >
          <div
            style={{
              backgroundColor: '#e9d700',
              padding: '25px 60px',
              borderRadius: '16px',
              boxShadow: '0 8px 35px rgba(233, 215, 0, 0.4)',
            }}
          >
            <p
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '52px',
                fontWeight: 800,
                color: '#1C1C1C',
                margin: 0,
              }}
            >
              अभी नहीं, वीडियो के बाद!
            </p>
          </div>
        </div>
      </div>

      {/* Stick Figure Character */}
      <div
        style={{
          position: 'absolute',
          bottom: '120px',
          left: '150px',
          transform: `translateY(${(1 - sp(80)) * 150}px)`,
          opacity: sp(80),
        }}
      >
        <svg width="160" height="220" viewBox="0 0 160 220">
          {/* Golden glow */}
          <circle cx="80" cy="35" r="42" fill="rgba(255, 193, 7, 0.1)" />

          {/* Head */}
          <circle
            cx="80"
            cy="35"
            r="25"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            transform={`rotate(${Math.sin(frame * 0.05) * 4} 80 35)`}
          />

          {/* Glasses */}
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="71" cy="35" r="8" />
            <circle cx="89" cy="35" r="8" />
            <line x1="79" y1="35" x2="81" y2="35" />
          </g>

          {/* Body */}
          <line x1="80" y1="60" x2="80" y2="135" stroke="#e9d700" strokeWidth="4" />

          {/* Arms - pointing gesture */}
          <line x1="80" y1="80" x2="40" y2="110" stroke="#e9d700" strokeWidth="4" />
          <line x1="80" y1="80" x2="120" y2="70" stroke="#e9d700" strokeWidth="4" />

          {/* Legs */}
          <line x1="80" y1="135" x2="60" y2="185" stroke="#e9d700" strokeWidth="4" />
          <line x1="80" y1="135" x2="100" y2="185" stroke="#e9d700" strokeWidth="4" />

          {/* Breathing */}
          <circle
            cx="80"
            cy="35"
            r="25"
            fill="none"
            stroke="rgba(233, 215, 0, 0.25)"
            strokeWidth="2"
            transform={`scale(${1 + Math.sin(frame * 0.05) * 0.06})`}
            style={{ transformOrigin: '80px 35px' }}
          />
        </svg>
      </div>

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.65) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default UnusedSubscriptions;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SubscriptionAudit',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SubscriptionAudit: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Checklist items
  const checklistItems = [
    { text: '1. सभी Subscriptions की लिस्ट बनाओ', delay: 20 },
    { text: '2. पिछले 30 दिनों का यूज देखो', delay: 50 },
    { text: '3. जो 0-1 बार यूज हुआ, उसे काटो', delay: 80 },
    { text: '4. महीने में कम से कम ₹1000 बचाओ', delay: 110 },
  ];

  // Strikethrough animation for cancelled subscriptions
  const strikeProgress = interpolate(frame, [140, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'linear-gradient(145deg, rgba(233, 215, 0, 0.06) 0%, rgba(255, 193, 7, 0.06) 100%)',
        }}
      />

      {/* Animated Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: '600px',
          height: '600px',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${frame * 0.15}deg)`,
          borderRadius: '30%',
          border: '2px solid rgba(233, 215, 0, 0.04)',
          opacity: 0.5,
        }}
      />

      {/* Grid texture */}
      <svg
        width="1920"
        height="1080"
        style={{
          position: 'absolute',
          opacity: 0.03,
        }}
      >
        <defs>
          <pattern id="grid" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#e9d700" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid)" />
      </svg>

      {/* Main Content */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
        }}
      >
        {/* Title */}
        <div
          style={{
            transform: `translateY(${(1 - sp(0)) * -80}px)`,
            opacity: sp(0),
            marginBottom: '70px',
          }}
        >
          <h1
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '130px',
              fontWeight: 900,
              color: '#e9d700',
              margin: 0,
              textAlign: 'center',
              textShadow: '0 0 50px rgba(233, 215, 0, 0.3)',
            }}
          >
            Subscription Audit
          </h1>
        </div>

        {/* Checklist Container */}
        <div
          style={{
            backgroundColor: '#2D2D2D',
            padding: '60px 80px',
            borderRadius: '30px',
            border: '4px solid #e9d700',
            boxShadow: '0 15px 60px rgba(233, 215, 0, 0.25)',
            minWidth: '1200px',
          }}
        >
          {checklistItems.map((item, i) => {
            const itemSpring = sp(item.delay);
            const checkboxScale = sp(item.delay + 15, { damping: 8, stiffness: 150 });

            return (
              <div
                key={i}
                style={{
                  transform: `translateX(${(1 - itemSpring) * -100}px)`,
                  opacity: itemSpring,
                  display: 'flex',
                  alignItems: 'center',
                  gap: '35px',
                  marginBottom: i < checklistItems.length - 1 ? '50px' : 0,
                }}
              >
                {/* Checkbox */}
                <div
                  style={{
                    width: '60px',
                    height: '60px',
                    border: '5px solid #e9d700',
                    borderRadius: '12px',
                    display: 'flex',
                    justifyContent: 'center',
                    alignItems: 'center',
                    backgroundColor: '#1C1C1C',
                    transform: `scale(${checkboxScale})`,
                    boxShadow: '0 0 20px rgba(233, 215, 0, 0.3)',
                  }}
                >
                  {checkboxScale > 0.95 && (
                    <svg width="40" height="40" viewBox="0 0 40 40">
                      <path
                        d="M 8 20 L 16 28 L 32 12"
                        fill="none"
                        stroke="#e9d700"
                        strokeWidth="5"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                        strokeDasharray="50"
                        strokeDashoffset={50 - checkboxScale * 50}
                      />
                    </svg>
                  )}
                </div>

                {/* Text */}
                <p
                  style={{
                    fontFamily: 'Noto Sans Devanagari, sans-serif',
                    fontSize: '52px',
                    fontWeight: 700,
                    color: '#F5F5F5',
                    margin: 0,
                  }}
                >
                  {item.text}
                </p>
              </div>
            );
          })}
        </div>

        {/* Example Strikethrough Demo */}
        <div
          style={{
            transform: `translateY(${(1 - sp(130)) * 60}px)`,
            opacity: sp(130),
            marginTop: '60px',
          }}
        >
          <div
            style={{
              position: 'relative',
              padding: '20px 50px',
              backgroundColor: '#2D2D2D',
              borderRadius: '16px',
              border: '3px solid #ff0000',
            }}
          >
            <span
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '48px',
                fontWeight: 600,
                color: '#9E9E9E',
              }}
            >
              Subscription X — 0 बार यूज़
            </span>

            {/* Animated strikethrough */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: `${strikeProgress * 100}%`,
                height: '6px',
                backgroundColor: '#ff0000',
                boxShadow: '0 0 15px rgba(255, 0, 0, 0.6)',
              }}
            />
          </div>
        </div>
      </div>

      {/* Stick Figure Character - Taking notes */}
      <div
        style={{
          position: 'absolute',
          bottom: '100px',
          right: '180px',
          transform: `translateY(${(1 - sp(90)) * 150}px)`,
          opacity: sp(90),
        }}
      >
        <svg width="180" height="240" viewBox="0 0 180 240">
          {/* Golden glow */}
          <circle cx="90" cy="40" r="45" fill="rgba(255, 193, 7, 0.1)" />

          {/* Head */}
          <circle
            cx="90"
            cy="40"
            r="26"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            transform={`rotate(${Math.sin(frame * 0.04) * 2} 90 40)`}
          />

          {/* Glasses */}
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="81" cy="40" r="8" />
            <circle cx="99" cy="40" r="8" />
            <line x1="89" y1="40" x2="91" y2="40" />
          </g>

          {/* Body */}
          <line x1="90" y1="66" x2="90" y2="145" stroke="#e9d700" strokeWidth="4" />

          {/* Left arm holding clipboard */}
          <line x1="90" y1="85" x2="50" y2="120" stroke="#e9d700" strokeWidth="4" />

          {/* Right arm with pen */}
          <line
            x1="90"
            y1="85"
            x2={110 + Math.sin(frame * 0.15) * 5}
            y2={105 + Math.sin(frame * 0.15) * 3}
            stroke="#e9d700"
            strokeWidth="4"
          />

          {/* Clipboard */}
          <rect x="20" y="110" width="40" height="55" fill="#2D2D2D" stroke="#e9d700" strokeWidth="3" rx="4" />
          <line x1="25" y1="120" x2="55" y2="120" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="25" y1="135" x2="55" y2="135" stroke="#F5F5F5" strokeWidth="2" />
          <line x1="25" y1="150" x2="55" y2="150" stroke="#F5F5F5" strokeWidth="2" />

          {/* Pen */}
          <line
            x1={110 + Math.sin(frame * 0.15) * 5}
            y1={105 + Math.sin(frame * 0.15) * 3}
            x2={120 + Math.sin(frame * 0.15) * 5}
            y2={95 + Math.sin(frame * 0.15) * 3}
            stroke="#e9d700"
            strokeWidth="3"
          />

          {/* Legs */}
          <line x1="90" y1="145" x2="70" y2="200" stroke="#e9d700" strokeWidth="4" />
          <line x1="90" y1="145" x2="110" y2="200" stroke="#e9d700" strokeWidth="4" />

          {/* Breathing */}
          <circle
            cx="90"
            cy="40"
            r="26"
            fill="none"
            stroke="rgba(233, 215, 0, 0.2)"
            strokeWidth="2"
            transform={`scale(${1 + Math.sin(frame * 0.05) * 0.05})`}
            style={{ transformOrigin: '90px 40px' }}
          />
        </svg>
      </div>

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default SubscriptionAudit;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SocialComparison',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SocialComparison: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera zoom out
  const cameraScale = interpolate(frame, [0, 240], [1.3, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Comparison triggers
  const triggers = [
    { icon: '🏠', text: 'पड़ोसी की नई कार', color: '#ff0000' },
    { icon: '✈️', text: 'रिश्तेदार यूरोप गया', color: '#ff0000' },
    { icon: '🏢', text: 'दोस्त ने प्रीमियम फ्लैट लिया', color: '#ff0000' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle at 50% 50%, rgba(255, 0, 0, 0.12), transparent 70%)',
        }}
      />

      {/* Animated Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: '800px',
          height: '800px',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${frame * 0.2}deg)`,
          border: '3px solid rgba(255, 0, 0, 0.05)',
          borderRadius: '40%',
          opacity: 0.6,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: '500px',
          height: '500px',
          left: '20%',
          top: '15%',
          transform: `rotate(${-frame * 0.15}deg)`,
          border: '2px solid rgba(233, 215, 0, 0.04)',
          borderRadius: '50%',
          opacity: 0.4,
        }}
      />

      {/* Main Content with Camera */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          transform: `scale(${cameraScale})`,
        }}
      >
        {/* Title */}
        <div
          style={{
            transform: `translateY(${(1 - sp(0)) * -100}px)`,
            opacity: sp(0),
            marginBottom: '80px',
          }}
        >
          <h1
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '150px',
              fontWeight: 900,
              color: '#ff0000',
              margin: 0,
              textAlign: 'center',
              textShadow: '0 0 60px rgba(255, 0, 0, 0.4)',
              lineHeight: 1.1,
            }}
          >
            Social Comparison
            <br />
            <span style={{ fontSize: '100px', color: '#F5F5F5' }}>का बोझ</span>
          </h1>
        </div>

        {/* Comparison Triggers */}
        <div
          style={{
            display: 'flex',
            gap: '50px',
            marginBottom: '70px',
          }}
        >
          {triggers.map((trigger, i) => {
            const triggerSpring = sp(30 + i * 20);
            const pulseScale = 1 + Math.sin((frame + i * 60) * 0.08) * 0.05;

            return (
              <div
                key={i}
                style={{
                  transform: `translateY(${(1 - triggerSpring) * 120}px) scale(${pulseScale})`,
                  opacity: triggerSpring,
                }}
              >
                <div
                  style={{
                    width: '380px',
                    height: '240px',
                    backgroundColor: '#2D2D2D',
                    borderRadius: '24px',
                    border: `4px solid ${trigger.color}`,
                    display: 'flex',
                    flexDirection: 'column',
                    justifyContent: 'center',
                    alignItems: 'center',
                    gap: '25px',
                    boxShadow: `0 12px 50px ${trigger.color}40`,
                    position: 'relative',
                    overflow: 'hidden',
                  }}
                >
                  {/* Pulsing glow */}
                  <div
                    style={{
                      position: 'absolute',
                      width: '100%',
                      height: '100%',
                      background: `radial-gradient(circle, rgba(255,0,0,${0.1 + Math.sin(frame * 0.1) * 0.05}), transparent 70%)`,
                    }}
                  />

                  <div style={{ fontSize: '100px', position: 'relative' }}>{trigger.icon}</div>

                  <p
                    style={{
                      fontFamily: 'Noto Sans Devanagari, sans-serif',
                      fontSize: '44px',
                      fontWeight: 700,
                      color: '#F5F5F5',
                      margin: 0,
                      textAlign: 'center',
                      padding: '0 20px',
                      position: 'relative',
                    }}
                  >
                    {trigger.text}
                  </p>
                </div>
              </div>
            );
          })}
        </div>

        {/* Arrow pointing down */}
        <div
          style={{
            transform: `translateY(${(1 - sp(90)) * -50}px)`,
            opacity: sp(90),
            marginBottom: '40px',
          }}
        >
          <svg width="100" height="100" viewBox="0 0 100 100">
            <path
              d="M 50 10 L 50 70"
              fill="none"
              stroke="#e9d700"
              strokeWidth="6"
              strokeLinecap="round"
              strokeDasharray="100"
              strokeDashoffset={100 - sp(90) * 100}
            />
            <path
              d="M 30 55 L 50 75 L 70 55"
              fill="none"
              stroke="#e9d700"
              strokeWidth="6"
              strokeLinecap="round"
              strokeLinejoin="round"
              opacity={sp(100)}
            />
          </svg>
        </div>

        {/* Result - Unnecessary Purchase */}
        <div
          style={{
            transform: `scale(${sp(110, { damping: 10, stiffness: 90 })})`,
            opacity: sp(110),
          }}
        >
          <div
            style={{
              backgroundColor: '#2D2D2D',
              padding: '40px 70px',
              borderRadius: '20px',
              border: '4px solid #ff0000',
              boxShadow: '0 15px 60px rgba(255, 0, 0, 0.5)',
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            {/* Screen shake effect */}
            <div
              style={{
                position: 'absolute',
                top: 0,
                left: 0,
                width: '100%',
                height: '100%',
                background: 'linear-gradient(90deg, transparent, rgba(255,0,0,0.15), transparent)',
                transform: `translateX(${(frame % 60) * 20 - 600}px)`,
              }}
            />

            <p
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '70px',
                fontWeight: 900,
                color: '#ff0000',
                margin: 0,
                textAlign: 'center',
                position: 'relative',
                textShadow: '0 0 30px rgba(255, 0, 0, 0.6)',
              }}
            >
              बेवजह की खरीदारी
            </p>
          </div>
        </div>

        {/* Subtitle */}
        <div
          style={{
            transform: `translateY(${(1 - sp(140)) * 40}px)`,
            opacity: sp(140),
            marginTop: '50px',
          }}
        >
          <p
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '52px',
              fontWeight: 700,
              color: '#9E9E9E',
              margin: 0,
              textAlign: 'center',
            }}
          >
            जो ज़रूरत से नहीं, Comparison से होती है
          </p>
        </div>
      </div>

      {/* Multiple Stick Figures - Showing society pressure */}
      <div
        style={{
          position: 'absolute',
          bottom: '80px',
          left: '100px',
          transform: `translateY(${(1 - sp(120)) * 150}px)`,
          opacity: sp(120),
          display: 'flex',
          gap: '40px',
        }}
      >
        {[0, 1, 2].map((idx) => (
          <svg key={idx} width="120" height="180" viewBox="0 0 120 180">
            <circle cx="60" cy="30" r="30" fill="rgba(233, 215, 0, 0.08)" />
            <circle cx="60" cy="30" r="20" fill="none" stroke="#9E9E9E" strokeWidth="3" />
            <g stroke="#9E9E9E" strokeWidth="2" fill="none">
              <circle cx="53" cy="30" r="6" />
              <circle cx="67" cy="30" r="6" />
              <line x1="59" y1="30" x2="61" y2="30" />
            </g>
            <line x1="60" y1="50" x2="60" y2="110" stroke="#9E9E9E" strokeWidth="3" />
            <line
              x1="60"
              y1="65"
              x2={40 + Math.sin((frame + idx * 40) * 0.1) * 3}
              y2="85"
              stroke="#9E9E9E"
              strokeWidth="3"
            />
            <line
              x1="60"
              y1="65"
              x2={80 - Math.sin((frame + idx * 40) * 0.1) * 3}
              y2="85"
              stroke="#9E9E9E"
              strokeWidth="3"
            />
            <line x1="60" y1="110" x2="45" y2="150" stroke="#9E9E9E" strokeWidth="3" />
            <line x1="60" y1="110" x2="75" y2="150" stroke="#9E9E9E" strokeWidth="3" />
          </svg>
        ))}
      </div>

      {/* Main Character - Stressed */}
      <div
        style={{
          position: 'absolute',
          bottom: '80px',
          right: '150px',
          transform: `translateY(${(1 - sp(130)) * 150}px)`,
          opacity: sp(130),
        }}
      >
        <svg width="150" height="200" viewBox="0 0 150 200">
          <circle cx="75" cy="35" r="40" fill="rgba(255, 0, 0, 0.12)" />
          <circle
            cx="75"
            cy="35"
            r="24"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            transform={`rotate(${Math.sin(frame * 0.15) * 5} 75 35)`}
          />
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="67" cy="35" r="8" />
            <circle cx="83" cy="35" r="8" />
            <line x1="75" y1="35" x2="77" y2="35" />
          </g>
          <line x1="75" y1="59" x2="75" y2="125" stroke="#e9d700" strokeWidth="4" />
          <line
            x1="75"
            y1="75"
            x2={50 + Math.sin(frame * 0.2) * 5}
            y2={65 + Math.sin(frame * 0.2) * 3}
            stroke="#e9d700"
            strokeWidth="4"
          />
          <line
            x1="75"
            y1="75"
            x2={100 - Math.sin(frame * 0.2) * 5}
            y2={65 - Math.sin(frame * 0.2) * 3}
            stroke="#e9d700"
            strokeWidth="4"
          />
          <line x1="75" y1="125" x2="55" y2="170" stroke="#e9d700" strokeWidth="4" />
          <line x1="75" y1="125" x2="95" y2="170" stroke="#e9d700" strokeWidth="4" />
          <circle
            cx="75"
            cy="35"
            r="24"
            fill="none"
            stroke="rgba(233, 215, 0, 0.25)"
            strokeWidth="2"
            transform={`scale(${1 + Math.sin(frame * 0.05) * 0.07})`}
            style={{ transformOrigin: '75px 35px' }}
          />
        </svg>
      </div>

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 40%, rgba(0,0,0,0.7) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default SocialComparison;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'ComparisonCycle',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ComparisonCycle: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Circular arrangement animation
  const circleRotation = interpolate(frame, [60, 300], [0, 360], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const comparisons = [
    { emoji: '🚗', text: 'नई कार', color: '#ff0000' },
    { emoji: '✈️', text: 'यूरोप ट्रिप', color: '#ff0000' },
    { emoji: '🏢', text: 'प्रीमियम फ्लैट', color: '#ff0000' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle at 50% 50%, rgba(255, 0, 0, 0.15), transparent 65%)',
        }}
      />

      {/* Animated Shape Layers - Spinning circles */}
      <div
        style={{
          position: 'absolute',
          width: '900px',
          height: '900px',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${circleRotation}deg)`,
          border: '2px solid rgba(255, 0, 0, 0.06)',
          borderRadius: '50%',
          opacity: 0.5,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: '600px',
          height: '600px',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${-circleRotation * 0.7}deg)`,
          border: '2px solid rgba(233, 215, 0, 0.05)',
          borderRadius: '50%',
          opacity: 0.4,
        }}
      />

      {/* Main Content */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
        }}
      >
        {/* Central "You" character */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            transform: `translate(-50%, -50%) scale(${sp(0, { damping: 8 })})`,
            opacity: sp(0),
            zIndex: 10,
          }}
        >
          <div
            style={{
              width: '200px',
              height: '200px',
              borderRadius: '50%',
              backgroundColor: '#2D2D2D',
              border: '5px solid #e9d700',
              display: 'flex',
              justifyContent: 'center',
              alignItems: 'center',
              boxShadow: '0 0 60px rgba(233, 215, 0, 0.4)',
              position: 'relative',
            }}
          >
            {/* Pulsing effect */}
            <div
              style={{
                position: 'absolute',
                width: '100%',
                height: '100%',
                borderRadius: '50%',
                border: '3px solid rgba(233, 215, 0, 0.5)',
                transform: `scale(${1 + Math.sin(frame * 0.08) * 0.1})`,
              }}
            />

            <span
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '70px',
                fontWeight: 900,
                color: '#e9d700',
                position: 'relative',
              }}
            >
              आप
            </span>
          </div>
        </div>

        {/* Comparison Items in Circle */}
        {comparisons.map((item, i) => {
          const angle = (i * 120 * Math.PI) / 180;
          const radius = 350;
          const x = 960 + Math.cos(angle + (circleRotation * Math.PI) / 180) * radius;
          const y = 540 + Math.sin(angle + (circleRotation * Math.PI) / 180) * radius;
          const itemSpring = sp(30 + i * 20);

          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                transform: `translate(-50%, -50%) scale(${itemSpring})`,
                opacity: itemSpring,
              }}
            >
              <div
                style={{
                  width: '280px',
                  height: '280px',
                  borderRadius: '50%',
                  backgroundColor: '#2D2D2D',
                  border: `5px solid ${item.color}`,
                  display: 'flex',
                  flexDirection: 'column',
                  justifyContent: 'center',
                  alignItems: 'center',
                  gap: '20px',
                  boxShadow: `0 15px 50px ${item.color}50`,
                  position: 'relative',
                  overflow: 'hidden',
                }}
              >
                {/* Glow pulse */}
                <div
                  style={{
                    position: 'absolute',
                    width: '100%',
                    height: '100%',
                    borderRadius: '50%',
                    background: `radial-gradient(circle, rgba(255,0,0,${0.15 + Math.sin((frame + i * 60) * 0.1) * 0.08}), transparent 70%)`,
                  }}
                />

                <div style={{ fontSize: '100px', position: 'relative' }}>{item.emoji}</div>
                <p
                  style={{
                    fontFamily: 'Noto Sans Devanagari, sans-serif',
                    fontSize: '46px',
                    fontWeight: 800,
                    color: item.color,
                    margin: 0,
                    position: 'relative',
                  }}
                >
                  {item.text}
                </p>
              </div>

              {/* Connecting line to center */}
              <svg
                width={Math.abs(960 - x) * 2}
                height={Math.abs(540 - y) * 2}
                style={{
                  position: 'absolute',
                  left: '50%',
                  top: '50%',
                  transform: 'translate(-50%, -50%)',
                  pointerEvents: 'none',
                  opacity: interpolate(frame, [60 + i * 20, 100 + i * 20], [0, 0.2], {
                    extrapolateLeft: 'clamp',
                    extrapolateRight: 'clamp',
                  }),
                }}
              >
                <line
                  x1={Math.abs(960 - x)}
                  y1={Math.abs(540 - y)}
                  x2={Math.abs(960 - x)}
                  y2={Math.abs(540 - y)}
                  stroke="#ff0000"
                  strokeWidth="3"
                  strokeDasharray="10,10"
                  strokeDashoffset={-frame * 2}
                />
              </svg>
            </div>
          );
        })}

        {/* Bottom text - feeling behind */}
        <div
          style={{
            position: 'absolute',
            bottom: '150px',
            left: '50%',
            transform: `translateX(-50%) translateY(${(1 - sp(120)) * 80}px)`,
            opacity: sp(120),
          }}
        >
          <div
            style={{
              backgroundColor: '#2D2D2D',
              padding: '30px 70px',
              borderRadius: '20px',
              border: '4px solid #ff0000',
              boxShadow: '0 10px 40px rgba(255, 0, 0, 0.5)',
            }}
          >
            <p
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '60px',
                fontWeight: 900,
                color: '#ff0000',
                margin: 0,
                textShadow: '0 0 25px rgba(255, 0, 0, 0.5)',
              }}
            >
              शायद मैं पीछे हूँ...
            </p>
          </div>
        </div>
      </div>

      {/* Pressure arrows pointing to center */}
      {[0, 1, 2].map((i) => {
        const arrowAngle = i * 120 + 60;
        const arrowSpring = sp(90 + i * 15);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              transform: `translate(-50%, -50%) rotate(${arrowAngle}deg)`,
              opacity: arrowSpring,
            }}
          >
            <svg width="400" height="100" viewBox="0 0 400 100">
              <defs>
                <marker id={`arrowhead-${i}`} markerWidth="10" markerHeight="10" refX="9" refY="3" orient="auto">
                  <polygon points="0 0, 10 3, 0 6" fill="#ff0000" />
                </marker>
              </defs>
              <line
                x1="250"
                y1="50"
                x2="100"
                y2="50"
                stroke="#ff0000"
                strokeWidth="4"
                markerEnd={`url(#arrowhead-${i})`}
                strokeDasharray="200"
                strokeDashoffset={200 - arrowSpring * 200}
              />
            </svg>
          </div>
        );
      })}

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 35%, rgba(0,0,0,0.75) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default ComparisonCycle;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'ComparisonPurchases',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ComparisonPurchases: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Camera shake for emphasis
  const shakeX = frame > 180 ? Math.sin(frame * 0.5) * 5 : 0;
  const shakeY = frame > 180 ? Math.cos(frame * 0.7) * 3 : 0;

  const badPurchases = [
    { icon: '💳', title: 'Loan', subtitle: 'जो एफोर्ड नहीं था', amount: '₹5L', color: '#ff0000' },
    { icon: '✈️', title: 'Vacation', subtitle: 'EMI पर गया', amount: '₹2L', color: '#ff0000' },
    { icon: '📱', title: 'Gadget', subtitle: 'सिर्फ दिखाने को', amount: '₹80K', color: '#ff0000' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'linear-gradient(135deg, rgba(255, 0, 0, 0.12) 0%, rgba(0, 0, 0, 0) 100%)',
        }}
      />

      {/* Animated Shape Layers */}
      <div
        style={{
          position: 'absolute',
          width: '700px',
          height: '700px',
          right: '-100px',
          top: '-50px',
          transform: `rotate(${frame * 0.3}deg)`,
          border: '3px solid rgba(255, 0, 0, 0.04)',
          borderRadius: '45%',
          opacity: 0.6,
        }}
      />

      {/* Warning triangles in background */}
      {[...Array(6)].map((_, i) => {
        const x = seededRandom(i * 234) * 1920;
        const y = seededRandom(i * 567) * 1080;
        const rotation = (frame + i * 30) * 0.5;
        const triangleOp = interpolate((frame + i * 40) % 180, [0, 90, 180], [0.05, 0.15, 0.05], {
          extrapolateLeft: 'clamp',
          extrapolateRight: 'clamp',
        });

        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              transform: `rotate(${rotation}deg)`,
              opacity: triangleOp,
            }}
          >
            <svg width="80" height="80" viewBox="0 0 80 80">
              <path d="M 40 10 L 70 70 L 10 70 Z" fill="none" stroke="#ff0000" strokeWidth="3" />
              <text x="40" y="55" fill="#ff0000" fontSize="30" fontWeight="900" textAnchor="middle">
                !
              </text>
            </svg>
          </div>
        );
      })}

      {/* Main Content with shake */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          transform: `translate(${shakeX}px, ${shakeY}px)`,
        }}
      >
        {/* Title */}
        <div
          style={{
            transform: `translateY(${(1 - sp(0)) * -100}px)`,
            opacity: sp(0),
            marginBottom: '70px',
          }}
        >
          <h1
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '110px',
              fontWeight: 900,
              color: '#ff0000',
              margin: 0,
              textAlign: 'center',
              textShadow: '0 0 50px rgba(255, 0, 0, 0.5)',
              lineHeight: 1.2,
            }}
          >
            Comparison से होती
            <br />
            <span style={{ fontSize: '130px' }}>खरीदारी</span>
          </h1>
        </div>

        {/* Purchase Cards */}
        <div
          style={{
            display: 'flex',
            gap: '50px',
            marginBottom: '60px',
          }}
        >
          {badPurchases.map((purchase, i) => {
            const cardSpring = sp(30 + i * 25);
            const cardShake = i === 0 && frame > 180 ? Math.sin(frame * 0.3) * 4 : 0;

            return (
              <div
                key={i}
                style={{
                  transform: `translateY(${(1 - cardSpring) * 150}px) rotate(${cardShake}deg)`,
                  opacity: cardSpring,
                }}
              >
                <div
                  style={{
                    width: '380px',
                    height: '400px',
                    backgroundColor: '#2D2D2D',
                    borderRadius: '25px',
                    border: `5px solid ${purchase.color}`,
                    display: 'flex',
                    flexDirection: 'column',
                    justifyContent: 'center',
                    alignItems: 'center',
                    gap: '25px',
                    boxShadow: `0 15px 60px ${purchase.color}60`,
                    position: 'relative',
                    overflow: 'hidden',
                  }}
                >
                  {/* Animated warning glow */}
                  <div
                    style={{
                      position: 'absolute',
                      width: '100%',
                      height: '100%',
                      background: `radial-gradient(circle at 50% 50%, rgba(255,0,0,${0.15 + Math.sin((frame + i * 50) * 0.12) * 0.1}), transparent 70%)`,
                    }}
                  />

                  {/* Warning icon top right */}
                  <div
                    style={{
                      position: 'absolute',
                      top: '20px',
                      right: '20px',
                      fontSize: '50px',
                      opacity: interpolate(frame, [60 + i * 25, 100 + i * 25], [0, 1], {
                        extrapolateLeft: 'clamp',
                        extrapolateRight: 'clamp',
                      }),
                    }}
                  >
                    ⚠️
                  </div>

                  <div style={{ fontSize: '120px', position: 'relative' }}>{purchase.icon}</div>

                  <p
                    style={{
                      fontFamily: 'Inter, sans-serif',
                      fontSize: '56px',
                      fontWeight: 900,
                      color: '#F5F5F5',
                      margin: 0,
                      position: 'relative',
                    }}
                  >
                    {purchase.title}
                  </p>

                  <p
                    style={{
                      fontFamily: 'Noto Sans Devanagari, sans-serif',
                      fontSize: '38px',
                      fontWeight: 600,
                      color: '#9E9E9E',
                      margin: 0,
                      textAlign: 'center',
                      padding: '0 20px',
                      position: 'relative',
                    }}
                  >
                    {purchase.subtitle}
                  </p>

                  <div
                    style={{
                      backgroundColor: purchase.color,
                      padding: '15px 40px',
                      borderRadius: '12px',
                      position: 'relative',
                      marginTop: '10px',
                    }}
                  >
                    <span
                      style={{
                        fontFamily: 'JetBrains Mono, monospace',
                        fontSize: '50px',
                        fontWeight: 900,
                        color: '#1C1C1C',
                      }}
                    >
                      {purchase.amount}
                    </span>
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* Total Damage */}
        <div
          style={{
            transform: `scale(${sp(130, { damping: 8, stiffness: 90 })})`,
            opacity: sp(130),
          }}
        >
          <div
            style={{
              backgroundColor: '#2D2D2D',
              padding: '35px 80px',
              borderRadius: '20px',
              border: '5px solid #ff0000',
              boxShadow: '0 20px 70px rgba(255, 0, 0, 0.6)',
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            {/* Animated danger stripe */}
            <div
              style={{
                position: 'absolute',
                top: 0,
                left: `-${frame % 100}px`,
                width: '100px',
                height: '100%',
                background: 'linear-gradient(90deg, transparent, rgba(255,0,0,0.3), transparent)',
              }}
            />

            <div style={{ display: 'flex', alignItems: 'center', gap: '30px', position: 'relative' }}>
              <span
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: '60px',
                  fontWeight: 800,
                  color: '#F5F5F5',
                }}
              >
                कुल नुकसान:
              </span>
              <span
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: '100px',
                  fontWeight: 900,
                  color: '#ff0000',
                  textShadow: '0 0 40px rgba(255, 0, 0, 0.7)',
                }}
              >
                ₹7.8L+
              </span>
            </div>
          </div>
        </div>
      </div>

      {/* Stick Figure - Regretful */}
      <div
        style={{
          position: 'absolute',
          bottom: '100px',
          left: '200px',
          transform: `translateY(${(1 - sp(140)) * 150}px)`,
          opacity: sp(140),
        }}
      >
        <svg width="160" height="220" viewBox="0 0 160 220">
          <circle cx="80" cy="35" r="45" fill="rgba(255, 0, 0, 0.15)" />
          <circle
            cx="80"
            cy="35"
            r="25"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            transform={`rotate(${Math.sin(frame * 0.1) * 6} 80 35)`}
          />
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="71" cy="35" r="8" />
            <circle cx="89" cy="35" r="8" />
            <line x1="79" y1="35" x2="81" y2="35" />
          </g>
          {/* Sad mouth */}
          <path d="M 70 48 Q 80 44 90 48" fill="none" stroke="#ff0000" strokeWidth="3" />
          <line x1="80" y1="60" x2="80" y2="135" stroke="#e9d700" strokeWidth="4" />
          {/* Arms down in regret */}
          <line x1="80" y1="80" x2="55" y2="120" stroke="#e9d700" strokeWidth="4" />
          <line x1="80" y1="80" x2="105" y2="120" stroke="#e9d700" strokeWidth="4" />
          <line x1="80" y1="135" x2="60" y2="185" stroke="#e9d700" strokeWidth="4" />
          <line x1="80" y1="135" x2="100" y2="185" stroke="#e9d700" strokeWidth="4" />
          <circle
            cx="80"
            cy="35"
            r="25"
            fill="none"
            stroke="rgba(233, 215, 0, 0.2)"
            strokeWidth="2"
            transform={`scale(${1 + Math.sin(frame * 0.05) * 0.06})`}
            style={{ transformOrigin: '80px 35px' }}
          />
        </svg>
      </div>

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 40%, rgba(0,0,0,0.8) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default ComparisonPurchases;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'MinimalistShield',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const MinimalistShield: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Shield expansion animation
  const shieldScale = interpolate(frame, [60, 120], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Attack waves bouncing off
  const attackWaves = [
    { angle: 45, delay: 140 },
    { angle: 135, delay: 160 },
    { angle: 225, delay: 180 },
    { angle: 315, delay: 200 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle at 50% 50%, rgba(233, 215, 0, 0.15), transparent 70%)',
        }}
      />

      {/* Animated Shape Layers - Protective circles */}
      {[...Array(4)].map((_, i) => {
        const size = 300 + i * 150;
        const opacity = 0.08 - i * 0.015;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              width: `${size}px`,
              height: `${size}px`,
              left: '50%',
              top: '50%',
              transform: `translate(-50%, -50%) rotate(${frame * (0.1 + i * 0.05)}deg) scale(${shieldScale})`,
              border: '2px solid #e9d700',
              borderRadius: '50%',
              opacity: opacity * shieldScale,
            }}
          />
        );
      })}

      {/* Main Content */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
        }}
      >
        {/* Title */}
        <div
          style={{
            transform: `translateY(${(1 - sp(0)) * -80}px)`,
            opacity: sp(0),
            marginBottom: '100px',
            zIndex: 10,
          }}
        >
          <h1
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '120px',
              fontWeight: 900,
              color: '#e9d700',
              margin: 0,
              textAlign: 'center',
              textShadow: '0 0 60px rgba(233, 215, 0, 0.5)',
              lineHeight: 1.2,
            }}
          >
            Minimalist Shield
          </h1>
        </div>

        {/* Central Shield */}
        <div
          style={{
            position: 'relative',
            width: '500px',
            height: '550px',
            transform: `scale(${shieldScale})`,
            opacity: shieldScale,
          }}
        >
          {/* Shield SVG */}
          <svg width="500" height="550" viewBox="0 0 500 550" style={{ filter: 'drop-shadow(0 0 40px rgba(233, 215, 0, 0.6))' }}>
            <defs>
              <linearGradient id="shieldGradient" x1="0%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stopColor="#e9d700" stopOpacity="0.9" />
                <stop offset="100%" stopColor="#FFC107" stopOpacity="0.7" />
              </linearGradient>
            </defs>

            {/* Shield shape */}
            <path
              d="M 250 20 L 450 100 L 450 300 Q 450 450 250 530 Q 50 450 50 300 L 50 100 Z"
              fill="url(#shieldGradient)"
              stroke="#e9d700"
              strokeWidth="6"
            />

            {/* Shield details */}
            <path d="M 250 80 L 380 130 L 380 280 Q 380 380 250 440 Q 120 380 120 280 L 120 130 Z" fill="none" stroke="#1C1C1C" strokeWidth="4" opacity="0.4" />

            {/* Center emblem */}
            <circle cx="250" cy="260" r="80" fill="#1C1C1C" stroke="#e9d700" strokeWidth="5" />

            {/* Checkmark */}
            <path d="M 210 260 L 235 285 L 290 230" fill="none" stroke="#e9d700" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round" />
          </svg>

          {/* Pulsing energy effect */}
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: '500px',
              height: '550px',
              transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.08) * 0.05})`,
              opacity: 0.3,
            }}
          >
            <svg width="500" height="550" viewBox="0 0 500 550">
              <path
                d="M 250 20 L 450 100 L 450 300 Q 450 450 250 530 Q 50 450 50 300 L 50 100 Z"
                fill="none"
                stroke="#e9d700"
                strokeWidth="3"
                opacity="0.5"
              />
            </svg>
          </div>
        </div>

        {/* Attack waves being deflected */}
        {attackWaves.map((wave, i) => {
          const waveProgress = sp(wave.delay, { damping: 15, stiffness: 100 });
          const distance = interpolate(waveProgress, [0, 1], [0, 400], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
          const angleRad = (wave.angle * Math.PI) / 180;
          const x = 960 + Math.cos(angleRad) * distance;
          const y = 540 + Math.sin(angleRad) * distance;

          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                transform: 'translate(-50%, -50%)',
                opacity: interpolate(waveProgress, [0, 0.5, 1], [0, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
            >
              <svg width="100" height="100" viewBox="0 0 100 100">
                <circle
                  cx="50"
                  cy="50"
                  r="40"
                  fill="none"
                  stroke="#ff0000"
                  strokeWidth="4"
                  strokeDasharray="10,5"
                  transform={`rotate(${frame * 5} 50 50)`}
                />
                <text x="50" y="60" fill="#ff0000" fontSize="40" fontWeight="900" textAnchor="middle">
                  ×
                </text>
              </svg>
            </div>
          );
        })}

        {/* Bottom Message */}
        <div
          style={{
            transform: `translateY(${(1 - sp(100)) * 60}px)`,
            opacity: sp(100),
            marginTop: '120px',
            zIndex: 10,
          }}
        >
          <div
            style={{
              backgroundColor: '#2D2D2D',
              padding: '35px 70px',
              borderRadius: '20px',
              border: '4px solid #e9d700',
              boxShadow: '0 15px 50px rgba(233, 215, 0, 0.4)',
            }}
          >
            <p
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '56px',
                fontWeight: 800,
                color: '#e9d700',
                margin: 0,
                textAlign: 'center',
                lineHeight: 1.3,
              }}
            >
              जब आप जानते हो क्या चाहिए,
              <br />
              <span style={{ color: '#F5F5F5' }}>तब कोई हिला नहीं सकता</span>
            </p>
          </div>
        </div>
      </div>

      {/* Stick Figure - Protected and Confident */}
      <div
        style={{
          position: 'absolute',
          bottom: '120px',
          right: '200px',
          transform: `translateY(${(1 - sp(80)) * 150}px)`,
          opacity: sp(80),
        }}
      >
        <svg width="180" height="240" viewBox="0 0 180 240">
          {/* Golden protective aura */}
          <circle cx="90" cy="40" r="50" fill="rgba(233, 215, 0, 0.15)" />

          {/* Head */}
          <circle
            cx="90"
            cy="40"
            r="26"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            transform={`rotate(${Math.sin(frame * 0.04) * 2} 90 40)`}
          />

          {/* Glasses */}
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="81" cy="40" r="8" />
            <circle cx="99" cy="40" r="8" />
            <line x1="89" y1="40" x2="91" y2="40" />
          </g>

          {/* Confident smile */}
          <path d="M 80 50 Q 90 54 100 50" fill="none" stroke="#e9d700" strokeWidth="3" />

          {/* Body */}
          <line x1="90" y1="66" x2="90" y2="145" stroke="#e9d700" strokeWidth="4" />

          {/* Arms - confident pose */}
          <line x1="90" y1="85" x2="60" y2="105" stroke="#e9d700" strokeWidth="4" />
          <line x1="90" y1="85" x2="120" y2="105" stroke="#e9d700" strokeWidth="4" />

          {/* Legs - stable stance */}
          <line x1="90" y1="145" x2="70" y2="200" stroke="#e9d700" strokeWidth="4" />
          <line x1="90" y1="145" x2="110" y2="200" stroke="#e9d700" strokeWidth="4" />

          {/* Breathing */}
          <circle
            cx="90"
            cy="40"
            r="26"
            fill="none"
            stroke="rgba(233, 215, 0, 0.25)"
            strokeWidth="2"
            transform={`scale(${1 + Math.sin(frame * 0.05) * 0.05})`}
            style={{ transformOrigin: '90px 40px' }}
          />
        </svg>
      </div>

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.7) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default MinimalistShield;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'PowerfulDecision',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PowerfulDecision: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Split screen reveal
  const splitPosition = interpolate(frame, [80, 140], [960, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Left Side - Wrong Decision (Red) */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          top: 0,
          width: '50%',
          height: '100%',
          background: 'linear-gradient(135deg, rgba(255, 0, 0, 0.15) 0%, rgba(255, 0, 0, 0.05) 100%)',
          clipPath: `inset(0 ${splitPosition}px 0 0)`,
        }}
      >
        {/* Animated shape */}
        <div
          style={{
            position: 'absolute',
            width: '400px',
            height: '400px',
            left: '30%',
            top: '20%',
            borderRadius: '50%',
            border: '2px solid rgba(255, 0, 0, 0.1)',
            transform: `rotate(${frame * 0.3}deg)`,
            opacity: 0.5,
          }}
        />

        <div
          style={{
            position: 'absolute',
            width: '100%',
            height: '100%',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            padding: '0 80px',
          }}
        >
          <div
            style={{
              transform: `translateX(${(1 - sp(20)) * -150}px)`,
              opacity: sp(20),
            }}
          >
            <div style={{ fontSize: '140px', marginBottom: '40px', textAlign: 'center' }}>❌</div>
            <h2
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '70px',
                fontWeight: 900,
                color: '#ff0000',
                margin: 0,
                textAlign: 'center',
                marginBottom: '30px',
              }}
            >
              दूसरों को Impress
              <br />
              करने के लिए
            </h2>
            <p
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '42px',
                fontWeight: 600,
                color: '#9E9E9E',
                margin: 0,
                textAlign: 'center',
              }}
            >
              खरीदारी करना
            </p>
          </div>
        </div>
      </div>

      {/* Right Side - Right Decision (Gold) */}
      <div
        style={{
          position: 'absolute',
          right: 0,
          top: 0,
          width: '50%',
          height: '100%',
          background: 'linear-gradient(135deg, rgba(233, 215, 0, 0.15) 0%, rgba(233, 215, 0, 0.05) 100%)',
          clipPath: `inset(0 0 0 ${960 - splitPosition}px)`,
        }}
      >
        {/* Animated shape */}
        <div
          style={{
            position: 'absolute',
            width: '450px',
            height: '450px',
            right: '25%',
            top: '15%',
            borderRadius: '40%',
            border: '2px solid rgba(233, 215, 0, 0.1)',
            transform: `rotate(${-frame * 0.25}deg)`,
            opacity: 0.5,
          }}
        />

        <div
          style={{
            position: 'absolute',
            width: '100%',
            height: '100%',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'center',
            alignItems: 'center',
            padding: '0 80px',
          }}
        >
          <div
            style={{
              transform: `translateX(${(1 - sp(50)) * 150}px)`,
              opacity: sp(50),
            }}
          >
            <div style={{ fontSize: '140px', marginBottom: '40px', textAlign: 'center' }}>✓</div>
            <h2
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '70px',
                fontWeight: 900,
                color: '#e9d700',
                margin: 0,
                textAlign: 'center',
                marginBottom: '30px',
                textShadow: '0 0 30px rgba(233, 215, 0, 0.4)',
              }}
            >
              खुद के लिए
              <br />
              सही Decision
            </h2>
            <p
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '42px',
                fontWeight: 600,
                color: '#F5F5F5',
                margin: 0,
                textAlign: 'center',
              }}
            >
              लेना
            </p>
          </div>
        </div>
      </div>

      {/* Center Divider Line */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: 0,
          width: '6px',
          height: '100%',
          background: 'linear-gradient(180deg, #ff0000 0%, #e9d700 100%)',
          boxShadow: '0 0 30px rgba(233, 215, 0, 0.6)',
          transform: 'translateX(-50%)',
          opacity: interpolate(frame, [100, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      />

      {/* Main Message */}
      <div
        style={{
          position: 'absolute',
          bottom: '120px',
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(120)) * 80}px)`,
          opacity: sp(120),
          zIndex: 10,
        }}
      >
        <div
          style={{
            backgroundColor: '#2D2D2D',
            padding: '40px 80px',
            borderRadius: '25px',
            border: '5px solid #e9d700',
            boxShadow: '0 20px 70px rgba(233, 215, 0, 0.5)',
          }}
        >
          <p
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '62px',
              fontWeight: 900,
              color: '#e9d700',
              margin: 0,
              textAlign: 'center',
              textShadow: '0 0 25px rgba(233, 215, 0, 0.4)',
            }}
          >
            सबसे Powerful Financial Decision
          </p>
        </div>
      </div>

      {/* CTA - Comment Question */}
      <div
        style={{
          position: 'absolute',
          top: '100px',
          left: '50%',
          transform: `translateX(-50%) translateY(${(1 - sp(160)) * -60}px)`,
          opacity: sp(160),
        }}
      >
        <div
          style={{
            backgroundColor: 'rgba(45, 45, 45, 0.95)',
            padding: '30px 60px',
            borderRadius: '20px',
            border: '3px solid #FFC107',
            boxShadow: '0 10px 40px rgba(255, 193, 7, 0.3)',
          }}
        >
          <p
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: '48px',
              fontWeight: 700,
              color: '#FFC107',
              margin: 0,
              textAlign: 'center',
            }}
          >
            💬 कमेंट में बताओ: Honest जवाब!
          </p>
        </div>
      </div>

      {/* Stick Figure - Thinking */}
      <div
        style={{
          position: 'absolute',
          bottom: '100px',
          right: '180px',
          transform: `translateY(${(1 - sp(90)) * 150}px)`,
          opacity: sp(90),
        }}
      >
        <svg width="160" height="220" viewBox="0 0 160 220">
          <circle cx="80" cy="35" r="42" fill="rgba(233, 215, 0, 0.12)" />
          <circle
            cx="80"
            cy="35"
            r="25"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            transform={`rotate(${Math.sin(frame * 0.05) * 3} 80 35)`}
          />
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="71" cy="35" r="8" />
            <circle cx="89" cy="35" r="8" />
            <line x1="79" y1="35" x2="81" y2="35" />
          </g>
          <line x1="80" y1="60" x2="80" y2="135" stroke="#e9d700" strokeWidth="4" />
          {/* Hand on chin - thinking pose */}
          <line x1="80" y1="80" x2="60" y2="100" stroke="#e9d700" strokeWidth="4" />
          <line x1="60" y1="100" x2="70" y2="50" stroke="#e9d700" strokeWidth="4" />
          <line x1="80" y1="80" x2="110" y2="110" stroke="#e9d700" strokeWidth="4" />
          <line x1="80" y1="135" x2="60" y2="185" stroke="#e9d700" strokeWidth="4" />
          <line x1="80" y1="135" x2="100" y2="185" stroke="#e9d700" strokeWidth="4" />
          {/* Thought bubble */}
          <circle cx="120" cy="20" r="15" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
          <circle cx="130" cy="10" r="8" fill="#2D2D2D" stroke="#e9d700" strokeWidth="2" />
          <text x="120" y="28" fill="#e9d700" fontSize="20" fontWeight="900" textAnchor="middle">
            ?
          </text>
          <circle
            cx="80"
            cy="35"
            r="25"
            fill="none"
            stroke="rgba(233, 215, 0, 0.2)"
            strokeWidth="2"
            transform={`scale(${1 + Math.sin(frame * 0.05) * 0.06})`}
            style={{ transformOrigin: '80px 35px' }}
          />
        </svg>
      </div>

      {/* Vignette */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.75) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default PowerfulDecision;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'InformationOverload',
  durationInSeconds: 25,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const InformationOverload: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Information flood animation - increasingly chaotic
  const infoFloodIntensity = interpolate(frame, [60, 300], [1, 50], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera zoom in (claustrophobic feeling)
  const cameraScale = interpolate(frame, [0, 400], [1, 1.4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const infoSources = ['📰', '📱', '💬', '📺', '🎧', '📧', '🌐', '📊'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient Background - increasingly red */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: `radial-gradient(circle at 50% 50%, rgba(255, 0, 0, ${0.05 + infoFloodIntensity * 0.003}), transparent 60%)`,
        }}
      />

      {/* Chaotic information symbols flooding in */}
      {[...Array(Math.floor(infoFloodIntensity))].map((_, i) => {
        const x = seededRandom(i * 234 + frame) * 1920;
        const yStart = -100;
        const yEnd = 1180;
        const speed = 2 + seededRandom(i * 456) * 3;
        const y = (yStart + ((frame * speed + i * 30) % (yEnd - yStart + 200))) % (yEnd + 100);
        const rotation = (frame + i * 20) * (1 + seededRandom(i * 789));
        const symbol = infoSources[i % infoSources.length];

        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              fontSize: `${40 + seededRandom(i * 567) * 40}px`,
              transform: `rotate(${rotation}deg)`,
              opacity: 0.15 + seededRandom(i * 891) * 0.3,
              pointerEvents: 'none',
            }}
          >
            {symbol}
          </div>
        );
      })}

      {/* Main Content with claustrophobic zoom */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          transform: `scale(${cameraScale})`,
        }}
      >
        {/* Title */}
        <div
          style={{
            transform: `translateY(${(1 - sp(0)) * -100}px)`,
            opacity: sp(0),
            marginBottom: '80px',
            zIndex: 10,
          }}
        >
          <h1
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: '140px',
              fontWeight: 900,
              color: '#ff0000',
              margin: 0,
              textAlign: 'center',
              textShadow: '0 0 60px rgba(255, 0, 0, 0.6)',
              letterSpacing: '-2px',
            }}
          >
            INFORMATION
            <br />
            <span style={{ color: '#F5F5F5' }}>OVERLOAD</span>
          </h1>
        </div>

        {/* Comparison boxes */}
        <div
          style={{
            display: 'flex',
            gap: '100px',
            alignItems: 'center',
            zIndex: 10,
          }}
        >
          {/* 18th Century */}
          <div
            style={{
              transform: `translateX(${(1 - sp(40)) * -200}px)`,
              opacity: sp(40),
            }}
          >
            <div
              style={{
                width: '420px',
                height: '450px',
                backgroundColor: '#2D2D2D',
                borderRadius: '25px',
                border: '4px solid #e9d700',
                display: 'flex',
                flexDirection: 'column',
                justifyContent: 'center',
                alignItems: 'center',
                gap: '30px',
                boxShadow: '0 15px 50px rgba(233, 215, 0, 0.3)',
                position: 'relative',
                overflow: 'hidden',
              }}
            >
              <div style={{ fontSize: '120px' }}>📜</div>

              <p
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: '48px',
                  fontWeight: 800,
                  color: '#e9d700',
                  margin: 0,
                  textAlign: 'center',
                }}
              >
                18वीं सदी
              </p>

              <p
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: '38px',
                  fontWeight: 600,
                  color: '#F5F5F5',
                  margin: 0,
                  textAlign: 'center',
                  padding: '0 20px',
                }}
              >
                पूरी ज़िन्दगी की
                <br />
                जानकारी
              </p>

              <div
                style={{
                  backgroundColor: '#e9d700',
                  padding: '15px 40px',
                  borderRadius: '12px',
                }}
              >
                <span
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: '44px',
                    fontWeight: 900,
                    color: '#1C1C1C',
                  }}
                >
                  X Units
                </span>
              </div>
            </div>
          </div>

          {/* Equals sign */}
          <div
            style={{
              transform: `scale(${sp(70)})`,
              opacity: sp(70),
            }}
          >
            <svg width="80" height="80" viewBox="0 0 80 80">
              <rect x="10" y="25" width="60" height="8" fill="#9E9E9E" rx="4" />
              <rect x="10" y="47" width="60" height="8" fill="#9E9E9E" rx="4" />
            </svg>
          </div>

          {/* Modern Day */}
          <div
            style={{
              transform: `translateX(${(1 - sp(100)) * 200}px)`,
              opacity: sp(100),
            }}
          >
            <div
              style={{
                width: '420px',
                height: '450px',
                backgroundColor: '#2D2D2D',
                borderRadius: '25px',
                border: '4px solid #ff0000',
                display: 'flex',
                flexDirection: 'column',
                justifyContent: 'center',
                alignItems: 'center',
                gap: '30px',
                boxShadow: '0 15px 50px rgba(255, 0, 0, 0.5)',
                position: 'relative',
                overflow: 'hidden',
              }}
            >
              {/* Pulsing danger glow */}
              <div
                style={{
                  position: 'absolute',
                  width: '100%',
                  height: '100%',
                  background: `radial-gradient(circle, rgba(255,0,0,${0.15 + Math.sin(frame * 0.12) * 0.1}), transparent 70%)`,
                }}
              />

              <div style={{ fontSize: '120px', position: 'relative' }}>📱</div>

              <p
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: '48px',
                  fontWeight: 800,
                  color: '#ff0000',
                  margin: 0,
                  textAlign: 'center',
                  position: 'relative',
                }}
              >
                आज का इंसान
              </p>

              <p
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: '38px',
                  fontWeight: 600,
                  color: '#F5F5F5',
                  margin: 0,
                  textAlign: 'center',
                  padding: '0 20px',
                  position: 'relative',
                }}
              >
                सिर्फ़ एक दिन में
              </p>

              <div
                style={{
                  backgroundColor: '#ff0000',
                  padding: '15px 40px',
                  borderRadius: '12px',
                  position: 'relative',
                }}
              >
                <span
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: '44px',
                    fontWeight: 900,
                    color: '#1C1C1C',
                  }}
                >
                  X Units
                </span>
              </div>
            </div>
          </div>
        </div>

        {/* Warning message */}
        <div
          style={{
            transform: `translateY(${(1 - sp(140)) * 60}px)`,
            opacity: sp(140),
            marginTop: '80px',
            zIndex: 10,
          }}
        >
          <div
            style={{
              backgroundColor: 'rgba(45, 45, 45, 0.95)',
              padding: '35px 70px',
              borderRadius: '20px',
              border: '4px solid #ff0000',
              boxShadow: '0 15px 60px rgba(255, 0, 0, 0.5)',
            }}
          >
            <p
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: '58px',
                fontWeight: 900,
                color: '#ff0000',
                margin: 0,
                textAlign: 'center',
                textShadow: '0 0 30px rgba(255, 0, 0, 0.5)',
              }}
            >
              दिमाग भरा, ठका, Confused!
            </p>
          </div>
        </div>
      </div>

      {/* Overwhelmed Stick Figure */}
      <div
        style={{
          position: 'absolute',
          bottom: '100px',
          right: '180px',
          transform: `translateY(${(1 - sp(120)) * 150}px)`,
          opacity: sp(120),
          zIndex: 10,
        }}
      >
        <svg width="180" height="240" viewBox="0 0 180 240">
          {/* Stress aura */}
          <circle cx="90" cy="40" r="55" fill="rgba(255, 0, 0, 0.15)" />

          {/* Head - shaking */}
          <circle
            cx="90"
            cy="40"
            r="26"
            fill="none"
            stroke="#e9d700"
            strokeWidth="4"
            transform={`rotate(${Math.sin(frame * 0.25) * 8} 90 40) translate(${Math.sin(frame * 0.2) * 2}, 0)`}
          />

          {/* Glasses */}
          <g stroke="#FFC107" strokeWidth="3" fill="none">
            <circle cx="81" cy="40" r="8" />
            <circle cx="99" cy="40" r="8" />
            <line x1="89" y1="40" x2="91" y2="40" />
          </g>

          {/* Stressed expression - X eyes */}
          <line x1="77" y1="37" x2="85" y2="43" stroke="#ff0000" strokeWidth="3" />
          <line x1="85" y1="37" x2="77" y2="43" stroke="#ff0000" strokeWidth="3" />
          <line x1="95" y1="37" x2="103" y2="43" stroke="#ff0000" strokeWidth="3" />
          <line x1="103" y1="37" x2="95" y2="43" stroke="#ff0000" strokeWidth="3" />

          {/* Body */}
          <line x1="90" y1="66" x2="90" y2="145" stroke="#e9d700" strokeWidth="4" />

          {/* Arms - clutching head */}
          <line
            x1="90"
            y1="80"
            x2={70 + Math.sin(frame * 0.3) * 3}
            y2={50 + Math.sin(frame * 0.3) * 2}
            stroke="#e9d700"
            strokeWidth="4"
          />
          <line
            x1="90"
            y1="80"
            x2={110 - Math.sin(frame * 0.3) * 3}
            y2={50 - Math.sin(frame * 0.3) * 2}
            stroke="#e9d700"
            strokeWidth="4"
          />

          {/* Legs */}
          <line x1="90" y1="145" x2="70" y2="195" stroke="#e9d700" strokeWidth="4" />
          <line x1="90" y1="145" x2="110" y2="195" stroke="#e9d700" strokeWidth="4" />

          {/* Stress lines around head */}
          {[...Array(6)].map((_, i) => {
            const angle = (i * 60 * Math.PI) / 180;
            const r = 45;
            const x1 = 90 + Math.cos(angle) * r;
            const y1 = 40 + Math.sin(angle) * r;
            const x2 = 90 + Math.cos(angle) * (r + 10);
            const y2 = 40 + Math.sin(angle) * (r + 10);
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#ff0000" strokeWidth="3" />;
          })}
        </svg>
      </div>

      {/* Vignette - stronger than usual */}
      <div
        style={{
          position: 'absolute',
          width: '100%',
          height: '100%',
          background: 'radial-gradient(circle, transparent 35%, rgba(0,0,0,0.85) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: '22px',
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default InformationOverload;
```

