```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene41_BuffettAdvice',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene41_BuffettAdvice: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06], { extrapolateRight: 'clamp' });

  // Quote intro
  const quoteIn = sp(15);
  const authorIn = sp(45);
  const lineDraw = interpolate(frame, [60, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bar chart - "Skill Value Over Time"
  const chartIn = sp(90);
  const bar1H = interpolate(frame, [110, 150], [0, 120], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const bar2H = interpolate(frame, [125, 165], [0, 200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const bar3H = interpolate(frame, [140, 180], [0, 290], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const bar4H = interpolate(frame, [155, 200], [0, 380], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Headline reveal
  const headIn = sp(200);

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 4,
    speed: 0.3 + seededRandom(i + 150) * 0.6,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 20%, rgba(238,238,238,0.25) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shape Layers - drifting geometry */}
      <div
        style={{
          position: 'absolute',
          width: 900,
          height: 900,
          borderRadius: '50%',
          border: '2px solid #FF5722',
          opacity: 0.06,
          top: -300 + Math.sin(frame * 0.01) * 20,
          right: -250 + Math.cos(frame * 0.012) * 25,
          transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          border: '2px solid #2D2D2D',
          opacity: 0.05,
          bottom: -200 + Math.cos(frame * 0.013) * 15,
          left: -180,
          transform: `rotate(${-frame * 0.04}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '250px solid transparent',
          borderRight: '250px solid transparent',
          borderBottom: '430px solid #FF5722',
          opacity: 0.04,
          top: 400,
          left: 200,
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />

      {/* Motion Texture - grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line
            key={`v${i}`}
            x1={i * 100 + (frame * 0.3) % 100}
            y1="0"
            x2={i * 100 + (frame * 0.3) % 100}
            y2="1080"
            stroke="#2D2D2D"
            strokeWidth="1"
          />
        ))}
        {Array.from({ length: 12 }).map((_, i) => (
          <line
            key={`h${i}`}
            x1="0"
            y1={i * 100}
            x2="1920"
            y2={i * 100}
            stroke="#2D2D2D"
            strokeWidth="1"
          />
        ))}
      </svg>

      {/* Floating particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y + frame * p.speed) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      {/* Main camera-scaled content */}
      <div style={{ position: 'absolute', width: '100%', height: '100%', transform: `scale(${camScale})`, transformOrigin: 'center' }}>

        {/* TOP LABEL — small tag */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 120,
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            opacity: quoteIn,
            transform: `translateX(${(1 - quoteIn) * -40}px)`,
          }}
        >
          <div style={{ width: 40, height: 4, backgroundColor: '#FF5722' }} />
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              color: '#2D2D2D',
              letterSpacing: 4,
              fontWeight: 600,
            }}
          >
            BUFFETT // PRINCIPLE 01
          </div>
        </div>

        {/* QUOTE BLOCK - Left side */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 120,
            width: 900,
            opacity: quoteIn,
            transform: `translateY(${(1 - quoteIn) * 40}px)`,
          }}
        >
          {/* Big quotation mark */}
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 280,
              color: '#FF5722',
              lineHeight: 0.7,
              opacity: 0.85,
              marginBottom: -40,
            }}
          >
            "
          </div>

          {/* Headline */}
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 102,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 1.0,
              letterSpacing: -2,
              marginBottom: 30,
            }}
          >
            SHARPEN
            <br />
            YOUR <span style={{ color: '#FF5722' }}>SKILLS</span>.
          </div>

          {/* Underline draw */}
          <div
            style={{
              height: 6,
              width: 480 * lineDraw,
              backgroundColor: '#FF5722',
              marginBottom: 36,
            }}
          />

          {/* Subhead */}
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 51,
              fontWeight: 600,
              color: '#EEEEEE',
              lineHeight: 1.25,
              opacity: authorIn,
              transform: `translateY(${(1 - authorIn) * 20}px)`,
              textShadow: '0 2px 8px rgba(0,0,0,0.15)',
            }}
          >
            The strongest protection<br />
            against inflation.
          </div>
        </div>

        {/* RIGHT SIDE - "Skill Value" bar chart */}
        <div
          style={{
            position: 'absolute',
            top: 240,
            right: 120,
            width: 620,
            opacity: chartIn,
            transform: `translateY(${(1 - chartIn) * 30}px)`,
          }}
        >
          {/* Chart title */}
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#2D2D2D',
              letterSpacing: 3,
              marginBottom: 20,
              fontWeight: 600,
            }}
          >
            SKILL VALUE → COMPOUNDS
          </div>

          {/* Chart frame */}
          <div
            style={{
              position: 'relative',
              height: 440,
              borderLeft: '3px solid #2D2D2D',
              borderBottom: '3px solid #2D2D2D',
              padding: '0 20px',
              display: 'flex',
              alignItems: 'flex-end',
              gap: 32,
            }}
          >
            {/* Bars */}
            {[
              { h: bar1H, label: 'YR 1', val: '×1' },
              { h: bar2H, label: 'YR 5', val: '×2.5' },
              { h: bar3H, label: 'YR 10', val: '×5' },
              { h: bar4H, label: 'YR 20', val: '×10' },
            ].map((b, i) => (
              <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', flex: 1 }}>
                <div
                  style={{
                    fontFamily: 'Mont Heavy, sans-serif',
                    fontSize: 26,
                    color: '#FF5722',
                    fontWeight: 900,
                    marginBottom: 6,
                    opacity: b.h > 30 ? 1 : 0,
                  }}
                >
                  {b.val}
                </div>
                <div
                  style={{
                    width: '100%',
                    height: b.h,
                    background: `linear-gradient(180deg, #FF5722 0%, #C84315 100%)`,
                    boxShadow: '0 4px 16px rgba(255,87,34,0.4)',
                  }}
                />
                <div
                  style={{
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 20,
                    color: '#2D2D2D',
                    marginTop: 12,
                    fontWeight: 700,
                    letterSpacing: 2,
                  }}
                >
                  {b.label}
                </div>
              </div>
            ))}

            {/* Trend arrow over bars */}
            <svg
              style={{ position: 'absolute', top: 0, left: 0, pointerEvents: 'none' }}
              width="100%"
              height="440"
            >
              <path
                d={`M 60 360 Q 200 280 340 180 T 540 60`}
                stroke="#EEEEEE"
                strokeWidth="4"
                fill="none"
                strokeDasharray="8 6"
                strokeDashoffset={500 - 500 * interpolate(frame, [170, 230], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
                style={{ filter: 'drop-shadow(0 2px 4px rgba(0,0,0,0.2))' }}
              />
            </svg>
          </div>
        </div>

        {/* BOTTOM BANNER — Headline payoff */}
        <div
          style={{
            position: 'absolute',
            bottom: 120,
            left: 120,
            right: 120,
            opacity: headIn,
            transform: `translateY(${(1 - headIn) * 30}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 30,
          }}
        >
          {/* Icon - upward chart SVG */}
          <svg width="80" height="80" viewBox="0 0 80 80">
            <rect x="6" y="6" width="68" height="68" rx="8" fill="#FF5722" />
            <polyline
              points="18,58 32,42 44,50 62,22"
              stroke="#EEEEEE"
              strokeWidth="5"
              fill="none"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
            <circle cx="62" cy="22" r="5" fill="#EEEEEE" />
          </svg>

          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 46,
              color: '#2D2D2D',
              fontWeight: 900,
              letterSpacing: -0.5,
              lineHeight: 1.1,
            }}
          >
            BE AT THE <span style={{ color: '#FF5722' }}>TOP</span> OF YOUR FIELD.
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.35) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene41_BuffettAdvice;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene42_TaxFreeCompound',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene42_TaxFreeCompound: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -25], { extrapolateRight: 'clamp' });

  // 3 cards stagger
  const card1 = sp(20);
  const card2 = sp(50);
  const card3 = sp(80);

  // Strike-through animation for "TAXED"
  const strikeIn = interpolate(frame, [180, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bottom payoff
  const payoffIn = sp(230);

  // Pulse for "0%"
  const pulse = 1 + Math.sin(frame * 0.15) * 0.04;

  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 3,
    speed: 0.4 + seededRandom(i + 150) * 0.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(238,238,238,0.18) 0%, rgba(161,188,152,0) 50%, rgba(255,87,34,0.08) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          border: '3px solid #FF5722',
          borderRadius: '30%',
          opacity: 0.05,
          top: 200 + Math.sin(frame * 0.015) * 20,
          left: -200,
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,87,34,0.08) 0%, rgba(255,87,34,0) 70%)',
          top: -300 + Math.cos(frame * 0.012) * 30,
          right: -300,
        }}
      />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 24 }).flatMap((_, i) =>
          Array.from({ length: 14 }).map((_, j) => (
            <circle key={`${i}-${j}`} cx={i * 85 + 40} cy={j * 85 + 40} r="2" fill="#2D2D2D" />
          ))
        )}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y - frame * p.speed + 1080) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.5,
          }}
        />
      ))}

      <div style={{ position: 'absolute', width: '100%', height: '100%', transform: `translateX(${camX}px)` }}>

        {/* TOP LABEL */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) translateY(${(1 - card1) * -30}px)`,
            opacity: card1,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
          }}
        >
          <div style={{ width: 50, height: 3, backgroundColor: '#FF5722' }} />
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              color: '#2D2D2D',
              letterSpacing: 5,
              fontWeight: 700,
            }}
          >
            THREE TRUTHS ABOUT YOU
          </div>
          <div style={{ width: 50, height: 3, backgroundColor: '#FF5722' }} />
        </div>

        {/* THREE CARDS row */}
        <div
          style={{
            position: 'absolute',
            top: 180,
            left: 80,
            right: 80,
            display: 'flex',
            gap: 30,
            justifyContent: 'center',
          }}
        >
          {/* Card 1 — Can't be taken */}
          <div
            style={{
              flex: 1,
              maxWidth: 540,
              backgroundColor: '#EEEEEE',
              borderRadius: 18,
              padding: '40px 36px',
              opacity: card1,
              transform: `translateY(${(1 - card1) * 60}px) rotate(${(1 - card1) * -3}deg)`,
              boxShadow: '0 16px 40px rgba(28,28,28,0.18)',
              borderLeft: '8px solid #FF5722',
            }}
          >
            {/* Lock icon */}
            <svg width="64" height="64" viewBox="0 0 64 64">
              <rect x="14" y="28" width="36" height="28" rx="4" fill="#FF5722" />
              <path d="M 20 28 V 20 a 12 12 0 0 1 24 0 V 28" stroke="#FF5722" strokeWidth="5" fill="none" />
              <circle cx="32" cy="40" r="4" fill="#EEEEEE" />
            </svg>
            <div
              style={{
                fontFamily: 'Nexa Bold, sans-serif',
                fontSize: 64,
                color: '#2D2D2D',
                fontWeight: 900,
                lineHeight: 1.0,
                marginTop: 18,
                letterSpacing: -1,
              }}
            >
              CAN'T BE
              <br />
              <span style={{ color: '#FF5722' }}>TAKEN.</span>
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 26,
                color: '#2D2D2D',
                marginTop: 18,
                fontWeight: 500,
                lineHeight: 1.4,
              }}
            >
              No bank run. No market crash. Your skills stay yours.
            </div>
          </div>

          {/* Card 2 — Can't be inflated */}
          <div
            style={{
              flex: 1,
              maxWidth: 540,
              backgroundColor: '#2D2D2D',
              borderRadius: 18,
              padding: '40px 36px',
              opacity: card2,
              transform: `translateY(${(1 - card2) * 60}px)`,
              boxShadow: '0 16px 40px rgba(28,28,28,0.25)',
              borderLeft: '8px solid #FF5722',
            }}
          >
            {/* Shield icon */}
            <svg width="64" height="64" viewBox="0 0 64 64">
              <path
                d="M 32 6 L 56 16 V 34 C 56 48 32 58 32 58 C 32 58 8 48 8 34 V 16 Z"
                fill="#FF5722"
              />
              <path d="M 22 32 L 30 40 L 44 24" stroke="#EEEEEE" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Nexa Bold, sans-serif',
                fontSize: 64,
                color: '#EEEEEE',
                fontWeight: 900,
                lineHeight: 1.0,
                marginTop: 18,
                letterSpacing: -1,
              }}
            >
              CAN'T BE
              <br />
              <span style={{ color: '#FF5722' }}>INFLATED.</span>
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 26,
                color: '#EEEEEE',
                marginTop: 18,
                fontWeight: 500,
                lineHeight: 1.4,
              }}
            >
              CPI prints 4.2% — your earning power doesn't shrink.
            </div>
          </div>

          {/* Card 3 — Not taxed */}
          <div
            style={{
              flex: 1,
              maxWidth: 540,
              backgroundColor: '#EEEEEE',
              borderRadius: 18,
              padding: '40px 36px',
              opacity: card3,
              transform: `translateY(${(1 - card3) * 60}px) rotate(${(1 - card3) * 3}deg)`,
              boxShadow: '0 16px 40px rgba(28,28,28,0.18)',
              borderLeft: '8px solid #FF5722',
              position: 'relative',
            }}
          >
            {/* Receipt/tax icon */}
            <svg width="64" height="64" viewBox="0 0 64 64">
              <rect x="14" y="8" width="36" height="48" fill="#FF5722" />
              <line x1="20" y1="20" x2="44" y2="20" stroke="#EEEEEE" strokeWidth="3" />
              <line x1="20" y1="30" x2="44" y2="30" stroke="#EEEEEE" strokeWidth="3" />
              <line x1="20" y1="40" x2="36" y2="40" stroke="#EEEEEE" strokeWidth="3" />
              <line x1="8" y1="8" x2="56" y2="56" stroke="#2D2D2D" strokeWidth="5" strokeLinecap="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Nexa Bold, sans-serif',
                fontSize: 64,
                color: '#2D2D2D',
                fontWeight: 900,
                lineHeight: 1.0,
                marginTop: 18,
                letterSpacing: -1,
              }}
            >
              NOT EVEN
              <br />
              <span style={{ color: '#FF5722', position: 'relative', display: 'inline-block' }}>
                TAXED.
                <div
                  style={{
                    position: 'absolute',
                    top: '50%',
                    left: 0,
                    height: 8,
                    width: `${strikeIn * 100}%`,
                    backgroundColor: '#2D2D2D',
                    transform: 'translateY(-50%) rotate(-2deg)',
                  }}
                />
              </span>
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 26,
                color: '#2D2D2D',
                marginTop: 18,
                fontWeight: 500,
                lineHeight: 1.4,
              }}
            >
              IRS can't touch what's inside your head.
            </div>

            {/* 0% badge */}
            <div
              style={{
                position: 'absolute',
                top: -30,
                right: -30,
                width: 110,
                height: 110,
                borderRadius: '50%',
                backgroundColor: '#FF5722',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 38,
                color: '#EEEEEE',
                fontWeight: 900,
                transform: `scale(${card3 * pulse}) rotate(-12deg)`,
                boxShadow: '0 8px 24px rgba(255,87,34,0.5)',
                border: '4px solid #EEEEEE',
              }}
            >
              0% TAX
            </div>
          </div>
        </div>

        {/* BOTTOM PAYOFF */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: payoffIn,
            transform: `translateY(${(1 - payoffIn) * 40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 60,
              color: '#2D2D2D',
              fontWeight: 900,
              letterSpacing: -1,
              lineHeight: 1.1,
            }}
          >
            THE BEST INVESTMENT BY FAR{' '}
            <span style={{ color: '#FF5722' }}>= YOURSELF.</span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene42_TaxFreeCompound;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene43_MoveOne',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene43_MoveOne: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big "01" entrance
  const numberIn = sp(10, { damping: 10, stiffness: 100 });
  const moveTextIn = sp(35);
  const headlineIn = sp(60);
  const auditorIn = sp(120);
  const stampIn = sp(200, { damping: 8, stiffness: 180 });

  // Stamp rotation
  const stampRot = interpolate(stampIn, [0, 1], [-30, -12]);

  // Camera subtle zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Screen shake on stamp impact
  const shake = frame > 200 && frame < 215 ? Math.sin(frame * 2) * 6 : 0;

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 4,
    speed: 0.3 + seededRandom(i + 150) * 0.6,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 70% 80%, rgba(255,87,34,0.12) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 1000,
          height: 1000,
          border: '3px solid #2D2D2D',
          borderRadius: '50%',
          opacity: 0.05,
          top: -400,
          left: -300 + Math.sin(frame * 0.01) * 30,
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          backgroundColor: '#FF5722',
          opacity: 0.06,
          top: 400,
          right: -100,
          transform: `rotate(${45 + frame * 0.05}deg)`,
        }}
      />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line
            key={i}
            x1={i * 80 - 200 + (frame * 0.2) % 80}
            y1="0"
            x2={i * 80 + 100 - 200 + (frame * 0.2) % 80}
            y2="1080"
            stroke="#2D2D2D"
            strokeWidth="1.5"
          />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y + frame * p.speed) % 1080,
            width: p.size,
            height: p.size,
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      <div style={{ position: 'absolute', width: '100%', height: '100%', transform: `scale(${camScale}) translate(${shake}px, 0)` }}>

        {/* MOVE label - top left */}
        <div
          style={{
            position: 'absolute',
            top: 100,
            left: 120,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            opacity: moveTextIn,
            transform: `translateX(${(1 - moveTextIn) * -30}px)`,
          }}
        >
          <div style={{ width: 60, height: 5, backgroundColor: '#FF5722' }} />
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 30,
              color: '#2D2D2D',
              letterSpacing: 6,
              fontWeight: 700,
            }}
          >
            THE PLAYBOOK / MOVE
          </div>
        </div>

        {/* GIANT 01 */}
        <div
          style={{
            position: 'absolute',
            top: 130,
            left: 90,
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 680,
            fontWeight: 900,
            color: '#FF5722',
            opacity: numberIn * 0.18,
            lineHeight: 0.85,
            letterSpacing: -30,
            transform: `scale(${numberIn})`,
            transformOrigin: 'left top',
            pointerEvents: 'none',
          }}
        >
          01
        </div>

        {/* HEADLINE */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 120,
            width: 1400,
            opacity: headlineIn,
            transform: `translateY(${(1 - headlineIn) * 50}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Nexa Bold, sans-serif',
              fontSize: 145,
              fontWeight: 900,
              color: '#2D2D2D',
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            AUDIT YOUR
            <br />
            <span style={{ color: '#FF5722' }}>FUEL & TRANSPORT</span>
            <br />
            SPENDING.
          </div>
        </div>

        {/* AUDITOR icon + caption — bottom left */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 120,
            display: 'flex',
            alignItems: 'center',
            gap: 28,
            opacity: auditorIn,
            transform: `translateY(${(1 - auditorIn) * 30}px)`,
          }}
        >
          {/* Magnifying glass + clipboard SVG */}
          <svg width="100" height="100" viewBox="0 0 100 100">
            <rect x="22" y="18" width="56" height="70" rx="4" fill="#2D2D2D" />
            <rect x="36" y="10" width="28" height="14" rx="2" fill="#FF5722" />
            <line x1="32" y1="40" x2="68" y2="40" stroke="#EEEEEE" strokeWidth="3" />
            <line x1="32" y1="52" x2="68" y2="52" stroke="#EEEEEE" strokeWidth="3" />
            <line x1="32" y1="64" x2="58" y2="64" stroke="#EEEEEE" strokeWidth="3" />
            <circle cx="72" cy="72" r="14" fill="none" stroke="#FF5722" strokeWidth="5" />
            <line x1="82" y1="82" x2="94" y2="94" stroke="#FF5722" strokeWidth="6" strokeLinecap="round" />
          </svg>

          <div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 51,
                color: '#2D2D2D',
                fontWeight: 800,
                lineHeight: 1.1,
                letterSpacing: -0.5,
              }}
            >
              Like an auditor
            </div>
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 64,
                color: '#FF5722',
                fontWeight: 900,
                lineHeight: 1.0,
                letterSpacing: -1,
              }}
            >
              WHO HATES YOU.
            </div>
          </div>
        </div>

        {/* RUTHLESS stamp - bottom right */}
        <div
          style={{
            position: 'absolute',
            bottom: 180,
            right: 140,
            opacity: stampIn,
            transform: `rotate(${stampRot}deg) scale(${stampIn})`,
            border: '8px solid #FF5722',
            padding: '24px 44px',
            backgroundColor: 'rgba(238,238,238,0.15)',
          }}
        >
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 72,
              color: '#FF5722',
              fontWeight: 900,
              letterSpacing: 4,
            }}
          >
            RUTHLESS
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 18,
              color: '#FF5722',
              letterSpacing: 6,
              textAlign: 'center',
              marginTop: 4,
              fontWeight: 700,
            }}
          >
            — NO MERCY —
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene43_MoveOne;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene44_FourActions',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene44_FourActions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const a1 = sp(30);
  const a2 = sp(55);
  const a3 = sp(80);
  const a4 = sp(105);
  const totalIn = sp(220);

  // Counter for savings
  const savingsCount = Math.floor(interpolate(frame, [220, 290], [0, 285], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  const particles = Array.from({ length: 25 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 3,
    speed: 0.3 + seededRandom(i + 150) * 0.5,
  }));

  const actions = [
    {
      sp: a1,
      label: 'CONSOLIDATE',
      sub: 'Errand runs → 1 looped trip',
      svg: (
        <svg width="70" height="70" viewBox="0 0 70 70">
          <circle cx="35" cy="35" r="28" fill="none" stroke="#FF5722" strokeWidth="5" strokeDasharray="6 4" />
          <circle cx="35" cy="10" r="6" fill="#FF5722" />
          <circle cx="58" cy="40" r="6" fill="#FF5722" />
          <circle cx="20" cy="55" r="6" fill="#FF5722" />
        </svg>
      ),
    },
    {
      sp: a2,
      label: 'CARPOOL',
      sub: 'Youth sports w/ 2 other families',
      svg: (
        <svg width="70" height="70" viewBox="0 0 70 70">
          <rect x="8" y="28" width="54" height="20" rx="6" fill="#FF5722" />
          <rect x="14" y="18" width="42" height="14" rx="4" fill="#FF5722" />
          <circle cx="20" cy="52" r="6" fill="#2D2D2D" />
          <circle cx="50" cy="52" r="6" fill="#2D2D2D" />
          <rect x="20" y="20" width="12" height="10" fill="#EEEEEE" />
          <rect x="38" y="20" width="12" height="10" fill="#EEEEEE" />
        </svg>
      ),
    },
    {
      sp: a3,
      label: 'CLAW BACK',
      sub: 'Work-from-home days',
      svg: (
        <svg width="70" height="70" viewBox="0 0 70 70">
          <path d="M 10 35 L 35 12 L 60 35 V 56 H 10 Z" fill="#FF5722" />
          <rect x="28" y="38" width="14" height="18" fill="#EEEEEE" />
          <rect x="20" y="42" width="6" height="6" fill="#EEEEEE" />
          <rect x="44" y="42" width="6" height="6" fill="#EEEEEE" />
        </svg>
      ),
    },
    {
      sp: a4,
      label: 'ASSESS',
      sub: 'That barely driven 2nd vehicle',
      svg: (
        <svg width="70" height="70" viewBox="0 0 70 70">
          <rect x="8" y="30" width="54" height="18" rx="4" fill="#FF5722" />
          <path d="M 14 30 L 20 18 H 50 L 56 30" fill="#FF5722" />
          <circle cx="20" cy="50" r="5" fill="#2D2D2D" />
          <circle cx="50" cy="50" r="5" fill="#2D2D2D" />
          <line x1="6" y1="10" x2="64" y2="64" stroke="#2D2D2D" strokeWidth="5" strokeLinecap="round" />
        </svg>
      ),
    },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(161,188,152,0) 0%, rgba(255,87,34,0.08) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '2px solid #FF5722',
          opacity: 0.05,
          top: -200,
          right: -150,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 0,
          height: 0,
          borderLeft: '200px solid transparent',
          borderRight: '200px solid transparent',
          borderBottom: '340px solid #2D2D2D',
          opacity: 0.05,
          bottom: -100,
          left: 100,
          transform: `rotate(${-frame * 0.03}deg)`,
        }}
      />

      {/* Crosshatch */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <line
            key={`a${i}`}
            x1={-100 + i * 140}
            y1="0"
            x2={400 + i * 140}
            y2="1080"
            stroke="#2D2D2D"
            strokeWidth="1.5"
          />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y + frame * p.speed) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * -30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#FF5722',
            letterSpacing: 6,
            fontWeight: 700,
            marginBottom: 10,
          }}
        >
          — FOUR FIELD ACTIONS —
        </div>
        <div
          style={{
            fontFamily: 'Nexa Bold, sans-serif',
            fontSize: 94,
            color: '#2D2D2D',
            fontWeight: 900,
            letterSpacing: -2,
            lineHeight: 1.0,
          }}
        >
          THE FUEL <span style={{ color: '#FF5722' }}>AUDIT</span> CHECKLIST.
        </div>
      </div>

      {/* FOUR ACTION CARDS */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          left: 80,
          right: 80,
          display: 'grid',
          gridTemplateColumns: '1fr 1fr',
          gap: 30,
        }}
      >
        {actions.map((a, i) => (
          <div
            key={i}
            style={{
              backgroundColor: i % 2 === 0 ? '#EEEEEE' : '#2D2D2D',
              borderRadius: 16,
              padding: '32px 36px',
              display: 'flex',
              alignItems: 'center',
              gap: 28,
              opacity: a.sp,
              transform: `translateX(${(1 - a.sp) * (i % 2 === 0 ? -60 : 60)}px)`,
              boxShadow: '0 12px 32px rgba(28,28,28,0.2)',
              borderLeft: '6px solid #FF5722',
            }}
          >
            {/* Number badge */}
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 72,
                color: '#FF5722',
                fontWeight: 900,
                lineHeight: 1,
                opacity: 0.5,
                minWidth: 80,
              }}
            >
              0{i + 1}
            </div>

            {/* Icon */}
            <div style={{ minWidth: 70 }}>{a.svg}</div>

            {/* Text */}
            <div style={{ flex: 1 }}>
              <div
                style={{
                  fontFamily: 'Nexa Bold, sans-serif',
                  fontSize: 51,
                  color: i % 2 === 0 ? '#2D2D2D' : '#EEEEEE',
                  fontWeight: 900,
                  letterSpacing: -0.5,
                  lineHeight: 1.0,
                }}
              >
                {a.label}
              </div>
              <div
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 26,
                  color: i % 2 === 0 ? '#2D2D2D' : '#EEEEEE',
                  fontWeight: 500,
                  marginTop: 6,
                  opacity: 0.85,
                }}
              >
                {a.sub}
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* TOTAL SAVINGS BAR — bottom */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: totalIn,
          transform: `translateY(${(1 - totalIn) * 30}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 24,
            backgroundColor: '#FF5722',
            padding: '18px 42px',
            borderRadius: 12,
            boxShadow: '0 10px 30px rgba(255,87,34,0.4)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              color: '#EEEEEE',
              letterSpacing: 4,
              fontWeight: 700,
            }}
          >
            POTENTIAL MONTHLY SAVE →
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 64,
              color: '#EEEEEE',
              fontWeight: 900,
              letterSpacing: -1,
            }}
          >
            ${savingsCount}+
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.35) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene44_FourActions;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene45_MoveTwo',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene45_MoveTwo: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const numIn = sp(10, { damping: 10, stiffness: 100 });
  const labelIn = sp(35);
  const headIn = sp(60);
  const fireIn = sp(140);
  const subIn = sp(200);

  // Fire flicker
  const flicker = 1 + Math.sin(frame * 0.3) * 0.06 + Math.sin(frame * 0.5) * 0.03;

  const particles = Array.from({ length: 30 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 5,
    speed: 0.5 + seededRandom(i + 150) * 1.2,
  }));

  // Ember particles rising
  const embers = Array.from({ length: 35 }, (_, i) => {
    const baseX = 1300 + seededRandom(i + 200) * 400;
    const baseY = 700 + seededRandom(i + 300) * 200;
    const speed = 1 + seededRandom(i + 400) * 2;
    const wave = Math.sin((frame + i * 10) * 0.05) * 30;
    return {
      x: baseX + wave,
      y: (baseY - (frame * speed)) % 1080,
      size: 2 + seededRandom(i + 500) * 4,
      opacity: 0.4 + seededRandom(i + 600) * 0.4,
    };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient with orange glow on right */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 80% 70%, rgba(255,87,34,0.25) 0%, rgba(161,188,152,0) 50%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          border: '3px solid #2D2D2D',
          opacity: 0.05,
          top: -300,
          left: -200,
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,87,34,0.18) 0%, rgba(255,87,34,0) 70%)',
          bottom: -100,
          right: -100,
          transform: `scale(${flicker})`,
        }}
      />

      {/* Wavy heat lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <path
            key={i}
            d={`M 0 ${i * 110 + 50} Q 480 ${i * 110 + 50 + Math.sin(frame * 0.05 + i) * 30} 960 ${i * 110 + 50} T 1920 ${i * 110 + 50}`}
            stroke="#FF5722"
            strokeWidth="1.5"
            fill="none"
          />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y - frame * p.speed + 1080) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      {/* MOVE label */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: 120,
          display: 'flex',
          alignItems: 'center',
          gap: 18,
          opacity: labelIn,
          transform: `translateX(${(1 - labelIn) * -30}px)`,
        }}
      >
        <div style={{ width: 60, height: 5, backgroundColor: '#FF5722' }} />
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 30,
            color: '#2D2D2D',
            letterSpacing: 6,
            fontWeight: 700,
          }}
        >
          THE PLAYBOOK / MOVE
        </div>
      </div>

      {/* GIANT 02 */}
      <div
        style={{
          position: 'absolute',
          top: 130,
          left: 90,
          fontFamily: 'Mont Heavy, sans-serif',
          fontSize: 680,
          fontWeight: 900,
          color: '#FF5722',
          opacity: numIn * 0.18,
          lineHeight: 0.85,
          letterSpacing: -30,
          transform: `scale(${numIn})`,
          transformOrigin: 'left top',
          pointerEvents: 'none',
        }}
      >
        02
      </div>

      {/* HEADLINE - left side */}
      <div
        style={{
          position: 'absolute',
          top: 290,
          left: 120,
          width: 1050,
          opacity: headIn,
          transform: `translateY(${(1 - headIn) * 50}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Nexa Bold, sans-serif',
            fontSize: 128,
            fontWeight: 900,
            color: '#2D2D2D',
            lineHeight: 0.95,
            letterSpacing: -3,
          }}
        >
          YOUR
          <br />
          PORTFOLIO IS
          <br />
          <span style={{ color: '#FF5722' }}>PRETENDING.</span>
        </div>
      </div>

      {/* FIRE / Burning house icon - right side */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          right: 120,
          width: 480,
          height: 480,
          opacity: fireIn,
          transform: `scale(${fireIn * flicker})`,
          transformOrigin: 'center bottom',
        }}
      >
        {/* Embers */}
        {embers.map((e, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: e.x - 1200,
              top: e.y - 320,
              width: e.size,
              height: e.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#FF5722' : '#EEEEEE',
              opacity: e.opacity,
              boxShadow: '0 0 8px #FF5722',
            }}
          />
        ))}

        {/* Globe/world icon with flames */}
        <svg width="480" height="480" viewBox="0 0 480 480">
          {/* Glow */}
          <circle cx="240" cy="260" r="180" fill="#FF5722" opacity="0.2" />

          {/* World/globe */}
          <circle cx="240" cy="260" r="140" fill="#2D2D2D" />
          <circle cx="240" cy="260" r="140" fill="none" stroke="#FF5722" strokeWidth="4" />

          {/* Continents - simplified */}
          <path
            d="M 160 220 Q 180 200 210 210 L 230 230 L 220 260 L 190 270 L 170 250 Z"
            fill="#A1BC98"
            opacity="0.8"
          />
          <path
            d="M 260 240 Q 300 230 320 260 L 310 290 L 280 300 L 260 280 Z"
            fill="#A1BC98"
            opacity="0.8"
          />
          <path
            d="M 200 310 L 240 305 L 260 330 L 230 350 L 200 340 Z"
            fill="#A1BC98"
            opacity="0.8"
          />

          {/* Latitude lines */}
          <ellipse cx="240" cy="260" rx="140" ry="40" fill="none" stroke="#FF5722" strokeWidth="1.5" opacity="0.5" />
          <ellipse cx="240" cy="260" rx="140" ry="80" fill="none" stroke="#FF5722" strokeWidth="1.5" opacity="0.5" />
          <line x1="100" y1="260" x2="380" y2="260" stroke="#FF5722" strokeWidth="1.5" opacity="0.5" />

          {/* Flames around the globe */}
          <path
            d={`M 240 100 Q 220 ${60 + Math.sin(frame * 0.2) * 10} 230 ${30 + Math.sin(frame * 0.25) * 8} Q 250 ${50 + Math.cos(frame * 0.2) * 10} 240 ${20 + Math.sin(frame * 0.3) * 6} Q 260 ${45 + Math.sin(frame * 0.22) * 8} 250 ${75 + Math.cos(frame * 0.2) * 6} Q 270 ${90 + Math.sin(frame * 0.2) * 5} 260 110 Z`}
            fill="#FF5722"
          />
          <path
            d={`M 200 110 Q 180 ${70 + Math.cos(frame * 0.25) * 8} 195 ${50 + Math.sin(frame * 0.3) * 6} Q 215 ${80 + Math.cos(frame * 0.2) * 6} 210 120 Z`}
            fill="#FF5722"
            opacity="0.8"
          />
          <path
            d={`M 280 115 Q 305 ${85 + Math.sin(frame * 0.22) * 8} 295 ${60 + Math.cos(frame * 0.28) * 6} Q 280 ${90 + Math.sin(frame * 0.2) * 6} 280 125 Z`}
            fill="#FF5722"
            opacity="0.8"
          />
        </svg>
      </div>

      {/* BOTTOM subhead */}
      <div
        style={{
          position: 'absolute',
          bottom: 110,
          left: 120,
          width: 1500,
          opacity: subIn,
          transform: `translateY(${(1 - subIn) * 30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 51,
            fontWeight: 700,
            color: '#EEEEEE',
            lineHeight: 1.2,
            textShadow: '0 2px 12px rgba(0,0,0,0.3)',
          }}
        >
          The real world{' '}
          <span
            style={{
              color: '#FF5722',
              backgroundColor: '#2D2D2D',
              padding: '4px 18px',
              borderRadius: 6,
            }}
          >
            ISN'T ON FIRE
          </span>
          ? Look again.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.45) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene45_MoveTwo;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene46_ZeroExposure',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene46_ZeroExposure: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const zeroIn = sp(10, { damping: 8, stiffness: 100 });
  const labelIn = sp(40);
  const pieIn = sp(80);
  const arrowIn = sp(160);
  const betIn = sp(200);

  // Pie chart sweep
  const pieSweep = interpolate(frame, [80, 160], [0, 360], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Arrow draw
  const arrowDraw = interpolate(frame, [160, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Counter
  const inflCount = interpolate(frame, [60, 140], [0, 4.2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 4,
    speed: 0.3 + seededRandom(i + 150) * 0.5,
  }));

  // Pie chart arc path generator
  const polarToCart = (cx: number, cy: number, r: number, angleDeg: number) => {
    const a = (angleDeg - 90) * Math.PI / 180;
    return { x: cx + r * Math.cos(a), y: cy + r * Math.sin(a) };
  };

  const describeArc = (cx: number, cy: number, r: number, startAngle: number, endAngle: number) => {
    const start = polarToCart(cx, cy, r, endAngle);
    const end = polarToCart(cx, cy, r, startAngle);
    const largeArc = endAngle - startAngle <= 180 ? '0' : '1';
    return `M ${cx} ${cy} L ${end.x} ${end.y} A ${r} ${r} 0 ${largeArc} 0 ${start.x} ${start.y} Z`;
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 50%, rgba(238,238,238,0.2) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 700,
          height: 700,
          border: '2px solid #FF5722',
          borderRadius: 30,
          opacity: 0.04,
          top: 150,
          right: -200,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 400,
          height: 400,
          borderRadius: '50%',
          border: '3px solid #2D2D2D',
          opacity: 0.05,
          bottom: -100,
          left: 200 + Math.sin(frame * 0.015) * 20,
        }}
      />

      {/* Dot grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 24 }).flatMap((_, i) =>
          Array.from({ length: 14 }).map((_, j) => (
            <circle key={`${i}-${j}`} cx={i * 85 + 40} cy={j * 85 + 40} r="1.5" fill="#2D2D2D" />
          ))
        )}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y + frame * p.speed) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      {/* LABEL top */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 120,
          display: 'flex',
          alignItems: 'center',
          gap: 16,
          opacity: labelIn,
          transform: `translateX(${(1 - labelIn) * -30}px)`,
        }}
      >
        <div style={{ width: 50, height: 4, backgroundColor: '#FF5722' }} />
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#2D2D2D',
            letterSpacing: 5,
            fontWeight: 700,
          }}
        >
          YOUR PORTFOLIO TODAY
        </div>
      </div>

      {/* LEFT - PIE CHART */}
      <div
        style={{
          position: 'absolute',
          top: 180,
          left: 100,
          width: 620,
          opacity: pieIn,
          transform: `translateX(${(1 - pieIn) * -40}px)`,
        }}
      >
        <svg width="560" height="560" viewBox="0 0 560 560">
          {/* Background pie circle */}
          <circle cx="280" cy="280" r="240" fill="#EEEEEE" stroke="#2D2D2D" strokeWidth="6" />

          {/* Animated stocks slice (gray fill) */}
          <path
            d={describeArc(280, 280, 240, 0, Math.min(pieSweep, 360))}
            fill="#9E9E9E"
          />

          {/* Center white circle - donut hole */}
          <circle cx="280" cy="280" r="110" fill="#A1BC98" stroke="#2D2D2D" strokeWidth="4" />

          {/* Center text */}
          <text
            x="280"
            y="270"
            textAnchor="middle"
            fontFamily="JetBrains Mono, monospace"
            fontSize="22"
            fill="#2D2D2D"
            fontWeight="700"
            letterSpacing="2"
          >
            ENERGY
          </text>
          <text
            x="280"
            y="320"
            textAnchor="middle"
            fontFamily="Mont Heavy, sans-serif"
            fontSize="64"
            fill="#FF5722"
            fontWeight="900"
          >
            0%
          </text>
        </svg>

        {/* Labels around pie */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            right: -40,
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            color: '#2D2D2D',
            fontWeight: 700,
          }}
        >
          STOCKS 70%
        </div>
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            right: -20,
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            color: '#2D2D2D',
            fontWeight: 700,
          }}
        >
          BONDS 30%
        </div>
      </div>

      {/* CENTER - VS arrow */}
      <div
        style={{
          position: 'absolute',
          top: 440,
          left: 740,
          opacity: arrowIn,
          transform: `scale(${arrowIn})`,
        }}
      >
        <svg width="180" height="80" viewBox="0 0 180 80">
          {/* Arrow line */}
          <line
            x1="10"
            y1="40"
            x2={10 + 140 * arrowDraw}
            y2="40"
            stroke="#FF5722"
            strokeWidth="8"
            strokeLinecap="round"
          />
          {/* Arrowhead */}
          {arrowDraw > 0.7 && (
            <polygon
              points="150,40 130,25 130,55"
              fill="#FF5722"
            />
          )}
        </svg>
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#2D2D2D',
            fontWeight: 700,
            letterSpacing: 4,
            textAlign: 'center',
            marginTop: 4,
          }}
        >
          BETTING AGAINST
        </div>
      </div>

      {/* RIGHT - Inflation force visual */}
      <div
        style={{
          position: 'absolute',
          top: 200,
          right: 120,
          width: 540,
          opacity: betIn,
          transform: `translateX(${(1 - betIn) * 40}px)`,
        }}
      >
        {/* Big inflation card */}
        <div
          style={{
            backgroundColor: '#2D2D2D',
            borderRadius: 18,
            padding: '40px',
            boxShadow: '0 16px 40px rgba(28,28,28,0.3)',
            borderTop: '8px solid #FF5722',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              color: '#FF5722',
              letterSpacing: 4,
              fontWeight: 700,
              marginBottom: 14,
            }}
          >
            THE FORCE //
          </div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 130,
              color: '#FF5722',
              fontWeight: 900,
              lineHeight: 0.9,
              letterSpacing: -3,
            }}
          >
            +{inflCount.toFixed(1)}%
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 30,
              color: '#EEEEEE',
              fontWeight: 600,
              marginTop: 10,
              lineHeight: 1.2,
            }}
          >
            jacking up<br />
            <span style={{ color: '#FF5722' }}>EVERYTHING ELSE.</span>
          </div>

          {/* Mini up arrows */}
          <div style={{ display: 'flex', gap: 12, marginTop: 24 }}>
            {['GAS', 'FOOD', 'RENT', 'TRAVEL'].map((tag, i) => (
              <div
                key={tag}
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 18,
                  color: '#FF5722',
                  fontWeight: 700,
                  backgroundColor: 'rgba(255,87,34,0.15)',
                  padding: '6px 12px',
                  borderRadius: 6,
                  border: '1px solid #FF5722',
                  letterSpacing: 1.5,
                }}
              >
                {tag} ↑
              </div>
            ))}
          </div>
        </div>
      </div>

      {/* BOTTOM payoff */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: betIn,
          transform: `translateY(${(1 - betIn) * 20}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 56,
            color: '#2D2D2D',
            fontWeight: 900,
            letterSpacing: -1,
          }}
        >
          ZERO ENERGY = <span style={{ color: '#FF5722' }}>A SILENT BET YOU'LL LOSE.</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene46_ZeroExposure;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene47_ShockAbsorber',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene47_ShockAbsorber: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const tag1 = sp(40);
  const tag2 = sp(65);
  const chartIn = sp(100);
  const absorberIn = sp(220);

  // Line chart - two paths drawing
  const lineProgress = interpolate(frame, [100, 240], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // Shock pulse
  const shockPulse = 1 + Math.sin(frame * 0.2) * 0.05;

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 4,
    speed: 0.3 + seededRandom(i + 150) * 0.5,
  }));

  // Generate chart points
  const portfolioLine = [
    { x: 60, y: 240 },
    { x: 180, y: 250 },
    { x: 300, y: 260 },
    { x: 420, y: 300 },
    { x: 540, y: 380 },
    { x: 660, y: 440 },
    { x: 780, y: 460 },
    { x: 900, y: 470 },
  ];

  const energyLine = [
    { x: 60, y: 280 },
    { x: 180, y: 260 },
    { x: 300, y: 240 },
    { x: 420, y: 200 },
    { x: 540, y: 150 },
    { x: 660, y: 110 },
    { x: 780, y: 85 },
    { x: 900, y: 70 },
  ];

  const blendedLine = [
    { x: 60, y: 260 },
    { x: 180, y: 255 },
    { x: 300, y: 250 },
    { x: 420, y: 240 },
    { x: 540, y: 230 },
    { x: 660, y: 220 },
    { x: 780, y: 210 },
    { x: 900, y: 200 },
  ];

  const pathString = (pts: { x: number; y: number }[]) =>
    pts.map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`).join(' ');

  // Animate path length
  const totalLen = 1200;
  const dashOffset = totalLen - totalLen * lineProgress;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(238,238,238,0.15) 0%, rgba(161,188,152,0) 50%, rgba(255,87,34,0.1) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          borderRadius: '50%',
          border: '3px solid #FF5722',
          opacity: 0.05,
          top: -200,
          right: -150 + Math.sin(frame * 0.012) * 25,
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          backgroundColor: '#2D2D2D',
          opacity: 0.04,
          bottom: -200,
          left: -100,
          transform: `rotate(${30 + frame * 0.03}deg)`,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line
            key={`v${i}`}
            x1={i * 100}
            y1="0"
            x2={i * 100}
            y2="1080"
            stroke="#2D2D2D"
            strokeWidth="1"
          />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y + frame * p.speed) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      {/* TITLE */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 120,
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * -30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 26,
            color: '#FF5722',
            letterSpacing: 5,
            fontWeight: 700,
            marginBottom: 6,
          }}
        >
          ENERGY SECTOR FUNDS
        </div>
        <div
          style={{
            fontFamily: 'Nexa Bold, sans-serif',
            fontSize: 89,
            color: '#2D2D2D',
            fontWeight: 900,
            letterSpacing: -2,
            lineHeight: 0.95,
          }}
        >
          THE BUILT-IN<br />
          <span style={{ color: '#FF5722' }}>SHOCK ABSORBER.</span>
        </div>
      </div>

      {/* LEFT TAGS - oil majors / pipeline */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          left: 120,
          display: 'flex',
          flexDirection: 'column',
          gap: 18,
        }}
      >
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            backgroundColor: '#2D2D2D',
            padding: '20px 28px',
            borderRadius: 12,
            opacity: tag1,
            transform: `translateX(${(1 - tag1) * -40}px)`,
            borderLeft: '6px solid #FF5722',
          }}
        >
          {/* Oil barrel SVG */}
          <svg width="48" height="48" viewBox="0 0 48 48">
            <ellipse cx="24" cy="10" rx="14" ry="4" fill="#FF5722" />
            <rect x="10" y="10" width="28" height="30" fill="#FF5722" />
            <ellipse cx="24" cy="40" rx="14" ry="4" fill="#C84315" />
            <line x1="10" y1="20" x2="38" y2="20" stroke="#2D2D2D" strokeWidth="2" />
            <line x1="10" y1="30" x2="38" y2="30" stroke="#2D2D2D" strokeWidth="2" />
          </svg>
          <div>
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 32,
                color: '#EEEEEE',
                fontWeight: 900,
                lineHeight: 1,
                letterSpacing: -0.5,
              }}
            >
              INTEGRATED OIL MAJORS
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 20,
                color: '#FF5722',
                fontWeight: 600,
                marginTop: 4,
                letterSpacing: 1,
              }}
            >
              Upstream → Downstream cashflow
            </div>
          </div>
        </div>

        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            backgroundColor: '#EEEEEE',
            padding: '20px 28px',
            borderRadius: 12,
            opacity: tag2,
            transform: `translateX(${(1 - tag2) * -40}px)`,
            borderLeft: '6px solid #FF5722',
          }}
        >
          {/* Pipeline SVG */}
          <svg width="48" height="48" viewBox="0 0 48 48">
            <rect x="4" y="20" width="40" height="10" fill="#FF5722" />
            <circle cx="4" cy="25" r="6" fill="#FF5722" />
            <circle cx="44" cy="25" r="6" fill="#FF5722" />
            <rect x="12" y="14" width="4" height="22" fill="#2D2D2D" />
            <rect x="32" y="14" width="4" height="22" fill="#2D2D2D" />
          </svg>
          <div>
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 32,
                color: '#2D2D2D',
                fontWeight: 900,
                lineHeight: 1,
                letterSpacing: -0.5,
              }}
            >
              PIPELINE OPERATORS
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 20,
                color: '#FF5722',
                fontWeight: 600,
                marginTop: 4,
                letterSpacing: 1,
              }}
            >
              Volume-based earnings, distributions
            </div>
          </div>
        </div>
      </div>

      {/* RIGHT — Chart */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          right: 80,
          width: 980,
          opacity: chartIn,
          transform: `translateX(${(1 - chartIn) * 40}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            color: '#2D2D2D',
            letterSpacing: 3,
            fontWeight: 700,
            marginBottom: 12,
          }}
        >
          DURING THE 2022 SUPPLY SHOCK //
        </div>

        <svg width="960" height="540" viewBox="0 0 960 540">
          {/* Background */}
          <rect x="40" y="20" width="900" height="450" fill="rgba(238,238,238,0.4)" rx="8" />

          {/* Grid */}
          {[0, 1, 2, 3, 4].map((i) => (
            <line
              key={i}
              x1="40"
              y1={20 + i * 112}
              x2="940"
              y2={20 + i * 112}
              stroke="#2D2D2D"
              strokeWidth="1"
              opacity="0.2"
            />
          ))}

          {/* Y-axis labels */}
          <text x="20" y="30" fontFamily="JetBrains Mono" fontSize="14" fill="#2D2D2D" textAnchor="end">+50%</text>
          <text x="20" y="250" fontFamily="JetBrains Mono" fontSize="14" fill="#2D2D2D" textAnchor="end">0%</text>
          <text x="20" y="470" fontFamily="JetBrains Mono" fontSize="14" fill="#2D2D2D" textAnchor="end">-50%</text>

          {/* Portfolio without energy — dropping */}
          <path
            d={pathString(portfolioLine)}
            stroke="#9E9E9E"
            strokeWidth="5"
            fill="none"
            strokeLinecap="round"
            strokeDasharray={totalLen}
            strokeDashoffset={dashOffset}
          />

          {/* Energy line — rising */}
          <path
            d={pathString(energyLine)}
            stroke="#FF5722"
            strokeWidth="6"
            fill="none"
            strokeLinecap="round"
            strokeDasharray={totalLen}
            strokeDashoffset={dashOffset}
          />

          {/* Blended — smooth flat (the absorber effect) */}
          <path
            d={pathString(blendedLine)}
            stroke="#2D2D2D"
            strokeWidth="5"
            fill="none"
            strokeLinecap="round"
            strokeDasharray="8 6"
            strokeDashoffset={dashOffset * 0.8}
          />

          {/* Legend */}
          <g transform="translate(50, 490)">
            <rect x="0" y="0" width="22" height="6" fill="#9E9E9E" />
            <text x="30" y="8" fontFamily="Inter" fontSize="16" fill="#2D2D2D" fontWeight="600">No energy</text>

            <rect x="190" y="0" width="22" height="6" fill="#FF5722" />
            <text x="220" y="8" fontFamily="Inter" fontSize="16" fill="#2D2D2D" fontWeight="600">Energy fund</text>

            <rect x="380" y="0" width="22" height="6" fill="#2D2D2D" />
            <text x="410" y="8" fontFamily="Inter" fontSize="16" fill="#2D2D2D" fontWeight="600">Blended (absorber)</text>
          </g>

          {/* End-point markers */}
          {lineProgress > 0.9 && (
            <>
              <circle cx="900" cy="70" r="9" fill="#FF5722" />
              <circle cx="900" cy="470" r="9" fill="#9E9E9E" />
              <circle cx="900" cy="200" r="9" fill="#2D2D2D" />
            </>
          )}
        </svg>
      </div>

      {/* BOTTOM ABSORBER label */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 120,
          display: 'flex',
          alignItems: 'center',
          gap: 24,
          opacity: absorberIn,
          transform: `translateY(${(1 - absorberIn) * 30}px) scale(${shockPulse})`,
          transformOrigin: 'left center',
        }}
      >
          <div
            style={{
              backgroundColor: '#FF5722',
              padding: '14px 30px',
              borderRadius: 10,
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 42,
              color: '#EEEEEE',
              fontWeight: 900,
              letterSpacing: -0.5,
              boxShadow: '0 8px 24px rgba(255,87,34,0.45)',
            }}
          >
            CRUDE SURGES → ENERGY GUSHES CASH
          </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene47_ShockAbsorber;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene48_MoveThree',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene48_MoveThree: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const numIn = sp(10, { damping: 10, stiffness: 100 });
  const labelIn = sp(35);
  const headIn = sp(60);
  const chartIn = sp(110);
  const stopIn = sp(200);

  // Volatile line drawing
  const drawProg = interpolate(frame, [110, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Pulse on stop
  const stopPulse = 1 + Math.sin(frame * 0.18) * 0.03;

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 4,
    speed: 0.3 + seededRandom(i + 150) * 0.5,
  }));

  // Volatile chart points
  const volatilePts = [
    { x: 30, y: 200 }, { x: 90, y: 240 }, { x: 150, y: 180 }, { x: 210, y: 260 },
    { x: 270, y: 140 }, { x: 330, y: 220 }, { x: 390, y: 100 }, { x: 450, y: 200 },
    { x: 510, y: 80 }, { x: 570, y: 180 }, { x: 630, y: 60 }, { x: 690, y: 160 },
    { x: 750, y: 100 }, { x: 810, y: 200 }, { x: 870, y: 140 },
  ];

  const pathStr = volatilePts.map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 90%, rgba(255,87,34,0.15) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 800,
          height: 800,
          border: '3px solid #2D2D2D',
          borderRadius: 40,
          opacity: 0.04,
          top: 100,
          right: -200,
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,87,34,0.12) 0%, rgba(255,87,34,0) 70%)',
          bottom: -150,
          left: -100,
        }}
      />

      {/* Diagonal stripes */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 25 }).map((_, i) => (
          <line
            key={i}
            x1={i * 90 - 200}
            y1="0"
            x2={i * 90 + 200 - 200}
            y2="1080"
            stroke="#2D2D2D"
            strokeWidth="1.5"
          />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y + frame * p.speed) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      {/* MOVE label */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: 120,
          display: 'flex',
          alignItems: 'center',
          gap: 18,
          opacity: labelIn,
          transform: `translateX(${(1 - labelIn) * -30}px)`,
        }}
      >
        <div style={{ width: 60, height: 5, backgroundColor: '#FF5722' }} />
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 30,
            color: '#2D2D2D',
            letterSpacing: 6,
            fontWeight: 700,
          }}
        >
          THE PLAYBOOK / MOVE
        </div>
      </div>

      {/* GIANT 03 */}
      <div
        style={{
          position: 'absolute',
          top: 130,
          left: 90,
          fontFamily: 'Mont Heavy, sans-serif',
          fontSize: 680,
          fontWeight: 900,
          color: '#FF5722',
          opacity: numIn * 0.18,
          lineHeight: 0.85,
          letterSpacing: -30,
          transform: `scale(${numIn})`,
          transformOrigin: 'left top',
          pointerEvents: 'none',
        }}
      >
        03
      </div>

      {/* HEADLINE */}
      <div
        style={{
          position: 'absolute',
          top: 290,
          left: 120,
          width: 1200,
          opacity: headIn,
          transform: `translateY(${(1 - headIn) * 50}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Nexa Bold, sans-serif',
            fontSize: 115,
            fontWeight: 900,
            color: '#2D2D2D',
            lineHeight: 0.95,
            letterSpacing: -3,
          }}
        >
          MENTAL,<br />
          NOT <span style={{ color: '#FF5722' }}>MECHANICAL.</span>
        </div>
      </div>

      {/* RIGHT side — volatile chart with X markers */}
      <div
        style={{
          position: 'absolute',
          top: 310,
          right: 100,
          width: 540,
          opacity: chartIn,
          transform: `translateX(${(1 - chartIn) * 40}px)`,
        }}
      >
        <div
          style={{
            backgroundColor: '#EEEEEE',
            borderRadius: 14,
            padding: 24,
            boxShadow: '0 12px 32px rgba(28,28,28,0.18)',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 18,
              color: '#2D2D2D',
              fontWeight: 700,
              letterSpacing: 2,
              marginBottom: 8,
            }}
          >
            OIL PRICE — DAILY
          </div>

          <svg width="490" height="290" viewBox="0 0 900 290">
            {/* Chart */}
            <path
              d={pathStr}
              stroke="#FF5722"
              strokeWidth="5"
              fill="none"
              strokeLinecap="round"
              strokeDasharray="2000"
              strokeDashoffset={2000 - 2000 * drawProg}
            />

            {/* Try-to-time X markers */}
            {drawProg > 0.4 && (
              <>
                <g transform="translate(390, 100)">
                  <circle r="18" fill="none" stroke="#2D2D2D" strokeWidth="4" />
                  <line x1="-10" y1="-10" x2="10" y2="10" stroke="#2D2D2D" strokeWidth="4" strokeLinecap="round" />
                  <line x1="10" y1="-10" x2="-10" y2="10" stroke="#2D2D2D" strokeWidth="4" strokeLinecap="round" />
                  <text x="0" y="-30" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="16" fontWeight="700" fill="#2D2D2D">TOP?</text>
                </g>
                <g transform="translate(630, 60)">
                  <circle r="18" fill="none" stroke="#2D2D2D" strokeWidth="4" />
                  <line x1="-10" y1="-10" x2="10" y2="10" stroke="#2D2D2D" strokeWidth="4" strokeLinecap="round" />
                  <line x1="10" y1="-10" x2="-10" y2="10" stroke="#2D2D2D" strokeWidth="4" strokeLinecap="round" />
                  <text x="0" y="-30" textAnchor="middle" fontFamily="JetBrains Mono" fontSize="16" fontWeight="700" fill="#2D2D2D">TOP?</text>
                </g>
              </>
            )}
          </svg>

          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 22,
              color: '#2D2D2D',
              fontWeight: 600,
              marginTop: 8,
              textAlign: 'center',
              fontStyle: 'italic',
            }}
          >
            Nobody rings a bell at the top.
          </div>
        </div>
      </div>

      {/* BOTTOM — STOP TRYING TO TIME tag */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: 120,
          display: 'flex',
          alignItems: 'center',
          gap: 22,
          opacity: stopIn,
          transform: `translateY(${(1 - stopIn) * 30}px) scale(${stopPulse})`,
          transformOrigin: 'left center',
        }}
      >
        {/* Octagon stop sign */}
        <svg width="100" height="100" viewBox="0 0 100 100">
          <polygon
            points="30,10 70,10 95,35 95,75 70,95 30,95 5,75 5,35"
            fill="#FF5722"
            stroke="#EEEEEE"
            strokeWidth="6"
          />
          <text
            x="50"
            y="60"
            textAnchor="middle"
            fontFamily="Mont Heavy"
            fontSize="22"
            fill="#EEEEEE"
            fontWeight="900"
          >
            STOP
          </text>
        </svg>

        <div>
          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 51,
              color: '#2D2D2D',
              fontWeight: 900,
              lineHeight: 1.0,
              letterSpacing: -0.5,
            }}
          >
            STOP TRYING TO TIME
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              color: '#FF5722',
              fontWeight: 700,
              marginTop: 6,
              letterSpacing: 1,
            }}
          >
            oil tops or growth-stock bottoms.
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene48_MoveThree;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene49_ProcessVsPrediction',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene49_ProcessVsPrediction: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const leftIn = sp(20);
  const vsIn = sp(60, { damping: 8, stiffness: 150 });
  const rightIn = sp(90);
  const winnerIn = sp(160);
  const quoteIn = sp(220);

  // VS pulse
  const vsPulse = 1 + Math.sin(frame * 0.2) * 0.05;

  // Camera subtle zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.0]);

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 4,
    speed: 0.3 + seededRandom(i + 150) * 0.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, rgba(255,87,34,0.1) 0%, rgba(161,188,152,0) 50%, rgba(238,238,238,0.18) 100%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          border: '3px solid #FF5722',
          borderRadius: '50%',
          opacity: 0.05,
          top: -200,
          left: -100,
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          border: '3px solid #2D2D2D',
          opacity: 0.05,
          bottom: -200,
          right: -100,
          transform: `rotate(${-frame * 0.04}deg)`,
        }}
      />

      {/* Vertical divider line */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: 140,
          bottom: 180,
          width: 2,
          background: 'linear-gradient(180deg, transparent 0%, #2D2D2D 30%, #2D2D2D 70%, transparent 100%)',
          opacity: 0.3,
        }}
      />

      {/* Texture circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <circle
            key={i}
            cx={(i * 180 + frame * 0.3) % 1920}
            cy={i * 90 + 50}
            r="40"
            fill="none"
            stroke="#2D2D2D"
            strokeWidth="1.5"
          />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y + frame * p.speed) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      {/* TOP LABEL */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: leftIn,
          transform: `translateY(${(1 - leftIn) * -20}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            color: '#FF5722',
            letterSpacing: 6,
            fontWeight: 700,
          }}
        >
          — THE GREAT DEBATE —
        </div>
      </div>

      <div style={{ position: 'absolute', width: '100%', height: '100%', transform: `scale(${camScale})`, transformOrigin: 'center' }}>

        {/* LEFT — PROCESS */}
        <div
          style={{
            position: 'absolute',
            top: 160,
            left: 70,
            width: 820,
            opacity: leftIn,
            transform: `translateX(${(1 - leftIn) * -60}px)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#2D2D2D',
              borderRadius: 18,
              padding: '50px 44px',
              boxShadow: '0 16px 40px rgba(28,28,28,0.3)',
              borderTop: '8px solid #FF5722',
              height: 540,
            }}
          >
            {/* Gears icon */}
            <svg width="70" height="70" viewBox="0 0 70 70">
              <circle cx="35" cy="35" r="14" fill="none" stroke="#FF5722" strokeWidth="5" />
              <circle cx="35" cy="35" r="6" fill="#FF5722" />
              {[0, 45, 90, 135, 180, 225, 270, 315].map((a, i) => {
                const rad = (a * Math.PI) / 180;
                const x = 35 + 22 * Math.cos(rad);
                const y = 35 + 22 * Math.sin(rad);
                return <rect key={i} x={x - 4} y={y - 4} width="8" height="8" fill="#FF5722" transform={`rotate(${a + (frame * 1)} ${x} ${y})`} />;
              })}
            </svg>

            <div
              style={{
                fontFamily: 'Nexa Bold, sans-serif',
                fontSize: 110,
                color: '#EEEEEE',
                fontWeight: 900,
                lineHeight: 0.95,
                letterSpacing: -3,
                marginTop: 18,
              }}
            >
              <span style={{ color: '#FF5722' }}>PROCESS</span>
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 24,
                color: '#A1BC98',
                letterSpacing: 4,
                fontWeight: 700,
                marginTop: 8,
              }}
            >
              CONSISTENCY · DISCIPLINE
            </div>

            <div style={{ marginTop: 30 }}>
              {['Repeatable', 'Compounding', 'Survives chaos'].map((item, i) => (
                <div
                  key={item}
                  style={{
                    display: 'flex',
                    alignItems: 'center',
                    gap: 14,
                    marginTop: 14,
                    opacity: interpolate(frame, [40 + i * 15, 70 + i * 15], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                  }}
                >
                  <svg width="28" height="28" viewBox="0 0 28 28">
                    <circle cx="14" cy="14" r="13" fill="#FF5722" />
                    <path d="M 8 14 L 12 18 L 20 10" stroke="#EEEEEE" strokeWidth="3" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                  </svg>
                  <div
                    style={{
                      fontFamily: 'Inter, sans-serif',
                      fontSize: 32,
                      color: '#EEEEEE',
                      fontWeight: 600,
                    }}
                  >
                    {item}
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* CENTER - VS badge */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: '50%',
            transform: `translate(-50%, -50%) scale(${vsIn * vsPulse})`,
            opacity: vsIn,
            zIndex: 5,
          }}
        >
          <div
            style={{
              width: 140,
              height: 140,
              borderRadius: '50%',
              backgroundColor: '#FF5722',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 64,
              color: '#EEEEEE',
              fontWeight: 900,
              letterSpacing: -2,
              boxShadow: '0 12px 30px rgba(255,87,34,0.5)',
              border: '5px solid #2D2D2D',
            }}
          >
            VS
          </div>
        </div>

        {/* RIGHT — PREDICTION (loser) */}
        <div
          style={{
            position: 'absolute',
            top: 160,
            right: 70,
            width: 820,
            opacity: rightIn,
            transform: `translateX(${(1 - rightIn) * 60}px)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#EEEEEE',
              borderRadius: 18,
              padding: '50px 44px',
              boxShadow: '0 16px 40px rgba(28,28,28,0.18)',
              borderTop: '8px solid #9E9E9E',
              height: 540,
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            {/* Crystal ball icon */}
            <svg width="70" height="70" viewBox="0 0 70 70">
              <circle cx="35" cy="32" r="22" fill="#9E9E9E" opacity="0.4" />
              <circle cx="35" cy="32" r="22" fill="none" stroke="#9E9E9E" strokeWidth="4" />
              <circle cx="28" cy="26" r="5" fill="#EEEEEE" opacity="0.7" />
              <path d="M 14 54 L 56 54 L 50 62 L 20 62 Z" fill="#9E9E9E" />
            </svg>

            <div
              style={{
                fontFamily: 'Nexa Bold, sans-serif',
                fontSize: 110,
                color: '#9E9E9E',
                fontWeight: 900,
                lineHeight: 0.95,
                letterSpacing: -3,
                marginTop: 18,
                position: 'relative',
                display: 'inline-block',
              }}
            >
              PREDICTION
              {/* Diagonal strikethrough */}
              <div
                style={{
                  position: 'absolute',
                  top: '50%',
                  left: 0,
                  height: 8,
                  width: `${interpolate(frame, [180, 230], [0, 105], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}%`,
                  backgroundColor: '#FF5722',
                  transform: 'translateY(-50%) rotate(-3deg)',
                }}
              />
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 24,
                color: '#9E9E9E',
                letterSpacing: 4,
                fontWeight: 700,
                marginTop: 8,
              }}
            >
              GUESSWORK · LUCK
            </div>

            <div style={{ marginTop: 30 }}>
              {['Inconsistent', 'Ego-driven', 'Breaks in chaos'].map((item, i) => (
                <div
                  key={item}
                  style={{
                    display: 'flex',
                    alignItems: 'center',
                    gap: 14,
                    marginTop: 14,
                    opacity: interpolate(frame, [100 + i * 15, 130 + i * 15], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                  }}
                >
                  <svg width="28" height="28" viewBox="0 0 28 28">
                    <circle cx="14" cy="14" r="13" fill="#9E9E9E" />
                    <line x1="9" y1="9" x2="19" y2="19" stroke="#EEEEEE" strokeWidth="3" strokeLinecap="round" />
                    <line x1="19" y1="9" x2="9" y2="19" stroke="#EEEEEE" strokeWidth="3" strokeLinecap="round" />
                  </svg>
                  <div
                    style={{
                      fontFamily: 'Inter, sans-serif',
                      fontSize: 32,
                      color: '#2D2D2D',
                      fontWeight: 600,
                      opacity: 0.7,
                    }}
                  >
                    {item}
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* BOTTOM Buffett quote ribbon */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 60,
            right: 60,
            opacity: quoteIn,
            transform: `translateY(${(1 - quoteIn) * 30}px)`,
          }}
        >
          <div
            style={{
              backgroundColor: '#FF5722',
              borderRadius: 12,
              padding: '20px 36px',
              display: 'flex',
              alignItems: 'center',
              gap: 24,
              boxShadow: '0 10px 28px rgba(255,87,34,0.4)',
            }}
          >
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 56,
                color: '#EEEEEE',
                lineHeight: 1,
                fontWeight: 900,
              }}
            >
              "
            </div>
            <div
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 36,
                color: '#EEEEEE',
                fontWeight: 700,
                flex: 1,
                lineHeight: 1.2,
                fontStyle: 'italic',
              }}
            >
              Predicting inflation is a fool's game.
              <span style={{ fontStyle: 'normal', fontSize: 22, opacity: 0.85, marginLeft: 14, letterSpacing: 2 }}>— BUFFETT</span>
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene49_ProcessVsPrediction;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene50_MoveFour',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene50_MoveFour: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const numIn = sp(10, { damping: 10, stiffness: 100 });
  const labelIn = sp(35);
  const headIn = sp(60);
  const curveIn = sp(110);
  const priceIn = sp(180);
  const ribbonIn = sp(230);

  // Exponential curve draw
  const curveProg = interpolate(frame, [110, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // Brain pulse
  const brainPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  // Counter for capability multiplier
  const multCount = interpolate(frame, [110, 220], [1, 10], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 26 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 2 + seededRandom(i + 100) * 4,
    speed: 0.3 + seededRandom(i + 150) * 0.6,
  }));

  // Curve points (exponential growth)
  const curvePts = Array.from({ length: 50 }, (_, i) => {
    const t = i / 49;
    return {
      x: 60 + t * 720,
      y: 380 - Math.pow(t, 2.2) * 340,
    };
  });

  const pathStr = curvePts.map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`).join(' ');

  // Animate path drawing based on curveProg
  const visiblePts = curvePts.slice(0, Math.floor(curveProg * curvePts.length));
  const animatedPath = visiblePts.map((p, i) => `${i === 0 ? 'M' : 'L'} ${p.x} ${p.y}`).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: '#A1BC98', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 70% 50%, rgba(255,87,34,0.15) 0%, rgba(161,188,152,0) 60%)',
        }}
      />

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          width: 900,
          height: 900,
          border: '3px solid #FF5722',
          borderRadius: '50%',
          opacity: 0.04,
          top: -350,
          right: -300 + Math.sin(frame * 0.01) * 25,
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          backgroundColor: '#2D2D2D',
          opacity: 0.05,
          bottom: -200,
          left: -150,
          transform: `rotate(${30 + frame * 0.03}deg)`,
        }}
      />

      {/* Concentric circles texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <circle
            key={i}
            cx="1400"
            cy="540"
            r={100 + i * 90}
            fill="none"
            stroke="#FF5722"
            strokeWidth="1.5"
          />
        ))}
      </svg>

      {/* Particles */}
      {particles.map((p, i) => (
        <div
          key={i}
          style={{
            position: 'absolute',
            left: p.x,
            top: (p.y - frame * p.speed + 1080) % 1080,
            width: p.size,
            height: p.size,
            borderRadius: '50%',
            backgroundColor: '#FF5722',
            opacity: 0.4,
          }}
        />
      ))}

      {/* MOVE label */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: 120,
          display: 'flex',
          alignItems: 'center',
          gap: 18,
          opacity: labelIn,
          transform: `translateX(${(1 - labelIn) * -30}px)`,
        }}
      >
        <div style={{ width: 60, height: 5, backgroundColor: '#FF5722' }} />
        <div
          style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 30,
            color: '#2D2D2D',
            letterSpacing: 6,
            fontWeight: 700,
          }}
        >
          THE PLAYBOOK / MOVE
        </div>
      </div>

      {/* GIANT 04 */}
      <div
        style={{
          position: 'absolute',
          top: 130,
          left: 90,
          fontFamily: 'Mont Heavy, sans-serif',
          fontSize: 680,
          fontWeight: 900,
          color: '#FF5722',
          opacity: numIn * 0.18,
          lineHeight: 0.85,
          letterSpacing: -30,
          transform: `scale(${numIn})`,
          transformOrigin: 'left top',
          pointerEvents: 'none',
        }}
      >
        04
      </div>

      {/* HEADLINE */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 120,
          width: 1050,
          opacity: headIn,
          transform: `translateY(${(1 - headIn) * 50}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Nexa Bold, sans-serif',
            fontSize: 119,
            fontWeight: 900,
            color: '#2D2D2D',
            lineHeight: 0.95,
            letterSpacing: -3,
          }}
        >
          POUR INTO<br />
          <span style={{ color: '#FF5722' }}>YOURSELF.</span>
        </div>
        <div
          style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 46,
            color: '#EEEEEE',
            fontWeight: 700,
            marginTop: 16,
            textShadow: '0 2px 8px rgba(0,0,0,0.2)',
          }}
        >
          The Buffett move that costs <span style={{ color: '#FF5722', backgroundColor: '#2D2D2D', padding: '2px 14px', borderRadius: 6 }}>$0</span>.
        </div>
      </div>

      {/* RIGHT — Capability compounding chart */}
      <div
        style={{
          position: 'absolute',
          top: 260,
          right: 90,
          width: 820,
          opacity: curveIn,
          transform: `translateX(${(1 - curveIn) * 40}px)`,
        }}
      >
        <div
          style={{
            backgroundColor: '#EEEEEE',
            borderRadius: 18,
            padding: '28px 32px',
            boxShadow: '0 16px 40px rgba(28,28,28,0.18)',
            borderTop: '8px solid #FF5722',
          }}
        >
          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
              marginBottom: 14,
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 20,
                color: '#2D2D2D',
                letterSpacing: 3,
                fontWeight: 700,
              }}
            >
              YOUR CAPABILITY · COMPOUNDS
            </div>
            <div
              style={{
                fontFamily: 'Mont Heavy, sans-serif',
                fontSize: 36,
                color: '#FF5722',
                fontWeight: 900,
                opacity: priceIn,
              }}
            >
              ×{multCount.toFixed(1)}
            </div>
          </div>

          {/* Brain icon + curve chart */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 20 }}>
            {/* Brain SVG with pulse */}
            <div style={{ transform: `scale(${brainPulse})`, transformOrigin: 'center' }}>
              <svg width="120" height="120" viewBox="0 0 120 120">
                <circle cx="60" cy="60" r="56" fill="rgba(255,87,34,0.15)" />
                <path
                  d="M 60 20 Q 40 20 32 38 Q 20 42 20 58 Q 20 74 32 80 Q 40 100 60 100 Q 80 100 88 80 Q 100 74 100 58 Q 100 42 88 38 Q 80 20 60 20 Z"
                  fill="#FF5722"
                />
                {/* Brain folds */}
                <path d="M 35 50 Q 50 40 60 50 Q 70 60 60 70 Q 50 80 35 70" stroke="#EEEEEE" strokeWidth="3" fill="none" />
                <path d="M 60 30 L 60 95" stroke="#EEEEEE" strokeWidth="3" />
                <path d="M 85 50 Q 70 60 85 70" stroke="#EEEEEE" strokeWidth="3" fill="none" />
              </svg>
            </div>

            {/* Exponential chart */}
            <svg width="500" height="400" viewBox="0 0 800 400" style={{ flex: 1 }}>
              {/* Axes */}
              <line x1="60" y1="380" x2="780" y2="380" stroke="#2D2D2D" strokeWidth="3" />
              <line x1="60" y1="40" x2="60" y2="380" stroke="#2D2D2D" strokeWidth="3" />

              {/* Grid */}
              {[0, 1, 2, 3].map((i) => (
                <line
                  key={i}
                  x1="60"
                  y1={380 - i * 90 - 30}
                  x2="780"
                  y2={380 - i * 90 - 30}
                  stroke="#2D2D2D"
                  strokeWidth="1"
                  opacity="0.15"
                  strokeDasharray="4 4"
                />
              ))}

              {/* Fill under curve */}
              {visiblePts.length > 1 && (
                <path
                  d={`${animatedPath} L ${visiblePts[visiblePts.length - 1].x} 380 L 60 380 Z`}
                  fill="#FF5722"
                  opacity="0.18"
                />
              )}

              {/* The exponential curve */}
              <path
                d={animatedPath}
                stroke="#FF5722"
                strokeWidth="6"
                fill="none"
                strokeLinecap="round"
              />

              {/* End dot */}
              {curveProg > 0.2 && visiblePts.length > 0 && (
                <circle
                  cx={visiblePts[visiblePts.length - 1].x}
                  cy={visiblePts[visiblePts.length - 1].y}
                  r="10"
                  fill="#FF5722"
                  stroke="#EEEEEE"
                  strokeWidth="3"
                />
              )}

              {/* Labels */}
              <text x="60" y="395" fontFamily="JetBrains Mono" fontSize="14" fill="#2D2D2D" fontWeight="700">YR 1</text>
              <text x="400" y="395" fontFamily="JetBrains Mono" fontSize="14" fill="#2D2D2D" fontWeight="700">YR 10</text>
              <text x="730" y="395" fontFamily="JetBrains Mono" fontSize="14" fill="#2D2D2D" fontWeight="700">YR 20</text>
            </svg>
          </div>
        </div>
      </div>

      {/* BOTTOM TAX-FREE RIBBON */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: ribbonIn,
          transform: `translateY(${(1 - ribbonIn) * 30}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 20,
            backgroundColor: '#FF5722',
            padding: '20px 44px',
            borderRadius: 14,
            boxShadow: '0 12px 32px rgba(255,87,34,0.45)',
          }}
        >
          {/* IRS strikethrough icon */}
          <svg width="56" height="56" viewBox="0 0 56 56">
            <rect x="10" y="6" width="36" height="44" rx="3" fill="#EEEEEE" />
            <line x1="16" y1="18" x2="40" y2="18" stroke="#FF5722" strokeWidth="3" />
            <line x1="16" y1="26" x2="40" y2="26" stroke="#FF5722" strokeWidth="3" />
            <line x1="16" y1="34" x2="32" y2="34" stroke="#FF5722" strokeWidth="3" />
            <line x1="4" y1="4" x2="52" y2="52" stroke="#2D2D2D" strokeWidth="6" strokeLinecap="round" />
          </svg>

          <div
            style={{
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 56,
              color: '#EEEEEE',
              fontWeight: 900,
              letterSpacing: -1,
            }}
          >
            COMPOUNDS <span style={{ color: '#2D2D2D' }}>TAX-FREE.</span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.4) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene50_MoveFour;
```

