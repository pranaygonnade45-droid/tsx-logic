```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01FactoryPulse',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const OLIVE = '#C9CC34';
const PAPER = '#FCFDE6';

const Scene01FactoryPulse: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push-in with lateral drift
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1.0], { extrapolateRight: 'clamp' });
  const camPan = Math.sin(frame * 0.006) * 16;

  // Impact shake when the IIP number lands
  const shake =
    frame >= 630 && frame <= 662 ? Math.sin((frame - 630) * 1.7) * (1 - (frame - 630) / 32) * 12 : 0;

  const headIn = sp(6);
  const subIn = sp(46);
  const stripIn = sp(150);
  const chartIn = sp(600, { damping: 10 });
  const iipValue = interpolate(frame, [636, 762], [0, 5.1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tiles = [
    { key: 'factory', label: 'फैक्ट्री', delay: 230 },
    { key: 'steel', label: 'स्टील प्लांट', delay: 275 },
    { key: 'power', label: 'इलेक्ट्रिकल', delay: 320 },
    { key: 'crane', label: 'कंस्ट्रक्शन', delay: 365 },
  ];

  const renderIcon = (key: string) => {
    const pulse = 1 + Math.sin(frame * 0.09) * 0.03;
    if (key === 'factory') {
      return (
        <svg width="120" height="120" viewBox="0 0 120 120">
          <rect x="14" y="62" width="92" height="44" fill="none" stroke={INK} strokeWidth="7" />
          <path d="M14 62 L44 44 L44 62 L74 44 L74 62 L104 44" fill="none" stroke={INK} strokeWidth="7" />
          <rect x="86" y="22" width="16" height="26" fill={RED} />
          {[0, 1, 2].map((i) => {
            const t = (frame * 1.6 + i * 22) % 66;
            return (
              <circle
                key={i}
                cx={94 + Math.sin((t + i * 12) * 0.1) * 8}
                cy={22 - t * 0.32}
                r={4 + t * 0.06}
                fill={INK}
                opacity={0.45 - t * 0.006}
              />
            );
          })}
          <rect x="34" y="80" width="18" height="26" fill={INK} opacity={0.85} />
        </svg>
      );
    }
    if (key === 'steel') {
      return (
        <svg width="120" height="120" viewBox="0 0 120 120">
          <path d="M24 88 L44 52 L84 52 L104 88 Z" fill="none" stroke={INK} strokeWidth="7" />
          <rect x="44" y="88" width="40" height="16" fill={INK} />
          {[0, 1, 2, 3].map((i) => {
            const t = (frame * 2.4 + i * 18) % 72;
            return <circle key={i} cx={64 + (seededRandom(i + 3) - 0.5) * 46} cy={52 - t * 0.5} r={3} fill={RED} opacity={1 - t / 72} />;
          })}
          <rect x="52" y="60" width="24" height="10" fill={RED} transform={`scale(${pulse}) translate(${(1 - pulse) * 64} ${(1 - pulse) * 65})`} />
        </svg>
      );
    }
    if (key === 'power') {
      const flash = Math.sin(frame * 0.22) > 0.4 ? 1 : 0.55;
      return (
        <svg width="120" height="120" viewBox="0 0 120 120">
          <rect x="22" y="24" width="76" height="72" fill="none" stroke={INK} strokeWidth="7" />
          <path d="M64 34 L44 66 L60 66 L54 90 L78 56 L62 56 Z" fill={RED} opacity={flash} />
          <line x1="22" y1="106" x2="98" y2="106" stroke={INK} strokeWidth="7" />
        </svg>
      );
    }
    return (
      <svg width="120" height="120" viewBox="0 0 120 120">
        <line x1="28" y1="104" x2="28" y2="26" stroke={INK} strokeWidth="7" />
        <line x1="28" y1="26" x2="100" y2="26" stroke={INK} strokeWidth="7" />
        <line x1="28" y1="26" x2="60" y2="60" stroke={INK} strokeWidth="5" />
        <line x1="86" y1="26" x2="86" y2={44 + Math.sin(frame * 0.05) * 8} stroke={INK} strokeWidth="5" />
        <rect x="74" y={44 + Math.sin(frame * 0.05) * 8} width="24" height="18" fill={RED} />
        <rect x="12" y="104" width="34" height="10" fill={INK} />
      </svg>
    );
  };

  const bars = [
    { h: 96, d: 612 },
    { h: 132, d: 636 },
    { h: 118, d: 660 },
    { h: 176, d: 684 },
    { h: 214, d: 708 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${28 + Math.sin(frame * 0.008) * 10}% 22%, #FBFD9A 0%, ${BG} 46%, ${OLIVE} 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute', left: 1180, top: -160, width: 760, height: 760, borderRadius: '50%',
            border: `70px solid ${INK}`, opacity: 0.05,
            transform: `rotate(${frame * 0.12}deg) scale(${1 + Math.sin(frame * 0.012) * 0.04})`,
          }}
        />
        <div
          style={{
            position: 'absolute', left: -120, top: 520, width: 620, height: 620,
            background: RED, opacity: 0.05,
            transform: `rotate(${-frame * 0.08}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute', left: 700, top: 660, width: 0, height: 0,
            borderLeft: '260px solid transparent', borderRight: '260px solid transparent',
            borderBottom: `440px solid ${INK}`, opacity: 0.04,
            transform: `rotate(${18 + Math.sin(frame * 0.01) * 6}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — halftone dot grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `radial-gradient(${INK} 2.2px, transparent 2.2px)`,
          backgroundSize: '30px 30px',
          backgroundPosition: `${(frame * 0.5) % 30}px ${(frame * 0.3) % 30}px`,
          opacity: 0.08,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(26).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 1);
          const r2 = seededRandom(i + 40);
          const y = (r2 * 1080 - frame * (0.5 + r1 * 1.1)) % 1180;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: r1 * 1900,
                top: y < 0 ? y + 1180 : y,
                width: 6 + r1 * 8,
                height: 6 + r1 * 8,
                background: i % 4 === 0 ? RED : INK,
                opacity: 0.16 + r2 * 0.14,
                transform: `rotate(${frame * (0.4 + r1)}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camPan + shake}px)` }}>
        {/* Torn collage headline card */}
        <div
          style={{
            position: 'absolute', left: 110, top: 118,
            transform: `translateY(${(1 - headIn) * 90}px) rotate(${-2.2 + (1 - headIn) * 6}deg)`,
            opacity: headIn,
          }}
        >
          <div style={{ background: INK, padding: '26px 44px', boxShadow: `18px 18px 0px ${RED}` }}>
            <div
              style={{
                fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                fontSize: 160, lineHeight: 1, color: BG, letterSpacing: -2,
              }}
            >
              एक तस्वीर
            </div>
          </div>
          <div
            style={{
              position: 'absolute', right: -60, top: -34, background: RED, color: '#FFFFFF',
              fontFamily: "'JetBrains Mono', monospace", fontSize: 26, fontWeight: 700,
              padding: '8px 18px', transform: `rotate(${8 + Math.sin(frame * 0.05) * 2}deg)`,
            }}
          >
            REALITY CHECK
          </div>
        </div>

        {/* Sub line */}
        <div
          style={{
            position: 'absolute', left: 118, top: 336,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 64, fontWeight: 700, color: INK,
            opacity: subIn, transform: `translateX(${(1 - subIn) * -70}px)`,
          }}
        >
          देश की मशीनें दिन-रात चल रही हैं
        </div>

        {/* Icon tile strip */}
        <div style={{ position: 'absolute', left: 118, top: 452, display: 'flex', gap: 34, opacity: stripIn }}>
          {tiles.map((t, i) => {
            const en = sp(t.delay, { damping: 11 });
            const tilt = -3 + i * 2 + Math.sin(frame * 0.02 + i) * 1.4;
            return (
              <div
                key={t.key}
                style={{
                  width: 232, height: 244, background: PAPER, border: `6px solid ${INK}`,
                  boxShadow: `10px 10px 0px ${INK}`,
                  transform: `translateY(${(1 - en) * 120}px) rotate(${tilt}deg) scale(${0.9 + en * 0.1})`,
                  opacity: en, display: 'flex', flexDirection: 'column',
                  alignItems: 'center', justifyContent: 'center', gap: 12,
                }}
              >
                {renderIcon(t.key)}
                <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 37, fontWeight: 700, color: INK }}>
                  {t.label}
                </div>
              </div>
            );
          })}
        </div>

        {/* IIP bar chart payoff */}
        <div
          style={{
            position: 'absolute', right: 130, top: 430, width: 560, height: 420,
            opacity: chartIn, transform: `translateY(${(1 - chartIn) * 80}px)`,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 26, height: 250, borderBottom: `7px solid ${INK}`, paddingLeft: 12 }}>
            {bars.map((b, i) => {
              const g = interpolate(frame, [b.d, b.d + 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
              return (
                <div
                  key={i}
                  style={{
                    width: 78, height: b.h * g,
                    background: i === bars.length - 1 ? RED : INK,
                    opacity: i === bars.length - 1 ? 1 : 0.82,
                  }}
                />
              );
            })}
          </div>
          <div
            style={{
              marginTop: 26, fontFamily: "'JetBrains Mono', monospace",
              fontSize: 25, fontWeight: 700, color: INK, letterSpacing: 2,
            }}
          >
            INDUSTRIAL PRODUCTION
          </div>
          <div
            style={{
              fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 119, fontWeight: 800, color: RED, lineHeight: 1.05,
            }}
          >
            +{iipValue.toFixed(1)}%
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 52%, rgba(0,0,0,0.30) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute', bottom: 30, right: 45,
          fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600,
          color: '#1C1C1C', opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01FactoryPulse;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02SplitSilence',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FBFCE2';

const Scene02SplitSilence: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: horizontal pan from growth side to silence side
  const camX = interpolate(frame, [0, 380, 520, durationInFrames], [40, 20, -40, -60], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.02], { extrapolateRight: 'clamp' });

  const splitIn = sp(300, { damping: 14 });
  const shake = frame >= 300 && frame <= 326 ? Math.sin((frame - 300) * 1.9) * (1 - (frame - 300) / 26) * 14 : 0;

  const stats = [
    { label: 'मैन्युफैक्चरिंग', value: 5.5, delay: 40 },
    { label: 'कंज्यूमर ड्यूरेबल्स', value: 7.2, delay: 110 },
    { label: 'इलेक्ट्रिसिटी जेनरेशन', value: 9.9, delay: 180 },
  ];

  // Flatline pulse for the corporate side
  const linePoints = new Array(60).fill(0).map((_, i) => {
    const x = i * 8;
    const beat = (frame * 1.4) % 480;
    const dist = Math.abs(beat - i * 8);
    const y = dist < 10 ? 40 - (10 - dist) * 2.4 : 40;
    return `${x},${y}`;
  }).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${112 + Math.sin(frame * 0.006) * 8}deg, #FAFC8E 0%, ${BG} 50%, #D5D83C 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: 120, top: 40, width: 520, height: 520, border: `56px solid ${INK}`, opacity: 0.05, transform: `rotate(${frame * 0.09}deg)` }} />
        <div style={{ position: 'absolute', right: 90, bottom: -180, width: 640, height: 640, borderRadius: '50%', background: RED, opacity: 0.045, transform: `scale(${1 + Math.sin(frame * 0.014) * 0.05})` }} />
      </AbsoluteFill>

      {/* Motion texture — diagonal stripes */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(45deg, ${INK} 0px, ${INK} 3px, transparent 3px, transparent 26px)`,
          backgroundPosition: `${(frame * 0.7) % 26}px 0px`,
          opacity: 0.07,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(22).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 7);
          const r2 = seededRandom(i + 60);
          const y = (r2 * 1080 + frame * (0.4 + r1)) % 1140;
          return (
            <div key={i} style={{ position: 'absolute', left: r1 * 1880, top: y, width: 10, height: 3, background: INK, opacity: 0.2, transform: `rotate(${r2 * 180}deg)` }} />
          );
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px) scale(${camScale})` }}>
        {/* LEFT — growth stack */}
        <div style={{ position: 'absolute', left: 96, top: 130, width: 780 }}>
          <div
            style={{
              fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 4,
              color: INK, opacity: sp(10), marginBottom: 22,
            }}
          >
            GROUND ECONOMY / GROWING
          </div>
          {stats.map((s, i) => {
            const en = sp(s.delay, { damping: 11 });
            const val = interpolate(frame, [s.delay + 8, s.delay + 70], [0, s.value], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            const barW = interpolate(frame, [s.delay + 8, s.delay + 80], [0, s.value * 62], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div
                key={s.label}
                style={{
                  marginBottom: 30, background: PAPER, border: `6px solid ${INK}`,
                  boxShadow: `12px 12px 0px ${INK}`, padding: '18px 24px',
                  transform: `translateX(${(1 - en) * -140}px) rotate(${-1.4 + i}deg)`, opacity: en,
                }}
              >
                <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
                  <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 45, fontWeight: 700, color: INK }}>{s.label}</div>
                  <div style={{ fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 76, fontWeight: 800, color: INK }}>
                    +{val.toFixed(1)}%
                  </div>
                </div>
                <div style={{ marginTop: 12, height: 20, background: '#E6E8B8', border: `3px solid ${INK}` }}>
                  <div style={{ width: barW, height: '100%', background: INK }} />
                </div>
              </div>
            );
          })}
        </div>

        {/* Torn divider */}
        <div
          style={{
            position: 'absolute', left: 930, top: 0, width: 26, height: 1080,
            background: INK, opacity: splitIn,
            transform: `scaleY(${splitIn}) skewX(${-3 + Math.sin(frame * 0.03) * 1.2}deg)`,
            transformOrigin: 'top center',
          }}
        />

        {/* RIGHT — corporate silence panel */}
        <div
          style={{
            position: 'absolute', right: 90, top: 150, width: 820, background: '#1C1C1C',
            border: `6px solid ${INK}`, padding: '40px 44px',
            transform: `translateY(${(1 - splitIn) * 160}px) rotate(1.4deg)`, opacity: splitIn,
            boxShadow: `16px 16px 0px ${RED}`,
          }}
        >
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 4, color: '#F4F754' }}>
            CORPORATE FLOOR / SILENT
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 153,
              color: '#F4F754', lineHeight: 1.05, marginTop: 6,
            }}
          >
            सन्नाटा
          </div>

          {/* Laptop icon with flatline monitor */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 26, marginTop: 18 }}>
            <svg width="150" height="120" viewBox="0 0 150 120">
              <rect x="24" y="16" width="102" height="66" fill="none" stroke="#F4F754" strokeWidth="6" />
              <rect x="10" y="86" width="130" height="12" fill="#F4F754" />
              <polyline points={linePoints} fill="none" stroke={RED} strokeWidth="4" transform="translate(28 24) scale(0.2 0.6)" />
            </svg>
            <div style={{ flex: 1 }}>
              <svg width="100%" height="80" viewBox="0 0 480 80" preserveAspectRatio="none">
                <polyline points={linePoints} fill="none" stroke={RED} strokeWidth="5" />
              </svg>
            </div>
          </div>

          <div style={{ display: 'flex', gap: 16, marginTop: 26 }}>
            {['हाइक ठंडी', 'नोटिस पीरियड', 'नई रोल की तलाश'].map((t, i) => {
              const en = sp(420 + i * 44, { damping: 10 });
              return (
                <div
                  key={t}
                  style={{
                    background: i === 2 ? RED : '#F4F754', color: i === 2 ? '#FFFFFF' : INK,
                    fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 32, fontWeight: 700,
                    padding: '10px 18px', opacity: en, transform: `scale(${0.8 + en * 0.2}) rotate(${-2 + i * 2}deg)`,
                  }}
                >
                  {t}
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 50%, rgba(0,0,0,0.32) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02SplitSilence;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03Divergence',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FCFDE6';

const Scene03Divergence: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: tilt + slow orbit feel
  const tilt = interpolate(frame, [0, durationInFrames], [1.6, -1.2], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 320, durationInFrames], [1.12, 1.0, 1.04], { extrapolateRight: 'clamp' });

  const titleIn = sp(8);
  const stampIn = sp(600, { damping: 8, stiffness: 90 });
  const shake = frame >= 600 && frame <= 634 ? Math.sin((frame - 600) * 1.8) * (1 - (frame - 600) / 34) * 16 : 0;

  // Diverging lines progress
  const draw = interpolate(frame, [140, 460], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const upPath = `M 80 420 L ${80 + 780 * draw} ${420 - 250 * draw}`;
  const downPath = `M 80 420 L ${80 + 780 * draw} ${420 + 210 * draw}`;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(ellipse at 70% ${70 + Math.sin(frame * 0.01) * 8}%, #FBFD96 0%, ${BG} 44%, #CFD238 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: -180, top: 120, width: 700, height: 700, border: `64px solid ${RED}`, borderRadius: '50%', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.011) * 0.06})` }} />
        <div style={{ position: 'absolute', right: 140, top: 60, width: 480, height: 480, background: INK, opacity: 0.04, transform: `rotate(${frame * 0.14}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — concentric rings */}
      <AbsoluteFill style={{ opacity: 0.09 }}>
        {new Array(7).fill(0).map((_, i) => {
          const size = 260 + i * 190 + Math.sin(frame * 0.02 + i) * 16;
          return (
            <div
              key={i}
              style={{
                position: 'absolute', left: 960 - size / 2, top: 540 - size / 2,
                width: size, height: size, borderRadius: '50%', border: `3px solid ${INK}`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(30).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 11);
          const r2 = seededRandom(i + 90);
          const x = (r1 * 1920 + frame * (0.3 + r2)) % 1980;
          return <div key={i} style={{ position: 'absolute', left: x, top: r2 * 1060, width: 5, height: 5, borderRadius: '50%', background: i % 5 === 0 ? RED : INK, opacity: 0.24 }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `rotate(${tilt}deg) scale(${camScale}) translateY(${shake}px)` }}>
        {/* Headline */}
        <div style={{ position: 'absolute', left: 100, top: 86, opacity: titleIn, transform: `translateY(${(1 - titleIn) * -60}px)` }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 5, color: INK }}>
            THE CONTRADICTION / परत दर परत
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 170, color: INK, lineHeight: 1 }}>
            विरोधाभास
          </div>
        </div>

        {/* Diverging graph */}
        <svg width="1000" height="700" viewBox="0 0 1000 700" style={{ position: 'absolute', left: 90, top: 300 }}>
          <line x1="80" y1="60" x2="80" y2="660" stroke={INK} strokeWidth="6" />
          <line x1="80" y1="660" x2="940" y2="660" stroke={INK} strokeWidth="6" />
          <path d={upPath} stroke={INK} strokeWidth="12" fill="none" strokeLinecap="round" />
          <path d={downPath} stroke={RED} strokeWidth="12" fill="none" strokeLinecap="round" />
          <circle cx={80 + 780 * draw} cy={420 - 250 * draw} r={14 + Math.sin(frame * 0.16) * 3} fill={INK} />
          <circle cx={80 + 780 * draw} cy={420 + 210 * draw} r={14 + Math.sin(frame * 0.16) * 3} fill={RED} />
        </svg>

        {/* Labels on the two lines */}
        <div
          style={{
            position: 'absolute', left: 780, top: 360, background: INK, color: BG,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700,
            padding: '10px 20px', opacity: sp(400), transform: `rotate(-4deg) scale(${0.85 + sp(400) * 0.15})`,
          }}
        >
          जमीन की अर्थव्यवस्था
        </div>
        <div
          style={{
            position: 'absolute', left: 800, top: 800, background: RED, color: '#FFFFFF',
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700,
            padding: '10px 20px', opacity: sp(470), transform: `rotate(3deg) scale(${0.85 + sp(470) * 0.15})`,
          }}
        >
          व्हाइट कॉलर इनकम
        </div>

        {/* 2026 marker card */}
        <div
          style={{
            position: 'absolute', right: 120, top: 210, background: PAPER, border: `7px solid ${INK}`,
            boxShadow: `14px 14px 0px ${INK}`, padding: '22px 30px', opacity: sp(230),
            transform: `rotate(${3 - sp(230) * 1.5}deg) translateY(${(1 - sp(230)) * 90}px)`,
          }}
        >
          <div style={{ fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 119, fontWeight: 800, color: INK, lineHeight: 1 }}>2026</div>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700, color: RED }}>टर्निंग पॉइंट</div>
        </div>

        {/* Red stamp payoff */}
        <div
          style={{
            position: 'absolute', right: 90, bottom: 170,
            border: `9px solid ${RED}`, padding: '18px 30px',
            transform: `rotate(${-7 + (1 - stampIn) * 22}deg) scale(${0.7 + stampIn * 0.3})`,
            opacity: stampIn, background: 'rgba(255,255,255,0.10)',
          }}
        >
          <div style={{ fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 66, fontWeight: 800, color: RED, letterSpacing: 2 }}>
            THE GREAT
          </div>
          <div style={{ fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 66, fontWeight: 800, color: RED, letterSpacing: 2 }}>
            CORPORATE STAGNATION
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 48%, rgba(0,0,0,0.34) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03Divergence;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04SilentTrap',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FBFCE2';

const Scene04SilentTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow zoom into the closing trap
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.12], { extrapolateRight: 'clamp' });
  const camY = Math.sin(frame * 0.008) * 12;

  // Trap jaws closing in three beats
  const jaw = interpolate(frame, [180, 300, 420, 560], [0, 0.35, 0.62, 0.94], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });
  const snap = frame >= 556 && frame <= 586 ? Math.sin((frame - 556) * 2.0) * (1 - (frame - 556) / 30) * 15 : 0;

  const titleIn = sp(10);
  const mutedIn = sp(120);

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${200 + Math.sin(frame * 0.005) * 10}deg, ${BG} 0%, #E9EC48 55%, #C6C930 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: 620, top: 180, width: 720, height: 720, borderRadius: '50%', background: INK, opacity: 0.045, transform: `scale(${1 + Math.sin(frame * 0.013) * 0.05})` }} />
        <div style={{ position: 'absolute', left: 60, bottom: 60, width: 420, height: 420, border: `48px solid ${RED}`, opacity: 0.05, transform: `rotate(${frame * 0.1}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — vertical scan bars */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(90deg, ${INK} 0px, ${INK} 2px, transparent 2px, transparent 42px)`,
          backgroundPosition: `${(frame * 1.1) % 42}px 0px`,
          opacity: 0.07,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(24).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 21);
          const r2 = seededRandom(i + 130);
          const y = (r2 * 1080 - frame * (0.3 + r1 * 0.8)) % 1140;
          return <div key={i} style={{ position: 'absolute', left: r1 * 1900, top: y < 0 ? y + 1140 : y, width: 3, height: 22, background: INK, opacity: 0.16 }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY + snap}px)` }}>
        {/* Title block */}
        <div style={{ position: 'absolute', left: 110, top: 92, opacity: titleIn, transform: `translateX(${(1 - titleIn) * -80}px)` }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 5, color: RED }}>
            SILENT / NO BREAKING NEWS
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 158, color: INK, lineHeight: 1.02 }}>
            इनकम ट्रैप
          </div>
        </div>

        {/* Trap jaws + salaried-class marker */}
        <div style={{ position: 'absolute', left: 520, top: 400, width: 900, height: 420 }}>
          <div
            style={{
              position: 'absolute', left: 0, top: 0, width: 900, height: 46, background: INK,
              transform: `translateY(${jaw * 150}px) rotate(${-1 + jaw * 1}deg)`,
            }}
          />
          <div
            style={{
              position: 'absolute', left: 0, bottom: 0, width: 900, height: 46, background: INK,
              transform: `translateY(${-jaw * 150}px) rotate(${1 - jaw * 1}deg)`,
            }}
          />
          <div
            style={{
              position: 'absolute', left: 300, top: 168, background: RED, color: '#FFFFFF',
              fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 45, fontWeight: 700,
              padding: '12px 22px', transform: `scale(${1 - jaw * 0.12})`,
            }}
          >
            सैलरीड क्लास
          </div>
          {/* teeth marks */}
          {new Array(12).fill(0).map((_, i) => (
            <div
              key={i}
              style={{
                position: 'absolute', left: 20 + i * 74, top: 46, width: 0, height: 0,
                borderLeft: '18px solid transparent', borderRight: '18px solid transparent',
                borderTop: `28px solid ${INK}`, transform: `translateY(${jaw * 150}px)`, opacity: 0.85,
              }}
            />
          ))}
        </div>

        {/* Muted-bell icon card */}
        <div
          style={{
            position: 'absolute', left: 120, top: 420, width: 330, background: PAPER,
            border: `6px solid ${INK}`, boxShadow: `12px 12px 0px ${INK}`, padding: '24px',
            opacity: mutedIn, transform: `rotate(-3deg) translateY(${(1 - mutedIn) * 90}px)`,
          }}
        >
          <svg width="120" height="120" viewBox="0 0 120 120">
            <path d="M32 84 L88 84 L80 68 L80 50 A20 20 0 0 0 40 50 L40 68 Z" fill="none" stroke={INK} strokeWidth="7" />
            <circle cx="60" cy="94" r="8" fill={INK} />
            <line x1="24" y1="26" x2="96" y2="98" stroke={RED} strokeWidth="9" opacity={0.6 + Math.sin(frame * 0.1) * 0.4} />
          </svg>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700, color: INK, marginTop: 8 }}>
            बिना शोर
          </div>
        </div>

        {/* Silent waveform card */}
        <div
          style={{
            position: 'absolute', left: 120, top: 720, width: 330, background: '#1C1C1C',
            border: `6px solid ${INK}`, padding: '20px', opacity: sp(240),
            transform: `rotate(2deg) translateY(${(1 - sp(240)) * 90}px)`,
          }}
        >
          <svg width="100%" height="90" viewBox="0 0 300 90" preserveAspectRatio="none">
            <polyline
              points={new Array(50).fill(0).map((_, i) => `${i * 6},${45 + (i * 6 > (frame * 1.2) % 300 - 12 && i * 6 < (frame * 1.2) % 300 ? -14 : 0)}`).join(' ')}
              fill="none" stroke={RED} strokeWidth="4"
            />
          </svg>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, color: '#F4F754', letterSpacing: 3, marginTop: 6 }}>
            NO SIGNAL / NO NOISE
          </div>
        </div>

        {/* Payoff line */}
        <div
          style={{
            position: 'absolute', right: 120, bottom: 150, textAlign: 'right', opacity: sp(600),
            transform: `translateY(${(1 - sp(600)) * 70}px)`,
          }}
        >
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 66, fontWeight: 800, color: INK }}>
            इसीलिए सबसे
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 119, color: RED, lineHeight: 1 }}>
            खतरनाक
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 46%, rgba(0,0,0,0.36) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04SilentTrap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05NewYardstick',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FCFDE6';

const Scene05NewYardstick: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle orbit-style rotation with push-in
  const camRot = Math.sin(frame * 0.007) * 1.3;
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1.01], { extrapolateRight: 'clamp' });

  const oldOut = interpolate(frame, [200, 300], [1, 0.18], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const strike = interpolate(frame, [210, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const cards = [
    { label: 'कैपिटल गुड्स', icon: 'crate', delay: 330 },
    { label: 'कंस्ट्रक्शन इनपुट्स', icon: 'brick', delay: 400 },
    { label: 'इलेक्ट्रिकल इक्विपमेंट', icon: 'bolt', delay: 470 },
    { label: 'बेसिक मेटल्स', icon: 'ingot', delay: 540 },
  ];

  const icon = (k: string) => {
    if (k === 'crate') {
      return (
        <svg width="110" height="110" viewBox="0 0 110 110">
          <rect x="16" y="26" width="78" height="64" fill="none" stroke={INK} strokeWidth="7" />
          <line x1="16" y1="46" x2="94" y2="46" stroke={INK} strokeWidth="6" />
          <line x1="16" y1="26" x2="94" y2="90" stroke={INK} strokeWidth="4" opacity="0.5" />
          <rect x="42" y="14" width="26" height="12" fill={RED} />
        </svg>
      );
    }
    if (k === 'brick') {
      return (
        <svg width="110" height="110" viewBox="0 0 110 110">
          {[0, 1, 2].map((r) => (
            <g key={r}>
              <rect x={12 + (r % 2) * 14} y={30 + r * 22} width="40" height="18" fill="none" stroke={INK} strokeWidth="6" />
              <rect x={56 + (r % 2) * 14 - 14} y={30 + r * 22} width="40" height="18" fill={r === 1 ? RED : 'none'} stroke={INK} strokeWidth="6" />
            </g>
          ))}
        </svg>
      );
    }
    if (k === 'bolt') {
      const flick = Math.sin(frame * 0.25) > 0 ? 1 : 0.5;
      return (
        <svg width="110" height="110" viewBox="0 0 110 110">
          <path d="M60 12 L34 60 L52 60 L44 98 L78 48 L58 48 Z" fill={RED} opacity={flick} stroke={INK} strokeWidth="5" />
        </svg>
      );
    }
    return (
      <svg width="110" height="110" viewBox="0 0 110 110">
        <path d="M18 78 L30 52 L80 52 L92 78 Z" fill="none" stroke={INK} strokeWidth="7" />
        <path d="M30 52 L38 34 L72 34 L80 52 Z" fill={RED} stroke={INK} strokeWidth="5" />
      </svg>
    );
  };

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% ${20 + Math.sin(frame * 0.009) * 6}%, #FCFEA2 0%, ${BG} 50%, #CDD035 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: 780, top: -220, width: 620, height: 620, background: INK, opacity: 0.04, transform: `rotate(${45 + frame * 0.07}deg)` }} />
        <div style={{ position: 'absolute', left: -160, bottom: -160, width: 620, height: 620, borderRadius: '50%', border: `60px solid ${INK}`, opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.01) * 0.05})` }} />
      </AbsoluteFill>

      {/* Motion texture — measurement grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `linear-gradient(${INK} 2px, transparent 2px), linear-gradient(90deg, ${INK} 2px, transparent 2px)`,
          backgroundSize: '90px 90px',
          backgroundPosition: `${(frame * 0.4) % 90}px ${(frame * 0.25) % 90}px`,
          opacity: 0.06,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(20).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 31);
          const r2 = seededRandom(i + 150);
          const x = (r1 * 1920 + frame * (0.6 + r2 * 0.8)) % 1990;
          return <div key={i} style={{ position: 'absolute', left: x, top: r2 * 1040, width: 14, height: 14, border: `3px solid ${INK}`, opacity: 0.18, transform: `rotate(${frame * 0.6 + i * 20}deg)` }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `rotate(${camRot}deg) scale(${camScale})` }}>
        {/* Headline */}
        <div style={{ position: 'absolute', left: 110, top: 78, opacity: sp(8), transform: `translateY(${(1 - sp(8)) * -50}px)` }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 5, color: INK }}>
            OFFICIAL DATA / NEW BASE
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 145, color: INK, lineHeight: 1.02 }}>
            अर्थव्यवस्था का चेहरा बदला
          </div>
        </div>

        {/* Old yardstick vs new yardstick */}
        <div style={{ position: 'absolute', left: 120, top: 400, display: 'flex', alignItems: 'center', gap: 40 }}>
          <div style={{ position: 'relative', opacity: oldOut }}>
            <div style={{ width: 360, height: 120, background: '#DDE07A', border: `6px solid ${INK}` }}>
              <div style={{ display: 'flex', height: '100%', alignItems: 'flex-end' }}>
                {new Array(12).fill(0).map((_, i) => (
                  <div key={i} style={{ width: 30, height: i % 2 === 0 ? 44 : 26, borderLeft: `3px solid ${INK}` }} />
                ))}
              </div>
            </div>
            <div style={{ position: 'absolute', left: 0, top: 44, width: 360 * strike, height: 10, background: RED }} />
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 37, fontWeight: 700, color: INK, marginTop: 10 }}>
              पुराने पैमाने
            </div>
          </div>

          <div style={{ fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 76, fontWeight: 800, color: RED, opacity: sp(280) }}>
            →
          </div>

          <div style={{ opacity: sp(300), transform: `scale(${0.86 + sp(300) * 0.14})` }}>
            <div style={{ width: 360, height: 120, background: INK, border: `6px solid ${INK}` }}>
              <div style={{ display: 'flex', height: '100%', alignItems: 'flex-end' }}>
                {new Array(12).fill(0).map((_, i) => (
                  <div key={i} style={{ width: 30, height: i % 2 === 0 ? 60 : 34, borderLeft: `3px solid ${BG}` }} />
                ))}
              </div>
            </div>
            <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 37, fontWeight: 700, color: RED, marginTop: 10 }}>
              नई कसौटी
            </div>
          </div>
        </div>

        {/* Four sector tiles */}
        <div style={{ position: 'absolute', right: 100, top: 300, display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 26 }}>
          {cards.map((c, i) => {
            const en = sp(c.delay, { damping: 11 });
            const tick = interpolate(frame, [c.delay + 30, c.delay + 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div
                key={c.label}
                style={{
                  width: 350, height: 250, background: PAPER, border: `6px solid ${INK}`,
                  boxShadow: `12px 12px 0px ${INK}`, padding: 18,
                  transform: `translateY(${(1 - en) * 110}px) rotate(${(i % 2 === 0 ? -2 : 2) + Math.sin(frame * 0.02 + i) * 0.8}deg)`,
                  opacity: en, display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
                }}
              >
                <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start' }}>
                  {icon(c.icon)}
                  <svg width="60" height="60" viewBox="0 0 60 60">
                    <path
                      d="M10 32 L26 46 L52 14"
                      fill="none" stroke={RED} strokeWidth="9" strokeLinecap="round"
                      strokeDasharray="80" strokeDashoffset={80 - 80 * tick}
                    />
                  </svg>
                </div>
                <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 40, fontWeight: 700, color: INK }}>
                  {c.label}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom strip */}
        <div
          style={{
            position: 'absolute', left: 120, bottom: 140, background: INK, color: BG,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 45, fontWeight: 700,
            padding: '14px 26px', opacity: sp(640), transform: `translateX(${(1 - sp(640)) * -100}px) rotate(-1.5deg)`,
          }}
        >
          जो जमीन पर बनता है, वही फल-फूल रहा है
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 50%, rgba(0,0,0,0.32) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05NewYardstick;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06HandVsKeyboard',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FBFCE2';

const Scene06HandVsKeyboard: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: dual-panel push with slight tilt
  const camScale = interpolate(frame, [0, 400, durationInFrames], [1.06, 1.0, 1.05], { extrapolateRight: 'clamp' });
  const camTilt = Math.sin(frame * 0.006) * 1.1;

  const leftIn = sp(30, { damping: 13 });
  const rightIn = sp(160, { damping: 13 });
  const qIn = sp(430, { damping: 9 });
  const shake = frame >= 430 && frame <= 458 ? Math.sin((frame - 430) * 2.1) * (1 - (frame - 430) / 28) * 11 : 0;

  const upLine = new Array(26).fill(0).map((_, i) => {
    const p = interpolate(frame, [60, 340], [0, 25], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
    const yy = 200 - i * 6 - Math.sin(i * 0.9) * 8;
    return i <= p ? `${20 + i * 16},${yy}` : null;
  }).filter(Boolean).join(' ');

  const downLine = new Array(26).fill(0).map((_, i) => {
    const p = interpolate(frame, [190, 470], [0, 25], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
    const yy = 60 + i * 5.4 + Math.sin(i * 0.7) * 7;
    return i <= p ? `${20 + i * 16},${yy}` : null;
  }).filter(Boolean).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(90deg, #FAFC8C 0%, ${BG} 48%, #D2D53A 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: 380, top: 120, width: 560, height: 560, borderRadius: '50%', background: INK, opacity: 0.04, transform: `translateY(${Math.sin(frame * 0.012) * 20}px)` }} />
        <div style={{ position: 'absolute', right: 240, bottom: 80, width: 500, height: 500, border: `54px solid ${RED}`, opacity: 0.05, transform: `rotate(${-frame * 0.11}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — moving checkerboard */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-conic-gradient(${INK} 0% 25%, transparent 0% 50%)`,
          backgroundSize: '64px 64px',
          backgroundPosition: `${(frame * 0.35) % 64}px ${(frame * 0.2) % 64}px`,
          opacity: 0.05,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(24).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 41);
          const r2 = seededRandom(i + 170);
          const y = (r2 * 1080 - frame * (0.4 + r1)) % 1140;
          return <div key={i} style={{ position: 'absolute', left: r1 * 1880, top: y < 0 ? y + 1140 : y, width: 8, height: 8, background: i % 3 === 0 ? RED : INK, opacity: 0.2, borderRadius: i % 2 ? '50%' : 0 }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camTilt}deg) translateY(${shake}px)` }}>
        {/* Header */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 70, textAlign: 'center', opacity: sp(6) }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 6, color: INK }}>
            TWO ECONOMIES / ONE COUNTRY
          </div>
        </div>

        {/* LEFT panel — hand economy */}
        <div
          style={{
            position: 'absolute', left: 100, top: 190, width: 780, background: PAPER,
            border: `7px solid ${INK}`, boxShadow: `14px 14px 0px ${INK}`, padding: 30,
            transform: `translateY(${(1 - leftIn) * 140}px) rotate(-1.6deg)`, opacity: leftIn,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 20 }}>
            <svg width="120" height="120" viewBox="0 0 120 120">
              <rect x="26" y="70" width="70" height="16" fill={INK} transform={`rotate(${-14 + Math.sin(frame * 0.16) * 10} 60 78)`} />
              <rect x="74" y="50" width="30" height="34" fill={RED} transform={`rotate(${-14 + Math.sin(frame * 0.16) * 10} 60 78)`} />
              <rect x="18" y="94" width="86" height="12" fill={INK} />
            </svg>
            <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 90, color: INK, lineHeight: 1 }}>
              हाथ की अर्थव्यवस्था
            </div>
          </div>
          <svg width="100%" height="220" viewBox="0 0 440 220" style={{ marginTop: 10 }}>
            <line x1="20" y1="200" x2="420" y2="200" stroke={INK} strokeWidth="5" />
            <polyline points={upLine} fill="none" stroke={INK} strokeWidth="10" strokeLinecap="round" />
          </svg>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 45, fontWeight: 700, color: INK }}>
            रफ्तार में
          </div>
        </div>

        {/* RIGHT panel — keyboard economy */}
        <div
          style={{
            position: 'absolute', right: 100, top: 260, width: 780, background: '#1C1C1C',
            border: `7px solid ${INK}`, boxShadow: `14px 14px 0px ${RED}`, padding: 30,
            transform: `translateY(${(1 - rightIn) * 160}px) rotate(1.8deg)`, opacity: rightIn,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 20 }}>
            <svg width="130" height="120" viewBox="0 0 130 120">
              <rect x="12" y="46" width="106" height="48" fill="none" stroke="#F4F754" strokeWidth="6" />
              {new Array(4).fill(0).map((_, r) =>
                new Array(6).fill(0).map((__, c) => {
                  const pressed = Math.floor((frame * 0.5) % 24) === r * 6 + c;
                  return (
                    <rect
                      key={`${r}-${c}`}
                      x={20 + c * 16} y={54 + r * 10} width="12" height="7"
                      fill={pressed ? RED : '#F4F754'} opacity={pressed ? 1 : 0.55}
                    />
                  );
                })
              )}
            </svg>
            <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 90, color: '#F4F754', lineHeight: 1 }}>
              कीबोर्ड की अर्थव्यवस्था
            </div>
          </div>
          <svg width="100%" height="220" viewBox="0 0 440 220" style={{ marginTop: 10 }}>
            <line x1="20" y1="200" x2="420" y2="200" stroke="#F4F754" strokeWidth="5" />
            <polyline points={downLine} fill="none" stroke={RED} strokeWidth="10" strokeLinecap="round" />
          </svg>
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 45, fontWeight: 700, color: RED }}>
            हांफ रही है
          </div>
        </div>

        {/* Big question mark payoff */}
        <div
          style={{
            position: 'absolute', left: 900, top: 700, opacity: qIn,
            transform: `scale(${0.5 + qIn * 0.5}) rotate(${(1 - qIn) * -30}deg)`,
          }}
        >
          <div style={{ fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 221, fontWeight: 800, color: RED, lineHeight: 0.9 }}>
            ?
          </div>
        </div>

        {/* Appraisal recall strip */}
        <div
          style={{
            position: 'absolute', left: 100, bottom: 96, display: 'flex', gap: 16, opacity: sp(560),
          }}
        >
          {['LAST 3 APPRAISALS', 'HIKE %', 'COMMENT करें'].map((t, i) => (
            <div
              key={t}
              style={{
                background: i === 2 ? RED : INK, color: i === 2 ? '#FFFFFF' : BG,
                fontFamily: "'JetBrains Mono', monospace", fontSize: 30, fontWeight: 700,
                padding: '10px 18px', transform: `rotate(${-2 + i * 2}deg)`,
              }}
            >
              {t}
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 50%, rgba(0,0,0,0.33) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06HandVsKeyboard;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07HiringDrop',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FCFDE6';

const Scene07HiringDrop: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: drop-down tilt following the falling bars
  const camY = interpolate(frame, [0, 500, durationInFrames], [-24, 10, 26], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.09], { extrapolateRight: 'clamp' });

  const yoy = interpolate(frame, [190, 300], [0, -4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const mom = interpolate(frame, [330, 440], [0, -6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shake = frame >= 440 && frame <= 470 ? Math.sin((frame - 440) * 1.9) * (1 - (frame - 440) / 30) * 13 : 0;

  const bars = [92, 84, 78, 66, 58, 47, 39];

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(170deg, ${BG} 0%, #EDF04D 48%, #C4C72E 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', right: -140, top: 80, width: 680, height: 680, borderRadius: '50%', background: RED, opacity: 0.05, transform: `translateY(${Math.sin(frame * 0.01) * 26}px)` }} />
        <div style={{ position: 'absolute', left: 80, bottom: -200, width: 560, height: 560, border: `58px solid ${INK}`, opacity: 0.05, transform: `rotate(${frame * 0.08}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — descending dashes */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(180deg, ${INK} 0px, ${INK} 4px, transparent 4px, transparent 38px)`,
          backgroundPosition: `0px ${(frame * 1.4) % 38}px`,
          opacity: 0.06,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(26).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 51);
          const r2 = seededRandom(i + 190);
          const y = (r2 * 1080 + frame * (0.8 + r1 * 1.4)) % 1160;
          return <div key={i} style={{ position: 'absolute', left: r1 * 1900, top: y, width: 4, height: 18, background: i % 4 === 0 ? RED : INK, opacity: 0.18 }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateY(${camY + shake}px) scale(${camScale})` }}>
        {/* Headline + badge icon */}
        <div style={{ position: 'absolute', left: 104, top: 92, display: 'flex', alignItems: 'center', gap: 26, opacity: sp(8) }}>
          <svg width="130" height="130" viewBox="0 0 130 130">
            <rect x="22" y="28" width="86" height="76" rx="8" fill={PAPER} stroke={INK} strokeWidth="7" />
            <circle cx="65" cy="56" r="14" fill={INK} />
            <rect x="40" y="76" width="50" height="10" fill={INK} />
            <rect x="56" y="14" width="18" height="18" fill={INK} />
            <line x1="20" y1="24" x2="110" y2="108" stroke={RED} strokeWidth="10" opacity={interpolate(frame, [120, 170], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })} />
          </svg>
          <div>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 5, color: INK }}>
              WHITE COLLAR RECRUITMENT / 2026
            </div>
            <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 130, color: INK, lineHeight: 1 }}>
              हायरिंग गिरी
            </div>
          </div>
        </div>

        {/* Descending bar chart */}
        <div style={{ position: 'absolute', left: 110, top: 380, width: 900 }}>
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 22, height: 320, borderBottom: `7px solid ${INK}` }}>
            {bars.map((h, i) => {
              const g = interpolate(frame, [120 + i * 26, 150 + i * 26], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
              return (
                <div key={i} style={{ width: 100, height: (h / 100) * 300 * g, background: i >= 5 ? RED : INK, opacity: i >= 5 ? 1 : 0.8 }} />
              );
            })}
          </div>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 3, color: INK, marginTop: 14 }}>
            MONTHLY HIRING INDEX / TREND
          </div>
        </div>

        {/* Stat cards */}
        <div style={{ position: 'absolute', right: 110, top: 400, display: 'flex', flexDirection: 'column', gap: 30 }}>
          {[
            { t: 'साल दर साल', v: yoy, d: 180 },
            { t: 'पिछले महीने से', v: mom, d: 320 },
          ].map((s, i) => {
            const en = sp(s.d, { damping: 10 });
            return (
              <div
                key={s.t}
                style={{
                  width: 620, background: i === 1 ? '#1C1C1C' : PAPER, border: `7px solid ${INK}`,
                  boxShadow: `14px 14px 0px ${i === 1 ? RED : INK}`, padding: '20px 28px',
                  transform: `translateX(${(1 - en) * 160}px) rotate(${i === 0 ? -1.6 : 1.6}deg)`, opacity: en,
                  display: 'flex', alignItems: 'center', justifyContent: 'space-between',
                }}
              >
                <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 51, fontWeight: 700, color: i === 1 ? '#F4F754' : INK }}>
                  {s.t}
                </div>
                <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
                  <svg width="46" height="46" viewBox="0 0 46 46">
                    <path d="M23 6 L23 34 M10 24 L23 38 L36 24" fill="none" stroke={RED} strokeWidth="7" strokeLinecap="round" />
                  </svg>
                  <div style={{ fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 104, fontWeight: 800, color: RED, lineHeight: 1 }}>
                    {s.v.toFixed(0)}%
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* Transition strip */}
        <div
          style={{
            position: 'absolute', right: 110, bottom: 140, background: INK, color: BG,
            fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 45, fontWeight: 700,
            padding: '14px 26px', opacity: sp(600), transform: `translateY(${(1 - sp(600)) * 60}px) rotate(1.4deg)`,
          }}
        >
          यहीं से असली खेल शुरू होता है
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 45%, rgba(0,0,0,0) 48%, rgba(0,0,0,0.35) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07HiringDrop;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08CautiousCapex',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FBFCE2';

const Scene08CautiousCapex: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow orbit around the gauge
  const camRot = Math.sin(frame * 0.0055) * 1.4;
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1.0], { extrapolateRight: 'clamp' });

  // Gauge needle creeps up, then stalls
  const needle = interpolate(frame, [120, 300, 380, 700], [-78, -30, -36, -32], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });

  const stampIn = sp(520, { damping: 8, stiffness: 90 });
  const shake = frame >= 520 && frame <= 548 ? Math.sin((frame - 520) * 2.0) * (1 - (frame - 520) / 28) * 12 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${64 + Math.sin(frame * 0.008) * 8}% 60%, #FCFE9E 0%, ${BG} 45%, #C9CC33 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: 140, top: -140, width: 640, height: 640, background: INK, opacity: 0.04, transform: `rotate(${30 + frame * 0.06}deg)` }} />
        <div style={{ position: 'absolute', right: 60, bottom: -120, width: 520, height: 520, borderRadius: '50%', border: `52px solid ${RED}`, opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.012) * 0.05})` }} />
      </AbsoluteFill>

      {/* Motion texture — rotating radial spokes */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {new Array(18).fill(0).map((_, i) => (
          <div
            key={i}
            style={{
              position: 'absolute', left: 960, top: 540, width: 1200, height: 3, background: INK,
              transformOrigin: 'left center', transform: `rotate(${i * 20 + frame * 0.15}deg)`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(22).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 61);
          const r2 = seededRandom(i + 210);
          const x = (r1 * 1920 - frame * (0.4 + r2 * 0.9)) % 1980;
          return <div key={i} style={{ position: 'absolute', left: x < 0 ? x + 1980 : x, top: r2 * 1050, width: 9, height: 9, background: INK, opacity: 0.18, transform: `rotate(45deg)` }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `rotate(${camRot}deg) scale(${camScale}) translateX(${shake}px)` }}>
        {/* Headline */}
        <div style={{ position: 'absolute', left: 108, top: 96, opacity: sp(8), transform: `translateY(${(1 - sp(8)) * -50}px)` }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 5, color: INK }}>
            PRIVATE SECTOR / INVESTMENT BEHAVIOUR
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 145, color: INK, lineHeight: 1.02 }}>
            कैपेक्स पर ब्रेक
          </div>
        </div>

        {/* CAPEX gauge */}
        <div style={{ position: 'absolute', left: 130, top: 380, opacity: sp(90), transform: `scale(${0.85 + sp(90) * 0.15})` }}>
          <svg width="620" height="420" viewBox="0 0 620 420">
            <path d="M60 340 A250 250 0 0 1 560 340" fill="none" stroke={INK} strokeWidth="18" />
            <path d="M60 340 A250 250 0 0 1 210 116" fill="none" stroke={RED} strokeWidth="18" />
            {new Array(11).fill(0).map((_, i) => {
              const a = (Math.PI * i) / 10;
              const x1 = 310 - Math.cos(a) * 214;
              const y1 = 340 - Math.sin(a) * 214;
              const x2 = 310 - Math.cos(a) * 186;
              const y2 = 340 - Math.sin(a) * 186;
              return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke={INK} strokeWidth="6" />;
            })}
            <g transform={`rotate(${needle} 310 340)`}>
              <line x1="310" y1="340" x2="310" y2="140" stroke={RED} strokeWidth="14" strokeLinecap="round" />
            </g>
            <circle cx="310" cy="340" r="24" fill={INK} />
            <text x="310" y="400" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="30" fill={INK}>
              CAPEX MOMENTUM
            </text>
          </svg>
        </div>

        {/* Vault / holding-back icon card */}
        <div
          style={{
            position: 'absolute', right: 140, top: 330, width: 620, background: PAPER,
            border: `7px solid ${INK}`, boxShadow: `14px 14px 0px ${INK}`, padding: 30,
            opacity: sp(220), transform: `translateX(${(1 - sp(220)) * 180}px) rotate(1.6deg)`,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 24 }}>
            <svg width="140" height="140" viewBox="0 0 140 140">
              <rect x="16" y="20" width="108" height="104" fill="none" stroke={INK} strokeWidth="8" />
              <circle cx="70" cy="72" r="28" fill="none" stroke={INK} strokeWidth="8" />
              <g transform={`rotate(${frame * 0.6} 70 72)`}>
                <line x1="70" y1="44" x2="70" y2="100" stroke={RED} strokeWidth="8" />
                <line x1="42" y1="72" x2="98" y2="72" stroke={RED} strokeWidth="8" />
              </g>
              <rect x="112" y="60" width="16" height="26" fill={INK} />
            </svg>
            <div>
              <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 51, fontWeight: 700, color: INK }}>
                पैसा है, पर खर्च नहीं
              </div>
              <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 30, color: RED, letterSpacing: 2, marginTop: 6 }}>
                EXPANSION ON HOLD
              </div>
            </div>
          </div>
        </div>

        {/* Flattening investment line */}
        <div style={{ position: 'absolute', right: 150, top: 600, opacity: sp(360) }}>
          <svg width="600" height="260" viewBox="0 0 600 260">
            <line x1="20" y1="230" x2="580" y2="230" stroke={INK} strokeWidth="6" />
            <polyline
              points={new Array(30).fill(0).map((_, i) => {
                const p = interpolate(frame, [370, 640], [0, 29], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
                if (i > p) return null;
                const y = i < 12 ? 200 - i * 11 : 68 + (i - 12) * 1.4;
                return `${20 + i * 19},${y}`;
              }).filter(Boolean).join(' ')}
              fill="none" stroke={INK} strokeWidth="10" strokeLinecap="round"
            />
          </svg>
        </div>

        {/* CAUTIOUS stamp */}
        <div
          style={{
            position: 'absolute', left: 640, bottom: 130, border: `9px solid ${RED}`, padding: '14px 30px',
            transform: `rotate(${-6 + (1 - stampIn) * 20}deg) scale(${0.7 + stampIn * 0.3})`, opacity: stampIn,
          }}
        >
          <div style={{ fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 90, fontWeight: 800, color: RED, letterSpacing: 4 }}>
            EXTREMELY CAUTIOUS
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 48%, rgba(0,0,0,0.34) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08CautiousCapex;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09DataStream',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FCFDE6';

const Scene09DataStream: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: continuous slow orbit + breathing zoom
  const camRot = Math.sin(frame * 0.006) * 2;
  const camScale = 1.02 + Math.sin(frame * 0.01) * 0.03;

  // Network nodes
  const nodes = new Array(11).fill(0).map((_, i) => {
    const r1 = seededRandom(i + 71);
    const r2 = seededRandom(i + 230);
    const baseX = 260 + r1 * 1360;
    const baseY = 300 + r2 * 520;
    return {
      x: baseX + Math.sin(frame * 0.012 + i) * 26,
      y: baseY + Math.cos(frame * 0.014 + i * 1.3) * 22,
      delay: 40 + i * 26,
      hot: i % 4 === 0,
    };
  });

  const sweep = (frame * 0.9) % 360;

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(${40 + Math.sin(frame * 0.006) * 14}deg, #FAFC8E 0%, ${BG} 52%, #CBCE34 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: 620, top: 140, width: 760, height: 760, borderRadius: '50%', border: `66px solid ${INK}`, opacity: 0.045, transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.011) * 0.04})` }} />
        <div style={{ position: 'absolute', left: -100, top: 200, width: 460, height: 460, background: RED, opacity: 0.05, transform: `rotate(${-frame * 0.09}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — radar sweep */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        <div
          style={{
            position: 'absolute', left: 960, top: 540, width: 1100, height: 6, background: `linear-gradient(90deg, ${INK} 0%, transparent 100%)`,
            transformOrigin: 'left center', transform: `rotate(${sweep}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(30).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 81);
          const r2 = seededRandom(i + 250);
          const y = (r2 * 1080 - frame * (0.5 + r1 * 1.2)) % 1150;
          return <div key={i} style={{ position: 'absolute', left: r1 * 1900, top: y < 0 ? y + 1150 : y, width: 6, height: 6, borderRadius: '50%', background: i % 5 === 0 ? RED : INK, opacity: 0.22 }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `rotate(${camRot}deg) scale(${camScale})` }}>
        {/* Connection network */}
        <svg width="1920" height="1080" style={{ position: 'absolute', left: 0, top: 0 }}>
          {nodes.map((n, i) => {
            if (i === 0) return null;
            const prev = nodes[i - 1];
            const g = interpolate(frame, [n.delay, n.delay + 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <line
                key={i}
                x1={prev.x} y1={prev.y}
                x2={prev.x + (n.x - prev.x) * g} y2={prev.y + (n.y - prev.y) * g}
                stroke={INK} strokeWidth="3" opacity="0.45"
              />
            );
          })}
          {nodes.map((n, i) => {
            const en = interpolate(frame, [n.delay, n.delay + 22], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            const pulse = 1 + Math.sin(frame * 0.1 + i) * 0.16;
            return (
              <g key={`n-${i}`} opacity={en}>
                <circle cx={n.x} cy={n.y} r={(n.hot ? 20 : 13) * pulse} fill={n.hot ? RED : INK} />
                <circle cx={n.x} cy={n.y} r={(n.hot ? 34 : 24) * pulse} fill="none" stroke={INK} strokeWidth="3" opacity="0.4" />
              </g>
            );
          })}
        </svg>

        {/* Title block */}
        <div style={{ position: 'absolute', left: 108, top: 92, opacity: sp(10) }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 6, color: INK }}>
            DATA LAYER / ANALYSIS IN MOTION
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 145, color: INK, lineHeight: 1.02 }}>
            आँकड़ों की परतें
          </div>
        </div>

        {/* Rolling metric strip */}
        <div style={{ position: 'absolute', left: 108, bottom: 220, display: 'flex', gap: 22, opacity: sp(260) }}>
          {['IIP', 'MANUFACTURING', 'HIRING INDEX', 'CAPEX'].map((t, i) => {
            const en = sp(260 + i * 40, { damping: 11 });
            const val = interpolate(frame, [280 + i * 40, 520 + i * 40], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div
                key={t}
                style={{
                  width: 380, background: i % 2 === 0 ? PAPER : '#1C1C1C', border: `6px solid ${INK}`,
                  boxShadow: `10px 10px 0px ${INK}`, padding: '16px 20px', opacity: en,
                  transform: `translateY(${(1 - en) * 90}px) rotate(${-2 + i * 1.4}deg)`,
                }}
              >
                <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 3, color: i % 2 === 0 ? INK : '#F4F754' }}>
                  {t}
                </div>
                <div style={{ height: 16, background: i % 2 === 0 ? '#E6E8B8' : '#333333', marginTop: 12 }}>
                  <div style={{ width: `${val}%`, height: '100%', background: i === 2 ? RED : (i % 2 === 0 ? INK : '#F4F754') }} />
                </div>
              </div>
            );
          })}
        </div>

        {/* Scanning bracket frame */}
        <div style={{ position: 'absolute', right: 120, top: 320, width: 520, height: 420, opacity: sp(180) }}>
          {[[0, 0], [1, 0], [0, 1], [1, 1]].map(([hx, hy], i) => (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: hx ? 'auto' : -10, right: hx ? -10 : 'auto',
                top: hy ? 'auto' : -10, bottom: hy ? -10 : 'auto',
                width: 90, height: 90,
                borderTop: hy ? 'none' : `10px solid ${RED}`,
                borderBottom: hy ? `10px solid ${RED}` : 'none',
                borderLeft: hx ? 'none' : `10px solid ${RED}`,
                borderRight: hx ? `10px solid ${RED}` : 'none',
              }}
            />
          ))}
          <div
            style={{
              position: 'absolute', left: 0, top: (frame * 3) % 420, width: '100%', height: 5,
              background: RED, opacity: 0.7,
            }}
          />
          <div style={{ position: 'absolute', left: 30, top: 160, fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 51, fontWeight: 700, color: INK }}>
            पैटर्न स्कैन हो रहा है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 50%, rgba(0,0,0,0.33) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09DataStream;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10ChapterBridge',
  durationInSeconds: 28.39,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BG = '#F4F754';
const INK = '#000000';
const RED = '#FF0000';
const PAPER = '#FBFCE2';

const Scene10ChapterBridge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: pull-back reveal then slow drift
  const camScale = interpolate(frame, [0, 420, durationInFrames], [1.18, 1.0, 0.98], { extrapolateRight: 'clamp' });
  const camX = Math.sin(frame * 0.007) * 18;

  const progress = interpolate(frame, [140, 640], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shake = frame >= 636 && frame <= 664 ? Math.sin((frame - 636) * 2.0) * (1 - (frame - 636) / 28) * 12 : 0;

  const orbitIcons = [
    { key: 'factory', a: 0 },
    { key: 'bolt', a: 72 },
    { key: 'chart', a: 144 },
    { key: 'badge', a: 216 },
    { key: 'vault', a: 288 },
  ];

  const drawIcon = (k: string) => {
    if (k === 'factory') {
      return (
        <svg width="86" height="86" viewBox="0 0 86 86">
          <rect x="10" y="44" width="66" height="32" fill="none" stroke={INK} strokeWidth="6" />
          <path d="M10 44 L32 30 L32 44 L54 30 L54 44 L76 30" fill="none" stroke={INK} strokeWidth="6" />
        </svg>
      );
    }
    if (k === 'bolt') {
      return (
        <svg width="86" height="86" viewBox="0 0 86 86">
          <path d="M46 8 L26 46 L40 46 L34 78 L60 38 L44 38 Z" fill={RED} stroke={INK} strokeWidth="4" />
        </svg>
      );
    }
    if (k === 'chart') {
      return (
        <svg width="86" height="86" viewBox="0 0 86 86">
          <line x1="12" y1="74" x2="76" y2="74" stroke={INK} strokeWidth="6" />
          <rect x="20" y="46" width="12" height="28" fill={INK} />
          <rect x="38" y="32" width="12" height="42" fill={INK} />
          <rect x="56" y="54" width="12" height="20" fill={RED} />
        </svg>
      );
    }
    if (k === 'badge') {
      return (
        <svg width="86" height="86" viewBox="0 0 86 86">
          <rect x="16" y="18" width="54" height="50" rx="6" fill="none" stroke={INK} strokeWidth="6" />
          <circle cx="43" cy="36" r="9" fill={INK} />
          <rect x="28" y="50" width="30" height="7" fill={INK} />
        </svg>
      );
    }
    return (
      <svg width="86" height="86" viewBox="0 0 86 86">
        <rect x="12" y="14" width="62" height="60" fill="none" stroke={INK} strokeWidth="6" />
        <circle cx="43" cy="44" r="16" fill="none" stroke={RED} strokeWidth="6" />
      </svg>
    );
  };

  return (
    <AbsoluteFill style={{ backgroundColor: BG, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% 50%, #FDFFA8 0%, ${BG} 42%, #C7CA31 100%)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', left: 260, top: 60, width: 560, height: 560, background: INK, opacity: 0.04, transform: `rotate(${frame * 0.07}deg)` }} />
        <div style={{ position: 'absolute', right: 200, bottom: 40, width: 620, height: 620, borderRadius: '50%', border: `62px solid ${RED}`, opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.012) * 0.05})` }} />
        <div style={{ position: 'absolute', left: 820, top: 620, width: 0, height: 0, borderLeft: '220px solid transparent', borderRight: '220px solid transparent', borderBottom: `380px solid ${INK}`, opacity: 0.035, transform: `rotate(${-frame * 0.06}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — expanding rings */}
      <AbsoluteFill style={{ opacity: 0.09 }}>
        {new Array(5).fill(0).map((_, i) => {
          const t = (frame * 1.6 + i * 90) % 450;
          return (
            <div
              key={i}
              style={{
                position: 'absolute', left: 960 - t, top: 540 - t, width: t * 2, height: t * 2,
                borderRadius: '50%', border: `4px solid ${INK}`, opacity: 1 - t / 450,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {new Array(28).fill(0).map((_, i) => {
          const r1 = seededRandom(i + 91);
          const r2 = seededRandom(i + 270);
          const x = (r1 * 1920 + frame * (0.5 + r2)) % 1980;
          return <div key={i} style={{ position: 'absolute', left: x, top: r2 * 1060, width: 7, height: 7, background: i % 6 === 0 ? RED : INK, opacity: 0.2, transform: `rotate(${frame + i * 12}deg)` }} />;
        })}
      </AbsoluteFill>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX + shake}px)` }}>
        {/* Orbiting icon ring */}
        <div style={{ position: 'absolute', left: 960, top: 470 }}>
          {orbitIcons.map((o, i) => {
            const en = sp(60 + i * 40, { damping: 11 });
            const ang = ((o.a + frame * 0.35) * Math.PI) / 180;
            const rx = 330;
            const ry = 200;
            return (
              <div
                key={o.key}
                style={{
                  position: 'absolute',
                  left: Math.cos(ang) * rx - 62,
                  top: Math.sin(ang) * ry - 62,
                  width: 124, height: 124, background: PAPER, border: `6px solid ${INK}`,
                  boxShadow: `9px 9px 0px ${INK}`, display: 'flex', alignItems: 'center', justifyContent: 'center',
                  transform: `rotate(${Math.sin(frame * 0.03 + i) * 6}deg) scale(${0.8 + en * 0.2})`, opacity: en,
                }}
              >
                {drawIcon(o.key)}
              </div>
            );
          })}
        </div>

        {/* Center chapter card */}
        <div
          style={{
            position: 'absolute', left: 620, top: 350, width: 680, background: INK,
            padding: '34px 40px', boxShadow: `18px 18px 0px ${RED}`,
            transform: `rotate(-1.4deg) scale(${0.86 + sp(20) * 0.14})`, opacity: sp(20),
          }}
        >
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 25, letterSpacing: 6, color: BG }}>
            CHAPTER 01 / COMPLETE
          </div>
          <div style={{ fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif", fontSize: 104, color: BG, lineHeight: 1.05, marginTop: 6 }}>
            बूम बनाम सन्नाटा
          </div>
          <div style={{ marginTop: 20, height: 22, background: '#3A3A3A', border: `4px solid ${BG}` }}>
            <div style={{ width: `${progress * 100}%`, height: '100%', background: RED }} />
          </div>
        </div>

        {/* Forward arrow to next chapter */}
        <div
          style={{
            position: 'absolute', left: 780, bottom: 150, display: 'flex', alignItems: 'center', gap: 20,
            opacity: sp(620), transform: `translateX(${(1 - sp(620)) * -80}px)`,
          }}
        >
          <div style={{ fontFamily: "'Noto Sans Devanagari', sans-serif", fontSize: 51, fontWeight: 700, color: INK }}>
            आगे और गहराई में
          </div>
          <svg width="120" height="60" viewBox="0 0 120 60">
            <line x1="6" y1="30" x2={70 + Math.sin(frame * 0.1) * 8} y2="30" stroke={RED} strokeWidth="10" strokeLinecap="round" />
            <path d={`M${70 + Math.sin(frame * 0.1) * 8} 10 L${104 + Math.sin(frame * 0.1) * 8} 30 L${70 + Math.sin(frame * 0.1) * 8} 50 Z`} fill={RED} />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 46%, rgba(0,0,0,0.36) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: "'Inter', 'Poppins', sans-serif", fontSize: 22, fontWeight: 600, color: '#1C1C1C', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10ChapterBridge;
```

