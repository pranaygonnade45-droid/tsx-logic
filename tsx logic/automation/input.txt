```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene41ValueSurvives',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene41ValueSurvives: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const camera = interpolate(frame, [0, durationInFrames], [1.08, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraX = interpolate(frame, [0, durationInFrames], [-30, 25], {
    easing: Easing.inOut(Easing.sin),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const orbitRotation = frame * 0.28;
  const shieldPulse = 1 + Math.sin(frame / 18) * 0.035;
  const priceShock = interpolate(frame, [245, 325], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const marginRecovery = interpolate(frame, [340, 470], [0.25, 0.78], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  const particles = Array.from({length: 46}, (_, i) => ({
    x: seededRandom(i + 10) * 1920,
    y: seededRandom(i + 80) * 1080,
    size: 2 + seededRandom(i + 140) * 6,
    speed: 0.25 + seededRandom(i + 210) * 0.8,
    phase: seededRandom(i + 290) * 360,
  }));

  const questions = [
    {
      label: 'NECESSARY?',
      icon: (
        <path
          d="M17 6h14l4 9-6 6H19l-6-6 4-9Zm7 15v10m-7 0h14"
          fill="none"
          stroke="currentColor"
          strokeWidth="3"
          strokeLinecap="round"
          strokeLinejoin="round"
        />
      ),
    },
    {
      label: 'RETURN?',
      icon: (
        <>
          <path
            d="M31 14a12 12 0 1 0 1 12"
            fill="none"
            stroke="currentColor"
            strokeWidth="3"
            strokeLinecap="round"
          />
          <path
            d="M25 10h8v8"
            fill="none"
            stroke="currentColor"
            strokeWidth="3"
            strokeLinecap="round"
            strokeLinejoin="round"
          />
        </>
      ),
    },
    {
      label: 'MARGIN?',
      icon: (
        <>
          <path
            d="M12 31V17m10 14V9m10 22V14"
            fill="none"
            stroke="currentColor"
            strokeWidth="4"
            strokeLinecap="round"
          />
          <path
            d="m10 13 9-6 8 4 9-7"
            fill="none"
            stroke="currentColor"
            strokeWidth="3"
            strokeLinecap="round"
            strokeLinejoin="round"
          />
        </>
      ),
    },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 42%, #FFB36F 0%, #FF9B51 34%, #C96E3D 67%, #25343F 118%)',
        }}
      />

      <AbsoluteFill
        style={{
          backgroundImage:
            'linear-gradient(rgba(37,52,63,0.09) 1px, transparent 1px), linear-gradient(90deg, rgba(37,52,63,0.09) 1px, transparent 1px)',
          backgroundSize: '70px 70px',
          transform: `translate(${(frame * 0.25) % 70}px, ${(frame * 0.12) % 70}px)`,
          opacity: 0.7,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 560,
          height: 560,
          border: '3px solid rgba(255,244,73,0.14)',
          borderRadius: '50%',
          left: -190,
          top: -170,
          transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame / 40) * 0.05})`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 470,
          height: 470,
          right: -100,
          bottom: -180,
          background: 'rgba(37,52,63,0.10)',
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${-18 + frame * 0.05}deg)`,
        }}
      />

      {particles.map((particle, index) => {
        const y =
          (particle.y - frame * particle.speed + particle.phase + 1080) %
          1080;
        const drift = Math.sin((frame + index * 17) / 34) * 18;

        return (
          <div
            key={index}
            style={{
              position: 'absolute',
              left: particle.x + drift,
              top: y,
              width: particle.size,
              height: particle.size,
              borderRadius: index % 3 === 0 ? 2 : '50%',
              backgroundColor:
                index % 4 === 0
                  ? 'rgba(255,244,73,0.50)'
                  : 'rgba(37,52,63,0.24)',
              transform: `rotate(${frame + index * 21}deg)`,
            }}
          />
        );
      })}

      <AbsoluteFill
        style={{
          transform: `translateX(${cameraX}px) scale(${camera})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 118,
            top: 78,
            opacity: sp(8),
            transform: `translateY(${(1 - sp(8)) * -35}px)`,
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 800,
              letterSpacing: 7,
            }}
          >
            EQUITY FILTER / 01
          </div>
          <div
            style={{
              marginTop: 12,
              width: 980,
              color: '#25343F',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 78,
              lineHeight: 0.94,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            VALUE MUST SURVIVE
            <br />
            THE PRICE TEST
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 660,
            top: 300,
            width: 600,
            height: 600,
            opacity: sp(50),
            transform: `scale(${sp(50)})`,
          }}
        >
          <svg width="600" height="600" viewBox="0 0 600 600">
            <circle
              cx="300"
              cy="300"
              r="218"
              fill="none"
              stroke="rgba(37,52,63,0.16)"
              strokeWidth="2"
              strokeDasharray="10 18"
              transform={`rotate(${orbitRotation} 300 300)`}
            />
            <circle
              cx="300"
              cy="300"
              r="164"
              fill="rgba(37,52,63,0.90)"
              stroke="#FFF449"
              strokeWidth="5"
              style={{
                filter: 'drop-shadow(0 25px 35px rgba(37,52,63,0.28))',
                transformOrigin: '300px 300px',
                transform: `scale(${shieldPulse})`,
              }}
            />

            <g
              transform={`translate(225 210) scale(${1 + priceShock * 0.04})`}
            >
              <path
                d="M75 0 142 25v62c0 66-40 108-67 126C48 195 8 153 8 87V25L75 0Z"
                fill="#FF9B51"
                stroke="#FFF449"
                strokeWidth="7"
              />
              <path
                d="m42 94 24 25 47-56"
                fill="none"
                stroke="#25343F"
                strokeWidth="13"
                strokeLinecap="round"
                strokeLinejoin="round"
                style={{
                  strokeDasharray: 110,
                  strokeDashoffset: 110 * (1 - sp(285)),
                }}
              />
            </g>

            <text
              x="300"
              y="456"
              textAnchor="middle"
              fill="#FFF449"
              fontFamily="Mont Heavy, sans-serif"
              fontSize="39"
              fontWeight="900"
              letterSpacing="4"
            >
              DURABLE VALUE
            </text>
          </svg>

          {questions.map((question, index) => {
            const angle = orbitRotation * 0.45 + index * 120;
            const radians = (angle * Math.PI) / 180;
            const x = 300 + Math.cos(radians) * 245;
            const y = 300 + Math.sin(radians) * 245;

            return (
              <div
                key={question.label}
                style={{
                  position: 'absolute',
                  left: x - 83,
                  top: y - 83,
                  width: 166,
                  height: 166,
                  borderRadius: 32,
                  background: '#FFF449',
                  border: '4px solid #25343F',
                  boxShadow: '0 18px 28px rgba(37,52,63,0.22)',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  flexDirection: 'column',
                  color: '#25343F',
                  transform: `rotate(${-angle}deg) scale(${sp(100 + index * 20)})`,
                }}
              >
                <svg width="52" height="52" viewBox="0 0 44 44">
                  {question.icon}
                </svg>
                <div
                  style={{
                    marginTop: 8,
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 19,
                    fontWeight: 900,
                    letterSpacing: 1,
                  }}
                >
                  {question.label}
                </div>
              </div>
            );
          })}
        </div>

        <div
          style={{
            position: 'absolute',
            left: 112,
            bottom: 102,
            width: 470,
            height: 258,
            borderRadius: 38,
            background: 'rgba(37,52,63,0.92)',
            border: '2px solid rgba(255,244,73,0.50)',
            padding: '32px 36px',
            boxSizing: 'border-box',
            opacity: sp(160),
            transform: `translateX(${(1 - sp(160)) * -70}px)`,
            boxShadow: '0 24px 50px rgba(37,52,63,0.25)',
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              fontWeight: 800,
              letterSpacing: 3,
            }}
          >
            CUSTOMER RETURN LOOP
          </div>

          <svg
            width="390"
            height="130"
            viewBox="0 0 390 130"
            style={{marginTop: 18}}
          >
            <path
              d="M45 66C45 24 105 15 158 23c49 7 74 32 113 32 38 0 66-17 76-39"
              fill="none"
              stroke="rgba(255,244,73,0.24)"
              strokeWidth="17"
              strokeLinecap="round"
            />
            <path
              d="M45 66C45 24 105 15 158 23c49 7 74 32 113 32 38 0 66-17 76-39"
              fill="none"
              stroke="#FFF449"
              strokeWidth="6"
              strokeLinecap="round"
              strokeDasharray="12 16"
              strokeDashoffset={-frame * 2}
            />
            <path
              d="m329 12 20 4-8 20"
              fill="none"
              stroke="#FFF449"
              strokeWidth="6"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
            {[55, 155, 265].map((x, index) => (
              <g key={x}>
                <circle
                  cx={x}
                  cy={94}
                  r={22 + Math.sin((frame + index * 20) / 13) * 2}
                  fill="#FF9B51"
                  stroke="#FFF449"
                  strokeWidth="4"
                />
                <path
                  d={`M${x - 9} 94h18M${x} 85v18`}
                  stroke="#25343F"
                  strokeWidth="4"
                  strokeLinecap="round"
                />
              </g>
            ))}
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 112,
            bottom: 112,
            width: 410,
            height: 292,
            borderRadius: 38,
            background: 'rgba(255,244,73,0.95)',
            border: '4px solid #25343F',
            padding: '30px 34px',
            boxSizing: 'border-box',
            opacity: sp(250),
            transform: `translateX(${(1 - sp(250)) * 80}px) rotate(${(1 - sp(250)) * 3}deg)`,
          }}
        >
          <div
            style={{
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontWeight: 900,
              fontSize: 24,
              letterSpacing: 2,
            }}
          >
            MARGIN SURVIVAL
          </div>

          <div
            style={{
              height: 128,
              display: 'flex',
              alignItems: 'flex-end',
              gap: 14,
              marginTop: 22,
            }}
          >
            {[0.34, 0.48, marginRecovery, 0.83].map((height, index) => (
              <div
                key={index}
                style={{
                  flex: 1,
                  height: `${height * 100}%`,
                  minHeight: 10,
                  borderRadius: '12px 12px 4px 4px',
                  background:
                    index === 2
                      ? '#FF9B51'
                      : index === 3
                        ? '#25343F'
                        : 'rgba(37,52,63,0.38)',
                  transform: `scaleY(${sp(310 + index * 16)})`,
                  transformOrigin: 'bottom',
                }}
              />
            ))}
          </div>

          <div
            style={{
              marginTop: 18,
              height: 13,
              borderRadius: 20,
              background: 'rgba(37,52,63,0.18)',
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                width: `${marginRecovery * 100}%`,
                height: '100%',
                background: '#25343F',
                borderRadius: 20,
              }}
            />
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 44%, rgba(37,52,63,0.26) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene41ValueSurvives;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene42CapitalIntensity',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene42CapitalIntensity: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.02, 1.09], {
    easing: Easing.inOut(Easing.sin),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const divider = interpolate(frame, [40, 125], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const warning = sp(390, {damping: 9, stiffness: 150});
  const machineHeat = 0.4 + Math.sin(frame / 10) * 0.16;

  const particles = Array.from({length: 42}, (_, index) => ({
    x: seededRandom(index + 20) * 1920,
    y: seededRandom(index + 90) * 1080,
    size: 2 + seededRandom(index + 170) * 5,
    speed: 0.25 + seededRandom(index + 260) * 0.75,
  }));

  const networkNodes = [
    [1340, 390],
    [1500, 300],
    [1650, 410],
    [1410, 560],
    [1600, 590],
    [1740, 530],
    [1510, 720],
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(120deg, #25343F 0%, #334652 31%, #C56C3E 31.2%, #FF9B51 70%, #E98347 100%)',
        }}
      />

      <AbsoluteFill
        style={{
          backgroundImage:
            'repeating-linear-gradient(120deg, transparent 0px, transparent 48px, rgba(255,244,73,0.055) 49px, transparent 51px)',
          transform: `translateX(${-(frame * 0.35) % 100}px)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: 560,
          top: -220,
          width: 580,
          height: 580,
          border: '70px solid rgba(255,244,73,0.055)',
          borderRadius: '50%',
          transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame / 45) * 0.04})`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -170,
          bottom: -120,
          width: 530,
          height: 530,
          background: 'rgba(37,52,63,0.12)',
          clipPath:
            'polygon(50% 0%, 62% 35%, 100% 38%, 70% 61%, 80% 100%, 50% 77%, 20% 100%, 30% 61%, 0% 38%, 38% 35%)',
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.sin((frame + index * 11) / 27) * (10 + (index % 4) * 4),
            top:
              (particle.y - frame * particle.speed + 1080 + index * 4) % 1080,
            width: particle.size,
            height: particle.size,
            borderRadius: index % 2 ? '50%' : 1,
            background:
              index % 3 === 0
                ? 'rgba(255,244,73,0.52)'
                : 'rgba(245,245,245,0.25)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `scale(${cameraScale}) translateY(${Math.sin(frame / 70) * 8}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            top: 64,
            left: 110,
            right: 110,
            display: 'flex',
            alignItems: 'flex-end',
            justifyContent: 'space-between',
            opacity: sp(8),
          }}
        >
          <div>
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                color: '#FFF449',
                fontSize: 27,
                fontWeight: 800,
                letterSpacing: 6,
              }}
            >
              CAPITAL INTENSITY
            </div>
            <div
              style={{
                marginTop: 9,
                fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
                color: '#F5F5F5',
                fontSize: 72,
                lineHeight: 0.92,
                fontWeight: 900,
                letterSpacing: -3,
              }}
            >
              SAME REVENUE.
              <br />
              DIFFERENT BURDEN.
            </div>
          </div>

          <div
            style={{
              width: 265,
              padding: '17px 24px',
              borderRadius: 100,
              border: '2px solid rgba(255,244,73,0.55)',
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 800,
              textAlign: 'center',
              background: 'rgba(37,52,63,0.65)',
            }}
          >
            $1 NEW REVENUE
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 958,
            top: 255,
            width: 4,
            height: 665 * divider,
            borderRadius: 10,
            background:
              'linear-gradient(180deg, transparent, #FFF449 15%, #FFF449 85%, transparent)',
            boxShadow: '0 0 20px rgba(255,244,73,0.45)',
          }}
        />

        <div
          style={{
            position: 'absolute',
            left: 100,
            top: 265,
            width: 760,
            height: 670,
            opacity: sp(45),
            transform: `translateX(${(1 - sp(45)) * -80}px)`,
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 25,
              fontWeight: 900,
              letterSpacing: 4,
            }}
          >
            HEAVY EQUIPMENT
          </div>

          <svg width="760" height="510" viewBox="0 0 760 510">
            <defs>
              <linearGradient id="machineBody" x1="0" y1="0" x2="1" y2="1">
                <stop offset="0" stopColor="#FFB676" />
                <stop offset="1" stopColor="#C96C3E" />
              </linearGradient>
            </defs>

            <rect
              x="88"
              y="150"
              width="538"
              height="250"
              rx="28"
              fill="url(#machineBody)"
              stroke="#FFF449"
              strokeWidth="5"
            />
            <rect
              x="142"
              y="103"
              width="122"
              height="85"
              rx="18"
              fill="#25343F"
              stroke="#FFF449"
              strokeWidth="4"
            />
            <rect
              x="457"
              y="88"
              width="104"
              height="103"
              rx="18"
              fill="#25343F"
              stroke="#FFF449"
              strokeWidth="4"
            />
            <path
              d="M203 103V48h307v40"
              fill="none"
              stroke="#FFF449"
              strokeWidth="13"
              strokeLinecap="round"
            />

            {[205, 357, 509].map((cx, index) => (
              <g
                key={cx}
                transform={`rotate(${frame * (index % 2 ? -0.65 : 0.65)} ${cx} 278)`}
              >
                <circle
                  cx={cx}
                  cy="278"
                  r="68"
                  fill="#25343F"
                  stroke="#FFF449"
                  strokeWidth="7"
                />
                {Array.from({length: 8}, (_, tooth) => {
                  const angle = (tooth * Math.PI * 2) / 8;
                  const x1 = cx + Math.cos(angle) * 48;
                  const y1 = 278 + Math.sin(angle) * 48;
                  const x2 = cx + Math.cos(angle) * 66;
                  const y2 = 278 + Math.sin(angle) * 66;
                  return (
                    <line
                      key={tooth}
                      x1={x1}
                      y1={y1}
                      x2={x2}
                      y2={y2}
                      stroke="#FF9B51"
                      strokeWidth="12"
                      strokeLinecap="round"
                    />
                  );
                })}
                <circle cx={cx} cy="278" r="18" fill="#FFF449" />
              </g>
            ))}

            <path
              d="M108 420h500"
              stroke="rgba(255,244,73,0.35)"
              strokeWidth="17"
              strokeLinecap="round"
            />
            <path
              d="M108 420h500"
              stroke="#FFF449"
              strokeWidth="5"
              strokeDasharray="18 22"
              strokeDashoffset={frame * 2}
              strokeLinecap="round"
            />

            <g opacity={machineHeat}>
              <path
                d="M270 95c-22-31 23-42 3-77M348 95c-25-34 28-48 3-81M425 94c-23-31 22-45 0-76"
                fill="none"
                stroke="#FF9B51"
                strokeWidth="8"
                strokeLinecap="round"
              />
            </g>
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 120,
              bottom: 35,
              width: 510,
              height: 77,
              borderRadius: 20,
              border: '3px solid rgba(255,244,73,0.65)',
              background: 'rgba(37,52,63,0.90)',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-around',
              color: '#F5F5F5',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 21,
              fontWeight: 800,
            }}
          >
            <span>EQUIPMENT</span>
            <span>DEBT</span>
            <span>REPLACEMENT</span>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 95,
            top: 265,
            width: 770,
            height: 665,
            opacity: sp(125),
            transform: `translateX(${(1 - sp(125)) * 90}px)`,
          }}
        >
          <div
            style={{
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 25,
              fontWeight: 900,
              letterSpacing: 4,
            }}
          >
            SCALABLE SYSTEM
          </div>

          <svg
            width="770"
            height="570"
            viewBox="0 0 770 570"
            style={{overflow: 'visible'}}
          >
            {networkNodes.map(([x, y], index) =>
              networkNodes.slice(index + 1).map(([x2, y2], target) => {
                const close =
                  Math.abs(x - x2) < 310 && Math.abs(y - y2) < 250;
                if (!close) return null;

                return (
                  <line
                    key={`${index}-${target}`}
                    x1={x - 1220}
                    y1={y - 235}
                    x2={x2 - 1220}
                    y2={y2 - 235}
                    stroke="rgba(37,52,63,0.32)"
                    strokeWidth="4"
                    strokeDasharray="9 14"
                    strokeDashoffset={-frame * 1.5}
                  />
                );
              }),
            )}

            {networkNodes.map(([x, y], index) => {
              const nx = x - 1220;
              const ny = y - 235;
              const pulse =
                1 + Math.sin((frame + index * 18) / 13) * 0.09;

              return (
                <g
                  key={index}
                  transform={`translate(${nx} ${ny}) scale(${pulse * sp(160 + index * 10)})`}
                >
                  <circle
                    r={index === 3 ? 72 : 47}
                    fill={index === 3 ? '#25343F' : '#FFF449'}
                    stroke={index === 3 ? '#FFF449' : '#25343F'}
                    strokeWidth="5"
                  />
                  {index === 3 ? (
                    <>
                      <rect
                        x="-30"
                        y="-24"
                        width="60"
                        height="48"
                        rx="9"
                        fill="#FF9B51"
                      />
                      <path
                        d="M-15-8h30M-15 8h22"
                        stroke="#25343F"
                        strokeWidth="6"
                        strokeLinecap="round"
                      />
                    </>
                  ) : (
                    <circle r="13" fill="#25343F" />
                  )}
                </g>
              );
            })}

            {Array.from({length: 10}, (_, index) => {
              const progress = ((frame * 2.3 + index * 60) % 500) / 500;
              const x = 85 + progress * 570;
              const y = 300 + Math.sin(progress * Math.PI * 3 + index) * 95;

              return (
                <circle
                  key={index}
                  cx={x}
                  cy={y}
                  r="7"
                  fill="#FFF449"
                  opacity={0.35 + (index % 3) * 0.2}
                />
              );
            })}
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 95,
              bottom: 12,
              right: 40,
              height: 88,
              borderRadius: 24,
              background: 'rgba(37,52,63,0.91)',
              border: '3px solid #FFF449',
              color: '#FFF449',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-around',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 800,
            }}
          >
            <span>SOFTWARE</span>
            <span>IP</span>
            <span>BRAND</span>
            <span>DISTRIBUTION</span>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: 465,
            width: 500,
            padding: '23px 34px',
            border: '5px solid #25343F',
            borderRadius: 26,
            background: '#FFF449',
            color: '#25343F',
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 37,
            fontWeight: 900,
            textAlign: 'center',
            letterSpacing: 1,
            opacity: warning,
            transform: `translate(-50%, -50%) rotate(${-3 + Math.sin(frame / 10) * 0.8}deg) scale(${warning})`,
            boxShadow: '0 24px 40px rgba(37,52,63,0.34)',
          }}
        >
          SCALE ≠ ANY VALUATION
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 42%, rgba(37,52,63,0.34) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene42CapitalIntensity;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene43ThreeConditionStressTest',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene43ThreeConditionStressTest: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraY = interpolate(frame, [0, durationInFrames], [22, -18], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.04, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const impact =
    frame > 382 && frame < 405
      ? Math.sin((frame - 382) * 2.8) * (1 - (frame - 382) / 23) * 12
      : 0;

  const verdict = sp(390, {damping: 8, stiffness: 180});

  const particles = Array.from({length: 50}, (_, index) => ({
    x: seededRandom(index + 32) * 1920,
    y: seededRandom(index + 103) * 1080,
    radius: 2 + seededRandom(index + 183) * 4,
    speed: 0.4 + seededRandom(index + 263) * 0.8,
  }));

  const tests = [
    {
      number: '01',
      label: 'COSTS',
      sublabel: 'OUTPACE REVENUE',
      delay: 95,
      value: 0.84,
      icon: (
        <>
          <path
            d="M8 34 22 20l10 10L47 10"
            fill="none"
            stroke="currentColor"
            strokeWidth="5"
            strokeLinecap="round"
            strokeLinejoin="round"
          />
          <path
            d="M36 10h11v11"
            fill="none"
            stroke="currentColor"
            strokeWidth="5"
            strokeLinecap="round"
            strokeLinejoin="round"
          />
        </>
      ),
    },
    {
      number: '02',
      label: 'INTEREST',
      sublabel: 'RISES AT REFINANCE',
      delay: 175,
      value: 0.72,
      icon: (
        <>
          <circle
            cx="28"
            cy="28"
            r="20"
            fill="none"
            stroke="currentColor"
            strokeWidth="4"
          />
          <path
            d="M28 14v28M36 19c-3-5-17-5-17 3 0 9 18 4 18 13 0 8-14 8-19 2"
            fill="none"
            stroke="currentColor"
            strokeWidth="4"
            strokeLinecap="round"
          />
        </>
      ),
    },
    {
      number: '03',
      label: 'DEMAND',
      sublabel: 'TRADES DOWN',
      delay: 255,
      value: 0.9,
      icon: (
        <>
          <path
            d="M10 13h39L43 33H17L10 13Z"
            fill="none"
            stroke="currentColor"
            strokeWidth="4"
            strokeLinejoin="round"
          />
          <circle cx="22" cy="42" r="4" fill="currentColor" />
          <circle cx="39" cy="42" r="4" fill="currentColor" />
          <path
            d="m18 8-7 5M40 7l9 6"
            stroke="currentColor"
            strokeWidth="4"
            strokeLinecap="round"
          />
        </>
      ),
    },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 49% 43%, #FFAE69 0%, #E48148 45%, #7D5345 73%, #25343F 112%)',
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.45,
          backgroundImage:
            'repeating-radial-gradient(circle at center, transparent 0, transparent 48px, rgba(255,244,73,0.10) 50px, transparent 52px)',
          transform: `scale(${1 + frame * 0.0005}) rotate(${frame * 0.012}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 550,
          height: 550,
          top: -270,
          left: 120,
          border: '95px solid rgba(37,52,63,0.11)',
          transform: `rotate(${20 + frame * 0.04}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 620,
          height: 620,
          right: -250,
          bottom: -290,
          borderRadius: '50%',
          border: '2px solid rgba(255,244,73,0.19)',
          boxShadow:
            'inset 0 0 0 70px rgba(255,244,73,0.025), inset 0 0 0 150px rgba(37,52,63,0.035)',
          transform: `rotate(${-frame * 0.05}deg)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.cos((frame + index * 21) / 40) * (12 + (index % 5) * 3),
            top: (particle.y - frame * particle.speed + 1200) % 1100,
            width: particle.radius * 2,
            height: particle.radius * 2,
            borderRadius: '50%',
            background:
              index % 4 === 0
                ? 'rgba(255,244,73,0.54)'
                : 'rgba(37,52,63,0.24)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `translate(${impact}px, ${cameraY}px) scale(${cameraScale})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 118,
            top: 78,
            opacity: sp(5),
            transform: `translateY(${(1 - sp(5)) * -40}px)`,
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 900,
              letterSpacing: 6,
            }}
          >
            THREE-CONDITION MODEL
          </div>
          <div
            style={{
              marginTop: 10,
              color: '#25343F',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 83,
              lineHeight: 0.9,
              fontWeight: 900,
              letterSpacing: -4,
            }}
          >
            STRESS THE BUSINESS,
            <br />
            NOT THE STORY.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 115,
            top: 92,
            width: 438,
            height: 145,
            borderRadius: 30,
            border: '3px solid #25343F',
            background: 'rgba(255,244,73,0.93)',
            padding: '22px 28px',
            boxSizing: 'border-box',
            opacity: sp(45),
          }}
        >
          <div
            style={{
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 21,
              fontWeight: 900,
            }}
          >
            INFRASTRUCTURE SPEND
          </div>
          <svg width="375" height="68" viewBox="0 0 375 68">
            <path
              d="M5 58C60 56 67 46 112 47c43 1 50-18 91-13 44 6 54-20 91-14 38 5 46-8 75-15"
              fill="none"
              stroke="rgba(37,52,63,0.20)"
              strokeWidth="13"
              strokeLinecap="round"
            />
            <path
              d="M5 58C60 56 67 46 112 47c43 1 50-18 91-13 44 6 54-20 91-14 38 5 46-8 75-15"
              fill="none"
              stroke="#25343F"
              strokeWidth="5"
              strokeLinecap="round"
              strokeDasharray="460"
              strokeDashoffset={460 * (1 - sp(75))}
            />
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 98,
            right: 98,
            top: 300,
            bottom: 110,
            borderRadius: 50,
            background: 'rgba(37,52,63,0.90)',
            border: '3px solid rgba(255,244,73,0.48)',
            boxShadow: '0 30px 70px rgba(37,52,63,0.27)',
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              position: 'absolute',
              inset: 0,
              opacity: 0.34,
              backgroundImage:
                'linear-gradient(rgba(255,244,73,0.10) 1px, transparent 1px), linear-gradient(90deg, rgba(255,244,73,0.10) 1px, transparent 1px)',
              backgroundSize: '50px 50px',
              transform: `translateY(${frame % 50}px)`,
            }}
          />

          <div
            style={{
              position: 'absolute',
              left: 60,
              right: 60,
              top: 45,
              display: 'flex',
              gap: 32,
            }}
          >
            {tests.map((test, index) => {
              const entrance = sp(test.delay);
              const gaugeProgress = interpolate(
                frame,
                [test.delay + 25, test.delay + 105],
                [0, test.value],
                {
                  extrapolateLeft: 'clamp',
                  extrapolateRight: 'clamp',
                  easing: Easing.out(Easing.cubic),
                },
              );

              return (
                <div
                  key={test.number}
                  style={{
                    position: 'relative',
                    flex: 1,
                    height: 535,
                    borderRadius: 37,
                    background:
                      index === 1
                        ? 'rgba(255,155,81,0.13)'
                        : 'rgba(245,245,245,0.055)',
                    border: '2px solid rgba(255,244,73,0.28)',
                    opacity: entrance,
                    transform: `translateY(${(1 - entrance) * 85}px) rotate(${(1 - entrance) * (index - 1) * 3}deg)`,
                    overflow: 'hidden',
                  }}
                >
                  <div
                    style={{
                      position: 'absolute',
                      right: 25,
                      top: 20,
                      color: 'rgba(255,244,73,0.25)',
                      fontFamily: 'Nexa Bold, sans-serif',
                      fontSize: 82,
                      fontWeight: 900,
                    }}
                  >
                    {test.number}
                  </div>

                  <div
                    style={{
                      position: 'absolute',
                      top: 45,
                      left: 38,
                      width: 82,
                      height: 82,
                      borderRadius: 24,
                      background: '#FFF449',
                      color: '#25343F',
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                    }}
                  >
                    <svg width="58" height="58" viewBox="0 0 56 56">
                      {test.icon}
                    </svg>
                  </div>

                  <div
                    style={{
                      position: 'absolute',
                      left: 38,
                      top: 160,
                      color: '#FFF449',
                      fontFamily: 'Mont Heavy, sans-serif',
                      fontSize: 48,
                      fontWeight: 900,
                    }}
                  >
                    {test.label}
                  </div>

                  <div
                    style={{
                      position: 'absolute',
                      left: 38,
                      top: 218,
                      color: '#F5F5F5',
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 21,
                      fontWeight: 700,
                      letterSpacing: 1,
                    }}
                  >
                    {test.sublabel}
                  </div>

                  <svg
                    width="420"
                    height="230"
                    viewBox="0 0 420 230"
                    style={{
                      position: 'absolute',
                      left: 15,
                      bottom: 25,
                    }}
                  >
                    <path
                      d="M44 194A165 165 0 0 1 376 194"
                      fill="none"
                      stroke="rgba(255,244,73,0.14)"
                      strokeWidth="34"
                      strokeLinecap="round"
                    />
                    <path
                      d="M44 194A165 165 0 0 1 376 194"
                      fill="none"
                      stroke={gaugeProgress > 0.8 ? '#FF9B51' : '#FFF449'}
                      strokeWidth="17"
                      strokeLinecap="round"
                      pathLength="1"
                      strokeDasharray={`${gaugeProgress} 1`}
                    />
                    <line
                      x1="210"
                      y1="194"
                      x2={
                        210 +
                        Math.cos(Math.PI - Math.PI * gaugeProgress) * 123
                      }
                      y2={
                        194 -
                        Math.sin(Math.PI - Math.PI * gaugeProgress) * 123
                      }
                      stroke="#F5F5F5"
                      strokeWidth="9"
                      strokeLinecap="round"
                    />
                    <circle
                      cx="210"
                      cy="194"
                      r="18"
                      fill="#FFF449"
                      stroke="#25343F"
                      strokeWidth="5"
                    />
                  </svg>
                </div>
              );
            })}
          </div>

          <div
            style={{
              position: 'absolute',
              left: '50%',
              bottom: 36,
              width: 685,
              padding: '22px 36px',
              borderRadius: 22,
              background: '#FFF449',
              border: '4px solid #FF9B51',
              color: '#25343F',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 37,
              fontWeight: 900,
              textAlign: 'center',
              opacity: verdict,
              transform: `translateX(-50%) scale(${verdict})`,
              boxShadow: '0 20px 40px rgba(0,0,0,0.28)',
            }}
          >
            REAL BUILDOUT ≠ SAFE STOCK PRICE
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 43%, rgba(37,52,63,0.35) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene43ThreeConditionStressTest;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene44RealEstateCashFlow',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene44RealEstateCashFlow: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraX = interpolate(frame, [0, durationInFrames], [-24, 22], {
    easing: Easing.inOut(Easing.sin),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.06, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cashFlow = interpolate(frame, [260, 440], [0.12, 0.66], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  const stamp = sp(395, {damping: 7, stiffness: 180});

  const particles = Array.from({length: 44}, (_, index) => ({
    x: seededRandom(index + 41) * 1920,
    y: seededRandom(index + 111) * 1080,
    size: 2 + seededRandom(index + 191) * 5,
    speed: 0.25 + seededRandom(index + 281) * 0.8,
  }));

  const costs = [
    {label: 'TAX', angle: -145, delay: 105},
    {label: 'INSURANCE', angle: -70, delay: 145},
    {label: 'REPAIRS', angle: 5, delay: 185},
    {label: 'VACANCY', angle: 78, delay: 225},
    {label: 'FINANCING', angle: 150, delay: 265},
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(145deg, #25343F 0%, #3A4650 22%, #B9623C 58%, #FF9B51 100%)',
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.4,
          backgroundImage:
            'linear-gradient(30deg, rgba(255,244,73,0.08) 12%, transparent 12.5%, transparent 87%, rgba(255,244,73,0.08) 87.5%), linear-gradient(150deg, rgba(255,244,73,0.08) 12%, transparent 12.5%, transparent 87%, rgba(255,244,73,0.08) 87.5%)',
          backgroundSize: '90px 156px',
          backgroundPosition: `${frame * 0.18}px ${frame * 0.1}px`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -170,
          bottom: -230,
          width: 630,
          height: 630,
          borderRadius: '50%',
          border: '2px solid rgba(255,244,73,0.18)',
          boxShadow:
            'inset 0 0 0 80px rgba(255,244,73,0.025), inset 0 0 0 190px rgba(255,244,73,0.025)',
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -80,
          top: -220,
          width: 560,
          height: 560,
          background: 'rgba(255,244,73,0.055)',
          clipPath: 'polygon(50% 0, 100% 25%, 82% 100%, 18% 100%, 0 25%)',
          transform: `rotate(${-8 + frame * 0.035}deg)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.sin((frame + index * 8) / 28) * (10 + index % 12),
            top: (particle.y - frame * particle.speed + 1100) % 1100,
            width: particle.size,
            height: particle.size,
            borderRadius: index % 3 === 0 ? 1 : '50%',
            background:
              index % 5 === 0
                ? 'rgba(255,244,73,0.52)'
                : 'rgba(245,245,245,0.23)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `translateX(${cameraX}px) scale(${cameraScale})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 72,
            opacity: sp(5),
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 900,
              letterSpacing: 6,
            }}
          >
            REAL ESTATE FILTER
          </div>
          <div
            style={{
              marginTop: 10,
              color: '#F5F5F5',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 76,
              lineHeight: 0.92,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            THE BUILDING DOES NOT
            <br />
            PAY YOU. CASH FLOW DOES.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 160,
            top: 312,
            width: 910,
            height: 640,
            opacity: sp(45),
            transform: `scale(${sp(45)})`,
          }}
        >
          <svg width="910" height="640" viewBox="0 0 910 640">
            <ellipse
              cx="455"
              cy="575"
              rx="300"
              ry="43"
              fill="rgba(37,52,63,0.30)"
            />

            <path
              d="M255 545V165L455 55l200 110v380Z"
              fill="#FF9B51"
              stroke="#FFF449"
              strokeWidth="7"
              strokeLinejoin="round"
            />

            <path
              d="M455 55v490M255 165l200 105 200-105"
              fill="none"
              stroke="rgba(37,52,63,0.38)"
              strokeWidth="5"
            />

            {Array.from({length: 4}, (_, row) =>
              Array.from({length: 3}, (_, col) => {
                const x = 305 + col * 95;
                const y = 230 + row * 75;
                const light =
                  Math.sin((frame + row * 17 + col * 23) / 20) > -0.15;

                return (
                  <rect
                    key={`l-${row}-${col}`}
                    x={x}
                    y={y}
                    width="57"
                    height="41"
                    rx="6"
                    fill={light ? '#FFF449' : '#25343F'}
                    opacity={light ? 0.92 : 0.6}
                  />
                );
              }),
            )}

            {Array.from({length: 4}, (_, row) =>
              Array.from({length: 3}, (_, col) => {
                const x = 503 + col * 39;
                const y = 230 + row * 75;
                const light =
                  Math.sin((frame + row * 31 + col * 19) / 18) > 0;

                return (
                  <rect
                    key={`r-${row}-${col}`}
                    x={x}
                    y={y}
                    width="27"
                    height="41"
                    rx="5"
                    fill={light ? '#FFF449' : '#25343F'}
                    opacity={light ? 0.9 : 0.62}
                  />
                );
              }),
            )}

            <rect
              x="402"
              y="445"
              width="106"
              height="100"
              rx="12"
              fill="#25343F"
              stroke="#FFF449"
              strokeWidth="5"
            />

            <path
              d="M148 575h615"
              stroke="#FFF449"
              strokeWidth="11"
              strokeLinecap="round"
            />
          </svg>

          {costs.map((cost, index) => {
            const radians = (cost.angle * Math.PI) / 180;
            const x = 455 + Math.cos(radians) * 370;
            const y = 330 + Math.sin(radians) * 240;
            const pulse = 1 + Math.sin((frame + index * 16) / 14) * 0.035;

            return (
              <div
                key={cost.label}
                style={{
                  position: 'absolute',
                  left: x - 90,
                  top: y - 43,
                  width: 180,
                  height: 86,
                  borderRadius: 21,
                  background: '#25343F',
                  border: '3px solid #FFF449',
                  color: '#FFF449',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: cost.label.length > 8 ? 18 : 21,
                  fontWeight: 900,
                  opacity: sp(cost.delay),
                  transform: `scale(${sp(cost.delay) * pulse})`,
                  boxShadow: '0 15px 28px rgba(37,52,63,0.25)',
                }}
              >
                {cost.label}
              </div>
            );
          })}
        </div>

        <div
          style={{
            position: 'absolute',
            right: 105,
            top: 305,
            width: 600,
            height: 630,
            borderRadius: 44,
            background: 'rgba(255,244,73,0.95)',
            border: '5px solid #25343F',
            boxShadow: '0 30px 70px rgba(37,52,63,0.33)',
            opacity: sp(145),
            transform: `translateX(${(1 - sp(145)) * 100}px)`,
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              padding: '36px 40px 20px',
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            MONTHLY CASH-FLOW PIPE
          </div>

          <div
            style={{
              position: 'absolute',
              left: 80,
              top: 115,
              width: 110,
              bottom: 78,
              borderRadius: 60,
              background: 'rgba(37,52,63,0.15)',
              border: '5px solid #25343F',
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                position: 'absolute',
                left: 0,
                right: 0,
                bottom: 0,
                height: `${cashFlow * 100}%`,
                background:
                  'linear-gradient(180deg, #FFB36F 0%, #FF9B51 100%)',
              }}
            />

            {Array.from({length: 7}, (_, index) => (
              <div
                key={index}
                style={{
                  position: 'absolute',
                  left: 14 + (index % 2) * 30,
                  bottom:
                    ((frame * (0.6 + index * 0.07) + index * 71) %
                      Math.max(40, cashFlow * 390)) +
                    4,
                  width: 13,
                  height: 13,
                  borderRadius: '50%',
                  background: '#FFF449',
                  border: '2px solid #25343F',
                }}
              />
            ))}
          </div>

          <div
            style={{
              position: 'absolute',
              left: 225,
              right: 45,
              top: 125,
              bottom: 78,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'space-between',
            }}
          >
            {[
              ['RENT', '+100%'],
              ['TAX', '-14%'],
              ['INSURANCE', '-12%'],
              ['REPAIRS', '-8%'],
              ['FINANCING', '-25%'],
              ['NET CASH', '+41%'],
            ].map(([label, amount], index) => {
              const rowEntrance = sp(180 + index * 23);
              return (
                <div
                  key={label}
                  style={{
                    height: 58,
                    borderRadius: 16,
                    background:
                      index === 5
                        ? '#25343F'
                        : index === 0
                          ? '#FF9B51'
                          : 'rgba(37,52,63,0.11)',
                    border:
                      index === 5
                        ? '3px solid #FF9B51'
                        : '2px solid rgba(37,52,63,0.27)',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'space-between',
                    padding: '0 21px',
                    boxSizing: 'border-box',
                    color: index === 5 ? '#FFF449' : '#25343F',
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 21,
                    fontWeight: 900,
                    opacity: rowEntrance,
                    transform: `translateX(${(1 - rowEntrance) * 55}px)`,
                  }}
                >
                  <span>{label}</span>
                  <span>{amount}</span>
                </div>
              );
            })}
          </div>

          <div
            style={{
              position: 'absolute',
              right: 32,
              top: 252,
              width: 315,
              padding: '18px 24px',
              borderRadius: 18,
              background: '#FF9B51',
              border: '4px solid #25343F',
              color: '#25343F',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 29,
              fontWeight: 900,
              textAlign: 'center',
              opacity: stamp,
              transform: `rotate(-7deg) scale(${stamp})`,
              boxShadow: '0 16px 30px rgba(37,52,63,0.28)',
            }}
          >
            ANALYZE THE NET
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 43%, rgba(37,52,63,0.38) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene44RealEstateCashFlow;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene45FinancingAndCompounding',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene45FinancingAndCompounding: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.05, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraRotation = Math.sin(frame / 95) * 0.35;
  const fixedNeedle = -28 + Math.sin(frame / 30) * 2;
  const floatingNeedle = -38 + Math.sin(frame / 13) * 30;
  const vaultEntrance = sp(335, {damping: 9, stiffness: 150});

  const particles = Array.from({length: 46}, (_, index) => ({
    x: seededRandom(index + 52) * 1920,
    y: seededRandom(index + 122) * 1080,
    size: 2 + seededRandom(index + 202) * 5,
    speed: 0.3 + seededRandom(index + 292) * 0.7,
  }));

  const wavePoints = Array.from({length: 52}, (_, index) => {
    const x = index * 12;
    const y =
      95 +
      Math.sin(index * 0.52 + frame * 0.075) * 38 +
      Math.sin(index * 0.2 + frame * 0.13) * 22;
    return `${x},${y}`;
  }).join(' ');

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 68% 43%, #FFAE67 0%, #E37C46 45%, #6A4D46 77%, #25343F 120%)',
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.42,
          backgroundImage:
            'linear-gradient(90deg, rgba(37,52,63,0.12) 1px, transparent 1px), linear-gradient(rgba(37,52,63,0.12) 1px, transparent 1px)',
          backgroundSize: '80px 80px',
          transform: `translate(${frame * 0.12}px, ${frame * 0.08}px)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -230,
          top: 120,
          width: 620,
          height: 620,
          borderRadius: '50%',
          border: '90px solid rgba(255,244,73,0.055)',
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -210,
          bottom: -190,
          width: 600,
          height: 600,
          background: 'rgba(37,52,63,0.12)',
          clipPath: 'polygon(50% 0%, 94% 25%, 77% 100%, 23% 100%, 6% 25%)',
          transform: `rotate(${-15 + frame * 0.04}deg)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.sin((frame + index * 10) / 30) * (10 + index % 13),
            top: (particle.y - frame * particle.speed + 1100) % 1100,
            width: particle.size,
            height: particle.size,
            borderRadius: index % 4 === 0 ? 2 : '50%',
            background:
              index % 4 === 0
                ? 'rgba(255,244,73,0.50)'
                : 'rgba(37,52,63,0.24)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `scale(${cameraScale}) rotate(${cameraRotation}deg)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 112,
            top: 70,
            opacity: sp(5),
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 900,
              letterSpacing: 6,
            }}
          >
            DEFENSE LAYER / 05
          </div>
          <div
            style={{
              marginTop: 10,
              color: '#25343F',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 78,
              lineHeight: 0.92,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            STABILITY FIRST.
            <br />
            COMPOUNDING NEXT.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 100,
            top: 292,
            width: 960,
            height: 470,
            borderRadius: 48,
            background: 'rgba(37,52,63,0.91)',
            border: '3px solid rgba(255,244,73,0.50)',
            boxShadow: '0 28px 65px rgba(37,52,63,0.30)',
            opacity: sp(45),
            transform: `translateX(${(1 - sp(45)) * -75}px)`,
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 45,
              top: 32,
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 23,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            FINANCING STABILITY
          </div>

          {[
            {
              label: 'FIXED RATE',
              x: 40,
              needle: fixedNeedle,
              color: '#FFF449',
              delay: 85,
            },
            {
              label: 'FLOATING DEBT',
              x: 500,
              needle: floatingNeedle,
              color: '#FF9B51',
              delay: 145,
            },
          ].map((gauge, index) => (
            <div
              key={gauge.label}
              style={{
                position: 'absolute',
                left: gauge.x,
                top: 90,
                width: 420,
                height: 325,
                borderRadius: 35,
                background:
                  index === 0
                    ? 'rgba(255,244,73,0.08)'
                    : 'rgba(255,155,81,0.09)',
                border: `2px solid ${index === 0 ? 'rgba(255,244,73,0.32)' : 'rgba(255,155,81,0.38)'}`,
                opacity: sp(gauge.delay),
                transform: `scale(${sp(gauge.delay)})`,
              }}
            >
              <svg width="420" height="250" viewBox="0 0 420 250">
                <path
                  d="M55 207A155 155 0 0 1 365 207"
                  fill="none"
                  stroke="rgba(245,245,245,0.11)"
                  strokeWidth="34"
                  strokeLinecap="round"
                />
                <path
                  d="M55 207A155 155 0 0 1 365 207"
                  fill="none"
                  stroke={gauge.color}
                  strokeWidth="15"
                  strokeLinecap="round"
                  strokeDasharray={index === 0 ? '260 230' : '380 110'}
                />
                {Array.from({length: 7}, (_, tick) => {
                  const angle = Math.PI + (tick * Math.PI) / 6;
                  const x1 = 210 + Math.cos(angle) * 129;
                  const y1 = 207 + Math.sin(angle) * 129;
                  const x2 = 210 + Math.cos(angle) * 146;
                  const y2 = 207 + Math.sin(angle) * 146;
                  return (
                    <line
                      key={tick}
                      x1={x1}
                      y1={y1}
                      x2={x2}
                      y2={y2}
                      stroke="#F5F5F5"
                      strokeWidth="4"
                      strokeLinecap="round"
                    />
                  );
                })}
                <g transform={`rotate(${gauge.needle} 210 207)`}>
                  <line
                    x1="210"
                    y1="207"
                    x2="210"
                    y2="82"
                    stroke={gauge.color}
                    strokeWidth="10"
                    strokeLinecap="round"
                  />
                </g>
                <circle
                  cx="210"
                  cy="207"
                  r="20"
                  fill={gauge.color}
                  stroke="#25343F"
                  strokeWidth="6"
                />
              </svg>

              <div
                style={{
                  position: 'absolute',
                  left: 0,
                  right: 0,
                  bottom: 27,
                  color: gauge.color,
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 34,
                  fontWeight: 900,
                  textAlign: 'center',
                  letterSpacing: 1,
                }}
              >
                {gauge.label}
              </div>
            </div>
          ))}
        </div>

        <div
          style={{
            position: 'absolute',
            right: 102,
            top: 98,
            width: 620,
            height: 218,
            borderRadius: 34,
            background: 'rgba(255,244,73,0.95)',
            border: '4px solid #25343F',
            padding: '26px 34px',
            boxSizing: 'border-box',
            opacity: sp(175),
            transform: `translateY(${(1 - sp(175)) * -65}px)`,
          }}
        >
          <div
            style={{
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 900,
              letterSpacing: 2,
            }}
          >
            COMMODITY VOLATILITY
          </div>

          <svg
            width="545"
            height="135"
            viewBox="0 0 624 190"
            style={{marginTop: 5}}
          >
            <line
              x1="0"
              y1="95"
              x2="624"
              y2="95"
              stroke="rgba(37,52,63,0.22)"
              strokeWidth="4"
              strokeDasharray="10 12"
            />
            <polyline
              points={wavePoints}
              fill="none"
              stroke="#25343F"
              strokeWidth="9"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
            <polyline
              points={wavePoints}
              fill="none"
              stroke="#FF9B51"
              strokeWidth="3"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 100,
            bottom: 98,
            width: 655,
            height: 570,
            opacity: vaultEntrance,
            transform: `translateY(${(1 - vaultEntrance) * 100}px) scale(${vaultEntrance})`,
          }}
        >
          <svg width="655" height="570" viewBox="0 0 655 570">
            <ellipse
              cx="330"
              cy="522"
              rx="260"
              ry="35"
              fill="rgba(37,52,63,0.28)"
            />
            <rect
              x="65"
              y="60"
              width="530"
              height="445"
              rx="55"
              fill="#FFF449"
              stroke="#25343F"
              strokeWidth="9"
            />
            <rect
              x="115"
              y="110"
              width="430"
              height="345"
              rx="42"
              fill="#FF9B51"
              stroke="#25343F"
              strokeWidth="7"
            />
            <circle
              cx="330"
              cy="282"
              r="112"
              fill="#25343F"
              stroke="#FFF449"
              strokeWidth="8"
            />
            <circle
              cx="330"
              cy="282"
              r="44"
              fill="#FFF449"
              stroke="#FF9B51"
              strokeWidth="7"
            />
            {Array.from({length: 6}, (_, index) => {
              const angle = (index * Math.PI * 2) / 6 + frame * 0.01;
              const x1 = 330 + Math.cos(angle) * 51;
              const y1 = 282 + Math.sin(angle) * 51;
              const x2 = 330 + Math.cos(angle) * 88;
              const y2 = 282 + Math.sin(angle) * 88;
              return (
                <line
                  key={index}
                  x1={x1}
                  y1={y1}
                  x2={x2}
                  y2={y2}
                  stroke="#FFF449"
                  strokeWidth="10"
                  strokeLinecap="round"
                />
              );
            })}
            <rect
              x="505"
              y="236"
              width="65"
              height="92"
              rx="17"
              fill="#25343F"
              stroke="#FFF449"
              strokeWidth="6"
            />
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: 140,
              textAlign: 'center',
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            2026 401(k) LIMIT
          </div>

          <div
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: 350,
              textAlign: 'center',
              color: '#25343F',
              fontFamily: 'Nexa Bold, Mont Heavy, sans-serif',
              fontSize: 80,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            $24,500
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 43%, rgba(37,52,63,0.34) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene45FinancingAndCompounding;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene46HSALimits',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene46HSALimits: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08], {
    easing: Easing.inOut(Easing.sin),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const selfFill = interpolate(frame, [80, 230], [0, 0.62], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const familyFill = interpolate(frame, [135, 300], [0, 0.82], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const reserveFill = interpolate(frame, [310, 455], [0, 0.68], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  const equationEntrance = sp(420, {damping: 8, stiffness: 160});

  const particles = Array.from({length: 43}, (_, index) => ({
    x: seededRandom(index + 63) * 1920,
    y: seededRandom(index + 133) * 1080,
    size: 2 + seededRandom(index + 213) * 5,
    speed: 0.3 + seededRandom(index + 303) * 0.7,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(135deg, #25343F 0%, #45505A 28%, #C66E42 63%, #FF9B51 100%)',
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.38,
          backgroundImage:
            'radial-gradient(circle, rgba(255,244,73,0.22) 2px, transparent 2px)',
          backgroundSize: '48px 48px',
          transform: `translate(${frame * 0.16}px, ${frame * 0.09}px)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -110,
          top: -190,
          width: 530,
          height: 530,
          borderRadius: '50%',
          border: '80px solid rgba(255,244,73,0.05)',
          transform: `scale(${1 + Math.sin(frame / 42) * 0.05})`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -180,
          bottom: -210,
          width: 610,
          height: 610,
          background: 'rgba(37,52,63,0.12)',
          clipPath:
            'polygon(50% 0%, 61% 37%, 100% 50%, 61% 63%, 50% 100%, 39% 63%, 0% 50%, 39% 37%)',
          transform: `rotate(${frame * 0.07}deg)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.sin((frame + index * 14) / 31) * (12 + index % 8),
            top: (particle.y - frame * particle.speed + 1100) % 1100,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            background:
              index % 4 === 0
                ? 'rgba(255,244,73,0.52)'
                : 'rgba(245,245,245,0.23)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `scale(${cameraScale}) translateY(${Math.sin(frame / 70) * 7}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 112,
            top: 72,
            opacity: sp(5),
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 900,
              letterSpacing: 6,
            }}
          >
            HSA CONTRIBUTION LIMITS
          </div>
          <div
            style={{
              marginTop: 10,
              color: '#F5F5F5',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 79,
              lineHeight: 0.92,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            A CEILING IS NOT
            <br />
            AN EMERGENCY.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 310,
            width: 1050,
            height: 650,
            borderRadius: 48,
            background: 'rgba(255,244,73,0.94)',
            border: '5px solid #25343F',
            boxShadow: '0 30px 70px rgba(37,52,63,0.30)',
            opacity: sp(45),
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 42,
              top: 30,
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 23,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            CONTRIBUTION CAPACITY
          </div>

          {[
            {
              x: 110,
              label: 'SELF-ONLY',
              amount: '$4,400',
              fill: selfFill,
              delay: 75,
            },
            {
              x: 570,
              label: 'FAMILY',
              amount: '$8,750',
              fill: familyFill,
              delay: 125,
            },
          ].map((tank, index) => (
            <div
              key={tank.label}
              style={{
                position: 'absolute',
                left: tank.x,
                top: 110,
                width: 360,
                height: 470,
                opacity: sp(tank.delay),
                transform: `translateY(${(1 - sp(tank.delay)) * 75}px)`,
              }}
            >
              <div
                style={{
                  position: 'absolute',
                  left: 60,
                  top: 0,
                  width: 240,
                  height: 350,
                  borderRadius: '36px 36px 70px 70px',
                  border: '7px solid #25343F',
                  background: 'rgba(37,52,63,0.10)',
                  overflow: 'hidden',
                }}
              >
                <div
                  style={{
                    position: 'absolute',
                    left: 0,
                    right: 0,
                    bottom: 0,
                    height: `${tank.fill * 100}%`,
                    background:
                      index === 0
                        ? 'linear-gradient(180deg, #FFB36F, #FF9B51)'
                        : 'linear-gradient(180deg, #FF9B51, #D66D42)',
                  }}
                />

                <div
                  style={{
                    position: 'absolute',
                    left: 0,
                    right: 0,
                    top: 55,
                    height: 5,
                    background: '#25343F',
                  }}
                />

                <div
                  style={{
                    position: 'absolute',
                    left: 0,
                    right: 0,
                    top: 47,
                    textAlign: 'center',
                    color: '#25343F',
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 16,
                    fontWeight: 900,
                  }}
                >
                  LIMIT
                </div>

                {Array.from({length: 7}, (_, bubble) => (
                  <div
                    key={bubble}
                    style={{
                      position: 'absolute',
                      left: 30 + ((bubble * 53) % 160),
                      bottom:
                        ((frame * (0.55 + bubble * 0.08) + bubble * 41) %
                          Math.max(30, tank.fill * 300)) +
                        5,
                      width: 10 + (bubble % 3) * 4,
                      height: 10 + (bubble % 3) * 4,
                      borderRadius: '50%',
                      background: '#FFF449',
                      border: '2px solid #25343F',
                    }}
                  />
                ))}
              </div>

              <div
                style={{
                  position: 'absolute',
                  left: 0,
                  right: 0,
                  bottom: 70,
                  color: '#25343F',
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 40,
                  fontWeight: 900,
                  textAlign: 'center',
                }}
              >
                {tank.label}
              </div>

              <div
                style={{
                  position: 'absolute',
                  left: 0,
                  right: 0,
                  bottom: 4,
                  color: '#25343F',
                  fontFamily: 'Nexa Bold, sans-serif',
                  fontSize: 57,
                  fontWeight: 900,
                  textAlign: 'center',
                }}
              >
                {tank.amount}
              </div>
            </div>
          ))}
        </div>

        <div
          style={{
            position: 'absolute',
            right: 108,
            top: 265,
            width: 600,
            height: 535,
            borderRadius: 45,
            background: 'rgba(37,52,63,0.93)',
            border: '3px solid rgba(255,244,73,0.55)',
            opacity: sp(245),
            transform: `translateX(${(1 - sp(245)) * 90}px)`,
            boxShadow: '0 28px 60px rgba(37,52,63,0.34)',
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 38,
              top: 31,
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 23,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            EMERGENCY RESERVE
          </div>

          <svg width="600" height="425" viewBox="0 0 600 425">
            <path
              d="M300 73 488 145v105c0 98-74 166-188 204C186 416 112 348 112 250V145L300 73Z"
              fill="rgba(255,155,81,0.18)"
              stroke="#FFF449"
              strokeWidth="7"
            />

            <path
              d="M300 408c-89-34-141-87-141-161v-70l141-54 141 54v70c0 74-52 127-141 161Z"
              fill="#FF9B51"
              opacity={0.2 + reserveFill * 0.8}
            />

            <line
              x1="155"
              y1={380 - reserveFill * 220}
              x2="445"
              y2={380 - reserveFill * 220}
              stroke="#FFF449"
              strokeWidth="7"
              strokeLinecap="round"
            />

            <path
              d="M257 260h86M300 217v86"
              stroke="#25343F"
              strokeWidth="18"
              strokeLinecap="round"
            />
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              bottom: 29,
              color: '#F5F5F5',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 38,
              fontWeight: 900,
              textAlign: 'center',
            }}
          >
            LIQUIDITY STAYS ACCESSIBLE
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 116,
            bottom: 105,
            width: 570,
            padding: '26px 35px',
            borderRadius: 28,
            background: '#FFF449',
            border: '5px solid #25343F',
            color: '#25343F',
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 43,
            fontWeight: 900,
            textAlign: 'center',
            opacity: equationEntrance,
            transform: `rotate(-3deg) scale(${equationEntrance})`,
            boxShadow: '0 22px 42px rgba(37,52,63,0.30)',
          }}
        >
          LIMIT ≠ TARGET
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 42%, rgba(37,52,63,0.36) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene46HSALimits;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene47RetirementAccountScanner',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene47RetirementAccountScanner: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraX = interpolate(frame, [0, durationInFrames], [25, -22], {
    easing: Easing.inOut(Easing.sin),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.06, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const scanY = 115 + ((frame * 2.25) % 610);
  const portfolioRotation = frame * 0.18;
  const auditComplete = sp(405, {damping: 8, stiffness: 160});

  const particles = Array.from({length: 47}, (_, index) => ({
    x: seededRandom(index + 74) * 1920,
    y: seededRandom(index + 144) * 1080,
    size: 2 + seededRandom(index + 224) * 5,
    speed: 0.25 + seededRandom(index + 314) * 0.8,
  }));

  const auditItems = [
    {label: 'FEES', value: '0.84%', delay: 115},
    {label: 'MATCH', value: '4%', delay: 165},
    {label: 'VESTING', value: '3 YRS', delay: 215},
    {label: 'DEFAULT', value: 'ACTIVE', delay: 265},
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(125deg, #25343F 0%, #354650 36%, #B85F3D 70%, #FF9B51 100%)',
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.33,
          backgroundImage:
            'linear-gradient(rgba(255,244,73,0.11) 1px, transparent 1px), linear-gradient(90deg, rgba(255,244,73,0.11) 1px, transparent 1px)',
          backgroundSize: '55px 55px',
          transform: `translate(${frame * 0.12}px, ${frame * 0.16}px)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -210,
          bottom: -180,
          width: 580,
          height: 580,
          borderRadius: '50%',
          border: '85px solid rgba(255,244,73,0.05)',
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -110,
          top: -210,
          width: 540,
          height: 540,
          border: '3px solid rgba(255,244,73,0.13)',
          transform: `rotate(${35 + frame * 0.035}deg)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.sin((frame + index * 12) / 29) * (10 + index % 10),
            top: (particle.y - frame * particle.speed + 1100) % 1100,
            width: particle.size,
            height: particle.size,
            borderRadius: index % 2 ? '50%' : 2,
            background:
              index % 5 === 0
                ? 'rgba(255,244,73,0.56)'
                : 'rgba(245,245,245,0.20)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `translateX(${cameraX}px) scale(${cameraScale})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 72,
            opacity: sp(5),
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 900,
              letterSpacing: 6,
            }}
          >
            RETIREMENT ACCOUNT X-RAY
          </div>
          <div
            style={{
              marginTop: 10,
              color: '#F5F5F5',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 76,
              lineHeight: 0.92,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            AUDIT WHAT YOUR
            <br />
            401(k) ACTUALLY OWNS.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 100,
            top: 285,
            width: 1080,
            height: 680,
            borderRadius: 48,
            background: 'rgba(37,52,63,0.93)',
            border: '3px solid rgba(255,244,73,0.50)',
            overflow: 'hidden',
            boxShadow: '0 32px 75px rgba(37,52,63,0.32)',
            opacity: sp(40),
          }}
        >
          <div
            style={{
              position: 'absolute',
              inset: 0,
              opacity: 0.35,
              backgroundImage:
                'linear-gradient(rgba(255,244,73,0.11) 1px, transparent 1px), linear-gradient(90deg, rgba(255,244,73,0.11) 1px, transparent 1px)',
              backgroundSize: '42px 42px',
            }}
          />

          <div
            style={{
              position: 'absolute',
              left: 44,
              top: 32,
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            PORTFOLIO SCAN / LIVE
          </div>

          <div
            style={{
              position: 'absolute',
              left: 65,
              top: 120,
              width: 470,
              height: 470,
              borderRadius: '50%',
              border: '2px solid rgba(255,244,73,0.24)',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}
          >
            <div
              style={{
                position: 'absolute',
                width: 390,
                height: 390,
                borderRadius: '50%',
                background:
                  'conic-gradient(#FFF449 0deg 214deg, #FF9B51 214deg 298deg, #72828A 298deg 336deg, #F5F5F5 336deg 360deg)',
                transform: `rotate(${portfolioRotation}deg) scale(${sp(90)})`,
                boxShadow: '0 20px 45px rgba(0,0,0,0.24)',
              }}
            />

            <div
              style={{
                position: 'absolute',
                width: 205,
                height: 205,
                borderRadius: '50%',
                background: '#25343F',
                border: '7px solid #FF9B51',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                flexDirection: 'column',
                color: '#FFF449',
              }}
            >
              <svg width="66" height="66" viewBox="0 0 64 64">
                <rect
                  x="11"
                  y="11"
                  width="42"
                  height="42"
                  rx="10"
                  fill="none"
                  stroke="currentColor"
                  strokeWidth="5"
                />
                <path
                  d="M22 40V29m10 11V20m10 20V25"
                  stroke="currentColor"
                  strokeWidth="5"
                  strokeLinecap="round"
                />
              </svg>
              <div
                style={{
                  marginTop: 7,
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 19,
                  fontWeight: 900,
                }}
              >
                ALLOCATION
              </div>
            </div>
          </div>

          <div
            style={{
              position: 'absolute',
              left: 575,
              right: 45,
              top: 110,
              bottom: 60,
              display: 'flex',
              flexDirection: 'column',
              gap: 21,
            }}
          >
            {auditItems.map((item, index) => {
              const entrance = sp(item.delay);

              return (
                <div
                  key={item.label}
                  style={{
                    flex: 1,
                    borderRadius: 24,
                    background:
                      index === 3
                        ? 'rgba(255,155,81,0.22)'
                        : 'rgba(245,245,245,0.06)',
                    border:
                      index === 3
                        ? '3px solid #FF9B51'
                        : '2px solid rgba(255,244,73,0.25)',
                    display: 'flex',
                    alignItems: 'center',
                    padding: '0 27px',
                    boxSizing: 'border-box',
                    opacity: entrance,
                    transform: `translateX(${(1 - entrance) * 75}px)`,
                  }}
                >
                  <div
                    style={{
                      width: 58,
                      height: 58,
                      borderRadius: 17,
                      background: index === 3 ? '#FF9B51' : '#FFF449',
                      color: '#25343F',
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'center',
                    }}
                  >
                    <svg width="37" height="37" viewBox="0 0 40 40">
                      {index === 0 && (
                        <>
                          <path
                            d="M20 5v30M28 10c-4-6-18-5-18 3 0 10 20 5 20 14 0 8-14 10-21 3"
                            fill="none"
                            stroke="currentColor"
                            strokeWidth="4"
                            strokeLinecap="round"
                          />
                        </>
                      )}
                      {index === 1 && (
                        <>
                          <path
                            d="m7 21 8 8L33 11"
                            fill="none"
                            stroke="currentColor"
                            strokeWidth="5"
                            strokeLinecap="round"
                            strokeLinejoin="round"
                          />
                        </>
                      )}
                      {index === 2 && (
                        <>
                          <path
                            d="M8 31h24M10 30V22h7v8m3 0V14h7v16m3 0V7h6v23"
                            fill="none"
                            stroke="currentColor"
                            strokeWidth="3.5"
                            strokeLinejoin="round"
                          />
                        </>
                      )}
                      {index === 3 && (
                        <>
                          <circle
                            cx="20"
                            cy="20"
                            r="13"
                            fill="none"
                            stroke="currentColor"
                            strokeWidth="4"
                          />
                          <path
                            d="M20 13v8l6 4"
                            fill="none"
                            stroke="currentColor"
                            strokeWidth="4"
                            strokeLinecap="round"
                          />
                        </>
                      )}
                    </svg>
                  </div>

                  <div
                    style={{
                      marginLeft: 22,
                      color: '#F5F5F5',
                      fontFamily: 'Mont Heavy, sans-serif',
                      fontSize: 35,
                      fontWeight: 900,
                    }}
                  >
                    {item.label}
                  </div>

                  <div
                    style={{
                      marginLeft: 'auto',
                      color: index === 3 ? '#FF9B51' : '#FFF449',
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 29,
                      fontWeight: 900,
                    }}
                  >
                    {item.value}
                  </div>
                </div>
              );
            })}
          </div>

          <div
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: scanY,
              height: 5,
              background:
                'linear-gradient(90deg, transparent, #FFF449 18%, #FFF449 82%, transparent)',
              boxShadow: '0 0 28px rgba(255,244,73,0.85)',
            }}
          />
        </div>

        <div
          style={{
            position: 'absolute',
            right: 102,
            top: 280,
            width: 590,
            height: 680,
            borderRadius: 47,
            background: 'rgba(255,244,73,0.95)',
            border: '5px solid #25343F',
            opacity: sp(285),
            transform: `translateX(${(1 - sp(285)) * 100}px)`,
            boxShadow: '0 30px 70px rgba(37,52,63,0.32)',
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 38,
              top: 34,
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 23,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            CONCENTRATION ALERT
          </div>

          <svg width="590" height="520" viewBox="0 0 590 520">
            <path
              d="M95 415V230M197 415V170M299 415V112M401 415V275M503 415V340"
              stroke="rgba(37,52,63,0.15)"
              strokeWidth="60"
              strokeLinecap="round"
            />
            {[0.56, 0.76, 0.96, 0.4, 0.23].map((height, index) => (
              <line
                key={index}
                x1={95 + index * 102}
                y1="415"
                x2={95 + index * 102}
                y2={415 - height * 305 * sp(310 + index * 12)}
                stroke={index === 2 ? '#FF9B51' : '#25343F'}
                strokeWidth="31"
                strokeLinecap="round"
              />
            ))}
            <path
              d="M35 435h520"
              stroke="#25343F"
              strokeWidth="6"
              strokeLinecap="round"
            />
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 55,
              right: 55,
              bottom: 45,
              height: 82,
              borderRadius: 22,
              background: '#25343F',
              color: '#FFF449',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 34,
              fontWeight: 900,
              opacity: auditComplete,
              transform: `scale(${auditComplete})`,
            }}
          >
            REVIEW BEFORE ASSUMING
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 42%, rgba(37,52,63,0.38) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene47RetirementAccountScanner;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene48TotalHealthcareCost',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene48TotalHealthcareCost: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.08, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraY = interpolate(frame, [0, durationInFrames], [15, -15], {
    easing: Easing.inOut(Easing.sin),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const waterOffset = Math.sin(frame / 18) * 9;
  const costGrowth = interpolate(frame, [180, 390], [0.25, 0.87], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.cubic),
  });

  const finalCallout = sp(410, {damping: 8, stiffness: 170});

  const particles = Array.from({length: 45}, (_, index) => ({
    x: seededRandom(index + 85) * 1920,
    y: seededRandom(index + 155) * 1080,
    size: 2 + seededRandom(index + 235) * 5,
    speed: 0.25 + seededRandom(index + 325) * 0.75,
  }));

  const submergedCosts = [
    {label: 'DEDUCTIBLE', width: 370, delay: 150},
    {label: 'COINSURANCE', width: 470, delay: 195},
    {label: 'COPAYS', width: 310, delay: 240},
    {label: 'OUT-OF-POCKET MAX', width: 570, delay: 285},
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(180deg, #FF9B51 0%, #D77343 44%, #40505A 71%, #25343F 100%)',
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.32,
          backgroundImage:
            'repeating-linear-gradient(115deg, transparent 0px, transparent 52px, rgba(255,244,73,0.08) 54px, transparent 56px)',
          transform: `translateX(${frame * 0.18}px)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -190,
          top: -210,
          width: 560,
          height: 560,
          borderRadius: '50%',
          border: '85px solid rgba(37,52,63,0.08)',
          transform: `scale(${1 + Math.sin(frame / 45) * 0.04})`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -160,
          bottom: -250,
          width: 650,
          height: 650,
          borderRadius: '50%',
          border: '3px solid rgba(255,244,73,0.13)',
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.sin((frame + index * 13) / 30) * (8 + index % 14),
            top: (particle.y - frame * particle.speed + 1100) % 1100,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            background:
              index % 4 === 0
                ? 'rgba(255,244,73,0.48)'
                : 'rgba(245,245,245,0.21)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `translateY(${cameraY}px) scale(${cameraScale})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 112,
            top: 70,
            opacity: sp(5),
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 900,
              letterSpacing: 6,
            }}
          >
            HEALTHCARE COST ICEBERG
          </div>
          <div
            style={{
              marginTop: 10,
              color: '#25343F',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 76,
              lineHeight: 0.92,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            THE PREMIUM IS ONLY
            <br />
            THE VISIBLE LAYER.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 105,
            top: 285,
            width: 1120,
            height: 690,
            borderRadius: 50,
            background: 'rgba(245,245,245,0.06)',
            border: '3px solid rgba(255,244,73,0.40)',
            overflow: 'hidden',
            opacity: sp(45),
            boxShadow: '0 30px 70px rgba(37,52,63,0.28)',
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: 230 + waterOffset,
              bottom: 0,
              background:
                'linear-gradient(180deg, rgba(37,52,63,0.80), rgba(37,52,63,0.97))',
            }}
          />

          <svg
            width="1120"
            height="690"
            viewBox="0 0 1120 690"
            style={{position: 'absolute', inset: 0}}
          >
            <path
              d={`M0 ${230 + waterOffset}C130 ${210 + waterOffset} 210 ${250 + waterOffset} 355 ${230 + waterOffset}c150-22 244 22 388 0 151-23 230 21 377 0`}
              fill="none"
              stroke="#FFF449"
              strokeWidth="8"
            />

            <path
              d="M560 75 735 232 938 635H176L385 232Z"
              fill="rgba(245,245,245,0.18)"
              stroke="rgba(245,245,245,0.48)"
              strokeWidth="5"
              strokeLinejoin="round"
            />

            <path
              d="M385 232 560 75l175 157Z"
              fill="#FFF449"
              stroke="#25343F"
              strokeWidth="6"
              strokeLinejoin="round"
            />

            <path
              d="M385 232h350l203 403H176Z"
              fill="rgba(255,155,81,0.20)"
              stroke="#FF9B51"
              strokeWidth="5"
              strokeLinejoin="round"
            />
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 410,
              top: 125,
              width: 300,
              height: 82,
              borderRadius: 22,
              background: '#25343F',
              color: '#FFF449',
              border: '3px solid #FFF449',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 40,
              fontWeight: 900,
              opacity: sp(90),
              transform: `scale(${sp(90)})`,
            }}
          >
            PREMIUM
          </div>

          <div
            style={{
              position: 'absolute',
              left: 255,
              right: 255,
              top: 310,
              bottom: 55,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'space-between',
              alignItems: 'center',
            }}
          >
            {submergedCosts.map((cost, index) => {
              const entrance = sp(cost.delay);
              return (
                <div
                  key={cost.label}
                  style={{
                    width: cost.width,
                    height: 65,
                    borderRadius: 20,
                    background:
                      index === 3 ? '#FF9B51' : 'rgba(255,244,73,0.90)',
                    border: '3px solid #25343F',
                    color: '#25343F',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: index === 3 ? 20 : 22,
                    fontWeight: 900,
                    opacity: entrance,
                    transform: `scaleX(${entrance})`,
                  }}
                >
                  {cost.label}
                </div>
              );
            })}
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 104,
            top: 286,
            width: 585,
            height: 690,
            borderRadius: 48,
            background: 'rgba(255,244,73,0.95)',
            border: '5px solid #25343F',
            opacity: sp(235),
            transform: `translateX(${(1 - sp(235)) * 100}px)`,
            boxShadow: '0 30px 70px rgba(37,52,63,0.32)',
          }}
        >
          <div
            style={{
              position: 'absolute',
              top: 35,
              left: 38,
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 900,
              letterSpacing: 2,
            }}
          >
            EMPLOYER COST GROWTH
          </div>

          <svg width="585" height="405" viewBox="0 0 585 405">
            <circle
              cx="292"
              cy="230"
              r="142"
              fill="none"
              stroke="rgba(37,52,63,0.16)"
              strokeWidth="36"
            />
            <circle
              cx="292"
              cy="230"
              r="142"
              fill="none"
              stroke="#FF9B51"
              strokeWidth="21"
              strokeLinecap="round"
              pathLength="1"
              strokeDasharray={`${costGrowth} 1`}
              transform="rotate(-90 292 230)"
            />
            <circle
              cx="292"
              cy="230"
              r="92"
              fill="#25343F"
              stroke="#FFF449"
              strokeWidth="7"
            />
            <text
              x="292"
              y="226"
              textAnchor="middle"
              fill="#FFF449"
              fontFamily="Nexa Bold, sans-serif"
              fontSize="64"
              fontWeight="900"
            >
              6.7%
            </text>
            <text
              x="292"
              y="270"
              textAnchor="middle"
              fill="#F5F5F5"
              fontFamily="JetBrains Mono, monospace"
              fontSize="20"
              fontWeight="800"
            >
              PROJECTED
            </text>
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 44,
              right: 44,
              top: 440,
              height: 115,
              display: 'flex',
              alignItems: 'flex-end',
              gap: 17,
            }}
          >
            {[0.27, 0.39, 0.51, 0.68, costGrowth].map((height, index) => (
              <div
                key={index}
                style={{
                  flex: 1,
                  height: `${height * 100}%`,
                  borderRadius: '12px 12px 3px 3px',
                  background: index === 4 ? '#FF9B51' : '#25343F',
                  transform: `scaleY(${sp(270 + index * 16)})`,
                  transformOrigin: 'bottom',
                }}
              />
            ))}
          </div>

          <div
            style={{
              position: 'absolute',
              left: 42,
              right: 42,
              bottom: 38,
              height: 82,
              borderRadius: 22,
              background: '#25343F',
              color: '#FFF449',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 32,
              fontWeight: 900,
              opacity: finalCallout,
              transform: `scale(${finalCallout})`,
            }}
          >
            COMPARE TOTAL ANNUAL COST
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 42%, rgba(37,52,63,0.37) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene48TotalHealthcareCost;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene49AIScenarioEngine',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene49AIScenarioEngine: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.04, 1.09], {
    easing: Easing.inOut(Easing.sin),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraX = interpolate(frame, [0, durationInFrames], [-20, 18], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const processorPulse = 1 + Math.sin(frame / 11) * 0.035;
  const verifyEntrance = sp(405, {damping: 8, stiffness: 165});

  const particles = Array.from({length: 48}, (_, index) => ({
    x: seededRandom(index + 96) * 1920,
    y: seededRandom(index + 166) * 1080,
    size: 2 + seededRandom(index + 246) * 5,
    speed: 0.3 + seededRandom(index + 336) * 0.8,
  }));

  const inputs = [
    {label: 'PREMIUM', delay: 75},
    {label: 'DEDUCTIBLE', delay: 115},
    {label: 'COINSURANCE', delay: 155},
    {label: 'OUT-OF-POCKET MAX', delay: 195},
  ];

  const scenarios = [
    {label: 'LOW', height: 0.34, delay: 255, color: '#6E858B'},
    {label: 'MEDIUM', height: 0.62, delay: 295, color: '#FF9B51'},
    {label: 'HIGH', height: 0.9, delay: 335, color: '#25343F'},
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 52% 48%, #FFAB64 0%, #D87543 49%, #594A48 77%, #25343F 115%)',
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.38,
          backgroundImage:
            'linear-gradient(rgba(255,244,73,0.09) 1px, transparent 1px), linear-gradient(90deg, rgba(255,244,73,0.09) 1px, transparent 1px)',
          backgroundSize: '64px 64px',
          transform: `translate(${frame * 0.22}px, ${frame * 0.11}px)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -160,
          top: -160,
          width: 510,
          height: 510,
          background: 'rgba(255,244,73,0.045)',
          clipPath:
            'polygon(50% 0%, 61% 38%, 100% 50%, 61% 62%, 50% 100%, 39% 62%, 0% 50%, 39% 38%)',
          transform: `rotate(${frame * 0.08}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -180,
          bottom: -220,
          width: 630,
          height: 630,
          borderRadius: '50%',
          border: '85px solid rgba(37,52,63,0.10)',
          transform: `scale(${1 + Math.sin(frame / 50) * 0.05})`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.sin((frame + index * 15) / 30) * (9 + index % 12),
            top: (particle.y - frame * particle.speed + 1100) % 1100,
            width: particle.size,
            height: particle.size,
            borderRadius: index % 3 ? '50%' : 1,
            background:
              index % 4 === 0
                ? 'rgba(255,244,73,0.54)'
                : 'rgba(37,52,63,0.24)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `translateX(${cameraX}px) scale(${cameraScale})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 112,
            top: 68,
            opacity: sp(5),
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 900,
              letterSpacing: 6,
            }}
          >
            REDACTED SCENARIO ENGINE
          </div>
          <div
            style={{
              marginTop: 10,
              color: '#25343F',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 76,
              lineHeight: 0.92,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            LET AI CALCULATE.
            <br />
            YOU VERIFY.
          </div>
        </div>

        <svg
          width="1920"
          height="1080"
          viewBox="0 0 1920 1080"
          style={{position: 'absolute', inset: 0}}
        >
          {inputs.map((_, index) => {
            const y = 365 + index * 125;
            return (
              <path
                key={index}
                d={`M550 ${y}C710 ${y} 695 ${545 + index * 8} 830 ${545 + index * 8}`}
                fill="none"
                stroke="rgba(255,244,73,0.40)"
                strokeWidth="4"
                strokeDasharray="11 16"
                strokeDashoffset={-frame * 1.7 - index * 20}
              />
            );
          })}

          {scenarios.map((_, index) => {
            const y = 425 + index * 125;
            return (
              <path
                key={index}
                d={`M1090 ${548 + index * 6}C1210 ${548 + index * 6} 1190 ${y} 1325 ${y}`}
                fill="none"
                stroke="rgba(37,52,63,0.44)"
                strokeWidth="5"
                strokeDasharray="12 15"
                strokeDashoffset={frame * 1.9 + index * 18}
              />
            );
          })}
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 92,
            top: 290,
            width: 515,
            height: 680,
            borderRadius: 46,
            background: 'rgba(37,52,63,0.93)',
            border: '3px solid rgba(255,244,73,0.48)',
            padding: '38px',
            boxSizing: 'border-box',
            opacity: sp(40),
            boxShadow: '0 30px 70px rgba(37,52,63,0.28)',
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 23,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            REDACTED INPUTS
          </div>

          <div
            style={{
              marginTop: 37,
              display: 'flex',
              flexDirection: 'column',
              gap: 25,
            }}
          >
            {inputs.map((input, index) => {
              const entrance = sp(input.delay);
              return (
                <div
                  key={input.label}
                  style={{
                    height: 102,
                    borderRadius: 24,
                    border: '2px solid rgba(255,244,73,0.24)',
                    background: 'rgba(245,245,245,0.055)',
                    padding: '17px 20px',
                    boxSizing: 'border-box',
                    opacity: entrance,
                    transform: `translateX(${(1 - entrance) * -65}px)`,
                  }}
                >
                  <div
                    style={{
                      color: '#F5F5F5',
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: input.label.length > 14 ? 17 : 20,
                      fontWeight: 800,
                    }}
                  >
                    {input.label}
                  </div>
                  <div
                    style={{
                      display: 'flex',
                      gap: 8,
                      marginTop: 13,
                    }}
                  >
                    {[70, 42, 83, 34].map((width, bar) => (
                      <div
                        key={bar}
                        style={{
                          width,
                          height: 15,
                          borderRadius: 7,
                          background:
                            bar === 2 ? '#FF9B51' : 'rgba(255,244,73,0.70)',
                        }}
                      />
                    ))}
                  </div>
                </div>
              );
            })}
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 745,
            top: 388,
            width: 430,
            height: 430,
            borderRadius: 65,
            background: '#FFF449',
            border: '7px solid #25343F',
            opacity: sp(205),
            transform: `scale(${sp(205) * processorPulse}) rotate(${Math.sin(frame / 30) * 1.2}deg)`,
            boxShadow: '0 30px 65px rgba(37,52,63,0.34)',
          }}
        >
          <svg width="430" height="430" viewBox="0 0 430 430">
            <rect
              x="105"
              y="105"
              width="220"
              height="220"
              rx="50"
              fill="#25343F"
              stroke="#FF9B51"
              strokeWidth="8"
            />

            {Array.from({length: 5}, (_, index) => (
              <React.Fragment key={index}>
                <line
                  x1={145 + index * 35}
                  y1="55"
                  x2={145 + index * 35}
                  y2="105"
                  stroke="#25343F"
                  strokeWidth="12"
                  strokeLinecap="round"
                />
                <line
                  x1={145 + index * 35}
                  y1="325"
                  x2={145 + index * 35}
                  y2="375"
                  stroke="#25343F"
                  strokeWidth="12"
                  strokeLinecap="round"
                />
                <line
                  x1="55"
                  y1={145 + index * 35}
                  x2="105"
                  y2={145 + index * 35}
                  stroke="#25343F"
                  strokeWidth="12"
                  strokeLinecap="round"
                />
                <line
                  x1="325"
                  y1={145 + index * 35}
                  x2="375"
                  y2={145 + index * 35}
                  stroke="#25343F"
                  strokeWidth="12"
                  strokeLinecap="round"
                />
              </React.Fragment>
            ))}

            <path
              d="M161 228c17-58 91-58 108 0M176 251h78"
              fill="none"
              stroke="#FFF449"
              strokeWidth="13"
              strokeLinecap="round"
            />
            <circle cx="184" cy="193" r="11" fill="#FF9B51" />
            <circle cx="246" cy="193" r="11" fill="#FF9B51" />
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              bottom: 42,
              color: '#25343F',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 35,
              fontWeight: 900,
              textAlign: 'center',
            }}
          >
            SCENARIO MODEL
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 92,
            top: 285,
            width: 535,
            height: 685,
            borderRadius: 46,
            background: 'rgba(255,244,73,0.95)',
            border: '5px solid #25343F',
            padding: '36px',
            boxSizing: 'border-box',
            opacity: sp(245),
            boxShadow: '0 30px 70px rgba(37,52,63,0.30)',
          }}
        >
          <div
            style={{
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 23,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            USAGE SCENARIOS
          </div>

          <div
            style={{
              height: 365,
              display: 'flex',
              alignItems: 'flex-end',
              justifyContent: 'space-around',
              gap: 25,
              marginTop: 45,
              padding: '0 15px',
            }}
          >
            {scenarios.map((scenario, index) => {
              const entrance = sp(scenario.delay);
              return (
                <div
                  key={scenario.label}
                  style={{
                    flex: 1,
                    height: '100%',
                    display: 'flex',
                    flexDirection: 'column',
                    justifyContent: 'flex-end',
                    alignItems: 'center',
                  }}
                >
                  <div
                    style={{
                      width: '100%',
                      height: `${scenario.height * 100}%`,
                      borderRadius: '22px 22px 5px 5px',
                      background: scenario.color,
                      border: '4px solid #25343F',
                      transform: `scaleY(${entrance})`,
                      transformOrigin: 'bottom',
                      position: 'relative',
                      overflow: 'hidden',
                    }}
                  >
                    <div
                      style={{
                        position: 'absolute',
                        left: 0,
                        right: 0,
                        top: `${20 + Math.sin((frame + index * 20) / 17) * 10}%`,
                        height: 5,
                        background: '#FFF449',
                      }}
                    />
                  </div>
                  <div
                    style={{
                      marginTop: 15,
                      color: '#25343F',
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 21,
                      fontWeight: 900,
                    }}
                  >
                    {scenario.label}
                  </div>
                </div>
              );
            })}
          </div>

          <div
            style={{
              position: 'absolute',
              left: 38,
              right: 38,
              bottom: 36,
              height: 115,
              borderRadius: 25,
              background: '#25343F',
              border: '3px solid #FF9B51',
              opacity: verifyEntrance,
              transform: `scale(${verifyEntrance})`,
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-around',
            }}
          >
            {['NETWORK', 'FORMULARY'].map((label, index) => (
              <div
                key={label}
                style={{
                  color: '#FFF449',
                  display: 'flex',
                  alignItems: 'center',
                  gap: 10,
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 17,
                  fontWeight: 900,
                }}
              >
                <svg width="38" height="38" viewBox="0 0 40 40">
                  {index === 0 ? (
                    <>
                      <circle
                        cx="20"
                        cy="20"
                        r="15"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="3"
                      />
                      <path
                        d="M5 20h30M20 5c8 8 8 22 0 30M20 5c-8 8-8 22 0 30"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="2.5"
                      />
                    </>
                  ) : (
                    <>
                      <rect
                        x="9"
                        y="6"
                        width="22"
                        height="28"
                        rx="4"
                        fill="none"
                        stroke="currentColor"
                        strokeWidth="3"
                      />
                      <path
                        d="M14 15h12M14 21h12M14 27h8"
                        stroke="currentColor"
                        strokeWidth="3"
                        strokeLinecap="round"
                      />
                    </>
                  )}
                </svg>
                {label}
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 42%, rgba(37,52,63,0.38) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene49AIScenarioEngine;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene50ScenarioDecision',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene50ScenarioDecision: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    },
  );

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.07, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraX = interpolate(frame, [0, durationInFrames], [20, -20], {
    easing: Easing.inOut(Easing.sin),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const surgeryShock = interpolate(frame, [215, 270, 330], [0, 1, 0.78], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  const folderEntrance = sp(410, {damping: 8, stiffness: 160});

  const impact =
    frame > 230 && frame < 252
      ? Math.sin((frame - 230) * 2.9) * (1 - (frame - 230) / 22) * 10
      : 0;

  const particles = Array.from({length: 46}, (_, index) => ({
    x: seededRandom(index + 107) * 1920,
    y: seededRandom(index + 177) * 1080,
    size: 2 + seededRandom(index + 257) * 5,
    speed: 0.3 + seededRandom(index + 347) * 0.75,
  }));

  const monthlyPoints = Array.from({length: 12}, (_, index) => {
    const x = 32 + index * 41;
    const y = 115 - Math.sin(index * 0.7) * 8;
    return `${x},${y}`;
  }).join(' ');

  const surgeryPoints = Array.from({length: 12}, (_, index) => {
    const x = 32 + index * 41;
    const normal = 115 - Math.sin(index * 0.7) * 8;
    const spike =
      index >= 7
        ? normal - surgeryShock * (index === 8 ? 92 : index === 9 ? 66 : 35)
        : normal;
    return `${x},${spike}`;
  }).join(' ');

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9B51',
        overflow: 'hidden',
        opacity: exitOp,
        fontFamily: 'Inter, Poppins, sans-serif',
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(140deg, #25343F 0%, #3D4951 31%, #C66B40 66%, #FF9B51 100%)',
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.36,
          backgroundImage:
            'linear-gradient(45deg, rgba(255,244,73,0.08) 25%, transparent 25%, transparent 75%, rgba(255,244,73,0.08) 75%), linear-gradient(45deg, rgba(255,244,73,0.08) 25%, transparent 25%, transparent 75%, rgba(255,244,73,0.08) 75%)',
          backgroundSize: '86px 86px',
          backgroundPosition: `0 0, ${43 + frame * 0.1}px ${43 + frame * 0.1}px`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -200,
          top: -190,
          width: 590,
          height: 590,
          borderRadius: '50%',
          border: '85px solid rgba(255,244,73,0.05)',
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -160,
          bottom: -190,
          width: 560,
          height: 560,
          background: 'rgba(37,52,63,0.11)',
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${-25 + frame * 0.04}deg)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left:
              particle.x +
              Math.sin((frame + index * 13) / 31) * (10 + index % 11),
            top: (particle.y - frame * particle.speed + 1100) % 1100,
            width: particle.size,
            height: particle.size,
            borderRadius: index % 3 ? '50%' : 2,
            background:
              index % 4 === 0
                ? 'rgba(255,244,73,0.54)'
                : 'rgba(245,245,245,0.21)',
          }}
        />
      ))}

      <AbsoluteFill
        style={{
          transform: `translateX(${cameraX + impact}px) scale(${cameraScale})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 112,
            top: 68,
            opacity: sp(5),
          }}
        >
          <div
            style={{
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 900,
              letterSpacing: 6,
            }}
          >
            SCENARIO DECISION MATRIX
          </div>
          <div
            style={{
              marginTop: 10,
              color: '#F5F5F5',
              fontFamily: 'Mont Heavy, Nexa Bold, sans-serif',
              fontSize: 76,
              lineHeight: 0.92,
              fontWeight: 900,
              letterSpacing: -3,
            }}
          >
            CHEAPER TODAY CAN
            <br />
            COST MORE TOMORROW.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 100,
            top: 290,
            width: 1080,
            height: 675,
            borderRadius: 48,
            background: 'rgba(255,244,73,0.95)',
            border: '5px solid #25343F',
            boxShadow: '0 30px 70px rgba(37,52,63,0.30)',
            opacity: sp(40),
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 39,
              top: 30,
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 23,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            PLAN COMPARISON
          </div>

          {[
            {
              x: 42,
              title: 'LOW PREMIUM',
              amount: '-$180 / MO',
              accent: '#FF9B51',
              delay: 75,
            },
            {
              x: 550,
              title: 'LOWER EXPOSURE',
              amount: 'HIGHER PREMIUM',
              accent: '#25343F',
              delay: 125,
            },
          ].map((plan, index) => (
            <div
              key={plan.title}
              style={{
                position: 'absolute',
                left: plan.x,
                top: 95,
                width: 485,
                height: 515,
                borderRadius: 36,
                background:
                  index === 0 ? 'rgba(255,155,81,0.20)' : 'rgba(37,52,63,0.09)',
                border: `4px solid ${plan.accent}`,
                opacity: sp(plan.delay),
                transform: `translateY(${(1 - sp(plan.delay)) * 80}px)`,
                overflow: 'hidden',
              }}
            >
              <div
                style={{
                  height: 98,
                  background: plan.accent,
                  color: index === 0 ? '#25343F' : '#FFF449',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Mont Heavy, sans-serif',
                  fontSize: 37,
                  fontWeight: 900,
                }}
              >
                {plan.title}
              </div>

              <div
                style={{
                  marginTop: 27,
                  color: '#25343F',
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: index === 0 ? 32 : 25,
                  fontWeight: 900,
                  textAlign: 'center',
                }}
              >
                {plan.amount}
              </div>

              <svg
                width="485"
                height="210"
                viewBox="0 0 485 210"
                style={{marginTop: 20}}
              >
                <line
                  x1="25"
                  y1="145"
                  x2="460"
                  y2="145"
                  stroke="rgba(37,52,63,0.20)"
                  strokeWidth="4"
                  strokeDasharray="8 10"
                />

                <polyline
                  points={index === 0 ? surgeryPoints : monthlyPoints}
                  fill="none"
                  stroke={plan.accent}
                  strokeWidth="9"
                  strokeLinecap="round"
                  strokeLinejoin="round"
                />

                {index === 0 && surgeryShock > 0.2 && (
                  <>
                    <circle
                      cx="360"
                      cy={115 - surgeryShock * 92}
                      r={14 + Math.sin(frame / 5) * 3}
                      fill="#FFF449"
                      stroke="#25343F"
                      strokeWidth="5"
                    />
                    <path
                      d="M360 8v35M343 25h34"
                      stroke="#25343F"
                      strokeWidth="7"
                      strokeLinecap="round"
                    />
                  </>
                )}
              </svg>

              <div
                style={{
                  position: 'absolute',
                  left: 28,
                  right: 28,
                  bottom: 27,
                  height: 75,
                  borderRadius: 20,
                  background: '#25343F',
                  color: '#FFF449',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'space-around',
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 18,
                  fontWeight: 900,
                }}
              >
                <span>HEALTHY YEAR</span>
                <span>SURGERY YEAR</span>
              </div>
            </div>
          ))}
        </div>

        <div
          style={{
            position: 'absolute',
            right: 102,
            top: 290,
            width: 590,
            height: 382,
            borderRadius: 45,
            background: 'rgba(37,52,63,0.94)',
            border: '3px solid rgba(255,244,73,0.55)',
            opacity: sp(250),
            transform: `translateX(${(1 - sp(250)) * 100}px)`,
            boxShadow: '0 30px 70px rgba(37,52,63,0.33)',
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 36,
              top: 30,
              color: '#FFF449',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 900,
              letterSpacing: 3,
            }}
          >
            DECISION WEIGHTS
          </div>

          <div
            style={{
              position: 'absolute',
              left: 42,
              right: 42,
              top: 95,
              bottom: 37,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'space-between',
            }}
          >
            {[
              ['MONTHLY PREMIUM', 0.42],
              ['DEDUCTIBLE', 0.66],
              ['OUT-OF-POCKET MAX', 0.84],
            ].map(([label, value], index) => {
              const numericValue = value as number;
              const entrance = sp(285 + index * 28);

              return (
                <div key={label as string}>
                  <div
                    style={{
                      color: '#F5F5F5',
                      fontFamily: 'JetBrains Mono, monospace',
                      fontSize: 19,
                      fontWeight: 800,
                      marginBottom: 8,
                    }}
                  >
                    {label}
                  </div>
                  <div
                    style={{
                      height: 27,
                      borderRadius: 20,
                      background: 'rgba(245,245,245,0.10)',
                      overflow: 'hidden',
                    }}
                  >
                    <div
                      style={{
                        height: '100%',
                        width: `${numericValue * 100}%`,
                        borderRadius: 20,
                        background: index === 0 ? '#FF9B51' : '#FFF449',
                        transform: `scaleX(${entrance})`,
                        transformOrigin: 'left',
                      }}
                    />
                  </div>
                </div>
              );
            })}
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 102,
            bottom: 92,
            width: 590,
            height: 355,
            opacity: folderEntrance,
            transform: `translateY(${(1 - folderEntrance) * 100}px) scale(${folderEntrance})`,
          }}
        >
          <svg width="590" height="355" viewBox="0 0 590 355">
            <path
              d="M55 78h180l45 48h255v177H55Z"
              fill="#FFF449"
              stroke="#25343F"
              strokeWidth="8"
              strokeLinejoin="round"
            />
            <path
              d="M55 126h480"
              stroke="#25343F"
              strokeWidth="7"
            />
            <rect
              x="105"
              y="166"
              width="380"
              height="95"
              rx="20"
              fill="#FF9B51"
              stroke="#25343F"
              strokeWidth="6"
            />
            <path
              d="M150 192h205M150 222h285"
              stroke="#25343F"
              strokeWidth="9"
              strokeLinecap="round"
            />
            <circle
              cx="455"
              cy="211"
              r="52"
              fill="#25343F"
              stroke="#FFF449"
              strokeWidth="6"
            />
            <path
              d="m431 212 16 17 34-39"
              fill="none"
              stroke="#FFF449"
              strokeWidth="9"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: 20,
              color: '#25343F',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 900,
              textAlign: 'center',
              letterSpacing: 3,
            }}
          >
            NEXT AUDIT
          </div>

          <div
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              bottom: 12,
              color: '#FFF449',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 43,
              fontWeight: 900,
              textAlign: 'center',
              textShadow: '0 4px 0 #25343F',
            }}
          >
            STUDENT LOANS
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          pointerEvents: 'none',
          background:
            'radial-gradient(circle at center, transparent 42%, rgba(37,52,63,0.38) 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#9E9E9E',
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: interpolate(frame, [0, 45], [0, 0.3], {
            extrapolateLeft: 'clamp',
            extrapolateRight: 'clamp',
          }),
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene50ScenarioDecision;
```

