```tsx
import React from 'react';
Audio: "Le remplacement d'un chauffage vétuste par une pompe à chaleur, ou l'isolation des murs par l'extérieur, coûtent plus cher mais font gagner plusieurs lettres d'un coup."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene41',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene41: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // Title reveals
  const titleY = interpolate(sp(8), [0, 1], [80, 0]);
  const titleOp = interpolate(sp(8), [0, 1], [0, 1]);

  // Card 1 - Pompe à chaleur
  const card1Op = interpolate(sp(35), [0, 1], [0, 1]);
  const card1Y = interpolate(sp(35), [0, 1], [120, 0]);

  // Card 2 - Isolation murs
  const card2Op = interpolate(sp(60), [0, 1], [0, 1]);
  const card2Y = interpolate(sp(60), [0, 1], [120, 0]);

  // Letter jump animation (DPE rising)
  const letterProgress = interpolate(frame, [120, 200], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.25, 1, 0.5, 1),
  });

  // Bottom badge
  const badgeOp = interpolate(sp(220), [0, 1], [0, 1]);
  const badgeScale = sp(220);

  // Pulse for arrow
  const pulse = 1 + Math.sin(frame * 0.15) * 0.05;

  // DPE letters array
  const letters = ['G', 'F', 'E', 'D', 'C', 'B', 'A'];
  const currentLetterIndex = Math.floor(letterProgress * (letters.length - 1));

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(251,245,167,0.45) 0%, transparent 55%), radial-gradient(circle at 75% 80%, rgba(30,32,34,0.12) 0%, transparent 60%)',
      }} />

      {/* Animated shape layers - large drifting geometry */}
      <div style={{
        position: 'absolute',
        top: -200 + Math.sin(frame * 0.012) * 30,
        right: -150 + Math.cos(frame * 0.01) * 25,
        width: 700, height: 700,
        borderRadius: '50%',
        border: '3px solid #1E2022',
        opacity: 0.06,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: -250 + Math.cos(frame * 0.014) * 35,
        left: -180,
        width: 600, height: 600,
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        top: '40%', left: '85%',
        width: 0, height: 0,
        borderLeft: '180px solid transparent',
        borderRight: '180px solid transparent',
        borderBottom: '320px solid #1E2022',
        opacity: 0.05,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Motion texture - grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.08 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line
            key={`v${i}`}
            x1={i * 96 + (frame * 0.3) % 96}
            y1={0}
            x2={i * 96 + (frame * 0.3) % 96}
            y2={1080}
            stroke="#1E2022"
            strokeWidth="1"
          />
        ))}
        {Array.from({ length: 12 }).map((_, i) => (
          <line
            key={`h${i}`}
            x1={0}
            y1={i * 90 + (frame * 0.2) % 90}
            x2={1920}
            y2={i * 90 + (frame * 0.2) % 90}
            stroke="#1E2022"
            strokeWidth="1"
          />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = i + 1;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        const size = 4 + seededRandom(seed * 3) * 8;
        const drift = Math.sin(frame * 0.02 + i) * 20;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x + drift,
              top: y + Math.cos(frame * 0.025 + i) * 15,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#1E2022' : '#FBF5A7',
              opacity: 0.35,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        
        {/* TOP TITLE BAR */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#FBF5A7',
            padding: '14px 36px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 30,
            fontWeight: 600,
            letterSpacing: 4,
            marginBottom: 22,
          }}>
            TRAVAUX LOURDS — IMPACT MAXIMAL
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 110,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            GAGNER PLUSIEURS
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 110,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            LETTRES <span style={{ color: '#FBF5A7', backgroundColor: '#1E2022', padding: '0 20px' }}>D'UN COUP</span>
          </div>
        </div>

        {/* TWO CARDS COMPARISON */}
        <div style={{
          position: 'absolute',
          top: 440,
          left: 100,
          display: 'flex',
          gap: 50,
          width: 'calc(100% - 200px)',
        }}>
          
          {/* CARD 1 - Pompe à chaleur */}
          <div style={{
            flex: 1,
            backgroundColor: '#1E2022',
            padding: 40,
            opacity: card1Op,
            transform: `translateY(${card1Y}px)`,
            border: '4px solid #1E2022',
            position: 'relative',
          }}>
            {/* Icon - Heat pump SVG */}
            <svg width="90" height="90" viewBox="0 0 100 100" style={{ marginBottom: 20 }}>
              <rect x="15" y="25" width="70" height="50" rx="4" fill="none" stroke="#FBF5A7" strokeWidth="4" />
              <circle cx="50" cy="50" r="14" fill="none" stroke="#FBF5A7" strokeWidth="3" />
              <line x1="50" y1="42" x2="50" y2="58" stroke="#FBF5A7" strokeWidth="3" />
              <line x1="42" y1="50" x2="58" y2="50" stroke="#FBF5A7" strokeWidth="3" />
              <path d="M 25 80 Q 30 88 35 80" fill="none" stroke="#F4CE14" strokeWidth="3" />
              <path d="M 45 80 Q 50 88 55 80" fill="none" stroke="#F4CE14" strokeWidth="3" />
              <path d="M 65 80 Q 70 88 75 80" fill="none" stroke="#F4CE14" strokeWidth="3" />
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 56,
              color: '#FBF5A7',
              lineHeight: 1,
              marginBottom: 14,
              letterSpacing: 1,
            }}>
              POMPE À CHALEUR
            </div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 28,
              color: '#F4CE14',
              fontWeight: 500,
              lineHeight: 1.3,
              marginBottom: 18,
            }}>
              Remplace le chauffage vétuste
            </div>
            <div style={{
              display: 'flex',
              alignItems: 'center',
              gap: 12,
              padding: '14px 18px',
              backgroundColor: '#F4CE14',
            }}>
              <div style={{
                fontFamily: 'Space Mono, monospace',
                fontSize: 48,
                fontWeight: 700,
                color: '#1E2022',
              }}>
                +2 à 3
              </div>
              <div style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 22,
                color: '#1E2022',
                fontWeight: 700,
                lineHeight: 1.1,
              }}>
                LETTRES<br/>DPE
              </div>
            </div>
          </div>

          {/* CARD 2 - Isolation murs extérieurs */}
          <div style={{
            flex: 1,
            backgroundColor: '#FBF5A7',
            padding: 40,
            opacity: card2Op,
            transform: `translateY(${card2Y}px)`,
            border: '4px solid #1E2022',
            position: 'relative',
          }}>
            {/* Icon - Wall insulation SVG */}
            <svg width="90" height="90" viewBox="0 0 100 100" style={{ marginBottom: 20 }}>
              <rect x="20" y="20" width="40" height="60" fill="none" stroke="#1E2022" strokeWidth="4" />
              <line x1="30" y1="30" x2="30" y2="70" stroke="#1E2022" strokeWidth="2" />
              <line x1="40" y1="30" x2="40" y2="70" stroke="#1E2022" strokeWidth="2" />
              <line x1="50" y1="30" x2="50" y2="70" stroke="#1E2022" strokeWidth="2" />
              <rect x="60" y="20" width="20" height="60" fill="#1E2022" opacity="0.3" />
              <path d="M 62 30 L 78 30 M 62 40 L 78 40 M 62 50 L 78 50 M 62 60 L 78 60 M 62 70 L 78 70" stroke="#1E2022" strokeWidth="2" />
              <path d="M 82 50 L 92 50 L 88 46 M 92 50 L 88 54" stroke="#1E2022" strokeWidth="3" fill="none" />
            </svg>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 56,
              color: '#1E2022',
              lineHeight: 1,
              marginBottom: 14,
              letterSpacing: 1,
            }}>
              ISOLATION MURS
            </div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 28,
              color: '#1E2022',
              fontWeight: 500,
              lineHeight: 1.3,
              marginBottom: 18,
              opacity: 0.75,
            }}>
              Par l'extérieur (ITE)
            </div>
            <div style={{
              display: 'flex',
              alignItems: 'center',
              gap: 12,
              padding: '14px 18px',
              backgroundColor: '#1E2022',
            }}>
              <div style={{
                fontFamily: 'Space Mono, monospace',
                fontSize: 48,
                fontWeight: 700,
                color: '#F4CE14',
              }}>
                +2 à 4
              </div>
              <div style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 22,
                color: '#FBF5A7',
                fontWeight: 700,
                lineHeight: 1.1,
              }}>
                LETTRES<br/>DPE
              </div>
            </div>
          </div>
        </div>

        {/* BOTTOM - Letter Ladder Animation */}
        <div style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          width: '100%',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 14,
          opacity: badgeOp,
          transform: `scale(${badgeScale})`,
        }}>
          {letters.map((letter, i) => {
            const isActive = i <= currentLetterIndex;
            const isCurrent = i === currentLetterIndex;
            return (
              <div
                key={letter}
                style={{
                  width: 80,
                  height: 80,
                  backgroundColor: isActive ? '#1E2022' : 'transparent',
                  border: '3px solid #1E2022',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'Clash Display, sans-serif',
                  fontSize: 52,
                  fontWeight: 800,
                  color: isActive ? '#FBF5A7' : '#1E2022',
                  transform: isCurrent ? `scale(${pulse})` : 'scale(1)',
                  transition: 'all 0.3s',
                }}
              >
                {letter}
              </div>
            );
          })}
        </div>

        {/* Side label for ladder */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: badgeOp,
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 28,
            color: '#1E2022',
            fontWeight: 600,
            letterSpacing: 3,
            backgroundColor: '#FBF5A7',
            padding: '8px 24px',
            border: '2px solid #1E2022',
          }}>
            ↑ MONTÉE DU DPE APRÈS TRAVAUX
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,32,34,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene41;
```

```tsx
import React from 'react';
Audio: "L'idée : cibler les travaux qui font basculer votre bien d'une catégorie interdite à une catégorie autorisée et attractive, pas viser le A absolu hors de prix."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene42',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene42: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -20], { extrapolateRight: 'clamp' });

  const headerOp = sp(5);
  const headerY = interpolate(sp(5), [0, 1], [-60, 0]);

  // Three zones reveal
  const zone1Op = sp(30);
  const zone2Op = sp(55);
  const zone3Op = sp(80);

  // Arrow animation pointing to sweet spot
  const arrowProgress = interpolate(frame, [110, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const arrowOp = interpolate(sp(110), [0, 1], [0, 1]);

  // Bottom verdict
  const verdictOp = sp(180);
  const verdictScale = sp(180);

  // Pulse for sweet spot
  const sweetPulse = 1 + Math.sin(frame * 0.12) * 0.08;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(251,245,167,0.4) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '20%', left: '10%',
        width: 400, height: 400,
        borderRadius: '50%',
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `translate(${Math.sin(frame * 0.01) * 30}px, ${Math.cos(frame * 0.012) * 25}px)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '15%', right: '8%',
        width: 0, height: 0,
        borderLeft: '200px solid transparent',
        borderRight: '200px solid transparent',
        borderBottom: '350px solid #1E2022',
        opacity: 0.05,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        top: '60%', left: '70%',
        width: 300, height: 300,
        border: '3px solid #1E2022',
        opacity: 0.06,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />

      {/* Motion texture - dots grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.1 }}>
        {Array.from({ length: 18 }).map((_, row) =>
          Array.from({ length: 32 }).map((_, col) => (
            <circle
              key={`${row}-${col}`}
              cx={col * 62 + 20}
              cy={row * 62 + 20}
              r={1.5}
              fill="#1E2022"
            />
          ))
        )}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i + 5;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed * 2) * 1080;
        const size = 3 + seededRandom(seed * 3) * 7;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x + Math.sin(frame * 0.02 + i) * 25,
              top: y + Math.cos(frame * 0.025 + i) * 20,
              width: size,
              height: size,
              backgroundColor: '#1E2022',
              opacity: 0.3,
              borderRadius: i % 2 === 0 ? '50%' : '0',
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        
        {/* HEADER */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: headerOp,
          transform: `translateY(${headerY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#F4CE14',
            padding: '10px 28px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 26,
            fontWeight: 600,
            letterSpacing: 4,
            marginBottom: 16,
          }}>
            STRATÉGIE DE CIBLAGE
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 96,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            VISEZ LE <span style={{ backgroundColor: '#1E2022', color: '#FBF5A7', padding: '0 18px' }}>POINT JUSTE</span>
          </div>
          <div style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 34,
            color: '#1E2022',
            fontWeight: 500,
            marginTop: 14,
            opacity: 0.85,
          }}>
            Pas le A absolu — la catégorie autorisée et attractive
          </div>
        </div>

        {/* THREE ZONES */}
        <div style={{
          position: 'absolute',
          top: 380,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 20,
          alignItems: 'flex-end',
        }}>
          
          {/* ZONE 1 - INTERDITE */}
          <div style={{
            opacity: zone1Op,
            transform: `translateY(${interpolate(zone1Op, [0, 1], [80, 0])}px)`,
          }}>
            <div style={{
              width: 420,
              height: 320,
              backgroundColor: '#1E2022',
              padding: 32,
              border: '4px solid #1E2022',
              position: 'relative',
            }}>
              <div style={{
                fontFamily: 'Bricolage Grotesque, sans-serif',
                fontSize: 22,
                color: '#F4CE14',
                fontWeight: 600,
                letterSpacing: 3,
                marginBottom: 10,
              }}>
                ZONE 1
              </div>
              <div style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 64,
                color: '#FBF5A7',
                lineHeight: 0.95,
                marginBottom: 18,
              }}>
                INTERDITE
              </div>
              <div style={{
                display: 'flex',
                gap: 10,
                marginBottom: 20,
              }}>
                {['F', 'G'].map((l) => (
                  <div key={l} style={{
                    width: 64, height: 64,
                    backgroundColor: '#F4CE14',
                    color: '#1E2022',
                    fontFamily: 'Clash Display, sans-serif',
                    fontSize: 44,
                    fontWeight: 800,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                  }}>{l}</div>
                ))}
              </div>
              <div style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 22,
                color: '#F4CE14',
                fontWeight: 500,
                lineHeight: 1.3,
              }}>
                Bien bloqué.<br/>Location impossible.
              </div>
              {/* X mark */}
              <svg width="50" height="50" viewBox="0 0 50 50" style={{ position: 'absolute', top: 28, right: 28 }}>
                <circle cx="25" cy="25" r="22" fill="none" stroke="#F4CE14" strokeWidth="3" />
                <line x1="15" y1="15" x2="35" y2="35" stroke="#F4CE14" strokeWidth="4" />
                <line x1="35" y1="15" x2="15" y2="35" stroke="#F4CE14" strokeWidth="4" />
              </svg>
            </div>
          </div>

          {/* ZONE 2 - SWEET SPOT */}
          <div style={{
            opacity: zone2Op,
            transform: `translateY(${interpolate(zone2Op, [0, 1], [80, 0])}px) scale(${sweetPulse})`,
            transformOrigin: 'bottom center',
          }}>
            <div style={{
              width: 460,
              height: 380,
              backgroundColor: '#FBF5A7',
              padding: 32,
              border: '6px solid #1E2022',
              position: 'relative',
              boxShadow: '0 20px 0 -4px #1E2022',
            }}>
              <div style={{
                fontFamily: 'Bricolage Grotesque, sans-serif',
                fontSize: 22,
                color: '#1E2022',
                fontWeight: 700,
                letterSpacing: 3,
                marginBottom: 10,
              }}>
                ZONE OPTIMALE
              </div>
              <div style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 64,
                color: '#1E2022',
                lineHeight: 0.95,
                marginBottom: 18,
              }}>
                LE BON ÉCART
              </div>
              <div style={{
                display: 'flex',
                gap: 10,
                marginBottom: 20,
              }}>
                {['E', 'D', 'C'].map((l) => (
                  <div key={l} style={{
                    width: 72, height: 72,
                    backgroundColor: '#1E2022',
                    color: '#FBF5A7',
                    fontFamily: 'Clash Display, sans-serif',
                    fontSize: 48,
                    fontWeight: 800,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                  }}>{l}</div>
                ))}
              </div>
              <div style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 24,
                color: '#1E2022',
                fontWeight: 600,
                lineHeight: 1.25,
              }}>
                Autorisée. Attractive.<br/>Rentable.
              </div>
              {/* Check mark */}
              <svg width="56" height="56" viewBox="0 0 50 50" style={{ position: 'absolute', top: 26, right: 26 }}>
                <circle cx="25" cy="25" r="22" fill="#1E2022" stroke="#1E2022" strokeWidth="3" />
                <path d="M 14 26 L 22 34 L 36 18" fill="none" stroke="#F4CE14" strokeWidth="5" strokeLinecap="round" />
              </svg>
            </div>
          </div>

          {/* ZONE 3 - LUXE */}
          <div style={{
            opacity: zone3Op,
            transform: `translateY(${interpolate(zone3Op, [0, 1], [80, 0])}px)`,
          }}>
            <div style={{
              width: 420,
              height: 320,
              backgroundColor: '#1E2022',
              padding: 32,
              border: '4px solid #1E2022',
              opacity: 0.85,
              position: 'relative',
            }}>
              <div style={{
                fontFamily: 'Bricolage Grotesque, sans-serif',
                fontSize: 22,
                color: '#F4CE14',
                fontWeight: 600,
                letterSpacing: 3,
                marginBottom: 10,
              }}>
                ZONE 3
              </div>
              <div style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 64,
                color: '#FBF5A7',
                lineHeight: 0.95,
                marginBottom: 18,
              }}>
                SURCOÛT
              </div>
              <div style={{
                display: 'flex',
                gap: 10,
                marginBottom: 20,
              }}>
                {['B', 'A'].map((l) => (
                  <div key={l} style={{
                    width: 64, height: 64,
                    backgroundColor: '#F4CE14',
                    color: '#1E2022',
                    fontFamily: 'Clash Display, sans-serif',
                    fontSize: 44,
                    fontWeight: 800,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                  }}>{l}</div>
                ))}
              </div>
              <div style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 22,
                color: '#F4CE14',
                fontWeight: 500,
                lineHeight: 1.3,
              }}>
                Coût excessif.<br/>Gain marginal.
              </div>
              {/* Money down icon */}
              <svg width="50" height="50" viewBox="0 0 50 50" style={{ position: 'absolute', top: 28, right: 28 }}>
                <circle cx="25" cy="25" r="22" fill="none" stroke="#F4CE14" strokeWidth="3" />
                <path d="M 25 14 L 25 36 M 18 28 L 25 36 L 32 28" stroke="#F4CE14" strokeWidth="4" fill="none" strokeLinecap="round" />
              </svg>
            </div>
          </div>
        </div>

        {/* ARROW pointing to sweet spot */}
        <div style={{
          position: 'absolute',
          top: 740,
          left: '50%',
          transform: `translateX(-50%) translateY(${interpolate(arrowProgress, [0, 1], [-30, 0])}px)`,
          opacity: arrowOp,
          textAlign: 'center',
        }}>
          <svg width="80" height="60" viewBox="0 0 80 60">
            <path d="M 40 10 L 40 50 M 20 35 L 40 55 L 60 35" stroke="#1E2022" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* BOTTOM VERDICT */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: verdictOp,
          transform: `scale(${verdictScale})`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#FBF5A7',
            padding: '18px 42px',
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 44,
            fontWeight: 700,
            letterSpacing: 1,
          }}>
            BASCULER, PAS PERFECTIONNER
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(30,32,34,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene42;
```

```tsx
import React from 'react';
Audio: "Si un bien subit une décote de 25 % et qu'une rénovation à 15 % de sa valeur récupère l'essentiel, le calcul est vite fait : l'opération est gagnante."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene43',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene43: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08], { extrapolateRight: 'clamp' });

  // Equation parts
  const part1Op = sp(10);
  const part2Op = sp(45);
  const part3Op = sp(80);
  const equalsOp = sp(120);
  const resultOp = sp(150);
  const resultScale = sp(150);

  // Number count up for decote
  const decoteCount = Math.floor(interpolate(frame, [15, 50], [0, 25], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const renoCount = Math.floor(interpolate(frame, [50, 85], [0, 15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const gainCount = Math.floor(interpolate(frame, [155, 200], [0, 10], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Bottom punchline
  const punchOp = sp(220);

  // Visual bar progress
  const barProgress = interpolate(frame, [180, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.25, 1, 0.5, 1) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient bg */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(251,245,167,0.5) 0%, transparent 65%)',
      }} />

      {/* Big shapes */}
      <div style={{
        position: 'absolute',
        top: '-15%', left: '-10%',
        width: 600, height: 600,
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '-20%', right: '-10%',
        width: 700, height: 700,
        borderRadius: '50%',
        border: '4px solid #1E2022',
        opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />

      {/* Texture - diagonal lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.08 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <line
            key={i}
            x1={-200 + i * 80 + (frame * 0.4) % 80}
            y1={0}
            x2={200 + i * 80 + (frame * 0.4) % 80}
            y2={1080}
            stroke="#1E2022"
            strokeWidth="1"
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = i + 10;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(seed) * 1920 + Math.sin(frame * 0.02 + i) * 20,
              top: seededRandom(seed * 2) * 1080 + Math.cos(frame * 0.025 + i) * 18,
              width: 5,
              height: 5,
              borderRadius: '50%',
              backgroundColor: '#1E2022',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        
        {/* TOP HEADER */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: sp(0),
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#FBF5A7',
            padding: '12px 32px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 28,
            fontWeight: 600,
            letterSpacing: 4,
            marginBottom: 18,
          }}>
            LE CALCUL DE L'INVESTISSEUR
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 100,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            L'OPÉRATION EST <span style={{ backgroundColor: '#1E2022', color: '#FBF5A7', padding: '0 18px' }}>GAGNANTE</span>
          </div>
        </div>

        {/* MAIN EQUATION */}
        <div style={{
          position: 'absolute',
          top: 360,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          alignItems: 'center',
          gap: 30,
        }}>
          
          {/* DÉCOTE */}
          <div style={{
            opacity: part1Op,
            transform: `scale(${part1Op})`,
            backgroundColor: '#1E2022',
            padding: '30px 40px',
            border: '5px solid #1E2022',
            textAlign: 'center',
            minWidth: 300,
          }}>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 22,
              color: '#F4CE14',
              letterSpacing: 3,
              marginBottom: 10,
            }}>
              DÉCOTE SUBIE
            </div>
            <div style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 130,
              fontWeight: 700,
              color: '#FBF5A7',
              lineHeight: 1,
            }}>
              {decoteCount}%
            </div>
          </div>

          {/* MINUS */}
          <div style={{
            opacity: part2Op,
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 120,
            fontWeight: 800,
            color: '#1E2022',
          }}>
            −
          </div>

          {/* RÉNOVATION */}
          <div style={{
            opacity: part2Op,
            transform: `scale(${part2Op})`,
            backgroundColor: '#FBF5A7',
            padding: '30px 40px',
            border: '5px solid #1E2022',
            textAlign: 'center',
            minWidth: 300,
          }}>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 22,
              color: '#1E2022',
              letterSpacing: 3,
              marginBottom: 10,
            }}>
              COÛT RÉNOVATION
            </div>
            <div style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 130,
              fontWeight: 700,
              color: '#1E2022',
              lineHeight: 1,
            }}>
              {renoCount}%
            </div>
          </div>

          {/* EQUALS */}
          <div style={{
            opacity: equalsOp,
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 120,
            fontWeight: 800,
            color: '#1E2022',
          }}>
            =
          </div>

          {/* RESULT */}
          <div style={{
            opacity: resultOp,
            transform: `scale(${resultScale})`,
            backgroundColor: '#1E2022',
            padding: '30px 40px',
            border: '5px solid #1E2022',
            textAlign: 'center',
            minWidth: 280,
            boxShadow: '0 0 40px rgba(30,32,34,0.4)',
          }}>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 22,
              color: '#F4CE14',
              letterSpacing: 3,
              marginBottom: 10,
            }}>
              GAIN NET
            </div>
            <div style={{
              fontFamily: 'Space Mono, monospace',
              fontSize: 130,
              fontWeight: 700,
              color: '#F4CE14',
              lineHeight: 1,
            }}>
              +{gainCount}%
            </div>
          </div>
        </div>

        {/* VISUAL BAR COMPARISON */}
        <div style={{
          position: 'absolute',
          bottom: 220,
          left: '50%',
          transform: 'translateX(-50%)',
          width: 1100,
          opacity: sp(160),
        }}>
          <div style={{
            fontFamily: 'Montserrat, sans-serif',
            fontSize: 26,
            color: '#1E2022',
            fontWeight: 600,
            marginBottom: 14,
            textAlign: 'center',
          }}>
            Valeur du bien après rénovation ciblée
          </div>
          <div style={{
            position: 'relative',
            height: 60,
            backgroundColor: '#1E2022',
            border: '4px solid #1E2022',
            overflow: 'hidden',
          }}>
            <div style={{
              position: 'absolute',
              top: 0, left: 0,
              height: '100%',
              width: `${barProgress * 85}%`,
              backgroundColor: '#FBF5A7',
              transition: 'width 0.3s',
            }} />
            <div style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: 'translate(-50%, -50%)',
              fontFamily: 'Space Mono, monospace',
              fontSize: 26,
              fontWeight: 700,
              color: barProgress > 0.5 ? '#1E2022' : '#FBF5A7',
              zIndex: 2,
            }}>
              VALEUR RÉCUPÉRÉE: 85%
            </div>
          </div>
        </div>

        {/* PUNCH LINE */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: punchOp,
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 52,
            fontWeight: 700,
            color: '#1E2022',
            letterSpacing: -1,
          }}>
            CRÉATION DE VALEUR PURE
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,32,34,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene43;
```

```tsx
import React from 'react';
Audio: "À l'inverse, si la rénovation dépasse la décote et les économies futures, mieux vaut parfois vendre en l'état plutôt que de s'enliser dans un chantier sans fin."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene44',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene44: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRotate = interpolate(frame, [0, durationInFrames], [-0.5, 0.5], { extrapolateRight: 'clamp' });

  const labelOp = sp(5);
  const titleOp = sp(15);
  const titleX = interpolate(sp(15), [0, 1], [-100, 0]);

  // Scale balance - tilts toward "sell"
  const balanceTilt = interpolate(frame, [80, 160], [0, -12], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.25, 1, 0.5, 1) });
  const balanceOp = sp(50);

  // Left side - heavy renovation
  const leftWeight = interpolate(frame, [60, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Right side - vendre
  const rightOp = sp(100);

  // Bottom message
  const messageOp = sp(180);
  const messageScale = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(251,245,167,0.45) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        top: '10%', right: '10%',
        width: 500, height: 500,
        borderRadius: '50%',
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `translate(${Math.sin(frame * 0.01) * 20}px, ${Math.cos(frame * 0.012) * 20}px)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '15%', left: '5%',
        width: 400, height: 400,
        border: '3px solid #1E2022',
        opacity: 0.05,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />

      {/* Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <circle
            key={i}
            cx={seededRandom(i * 7) * 1920}
            cy={seededRandom(i * 9) * 1080}
            r={40 + seededRandom(i * 11) * 60}
            fill="none"
            stroke="#1E2022"
            strokeWidth="2"
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i + 20;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: seededRandom(seed) * 1920 + Math.sin(frame * 0.022 + i) * 25,
              top: seededRandom(seed * 2) * 1080 + Math.cos(frame * 0.018 + i) * 22,
              width: 4,
              height: 4,
              backgroundColor: '#1E2022',
              opacity: 0.35,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRotate}deg)`, transformOrigin: 'center center' }}>
        
        {/* TOP */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: labelOp,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#F4CE14',
            padding: '12px 30px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 26,
            fontWeight: 600,
            letterSpacing: 4,
            marginBottom: 16,
          }}>
            QUAND LE CHANTIER NE VAUT PLUS LA PEINE
          </div>
        </div>

        <div style={{
          position: 'absolute',
          top: 160,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateX(${titleX}px)`,
        }}>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 96,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            MIEUX VAUT <span style={{ backgroundColor: '#1E2022', color: '#FBF5A7', padding: '0 18px' }}>VENDRE</span>
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 96,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
            marginTop: 6,
          }}>
            EN L'ÉTAT
          </div>
        </div>

        {/* BALANCE SCALE VISUAL */}
        <div style={{
          position: 'absolute',
          top: 460,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: balanceOp,
          width: 1200,
          height: 400,
        }}>
          {/* Balance pole */}
          <div style={{
            position: 'absolute',
            top: 200,
            left: '50%',
            width: 8,
            height: 200,
            backgroundColor: '#1E2022',
            transform: 'translateX(-50%)',
          }} />
          {/* Base */}
          <div style={{
            position: 'absolute',
            top: 380,
            left: '50%',
            width: 180,
            height: 20,
            backgroundColor: '#1E2022',
            transform: 'translateX(-50%)',
          }} />
          
          {/* Balance bar */}
          <div style={{
            position: 'absolute',
            top: 190,
            left: '50%',
            width: 900,
            height: 14,
            backgroundColor: '#1E2022',
            transform: `translateX(-50%) rotate(${balanceTilt}deg)`,
            transformOrigin: 'center center',
            transition: 'transform 0.3s',
          }}>
            {/* LEFT PAN - Renovation cost */}
            <div style={{
              position: 'absolute',
              top: -180 + leftWeight * 80,
              left: -30,
              width: 380,
              height: 200,
              backgroundColor: '#1E2022',
              color: '#FBF5A7',
              padding: 24,
              border: '4px solid #1E2022',
              transition: 'top 0.4s',
            }}>
              <div style={{
                fontFamily: 'Bricolage Grotesque, sans-serif',
                fontSize: 20,
                letterSpacing: 3,
                color: '#F4CE14',
                marginBottom: 8,
              }}>
                COÛT TOTAL
              </div>
              <div style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 48,
                lineHeight: 0.95,
                marginBottom: 10,
              }}>
                RÉNOVATION
              </div>
              <div style={{
                fontFamily: 'Space Mono, monospace',
                fontSize: 56,
                fontWeight: 700,
                color: '#F4CE14',
              }}>
                45 000 €
              </div>
            </div>
            {/* Chain left */}
            <div style={{
              position: 'absolute',
              top: -100,
              left: 130,
              width: 2,
              height: 100,
              backgroundColor: '#1E2022',
            }} />

            {/* RIGHT PAN - Decote + savings */}
            <div style={{
              position: 'absolute',
              top: -100 - leftWeight * 60,
              right: -30,
              width: 380,
              height: 180,
              backgroundColor: '#FBF5A7',
              color: '#1E2022',
              padding: 24,
              border: '4px solid #1E2022',
              transition: 'top 0.4s',
              opacity: rightOp,
            }}>
              <div style={{
                fontFamily: 'Bricolage Grotesque, sans-serif',
                fontSize: 20,
                letterSpacing: 3,
                color: '#1E2022',
                marginBottom: 8,
              }}>
                DÉCOTE + ÉCONOMIES
              </div>
              <div style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 48,
                lineHeight: 0.95,
                marginBottom: 10,
              }}>
                BÉNÉFICE
              </div>
              <div style={{
                fontFamily: 'Space Mono, monospace',
                fontSize: 56,
                fontWeight: 700,
                color: '#1E2022',
              }}>
                28 000 €
              </div>
            </div>
            <div style={{
              position: 'absolute',
              top: -100,
              right: 130,
              width: 2,
              height: 100,
              backgroundColor: '#1E2022',
            }} />
          </div>
        </div>

        {/* BOTTOM MESSAGE */}
        <div style={{
          position: 'absolute',
          bottom: 50,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: messageOp,
          transform: `scale(${messageScale})`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#FBF5A7',
            padding: '14px 36px',
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 42,
            fontWeight: 700,
            letterSpacing: 0.5,
          }}>
            NE VOUS ENLISEZ PAS DANS UN CHANTIER SANS FIN
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,32,34,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene44;
```

```tsx
import React from 'react';
Audio: "Tout est question de chiffres, pas d'émotions. Beaucoup s'attachent à leur bien et engloutissent des sommes folles. Décidez avec votre calculatrice, pas avec votre cœur."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene45',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene45: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  // VS layout
  const heartOp = sp(15);
  const heartScale = sp(15);
  const calcOp = sp(60);
  const calcScale = sp(60);
  const vsOp = sp(100);
  const vsPulse = 1 + Math.sin(frame * 0.18) * 0.1;

  // Heart cracking (fade red overlay)
  const heartCrack = interpolate(frame, [120, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Verdict
  const verdictOp = sp(180);
  const verdictY = interpolate(sp(180), [0, 1], [60, 0]);

  // Numbers ticking on calculator
  const calcNum = Math.floor(interpolate(frame, [70, 180], [0, 47823], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(251,245,167,0.4) 0%, transparent 50%, rgba(30,32,34,0.08) 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        top: '-20%', left: '50%',
        width: 800, height: 800,
        borderRadius: '50%',
        border: '3px solid #1E2022',
        opacity: 0.05,
        transform: `translateX(-50%) rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '-15%', left: '-10%',
        width: 0, height: 0,
        borderLeft: '300px solid transparent',
        borderRight: '300px solid transparent',
        borderBottom: '500px solid #1E2022',
        opacity: 0.04,
        transform: `rotate(${15 + frame * 0.08}deg)`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.07 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 68} x2={1920} y2={i * 68} stroke="#1E2022" strokeWidth="1" />
        ))}
        {Array.from({ length: 28 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 68} y1={0} x2={i * 68} y2={1080} stroke="#1E2022" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = i + 30;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920 + Math.sin(frame * 0.02 + i) * 20,
            top: seededRandom(seed * 2) * 1080 + Math.cos(frame * 0.025 + i) * 18,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.32,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        
        {/* TOP TITLE */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: sp(0),
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#FBF5A7',
            padding: '12px 32px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 26,
            fontWeight: 600,
            letterSpacing: 4,
            marginBottom: 16,
          }}>
            LE PIÈGE ÉMOTIONNEL
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 100,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            CHIFFRES <span style={{ color: '#1E2022', fontSize: 70 }}>contre</span> ÉMOTIONS
          </div>
        </div>

        {/* MAIN VS LAYOUT */}
        <div style={{
          position: 'absolute',
          top: 340,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          alignItems: 'center',
          gap: 70,
        }}>
          
          {/* HEART side */}
          <div style={{
            opacity: heartOp,
            transform: `scale(${heartScale})`,
            position: 'relative',
            width: 480,
            height: 420,
            backgroundColor: '#1E2022',
            border: '5px solid #1E2022',
            padding: 32,
            textAlign: 'center',
          }}>
            {/* Heart SVG */}
            <div style={{ position: 'relative', height: 180, display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
              <svg width="180" height="160" viewBox="0 0 100 90">
                <path
                  d="M 50 80 C 50 80 10 55 10 30 C 10 15 22 8 32 8 C 40 8 46 12 50 18 C 54 12 60 8 68 8 C 78 8 90 15 90 30 C 90 55 50 80 50 80 Z"
                  fill="#F4CE14"
                  stroke="#FBF5A7"
                  strokeWidth="3"
                />
                {/* Cracks */}
                <path
                  d="M 50 18 L 45 35 L 52 45 L 46 60"
                  stroke="#1E2022"
                  strokeWidth={2 + heartCrack * 3}
                  fill="none"
                  strokeOpacity={heartCrack}
                />
              </svg>
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 64,
              color: '#FBF5A7',
              lineHeight: 0.95,
              marginTop: 8,
              letterSpacing: 1,
            }}>
              LE CŒUR
            </div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 26,
              color: '#F4CE14',
              fontWeight: 500,
              marginTop: 16,
              lineHeight: 1.3,
            }}>
              Attachement.<br/>Refus de la décote.<br/>Travaux à fonds perdus.
            </div>
          </div>

          {/* VS */}
          <div style={{
            opacity: vsOp,
            transform: `scale(${vsPulse})`,
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 130,
            fontWeight: 800,
            color: '#1E2022',
          }}>
            VS
          </div>

          {/* CALCULATOR side */}
          <div style={{
            opacity: calcOp,
            transform: `scale(${calcScale})`,
            position: 'relative',
            width: 480,
            height: 420,
            backgroundColor: '#FBF5A7',
            border: '5px solid #1E2022',
            padding: 32,
            textAlign: 'center',
          }}>
            {/* Calculator SVG */}
            <div style={{ position: 'relative', height: 180, display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
              <svg width="200" height="180" viewBox="0 0 100 100">
                <rect x="15" y="10" width="70" height="80" rx="4" fill="#1E2022" stroke="#1E2022" strokeWidth="2" />
                <rect x="22" y="18" width="56" height="18" fill="#F4CE14" />
                <text x="74" y="32" textAnchor="end" fontFamily="Space Mono" fontSize="11" fontWeight="700" fill="#1E2022">
                  {calcNum.toLocaleString('fr-FR')}
                </text>
                {Array.from({ length: 4 }).map((_, row) =>
                  Array.from({ length: 4 }).map((_, col) => (
                    <rect
                      key={`${row}-${col}`}
                      x={22 + col * 14}
                      y={42 + row * 11}
                      width={11}
                      height={8}
                      fill="#FBF5A7"
                      stroke="#F4CE14"
                      strokeWidth="0.5"
                    />
                  ))
                )}
              </svg>
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 64,
              color: '#1E2022',
              lineHeight: 0.95,
              marginTop: 8,
              letterSpacing: 1,
            }}>
              LA CALCULATRICE
            </div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 26,
              color: '#1E2022',
              fontWeight: 500,
              marginTop: 16,
              lineHeight: 1.3,
              opacity: 0.85,
            }}>
              ROI. Décote vs coût.<br/>Décision lucide.<br/>Patrimoine préservé.
            </div>
          </div>
        </div>

        {/* VERDICT */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: verdictOp,
          transform: `translateY(${verdictY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#FBF5A7',
            padding: '18px 44px',
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 46,
            fontWeight: 700,
          }}>
            DÉCIDEZ AVEC VOS <span style={{ color: '#F4CE14' }}>CHIFFRES</span>
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,32,34,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene45;
```

```tsx
import React from 'react';
Audio: "Troisième levier crucial : les aides. MaPrimeRénov' reste le dispositif central pour financer une partie de vos travaux de rénovation énergétique."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene46',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene46: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.07], { extrapolateRight: 'clamp' });

  // Numbered badge "3"
  const badgeOp = sp(5);
  const badgeRot = interpolate(sp(5), [0, 1], [-30, 0]);

  // Title
  const titleOp = sp(20);
  const titleY = interpolate(sp(20), [0, 1], [50, 0]);

  // Mega card MaPrimeRenov
  const cardOp = sp(60);
  const cardScale = sp(60);

  // Progress bar fill (state coverage)
  const barFill = interpolate(frame, [120, 200], [0, 0.65], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.bezier(0.25, 1, 0.5, 1) });

  // Percentage count
  const pctNum = Math.floor(interpolate(frame, [120, 200], [0, 65], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Bottom three labels
  const tag1Op = sp(180);
  const tag2Op = sp(200);
  const tag3Op = sp(220);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 50%, rgba(251,245,167,0.5) 0%, transparent 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        top: '5%', right: '-10%',
        width: 600, height: 600,
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '-20%', left: '50%',
        width: 700, height: 700,
        borderRadius: '50%',
        border: '4px solid #1E2022',
        opacity: 0.05,
        transform: `translateX(-50%) scale(${1 + Math.sin(frame * 0.018) * 0.04})`,
      }} />

      {/* Texture - + signs */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.07 }}>
        {Array.from({ length: 60 }).map((_, i) => {
          const x = seededRandom(i) * 1920;
          const y = seededRandom(i * 2) * 1080;
          return (
            <g key={i} transform={`translate(${x}, ${y}) rotate(${frame * 0.2 + i * 30})`}>
              <line x1={-10} y1={0} x2={10} y2={0} stroke="#1E2022" strokeWidth="2" />
              <line x1={0} y1={-10} x2={0} y2={10} stroke="#1E2022" strokeWidth="2" />
            </g>
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = i + 40;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920 + Math.sin(frame * 0.022 + i) * 22,
            top: seededRandom(seed * 2) * 1080 + Math.cos(frame * 0.02 + i) * 20,
            width: 4,
            height: 4,
            backgroundColor: '#1E2022',
            opacity: 0.35,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        
        {/* TOP LEFT - Big "3" badge */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 100,
          opacity: badgeOp,
          transform: `rotate(${badgeRot}deg)`,
        }}>
          <div style={{
            width: 180,
            height: 180,
            backgroundColor: '#1E2022',
            color: '#F4CE14',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 160,
            fontWeight: 800,
            border: '6px solid #1E2022',
          }}>
            3
          </div>
          <div style={{
            position: 'absolute',
            top: 60,
            left: 200,
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 26,
            color: '#1E2022',
            fontWeight: 600,
            letterSpacing: 3,
          }}>
            ÈME LEVIER
          </div>
        </div>

        {/* TITLE */}
        <div style={{
          position: 'absolute',
          top: 90,
          right: 100,
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
          textAlign: 'right',
        }}>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 110,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            LES <span style={{ backgroundColor: '#1E2022', color: '#FBF5A7', padding: '0 16px' }}>AIDES</span>
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 56,
            color: '#1E2022',
            lineHeight: 0.95,
            marginTop: 8,
          }}>
            CHANGENT TOUT
          </div>
        </div>

        {/* MEGA CARD - MaPrimeRénov' */}
        <div style={{
          position: 'absolute',
          top: 360,
          left: '50%',
          transform: `translateX(-50%) scale(${cardScale})`,
          opacity: cardOp,
          width: 1400,
          backgroundColor: '#1E2022',
          padding: '40px 50px',
          border: '6px solid #1E2022',
        }}>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 30,
            marginBottom: 24,
          }}>
            {/* House + euro icon */}
            <svg width="100" height="100" viewBox="0 0 100 100">
              <path d="M 20 50 L 50 22 L 80 50 L 80 82 L 20 82 Z" fill="none" stroke="#F4CE14" strokeWidth="4" strokeLinejoin="round" />
              <circle cx="50" cy="62" r="16" fill="#F4CE14" />
              <text x="50" y="70" textAnchor="middle" fontFamily="Clash Display" fontSize="24" fontWeight="800" fill="#1E2022">€</text>
            </svg>
            <div>
              <div style={{
                fontFamily: 'Bricolage Grotesque, sans-serif',
                fontSize: 24,
                color: '#F4CE14',
                letterSpacing: 4,
                fontWeight: 600,
              }}>
                DISPOSITIF CENTRAL DE L'ÉTAT
              </div>
              <div style={{
                fontFamily: 'Clash Display, sans-serif',
                fontSize: 96,
                fontWeight: 800,
                color: '#FBF5A7',
                lineHeight: 1,
                letterSpacing: -1,
              }}>
                MaPrimeRénov'
              </div>
            </div>
          </div>

          {/* Coverage bar */}
          <div style={{ marginTop: 24 }}>
            <div style={{
              display: 'flex',
              justifyContent: 'space-between',
              marginBottom: 12,
            }}>
              <div style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 26,
                color: '#FBF5A7',
                fontWeight: 600,
              }}>
                Prise en charge de la facture (selon revenus)
              </div>
              <div style={{
                fontFamily: 'Space Mono, monospace',
                fontSize: 48,
                fontWeight: 700,
                color: '#F4CE14',
                lineHeight: 0.9,
              }}>
                jusqu'à {pctNum}%
              </div>
            </div>
            <div style={{
              position: 'relative',
              height: 50,
              backgroundColor: '#F4CE14',
              border: '3px solid #F4CE14',
              overflow: 'hidden',
            }}>
              <div style={{
                position: 'absolute',
                top: 0, left: 0,
                height: '100%',
                width: `${barFill * 100}%`,
                backgroundColor: '#FBF5A7',
                transition: 'width 0.3s',
              }} />
              {/* Tick marks */}
              {[25, 50, 75].map((pct) => (
                <div key={pct} style={{
                  position: 'absolute',
                  top: 0,
                  left: `${pct}%`,
                  height: '100%',
                  width: 2,
                  backgroundColor: '#1E2022',
                  opacity: 0.4,
                }} />
              ))}
            </div>
          </div>
        </div>

        {/* BOTTOM TAGS */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 24,
        }}>
          {[
            { label: 'AMÉLIORE LE ROI', op: tag1Op },
            { label: 'SELON REVENUS', op: tag2Op },
            { label: 'PRO CERTIFIÉS RGE', op: tag3Op },
          ].map((t, i) => (
            <div key={i} style={{
              opacity: t.op,
              transform: `scale(${t.op})`,
              backgroundColor: '#FBF5A7',
              border: '3px solid #1E2022',
              padding: '16px 30px',
              fontFamily: 'Anton, sans-serif',
              fontSize: 38,
              color: '#1E2022',
              letterSpacing: 1,
            }}>
              {t.label}
            </div>
          ))}
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,32,34,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene46;
```

```tsx
import React from 'react';
Audio: "Selon vos revenus, l'État peut prendre en charge une part substantielle de la facture. Vérifiez votre éligibilité et passez par des professionnels certifiés RGE."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene47',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene47: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // Title
  const titleOp = sp(8);
  const titleX = interpolate(sp(8), [0, 1], [80, 0]);

  // 4 income tiers reveal sequentially
  const tier1Op = sp(40);
  const tier2Op = sp(60);
  const tier3Op = sp(80);
  const tier4Op = sp(100);

  // RGE badge
  const rgeOp = sp(170);
  const rgeScale = sp(170);
  const rgePulse = 1 + Math.sin(frame * 0.15) * 0.05;

  // Check animation
  const checkProgress = interpolate(frame, [200, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(251,245,167,0.4) 0%, transparent 50%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        top: '20%', left: '-15%',
        width: 700, height: 700,
        borderRadius: '50%',
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `translate(${Math.sin(frame * 0.012) * 30}px, ${Math.cos(frame * 0.01) * 25}px)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '10%', right: '5%',
        width: 350, height: 350,
        border: '3px solid #1E2022',
        opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Bar texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.06 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <rect
            key={i}
            x={i * 64 + (frame * 0.3) % 64}
            y={0}
            width={2}
            height={1080}
            fill="#1E2022"
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const seed = i + 50;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920 + Math.sin(frame * 0.02 + i) * 22,
            top: seededRandom(seed * 2) * 1080 + Math.cos(frame * 0.024 + i) * 18,
            width: 4,
            height: 4,
            backgroundColor: '#1E2022',
            opacity: 0.32,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        
        {/* TITLE */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateX(${titleX}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#F4CE14',
            padding: '12px 32px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 26,
            fontWeight: 600,
            letterSpacing: 4,
            marginBottom: 16,
          }}>
            L'ÉTAT FINANCE — SELON VOS REVENUS
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 100,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            PRISE EN CHARGE <span style={{ backgroundColor: '#1E2022', color: '#FBF5A7', padding: '0 18px' }}>JUSQU'À 90%</span>
          </div>
        </div>

        {/* 4 INCOME TIERS - HORIZONTAL BARS */}
        <div style={{
          position: 'absolute',
          top: 380,
          left: '50%',
          transform: 'translateX(-50%)',
          width: 1500,
        }}>
          {[
            { label: 'BLEU', sub: 'Très modestes', pct: 90, color: '#1E2022', textColor: '#FBF5A7', op: tier1Op },
            { label: 'JAUNE', sub: 'Modestes', pct: 75, color: '#FBF5A7', textColor: '#1E2022', op: tier2Op },
            { label: 'VIOLET', sub: 'Intermédiaires', pct: 60, color: '#1E2022', textColor: '#FBF5A7', op: tier3Op },
            { label: 'ROSE', sub: 'Supérieurs', pct: 40, color: '#FBF5A7', textColor: '#1E2022', op: tier4Op },
          ].map((tier, i) => (
            <div key={i} style={{
              opacity: tier.op,
              transform: `translateX(${interpolate(tier.op, [0, 1], [-60, 0])}px)`,
              display: 'flex',
              alignItems: 'center',
              gap: 20,
              marginBottom: 18,
            }}>
              {/* Category */}
              <div style={{
                width: 280,
                backgroundColor: tier.color,
                color: tier.textColor,
                padding: '18px 24px',
                border: '4px solid #1E2022',
              }}>
                <div style={{
                  fontFamily: 'Bricolage Grotesque, sans-serif',
                  fontSize: 18,
                  letterSpacing: 3,
                  opacity: 0.7,
                  marginBottom: 4,
                }}>
                  PROFIL
                </div>
                <div style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 40,
                  lineHeight: 0.95,
                }}>
                  {tier.label}
                </div>
                <div style={{
                  fontFamily: 'Montserrat, sans-serif',
                  fontSize: 18,
                  fontWeight: 500,
                  marginTop: 4,
                  opacity: 0.85,
                }}>
                  {tier.sub}
                </div>
              </div>
              
              {/* Bar */}
              <div style={{
                flex: 1,
                position: 'relative',
                height: 80,
                backgroundColor: 'rgba(30,32,34,0.15)',
                border: '3px solid #1E2022',
                overflow: 'hidden',
              }}>
                <div style={{
                  position: 'absolute',
                  top: 0, left: 0,
                  height: '100%',
                  width: `${interpolate(tier.op, [0, 1], [0, tier.pct])}%`,
                  backgroundColor: '#1E2022',
                }} />
                <div style={{
                  position: 'absolute',
                  top: '50%',
                  right: 24,
                  transform: 'translateY(-50%)',
                  fontFamily: 'Space Mono, monospace',
                  fontSize: 44,
                  fontWeight: 700,
                  color: '#1E2022',
                }}>
                  {tier.pct}%
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* RGE CERTIFICATION BADGE */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) scale(${rgePulse})`,
          opacity: rgeOp,
          display: 'flex',
          alignItems: 'center',
          gap: 24,
          backgroundColor: '#1E2022',
          padding: '18px 36px',
          border: '5px solid #1E2022',
        }}>
          {/* Certified seal SVG */}
          <svg width="70" height="70" viewBox="0 0 70 70">
            <circle cx="35" cy="35" r="30" fill="none" stroke="#F4CE14" strokeWidth="4" />
            <circle cx="35" cy="35" r="22" fill="#F4CE14" />
            <path
              d={`M 22 35 L 32 45 L 50 25`}
              fill="none"
              stroke="#1E2022"
              strokeWidth="5"
              strokeLinecap="round"
              strokeDasharray="40"
              strokeDashoffset={40 - checkProgress * 40}
            />
          </svg>
          <div>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 20,
              color: '#F4CE14',
              letterSpacing: 3,
              fontWeight: 600,
            }}>
              CONDITION ESSENTIELLE
            </div>
            <div style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 52,
              fontWeight: 800,
              color: '#FBF5A7',
              lineHeight: 1,
            }}>
              ARTISAN CERTIFIÉ <span style={{ color: '#F4CE14' }}>RGE</span>
            </div>
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,32,34,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene47;
```

```tsx
import React from 'react';
Audio: "N'oubliez pas l'éco-prêt à taux zéro, qui finance les travaux sans intérêt, ni les aides locales que certaines régions ou collectivités proposent en complément."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene48',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene48: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  const titleOp = sp(5);
  const titleY = interpolate(sp(5), [0, 1], [-60, 0]);

  // Two big cards
  const card1Op = sp(30);
  const card1X = interpolate(sp(30), [0, 1], [-120, 0]);
  const card2Op = sp(75);
  const card2X = interpolate(sp(75), [0, 1], [120, 0]);

  // Plus connector
  const plusOp = sp(120);
  const plusRotate = interpolate(sp(120), [0, 1], [-180, 0]);

  // Bottom note
  const noteOp = sp(180);

  // Zero animation pulse
  const zeroPulse = 1 + Math.sin(frame * 0.2) * 0.06;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(251,245,167,0.5) 0%, transparent 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        top: '-10%', right: '-10%',
        width: 500, height: 500,
        borderRadius: '50%',
        border: '4px solid #1E2022',
        opacity: 0.05,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '-15%', left: '-10%',
        width: 600, height: 600,
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `rotate(${15 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        top: '40%', left: '5%',
        width: 0, height: 0,
        borderLeft: '120px solid transparent',
        borderRight: '120px solid transparent',
        borderBottom: '220px solid #1E2022',
        opacity: 0.05,
        transform: `rotate(${frame * 0.18}deg)`,
      }} />

      {/* Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.07 }}>
        {Array.from({ length: 50 }).map((_, i) => {
          const x = seededRandom(i) * 1920;
          const y = seededRandom(i * 3) * 1080;
          return (
            <circle key={i} cx={x} cy={y} r={2 + seededRandom(i * 5) * 3} fill="#1E2022" />
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i + 60;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920 + Math.sin(frame * 0.02 + i) * 22,
            top: seededRandom(seed * 2) * 1080 + Math.cos(frame * 0.025 + i) * 18,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.32,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        
        {/* TITLE */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#F4CE14',
            padding: '12px 32px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 26,
            fontWeight: 600,
            letterSpacing: 4,
            marginBottom: 16,
          }}>
            DEUX LEVIERS À NE PAS OUBLIER
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 102,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            EMPILEZ LES <span style={{ backgroundColor: '#1E2022', color: '#FBF5A7', padding: '0 16px' }}>AIDES</span>
          </div>
        </div>

        {/* TWO CARDS */}
        <div style={{
          position: 'absolute',
          top: 360,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          alignItems: 'center',
          gap: 30,
        }}>
          
          {/* CARD 1 - ECO-PTZ */}
          <div style={{
            opacity: card1Op,
            transform: `translateX(${card1X}px)`,
            width: 600,
            height: 480,
            backgroundColor: '#1E2022',
            border: '5px solid #1E2022',
            padding: 36,
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 22,
              color: '#F4CE14',
              letterSpacing: 3,
              marginBottom: 10,
              fontWeight: 600,
            }}>
              LEVIER #1 — ÉTAT
            </div>
            <div style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 78,
              fontWeight: 800,
              color: '#FBF5A7',
              lineHeight: 0.9,
              marginBottom: 8,
            }}>
              ÉCO-PTZ
            </div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 28,
              color: '#F4CE14',
              fontWeight: 500,
              marginBottom: 24,
            }}>
              Prêt à taux zéro
            </div>

            {/* Big zero visual */}
            <div style={{
              display: 'flex',
              alignItems: 'center',
              gap: 20,
              marginBottom: 22,
            }}>
              <div style={{
                fontFamily: 'Clash Display, sans-serif',
                fontSize: 180,
                fontWeight: 800,
                color: '#F4CE14',
                lineHeight: 0.85,
                transform: `scale(${zeroPulse})`,
                transformOrigin: 'center',
              }}>
                0%
              </div>
              <div style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 36,
                color: '#FBF5A7',
                lineHeight: 1,
              }}>
                D'INTÉRÊTS<br/>À PAYER
              </div>
            </div>

            <div style={{
              padding: '14px 18px',
              backgroundColor: '#F4CE14',
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 22,
              color: '#1E2022',
              fontWeight: 600,
            }}>
              Finance les travaux de rénovation énergétique
            </div>
          </div>

          {/* PLUS connector */}
          <div style={{
            opacity: plusOp,
            transform: `rotate(${plusRotate}deg) scale(${plusOp})`,
            width: 100,
            height: 100,
            backgroundColor: '#FBF5A7',
            border: '5px solid #1E2022',
            borderRadius: '50%',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 80,
            fontWeight: 800,
            color: '#1E2022',
          }}>
            +
          </div>

          {/* CARD 2 - LOCAL AIDS */}
          <div style={{
            opacity: card2Op,
            transform: `translateX(${card2X}px)`,
            width: 600,
            height: 480,
            backgroundColor: '#FBF5A7',
            border: '5px solid #1E2022',
            padding: 36,
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif',
              fontSize: 22,
              color: '#1E2022',
              letterSpacing: 3,
              marginBottom: 10,
              fontWeight: 600,
            }}>
              LEVIER #2 — TERRITOIRE
            </div>
            <div style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 78,
              fontWeight: 800,
              color: '#1E2022',
              lineHeight: 0.9,
              marginBottom: 8,
            }}>
              AIDES LOCALES
            </div>
            <div style={{
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 28,
              color: '#1E2022',
              fontWeight: 500,
              marginBottom: 24,
              opacity: 0.8,
            }}>
              Régions & collectivités
            </div>

            {/* Map France SVG simplified */}
            <div style={{
              display: 'flex',
              alignItems: 'center',
              gap: 24,
              marginBottom: 22,
            }}>
              <svg width="140" height="160" viewBox="0 0 100 110">
                <path
                  d="M 50 8 L 65 12 L 78 22 L 85 38 L 82 55 L 88 70 L 80 85 L 70 95 L 55 100 L 40 98 L 28 90 L 20 75 L 15 60 L 18 45 L 25 30 L 38 18 Z"
                  fill="#1E2022"
                  stroke="#1E2022"
                  strokeWidth="2"
                />
                {/* Region dots */}
                <circle cx="40" cy="35" r="4" fill="#F4CE14" />
                <circle cx="65" cy="45" r="4" fill="#F4CE14" />
                <circle cx="50" cy="65" r="4" fill="#F4CE14" />
                <circle cx="35" cy="75" r="4" fill="#F4CE14" />
                <circle cx="70" cy="78" r="4" fill="#F4CE14" />
              </svg>
              <div>
                <div style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 64,
                  color: '#1E2022',
                  lineHeight: 0.95,
                }}>
                  EN
                </div>
                <div style={{
                  fontFamily: 'Anton, sans-serif',
                  fontSize: 64,
                  color: '#1E2022',
                  lineHeight: 0.95,
                }}>
                  COMPLÉMENT
                </div>
              </div>
            </div>

            <div style={{
              padding: '14px 18px',
              backgroundColor: '#1E2022',
              fontFamily: 'Montserrat, sans-serif',
              fontSize: 22,
              color: '#FBF5A7',
              fontWeight: 600,
            }}>
              Selon votre région, ville ou département
            </div>
          </div>
        </div>

        {/* BOTTOM NOTE */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: noteOp,
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 46,
            fontWeight: 700,
            color: '#1E2022',
            letterSpacing: -1,
          }}>
            CHAQUE EURO D'AIDE = UN ROI <span style={{ backgroundColor: '#1E2022', color: '#FBF5A7', padding: '0 14px' }}>MEILLEUR</span>
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,32,34,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene48;
```

```tsx
import React from 'react';
Audio: "L'erreur classique : commencer les travaux puis chercher les aides. Trop tard. Bâtissez votre plan de financement complet en amont, aides comprises."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene49',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene49: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera shake on title impact
  const shake = frame < 30 ? Math.sin(frame * 1.5) * (1 - frame / 30) * 5 : 0;
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  // Warning title
  const titleOp = sp(5);
  const titleScale = sp(5);

  // Wrong way row
  const wrongOp = sp(35);
  const wrongX = interpolate(sp(35), [0, 1], [-100, 0]);

  // Right way row
  const rightOp = sp(120);
  const rightX = interpolate(sp(120), [0, 1], [100, 0]);

  // Verdict
  const verdictOp = sp(200);
  const verdictScale = sp(200);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(251,245,167,0.3) 50%, transparent 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute',
        top: '15%', right: '-10%',
        width: 600, height: 600,
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `rotate(${frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '-15%', left: '20%',
        width: 500, height: 500,
        borderRadius: '50%',
        border: '4px solid #1E2022',
        opacity: 0.05,
      }} />

      {/* Warning stripes texture top */}
      <svg width="1920" height="60" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.15 }}>
        {Array.from({ length: 60 }).map((_, i) => (
          <polygon
            key={i}
            points={`${i * 36 + (frame * 0.5) % 36},0 ${i * 36 + 18 + (frame * 0.5) % 36},0 ${i * 36 + 36 + (frame * 0.5) % 36},60 ${i * 36 + 18 + (frame * 0.5) % 36},60`}
            fill={i % 2 === 0 ? '#1E2022' : 'transparent'}
          />
        ))}
      </svg>
      <svg width="1920" height="60" style={{ position: 'absolute', bottom: 0, left: 0, opacity: 0.15 }}>
        {Array.from({ length: 60 }).map((_, i) => (
          <polygon
            key={i}
            points={`${i * 36 - (frame * 0.5) % 36},0 ${i * 36 + 18 - (frame * 0.5) % 36},0 ${i * 36 + 36 - (frame * 0.5) % 36},60 ${i * 36 + 18 - (frame * 0.5) % 36},60`}
            fill={i % 2 === 0 ? '#1E2022' : 'transparent'}
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i + 70;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920 + Math.sin(frame * 0.02 + i) * 22,
            top: seededRandom(seed * 2) * 1080 + Math.cos(frame * 0.024 + i) * 18,
            width: 4,
            height: 4,
            backgroundColor: '#1E2022',
            opacity: 0.32,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, transformOrigin: 'center center' }}>
        
        {/* TITLE - ERROR WARNING */}
        <div style={{
          position: 'absolute',
          top: 100,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: titleOp,
          transform: `scale(${titleScale})`,
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 20,
            backgroundColor: '#1E2022',
            color: '#F4CE14',
            padding: '14px 36px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 28,
            fontWeight: 600,
            letterSpacing: 4,
            marginBottom: 18,
          }}>
            <svg width="34" height="34" viewBox="0 0 30 30">
              <path d="M 15 3 L 28 26 L 2 26 Z" fill="none" stroke="#F4CE14" strokeWidth="3" strokeLinejoin="round" />
              <line x1="15" y1="12" x2="15" y2="19" stroke="#F4CE14" strokeWidth="3" />
              <circle cx="15" cy="23" r="1.6" fill="#F4CE14" />
            </svg>
            ERREUR CLASSIQUE
          </div>
          <div style={{
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 100,
            fontWeight: 800,
            color: '#1E2022',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            L'ORDRE <span style={{ backgroundColor: '#1E2022', color: '#FBF5A7', padding: '0 18px' }}>COMPTE</span>
          </div>
        </div>

        {/* WRONG WAY - Timeline */}
        <div style={{
          position: 'absolute',
          top: 380,
          left: 100,
          right: 100,
          opacity: wrongOp,
          transform: `translateX(${wrongX}px)`,
        }}>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            marginBottom: 10,
          }}>
            <div style={{
              backgroundColor: '#1E2022',
              color: '#F4CE14',
              padding: '6px 16px',
              fontFamily: 'Anton, sans-serif',
              fontSize: 26,
              letterSpacing: 2,
            }}>
              ❌ MAUVAISE APPROCHE
            </div>
          </div>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 12,
          }}>
            {[
              { label: 'TRAVAUX', sub: 'lancés', bg: '#1E2022', fg: '#FBF5A7' },
              { label: 'FACTURES', sub: 'reçues', bg: '#1E2022', fg: '#FBF5A7' },
              { label: 'AIDES', sub: 'cherchées', bg: '#1E2022', fg: '#F4CE14' },
              { label: 'TROP TARD', sub: 'refusées', bg: '#F4CE14', fg: '#1E2022', highlight: true },
            ].map((step, i) => (
              <React.Fragment key={i}>
                <div style={{
                  flex: 1,
                  backgroundColor: step.bg,
                  color: step.fg,
                  padding: '20px 16px',
                  border: step.highlight ? '4px solid #1E2022' : '3px solid #1E2022',
                  textAlign: 'center',
                  position: 'relative',
                }}>
                  <div style={{
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 36,
                    lineHeight: 0.95,
                    marginBottom: 4,
                  }}>
                    {step.label}
                  </div>
                  <div style={{
                    fontFamily: 'Montserrat, sans-serif',
                    fontSize: 20,
                    fontWeight: 500,
                    opacity: 0.8,
                  }}>
                    {step.sub}
                  </div>
                </div>
                {i < 3 && (
                  <svg width="40" height="40" viewBox="0 0 40 40">
                    <path d="M 8 20 L 32 20 M 24 12 L 32 20 L 24 28" stroke="#1E2022" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                  </svg>
                )}
              </React.Fragment>
            ))}
          </div>
        </div>

        {/* RIGHT WAY */}
        <div style={{
          position: 'absolute',
          top: 620,
          left: 100,
          right: 100,
          opacity: rightOp,
          transform: `translateX(${rightX}px)`,
        }}>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            marginBottom: 10,
          }}>
            <div style={{
              backgroundColor: '#FBF5A7',
              color: '#1E2022',
              padding: '6px 16px',
              fontFamily: 'Anton, sans-serif',
              fontSize: 26,
              letterSpacing: 2,
              border: '3px solid #1E2022',
            }}>
              ✓ BONNE APPROCHE
            </div>
          </div>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 12,
          }}>
            {[
              { label: 'AIDES', sub: 'identifiées' },
              { label: 'DOSSIER', sub: 'monté' },
              { label: 'PLAN', sub: 'validé' },
              { label: 'TRAVAUX', sub: 'lancés' },
            ].map((step, i) => (
              <React.Fragment key={i}>
                <div style={{
                  flex: 1,
                  backgroundColor: '#FBF5A7',
                  color: '#1E2022',
                  padding: '20px 16px',
                  border: '3px solid #1E2022',
                  textAlign: 'center',
                }}>
                  <div style={{
                    fontFamily: 'Anton, sans-serif',
                    fontSize: 36,
                    lineHeight: 0.95,
                    marginBottom: 4,
                  }}>
                    {step.label}
                  </div>
                  <div style={{
                    fontFamily: 'Montserrat, sans-serif',
                    fontSize: 20,
                    fontWeight: 500,
                    opacity: 0.7,
                  }}>
                    {step.sub}
                  </div>
                </div>
                {i < 3 && (
                  <svg width="40" height="40" viewBox="0 0 40 40">
                    <path d="M 8 20 L 32 20 M 24 12 L 32 20 L 24 28" stroke="#1E2022" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                  </svg>
                )}
              </React.Fragment>
            ))}
          </div>
        </div>

        {/* VERDICT */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: verdictOp,
          transform: `scale(${verdictScale})`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#FBF5A7',
            padding: '16px 40px',
            fontFamily: 'Clash Display, sans-serif',
            fontSize: 44,
            fontWeight: 700,
          }}>
            BÂTIR <span style={{ color: '#F4CE14' }}>EN AMONT</span> — TOUT INCLUS
          </div>
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(30,32,34,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene49;
```

```tsx
import React from 'react';
Audio: "Ce qui se joue avec le DPE est une magnifique leçon de stratégie patrimoniale. Ce qui distingue les patrimoines qui traversent les tempêtes, c'est l'anticipation."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene50',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene50: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow zoom for grandeur
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.12], { extrapolateRight: 'clamp' });

  // Quote intro
  const labelOp = sp(5);
  const titleOp = sp(20);
  const titleY = interpolate(sp(20), [0, 1], [70, 0]);

  // Big word "ANTICIPATION"
  const bigWordOp = sp(80);
  const bigWordScale = sp(80);

  // Wave/storm animation
  const waveProgress = interpolate(frame, [60, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Lighthouse / arrow forward icon
  const iconOp = sp(140);

  // Bottom tags
  const tag1Op = sp(190);
  const tag2Op = sp(210);
  const tag3Op = sp(230);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 60%, rgba(251,245,167,0.5) 0%, transparent 70%)',
      }} />

      {/* Big shapes drifting */}
      <div style={{
        position: 'absolute',
        top: '-20%', left: '-10%',
        width: 800, height: 800,
        borderRadius: '50%',
        backgroundColor: '#1E2022',
        opacity: 0.04,
        transform: `translate(${Math.sin(frame * 0.01) * 30}px, ${Math.cos(frame * 0.008) * 25}px)`,
      }} />
      <div style={{
        position: 'absolute',
        bottom: '-25%', right: '-15%',
        width: 900, height: 900,
        border: '4px solid #1E2022',
        opacity: 0.05,
        transform: `rotate(${frame * 0.06}deg)`,
      }} />

      {/* Animated wave lines bottom (representing storm) */}
      <svg width="1920" height="200" style={{ position: 'absolute', bottom: 100, left: 0, opacity: 0.18 }}>
        {Array.from({ length: 4 }).map((_, w) => (
          <path
            key={w}
            d={`M 0 ${100 + w * 18} ${Array.from({ length: 40 }).map((_, i) => 
              `L ${i * 50} ${100 + w * 18 + Math.sin((i * 0.4) + (frame * 0.08) + (w * 1.5)) * (20 + w * 5)}`
            ).join(' ')}`}
            fill="none"
            stroke="#1E2022"
            strokeWidth={3 - w * 0.4}
          />
        ))}
      </svg>

      {/* Dot constellation texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0, opacity: 0.1 }}>
        {Array.from({ length: 50 }).map((_, i) => {
          const x = seededRandom(i) * 1920;
          const y = seededRandom(i * 3) * 700;
          return (
            <circle key={i} cx={x} cy={y} r={1.5 + seededRandom(i * 5) * 2} fill="#1E2022" />
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = i + 80;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920 + Math.sin(frame * 0.02 + i) * 22,
            top: seededRandom(seed * 2) * 1080 + Math.cos(frame * 0.024 + i) * 20,
            width: 4 + seededRandom(seed * 3) * 3,
            height: 4 + seededRandom(seed * 3) * 3,
            borderRadius: '50%',
            backgroundColor: '#1E2022',
            opacity: 0.32,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        
        {/* TOP LABEL */}
        <div style={{
          position: 'absolute',
          top: 80,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: labelOp,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#1E2022',
            color: '#F4CE14',
            padding: '12px 32px',
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 28,
            fontWeight: 600,
            letterSpacing: 4,
          }}>
            LEÇON DE STRATÉGIE PATRIMONIALE
          </div>
        </div>

        {/* PRINCIPAL TITLE */}
        <div style={{
          position: 'absolute',
          top: 170,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif',
            fontSize: 48,
            fontStyle: 'italic',
            color: '#1E2022',
            fontWeight: 400,
            lineHeight: 1.2,
            maxWidth: 1400,
            margin: '0 auto',
          }}>
            « Ce qui distingue les patrimoines qui traversent les tempêtes... »
          </div>
        </div>

        {/* MEGA WORD */}
        <div style={{
          position: 'absolute',
          top: 350,
          left: 0,
          width: '100%',
          textAlign: 'center',
          opacity: bigWordOp,
          transform: `scale(${bigWordScale})`,
        }}>
          <div style={{
            display: 'inline-block',
            position: 'relative',
          }}>
            <div style={{
              fontFamily: 'Clash Display, sans-serif',
              fontSize: 240,
              fontWeight: 800,
              color: '#1E2022',
              lineHeight: 0.85,
              letterSpacing: -6,
            }}>
              ANTICIPATION
            </div>
            {/* Underline animated */}
            <div style={{
              position: 'absolute',
              bottom: -8,
              left: '50%',
              transform: 'translateX(-50%)',
              width: `${waveProgress * 100}%`,
              height: 14,
              backgroundColor: '#1E2022',
              maxWidth: '95%',
              minWidth: 0,
            }} />
          </div>
        </div>

        {/* Forward arrow icon */}
        <div style={{
          position: 'absolute',
          top: 640,
          left: '50%',
          transform: `translateX(-50%) scale(${iconOp})`,
          opacity: iconOp,
        }}>
          <svg width="140" height="80" viewBox="0 0 140 80">
            <path
              d="M 10 40 L 110 40 M 90 20 L 110 40 L 90 60"
              stroke="#1E2022"
              strokeWidth="8"
              fill="none"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
        </div>

        {/* BOTTOM 3 BENEFITS */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 20,
        }}>
          {[
            { label: 'PRÉVOIR', op: tag1Op },
            { label: 'PILOTER', op: tag2Op },
            { label: 'PROSPÉRER', op: tag3Op },
          ].map((t, i) => (
            <div key={i} style={{
              opacity: t.op,
              transform: `translateY(${interpolate(t.op, [0, 1], [40, 0])}px)`,
              backgroundColor: i === 1 ? '#1E2022' : '#FBF5A7',
              color: i === 1 ? '#F4CE14' : '#1E2022',
              border: '4px solid #1E2022',
              padding: '20px 44px',
              fontFamily: 'Anton, sans-serif',
              fontSize: 52,
              letterSpacing: 2,
              lineHeight: 1,
            }}>
              {t.label}
            </div>
          ))}
        </div>

      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(30,32,34,0.4) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Montserrat, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#1E2022',
        opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene50;
```

