```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene41WomenLongevity',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene41WomenLongevity: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom in slowly
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04], { extrapolateRight: 'clamp' });

  // Title entrance
  const titleY = sp(8) * 1;
  const titleOp = interpolate(frame, [0, 20], [0, 1], { extrapolateRight: 'clamp' });

  // Bar chart reveal
  const barMaleH = interpolate(sp(40, { damping: 14 }), [0, 1], [0, 280]);
  const barFemaleH = interpolate(sp(55, { damping: 14 }), [0, 1], [0, 420]);

  // Age counters
  const maleAge = Math.floor(interpolate(frame, [40, 90], [60, 76], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const femaleAge = Math.floor(interpolate(frame, [55, 110], [60, 87], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Bottom callout
  const calloutOp = interpolate(frame, [160, 185], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });
  const calloutY = interpolate(sp(160, { damping: 14 }), [0, 1], [40, 0]);

  // Probability pulse
  const pulse = 1 + Math.sin(frame * 0.12) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 20%, rgba(254,236,65,0.18) 0%, rgba(252,209,209,0) 55%), radial-gradient(circle at 80% 90%, rgba(0,0,0,0.08) 0%, rgba(252,209,209,0) 60%)',
        }}
      />

      {/* Shape Layer - drifting transparent geometry */}
      {[...Array(7)].map((_, i) => {
        const r = seededRandom(i + 11);
        const r2 = seededRandom(i + 33);
        const size = 220 + r * 320;
        const x = r * 1920;
        const y = r2 * 1080;
        const drift = Math.sin(frame * 0.01 + i) * 25;
        const rot = frame * 0.08 + i * 30;
        return (
          <div
            key={`shape-${i}`}
            style={{
              position: 'absolute',
              left: x - size / 2,
              top: y - size / 2 + drift,
              width: size,
              height: size,
              borderRadius: i % 2 === 0 ? '50%' : '24px',
              border: `2px solid rgba(0,0,0,${0.05 + r * 0.03})`,
              transform: `rotate(${rot}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.04})`,
              backgroundColor: i % 3 === 0 ? 'rgba(254,236,65,0.06)' : 'transparent',
            }}
          />
        );
      })}

      {/* Motion texture - vertical grid lines */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {[...Array(20)].map((_, i) => (
          <div
            key={`grid-${i}`}
            style={{
              position: 'absolute',
              left: (i * 1920) / 20,
              top: 0,
              width: 1,
              height: '100%',
              backgroundColor: '#000000',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {[...Array(28)].map((_, i) => {
        const r = seededRandom(i + 77);
        const r2 = seededRandom(i + 91);
        const baseX = r * 1920;
        const baseY = r2 * 1080;
        const float = Math.sin(frame * 0.03 + i) * 40;
        const size = 4 + r * 6;
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: baseX,
              top: baseY + float,
              width: size,
              height: size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#FEEC41' : '#000000',
              opacity: 0.25 + r * 0.25,
            }}
          />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top eyebrow tag */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 120,
            opacity: titleOp,
            transform: `translateY(${-20 + titleY * 20}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <div style={{ width: 60, height: 6, backgroundColor: '#000000' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            Longevity Reality
          </div>
        </div>

        {/* Main Headline */}
        <div
          style={{
            position: 'absolute',
            top: 140,
            left: 120,
            opacity: titleOp,
            transform: `translateY(${-30 + titleY * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.0,
              letterSpacing: -3,
            }}
          >
            WOMEN LIVE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.0,
              letterSpacing: -3,
              display: 'flex',
              alignItems: 'center',
              gap: 30,
            }}
          >
            <span
              style={{
                backgroundColor: '#FEEC41',
                padding: '4px 28px',
                borderRadius: 12,
                border: '5px solid #000000',
                transform: `scale(${pulse})`,
                display: 'inline-block',
              }}
            >
              LONGER
            </span>
            <span style={{ fontSize: 72, fontWeight: 700 }}>— THAT'S MATH.</span>
          </div>
        </div>

        {/* Comparison Bars */}
        <div
          style={{
            position: 'absolute',
            bottom: 230,
            left: 200,
            display: 'flex',
            alignItems: 'flex-end',
            gap: 120,
          }}
        >
          {/* MALE column */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 102,
                fontWeight: 900,
                color: '#000000',
                marginBottom: 8,
                opacity: interpolate(frame, [40, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
            >
              {maleAge}
            </div>
            <div
              style={{
                width: 220,
                height: barMaleH,
                backgroundColor: '#000000',
                borderRadius: '8px 8px 0 0',
                position: 'relative',
              }}
            >
              {/* MAN icon */}
              <svg
                width="120"
                height="120"
                viewBox="0 0 120 120"
                style={{ position: 'absolute', top: -100, left: 50 }}
              >
                <circle cx="60" cy="28" r="20" fill="#000000" />
                <rect x="40" y="52" width="40" height="52" rx="8" fill="#000000" />
              </svg>
            </div>
            <div
              style={{
                marginTop: 14,
                fontFamily: 'Inter, Poppins',
                fontSize: 38,
                fontWeight: 800,
                color: '#000000',
                letterSpacing: 4,
              }}
            >
              MEN
            </div>
          </div>

          {/* FEMALE column */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 119,
                fontWeight: 900,
                color: '#000000',
                marginBottom: 8,
                opacity: interpolate(frame, [55, 75], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                textShadow: '0 0 30px rgba(254,236,65,0.6)',
              }}
            >
              {femaleAge}
            </div>
            <div
              style={{
                width: 220,
                height: barFemaleH,
                backgroundColor: '#FEEC41',
                border: '5px solid #000000',
                borderRadius: '8px 8px 0 0',
                position: 'relative',
                boxShadow: '0 0 40px rgba(254,236,65,0.5)',
              }}
            >
              {/* WOMAN icon */}
              <svg
                width="140"
                height="140"
                viewBox="0 0 140 140"
                style={{ position: 'absolute', top: -120, left: 40 }}
              >
                <circle cx="70" cy="32" r="22" fill="#000000" />
                <path d="M 40 56 L 100 56 L 115 120 L 25 120 Z" fill="#000000" />
              </svg>

              {/* Up arrow inside bar */}
              <div
                style={{
                  position: 'absolute',
                  top: '50%',
                  left: '50%',
                  transform: 'translate(-50%, -50%)',
                  fontFamily: 'JetBrains Mono',
                  fontWeight: 900,
                  fontSize: 90,
                  color: '#000000',
                  opacity: interpolate(frame, [80, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                }}
              >
                ↑
              </div>
            </div>
            <div
              style={{
                marginTop: 14,
                fontFamily: 'Inter, Poppins',
                fontSize: 38,
                fontWeight: 800,
                color: '#000000',
                letterSpacing: 4,
                backgroundColor: '#FEEC41',
                padding: '4px 18px',
                border: '3px solid #000000',
              }}
            >
              WOMEN
            </div>
          </div>

          {/* Side data block */}
          <div
            style={{
              opacity: interpolate(frame, [100, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              transform: `translateX(${interpolate(sp(100, { damping: 14 }), [0, 1], [40, 0])}px)`,
              borderLeft: '6px solid #000000',
              paddingLeft: 28,
              maxWidth: 460,
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 32,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.7,
                letterSpacing: 3,
              }}
            >
              AVG LIFE EXPECTANCY
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 72,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.05,
                marginTop: 10,
              }}
            >
              HEALTHY 62yr<br />
              WOMAN →
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 55,
                fontWeight: 900,
                color: '#000000',
                marginTop: 10,
                backgroundColor: '#FEEC41',
                padding: '6px 16px',
                display: 'inline-block',
                border: '4px solid #000000',
              }}
            >
              LATE 80s — 90s
            </div>
          </div>
        </div>

        {/* Bottom Callout */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 120,
            right: 120,
            opacity: calloutOp,
            transform: `translateY(${calloutY}px)`,
            backgroundColor: '#000000',
            padding: '24px 40px',
            borderRadius: 14,
            display: 'flex',
            alignItems: 'center',
            gap: 28,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 60,
              fontWeight: 900,
              color: '#FEEC41',
              padding: '0 18px',
              borderRight: '3px solid #FEEC41',
            }}
          >
            !
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 800,
              color: '#FCD1D1',
              lineHeight: 1.15,
            }}
          >
            HIGH PROBABILITY → late <span style={{ color: '#FEEC41' }}>80s</span> or even <span style={{ color: '#FEEC41' }}>90s</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene41WomenLongevity;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene42PayCut30',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene42PayCut30: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -20]);

  // 30% giant number animation
  const numScale = sp(20, { damping: 9, stiffness: 90 });
  const numShake = frame > 40 && frame < 55 ? Math.sin(frame * 2) * 6 : 0;

  // Scissor cut line sweep
  const cutProgress = interpolate(frame, [60, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Timeline bars
  const tlOp = interpolate(frame, [120, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const tlY = interpolate(sp(120, { damping: 14 }), [0, 1], [30, 0]);

  // Years counter
  const yearsCount = Math.floor(interpolate(frame, [150, 200], [0, 30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Diagonal gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(254,236,65,0.15) 0%, rgba(252,209,209,0) 40%, rgba(0,0,0,0.10) 100%)',
        }}
      />

      {/* Shape layer - large triangles + circles */}
      {[...Array(6)].map((_, i) => {
        const r = seededRandom(i + 5);
        const size = 280 + r * 280;
        const x = r * 1920;
        const y = seededRandom(i + 13) * 1080;
        const rot = frame * 0.1 + i * 40;
        return (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: x - size / 2,
              top: y - size / 2,
              width: size,
              height: size,
              border: `3px solid rgba(0,0,0,0.06)`,
              borderRadius: i % 2 === 0 ? 18 : '50%',
              transform: `rotate(${rot}deg)`,
            }}
          />
        );
      })}

      {/* Texture - diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {[...Array(30)].map((_, i) => (
          <div
            key={`dl-${i}`}
            style={{
              position: 'absolute',
              top: -200,
              left: i * 100 - 400,
              width: 2,
              height: 1600,
              backgroundColor: '#000000',
              transform: 'rotate(35deg)',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {[...Array(24)].map((_, i) => {
        const r = seededRandom(i + 41);
        const r2 = seededRandom(i + 67);
        const y = (r2 * 1080 + frame * 0.4) % 1080;
        return (
          <div
            key={`pt-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: y,
              width: 5,
              height: 5,
              borderRadius: '50%',
              backgroundColor: '#FEEC41',
              opacity: 0.5,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [0, 18], [0, 1], { extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '12px 36px',
              backgroundColor: '#000000',
              color: '#FEEC41',
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              letterSpacing: 8,
              borderRadius: 8,
            }}
          >
            VOLUNTARY PAY CUT
          </div>
        </div>

        {/* GIANT 30% with scissor cut */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            transform: `translateX(${numShake}px) scale(${numScale})`,
          }}
        >
          <div style={{ position: 'relative' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 442,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 0.9,
                letterSpacing: -16,
                display: 'flex',
                alignItems: 'flex-start',
              }}
            >
              <span style={{ color: '#000000' }}>−30</span>
              <span style={{ fontSize: 238, marginTop: 50, color: '#000000' }}>%</span>
            </div>

            {/* Scissor cut diagonal line */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: -40,
                width: `${cutProgress * 110}%`,
                height: 12,
                backgroundColor: '#FEEC41',
                border: '3px solid #000000',
                transform: 'translateY(-50%) rotate(-8deg)',
                transformOrigin: 'left center',
                boxShadow: '0 0 20px rgba(254,236,65,0.6)',
              }}
            />
          </div>
        </div>

        {/* Sub-label under number */}
        <div
          style={{
            position: 'absolute',
            top: 660,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [80, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 800,
              color: '#000000',
            }}
          >
            permanent cut on your check
          </div>
        </div>

        {/* Timeline visualization - 20-30 years */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 120,
            right: 120,
            opacity: tlOp,
            transform: `translateY(${tlY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
              letterSpacing: 3,
              marginBottom: 16,
            }}
          >
            APPLIED TO THE LAST →
          </div>

          <div style={{ display: 'flex', alignItems: 'center', gap: 24 }}>
            {/* Big years number */}
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 187,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 0.9,
                backgroundColor: '#FEEC41',
                padding: '0 28px',
                border: '6px solid #000000',
                borderRadius: 14,
              }}
            >
              {yearsCount}
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 76,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.0,
              }}
            >
              YEARS<br />
              <span style={{ fontSize: 42, fontWeight: 700, opacity: 0.75 }}>of your life</span>
            </div>

            {/* Bar segments */}
            <div style={{ flex: 1, display: 'flex', gap: 4, marginLeft: 30 }}>
              {[...Array(30)].map((_, i) => {
                const segOp = interpolate(frame, [150 + i * 1.5, 165 + i * 1.5], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
                return (
                  <div
                    key={`seg-${i}`}
                    style={{
                      flex: 1,
                      height: 70,
                      backgroundColor: '#000000',
                      opacity: segOp,
                      borderRadius: 2,
                    }}
                  />
                );
              })}
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene42PayCut30;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene43Broke80s',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene43Broke80s: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [0, 1]);
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.05]);

  // 80s big text
  const eightyScale = sp(10, { damping: 10 });

  // Cards staggered entrance
  const card1 = sp(60, { damping: 13 });
  const card2 = sp(110, { damping: 13 });
  const card3 = sp(160, { damping: 13 });

  // Spike line for medical bills (animated path)
  const spikeProgress = interpolate(frame, [80, 140], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // pulse for "MOST"
  const mostPulse = 1 + Math.sin(frame * 0.18) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 70% 30%, rgba(254,236,65,0.20) 0%, rgba(252,209,209,0) 50%)',
        }}
      />

      {/* Shape layer - concentric circles */}
      {[...Array(5)].map((_, i) => {
        const size = 600 + i * 260;
        return (
          <div
            key={`c-${i}`}
            style={{
              position: 'absolute',
              left: 1450 - size / 2,
              top: 280 - size / 2 + Math.sin(frame * 0.02 + i) * 8,
              width: size,
              height: size,
              borderRadius: '50%',
              border: `2px solid rgba(0,0,0,0.06)`,
              transform: `scale(${1 + Math.sin(frame * 0.015 + i) * 0.03})`,
            }}
          />
        );
      })}

      {/* Texture - dot grid */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {[...Array(15)].map((_, row) =>
          [...Array(25)].map((_, col) => (
            <div
              key={`d-${row}-${col}`}
              style={{
                position: 'absolute',
                top: row * 75,
                left: col * 80,
                width: 4,
                height: 4,
                borderRadius: '50%',
                backgroundColor: '#000000',
              }}
            />
          ))
        )}
      </AbsoluteFill>

      {/* Particles drifting up */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 23);
        const y = (1100 - (frame * 0.8 + i * 60) % 1200);
        return (
          <div
            key={`pa-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: y,
              width: 6,
              height: 6,
              borderRadius: '50%',
              backgroundColor: '#FEEC41',
              opacity: 0.4,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)`, transformOrigin: 'center' }}>
        {/* Top headline */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 120,
            opacity: interpolate(frame, [0, 18], [0, 1], { extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.75,
              letterSpacing: 2,
            }}
          >
            BROKE IN YOUR
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 357,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.85,
              letterSpacing: -16,
              transform: `scale(${eightyScale})`,
              transformOrigin: 'left',
              display: 'flex',
              alignItems: 'flex-end',
            }}
          >
            <span>80</span>
            <span style={{ fontSize: 187, marginLeft: -10 }}>s</span>
            <span style={{ fontSize: 187, marginLeft: 24, color: '#FEEC41', WebkitTextStroke: '6px #000000' }}>?</span>
          </div>
        </div>

        {/* Right side - WHEN YOU NEED IT MOST */}
        <div
          style={{
            position: 'absolute',
            top: 130,
            right: 100,
            textAlign: 'right',
            opacity: interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
              letterSpacing: 4,
            }}
          >
            EXACTLY WHEN
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 89,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.0,
              marginTop: 8,
            }}
          >
            YOU NEED IT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 153,
              fontWeight: 900,
              color: '#000000',
              backgroundColor: '#FEEC41',
              padding: '0 32px',
              border: '6px solid #000000',
              display: 'inline-block',
              marginTop: 10,
              transform: `scale(${mostPulse})`,
              transformOrigin: 'right',
            }}
          >
            MOST
          </div>
        </div>

        {/* Bottom three info cards */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 120,
            right: 120,
            display: 'flex',
            gap: 28,
          }}
        >
          {/* Card 1 - Medical Bills SPIKE */}
          <div
            style={{
              flex: 1,
              backgroundColor: '#000000',
              borderRadius: 16,
              padding: '28px 30px',
              transform: `scale(${card1}) translateY(${(1 - card1) * 40}px)`,
              opacity: card1,
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 26,
                fontWeight: 700,
                color: '#FEEC41',
                letterSpacing: 3,
                marginBottom: 6,
              }}
            >
              01 — HEALTHCARE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 60,
                fontWeight: 900,
                color: '#FCD1D1',
                lineHeight: 1.0,
              }}
            >
              MEDICAL BILLS
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 76,
                fontWeight: 900,
                color: '#FEEC41',
                lineHeight: 1.0,
                marginTop: 6,
              }}
            >
              SPIKE ↗
            </div>

            {/* Animated spike line */}
            <svg width="100%" height="60" style={{ marginTop: 14 }} viewBox="0 0 400 60" preserveAspectRatio="none">
              <path
                d={`M 0 50 L ${100 * spikeProgress} 45 L ${180 * spikeProgress} 40 L ${260 * spikeProgress} 25 L ${340 * spikeProgress} 10 L ${400 * spikeProgress} 5`}
                stroke="#FEEC41"
                strokeWidth="5"
                fill="none"
                strokeLinecap="round"
              />
            </svg>
          </div>

          {/* Card 2 - In-Home Care */}
          <div
            style={{
              flex: 1,
              backgroundColor: '#FEEC41',
              border: '5px solid #000000',
              borderRadius: 16,
              padding: '28px 30px',
              transform: `scale(${card2}) translateY(${(1 - card2) * 40}px)`,
              opacity: card2,
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 26,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.8,
                letterSpacing: 3,
                marginBottom: 6,
              }}
            >
              02 — CARE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 60,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.0,
              }}
            >
              IN-HOME
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 76,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.0,
                marginTop: 6,
              }}
            >
              CARE COST
            </div>
            {/* House icon SVG */}
            <svg width="80" height="60" viewBox="0 0 80 60" style={{ marginTop: 14 }}>
              <path d="M 10 50 L 10 28 L 40 8 L 70 28 L 70 50 Z" stroke="#000000" strokeWidth="5" fill="none" />
              <rect x="32" y="32" width="16" height="18" fill="#000000" />
            </svg>
          </div>

          {/* Card 3 - Daily Survival */}
          <div
            style={{
              flex: 1,
              backgroundColor: '#FCD1D1',
              border: '5px solid #000000',
              borderRadius: 16,
              padding: '28px 30px',
              transform: `scale(${card3}) translateY(${(1 - card3) * 40}px)`,
              opacity: card3,
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 26,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.7,
                letterSpacing: 3,
                marginBottom: 6,
              }}
            >
              03 — DAILY
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 60,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.0,
              }}
            >
              ZERO ROOM
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 76,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.0,
                marginTop: 6,
              }}
            >
              FOR CUTS
            </div>
            {/* X mark inside circle */}
            <svg width="70" height="70" viewBox="0 0 70 70" style={{ marginTop: 14 }}>
              <circle cx="35" cy="35" r="30" stroke="#000000" strokeWidth="5" fill="none" />
              <line x1="20" y1="20" x2="50" y2="50" stroke="#000000" strokeWidth="6" strokeLinecap="round" />
              <line x1="50" y1="20" x2="20" y2="50" stroke="#000000" strokeWidth="6" strokeLinecap="round" />
            </svg>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene43Broke80s;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene44CannotRunOut',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene44CannotRunOut: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // VS split entrance
  const leftX = interpolate(sp(10, { damping: 14 }), [0, 1], [-300, 0]);
  const rightX = interpolate(sp(20, { damping: 14 }), [0, 1], [300, 0]);

  // Crash line for retirement account
  const crashLine = interpolate(frame, [40, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // SS check infinity flow
  const flowOffset = (frame * 4) % 80;

  // 105 number
  const ageScale = sp(160, { damping: 9 });
  const ageOp = interpolate(frame, [160, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(254,236,65,0.10) 0%, rgba(252,209,209,0) 50%, rgba(0,0,0,0.10) 100%)',
        }}
      />

      {/* Shape Layer */}
      {[...Array(8)].map((_, i) => {
        const r = seededRandom(i + 19);
        const size = 250 + r * 280;
        const x = r * 1920;
        const y = seededRandom(i + 49) * 1080;
        return (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: x - size / 2,
              top: y - size / 2 + Math.sin(frame * 0.02 + i) * 14,
              width: size,
              height: size,
              borderRadius: 16,
              backgroundColor: 'rgba(0,0,0,0.04)',
              transform: `rotate(${frame * 0.08 + i * 30}deg)`,
            }}
          />
        );
      })}

      {/* Texture - vertical bars */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {[...Array(60)].map((_, i) => (
          <div
            key={`vb-${i}`}
            style={{
              position: 'absolute',
              top: 0,
              left: i * 32,
              width: 1,
              height: '100%',
              backgroundColor: '#000000',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {[...Array(22)].map((_, i) => {
        const r = seededRandom(i + 71);
        const drift = Math.sin(frame * 0.04 + i) * 30;
        return (
          <div
            key={`pt-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920 + drift,
              top: seededRandom(i + 91) * 1080,
              width: 5,
              height: 5,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#FEEC41' : '#000000',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* Top headline */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [0, 20], [0, 1], { extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -2,
          }}
        >
          YOUR SS CHECK <span style={{ backgroundColor: '#FEEC41', padding: '0 18px', border: '4px solid #000000' }}>CAN'T RUN OUT</span>
        </div>
      </div>

      {/* Two column split */}
      <div
        style={{
          position: 'absolute',
          top: 230,
          left: 80,
          right: 80,
          display: 'flex',
          gap: 40,
          height: 620,
        }}
      >
        {/* LEFT - Retirement Account that runs out */}
        <div
          style={{
            flex: 1,
            backgroundColor: '#000000',
            borderRadius: 20,
            padding: 36,
            transform: `translateX(${leftX}px)`,
            position: 'relative',
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#FEEC41',
              letterSpacing: 4,
            }}
          >
            RETIREMENT ACCOUNT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 89,
              fontWeight: 900,
              color: '#FCD1D1',
              lineHeight: 1.0,
              marginTop: 8,
            }}
          >
            CAN GO TO<br />
            <span style={{ color: '#FEEC41' }}>ZERO.</span>
          </div>

          {/* Crash chart SVG */}
          <svg width="100%" height="240" viewBox="0 0 500 240" style={{ marginTop: 24 }}>
            {/* grid */}
            <line x1="0" y1="220" x2="500" y2="220" stroke="#FCD1D1" strokeWidth="2" opacity="0.4" />
            <line x1="0" y1="160" x2="500" y2="160" stroke="#FCD1D1" strokeWidth="1" opacity="0.2" />
            <line x1="0" y1="100" x2="500" y2="100" stroke="#FCD1D1" strokeWidth="1" opacity="0.2" />

            {/* Crash line drawing */}
            <path
              d={`M 0 60 L ${80 * Math.min(crashLine * 1.5, 1)} 70 L ${160 * Math.min(crashLine * 1.5, 1)} 90 L ${240 * Math.min(crashLine * 1.5, 1)} 130 L ${320 * Math.min(crashLine * 1.5, 1)} 180 L ${400 * Math.min(crashLine * 1.5, 1)} 215 L ${500 * Math.min(crashLine * 1.5, 1)} 220`}
              stroke="#FEEC41"
              strokeWidth="6"
              fill="none"
              strokeLinecap="round"
              strokeLinejoin="round"
            />

            {/* Zero marker */}
            {crashLine > 0.85 && (
              <g>
                <circle cx="490" cy="220" r="14" fill="#FEEC41" />
                <text x="490" y="200" fontFamily="JetBrains Mono" fontSize="20" fontWeight="900" fill="#FEEC41" textAnchor="middle">
                  ZERO
                </text>
              </g>
            )}
          </svg>

          <div
            style={{
              marginTop: 12,
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 700,
              color: '#FCD1D1',
              opacity: 0.8,
            }}
          >
            Market crash = balance dies
          </div>
        </div>

        {/* RIGHT - SS Check infinite */}
        <div
          style={{
            flex: 1,
            backgroundColor: '#FEEC41',
            border: '6px solid #000000',
            borderRadius: 20,
            padding: 36,
            transform: `translateX(${rightX}px)`,
            position: 'relative',
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              letterSpacing: 4,
              opacity: 0.85,
            }}
          >
            SOCIAL SECURITY CHECK
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 89,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.0,
              marginTop: 8,
            }}
          >
            NEVER<br />
            STOPS.
          </div>

          {/* Infinity flow visual */}
          <div style={{ marginTop: 28, position: 'relative', height: 240 }}>
            {/* Infinity symbol */}
            <svg width="100%" height="180" viewBox="0 0 500 180">
              <path
                d="M 100 90 C 100 30, 200 30, 250 90 C 300 150, 400 150, 400 90 C 400 30, 300 30, 250 90 C 200 150, 100 150, 100 90 Z"
                stroke="#000000"
                strokeWidth="10"
                fill="none"
                strokeDasharray="20 8"
                strokeDashoffset={-flowOffset}
              />
              {/* Dollar signs flowing */}
              <text x="115" y="100" fontFamily="Inter" fontSize="44" fontWeight="900" fill="#000000">$</text>
              <text x="380" y="100" fontFamily="Inter" fontSize="44" fontWeight="900" fill="#000000">$</text>
              <text x="247" y="100" fontFamily="Inter" fontSize="36" fontWeight="900" fill="#000000" textAnchor="middle">$</text>
            </svg>

            <div
              style={{
                position: 'absolute',
                bottom: 0,
                left: 0,
                right: 0,
                fontFamily: 'Inter, Poppins',
                fontSize: 36,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.85,
                textAlign: 'center',
              }}
            >
              Inflation-adjusted • For life
            </div>
          </div>
        </div>
      </div>

      {/* Bottom 105 callout */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: ageOp,
          transform: `scale(${ageScale})`,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 18,
            backgroundColor: '#000000',
            padding: '12px 32px',
            borderRadius: 12,
          }}
        >
          <span
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 800,
              color: '#FCD1D1',
            }}
          >
            EVEN IF YOU LIVE TO
          </span>
          <span
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 89,
              fontWeight: 900,
              color: '#FEEC41',
              lineHeight: 1.0,
            }}
          >
            105
          </span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene44CannotRunOut;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene45LongevityInsurance',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene45LongevityInsurance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Insurance policy card scale-in
  const cardScale = sp(15, { damping: 11, stiffness: 100 });

  // Stamp slam
  const stampScale = sp(140, { damping: 7, stiffness: 200 });
  const stampRot = -15;
  const stampOp = interpolate(frame, [140, 155], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shake = frame > 140 && frame < 165 ? Math.sin(frame * 3) * 6 : 0;

  // Inflation arrow grow
  const inflArrow = interpolate(sp(60, { damping: 12 }), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(254,236,65,0.18) 0%, rgba(252,209,209,0) 60%)',
        }}
      />

      {/* Shape Layer */}
      {[...Array(6)].map((_, i) => {
        const r = seededRandom(i + 31);
        const size = 280 + r * 320;
        return (
          <div
            key={`s-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920 - size / 2,
              top: seededRandom(i + 53) * 1080 - size / 2,
              width: size,
              height: size,
              borderRadius: '50%',
              border: '3px solid rgba(0,0,0,0.06)',
              transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.04})`,
            }}
          />
        );
      })}

      {/* Texture - cross pattern */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[...Array(12)].map((_, row) =>
          [...Array(20)].map((_, col) => (
            <div
              key={`x-${row}-${col}`}
              style={{
                position: 'absolute',
                top: row * 95 + 40,
                left: col * 100 + 50,
                width: 14,
                height: 2,
                backgroundColor: '#000000',
              }}
            >
              <div
                style={{
                  position: 'absolute',
                  top: -6,
                  left: 6,
                  width: 2,
                  height: 14,
                  backgroundColor: '#000000',
                }}
              />
            </div>
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 11);
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (seededRandom(i + 17) * 1080 + Math.sin(frame * 0.03 + i) * 25),
              width: 6,
              height: 6,
              borderRadius: '50%',
              backgroundColor: '#FEEC41',
              opacity: 0.5,
            }}
          />
        );
      })}

      {/* Top title */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [0, 18], [0, 1], { extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 102,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -3,
          }}
        >
          THE ULTIMATE <span style={{ backgroundColor: '#FEEC41', padding: '0 22px', border: '5px solid #000000' }}>LONGEVITY</span>
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 102,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -3,
            lineHeight: 1.0,
            marginTop: 8,
          }}
        >
          INSURANCE POLICY
        </div>
      </div>

      {/* Insurance Policy Card center */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          left: '50%',
          transform: `translateX(-50%) translateX(${shake}px) scale(${cardScale})`,
          width: 1080,
          backgroundColor: '#000000',
          borderRadius: 24,
          padding: '40px 50px',
          border: '6px solid #000000',
        }}
      >
        {/* Card header */}
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '3px dashed #FEEC41', paddingBottom: 18 }}>
          <div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 26,
                fontWeight: 700,
                color: '#FEEC41',
                letterSpacing: 4,
              }}
            >
              POLICY # SSA-001
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 900,
                color: '#FCD1D1',
                marginTop: 4,
              }}
            >
              LIFETIME INCOME GUARANTEE
            </div>
          </div>
          {/* Shield icon */}
          <svg width="90" height="100" viewBox="0 0 90 100">
            <path d="M 45 5 L 80 20 L 80 55 C 80 80 60 92 45 95 C 30 92 10 80 10 55 L 10 20 Z" stroke="#FEEC41" strokeWidth="5" fill="none" />
            <path d="M 28 50 L 42 64 L 65 38" stroke="#FEEC41" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* Policy benefits */}
        <div style={{ marginTop: 26, display: 'flex', gap: 28 }}>
          <div style={{ flex: 1 }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#FEEC41',
                letterSpacing: 3,
                opacity: 0.9,
              }}
            >
              BENEFIT 01
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 900,
                color: '#FCD1D1',
                lineHeight: 1.0,
                marginTop: 4,
              }}
            >
              CHECK KEEPS COMING
            </div>
          </div>
          <div style={{ width: 4, backgroundColor: '#FEEC41', opacity: 0.5 }} />
          <div style={{ flex: 1 }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#FEEC41',
                letterSpacing: 3,
                opacity: 0.9,
              }}
            >
              BENEFIT 02
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 900,
                color: '#FCD1D1',
                lineHeight: 1.0,
                marginTop: 4,
                display: 'flex',
                alignItems: 'center',
                gap: 14,
              }}
            >
              INFLATION ADJUSTED
              <span
                style={{
                  color: '#FEEC41',
                  fontSize: 76,
                  transform: `scale(${inflArrow})`,
                }}
              >
                ↑
              </span>
            </div>
          </div>
        </div>

        {/* Policy footer */}
        <div
          style={{
            marginTop: 30,
            paddingTop: 18,
            borderTop: '3px solid #FEEC41',
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#FCD1D1',
              opacity: 0.7,
            }}
          >
            HOLDER: YOU  •  TERM: LIFE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 900,
              color: '#FEEC41',
            }}
          >
            PREMIUM PAID ✓
          </div>
        </div>
      </div>

      {/* CANCELLED stamp */}
      <div
        style={{
          position: 'absolute',
          top: 480,
          right: 200,
          transform: `rotate(${stampRot}deg) scale(${stampScale})`,
          opacity: stampOp,
          border: '10px solid #000000',
          padding: '14px 40px',
          backgroundColor: 'rgba(252,209,209,0.85)',
          borderRadius: 10,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 119,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: 4,
            lineHeight: 1.0,
          }}
        >
          CANCELLED
        </div>
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 24,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.75,
            textAlign: 'center',
            letterSpacing: 5,
          }}
        >
          BY CLAIMING AT 62
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene45LongevityInsurance;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene46BringItTogether',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene46BringItTogether: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title in
  const titleOp = interpolate(frame, [0, 20], [0, 1], { extrapolateRight: 'clamp' });
  const titleY = interpolate(sp(0, { damping: 14 }), [0, 1], [-30, 0]);

  // Three columns staggered
  const col1 = sp(40, { damping: 13 });
  const col2 = sp(75, { damping: 13 });
  const col3 = sp(115, { damping: 13 });

  // Slash animation for 30%
  const slashW = interpolate(frame, [160, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const baseline2000 = Math.floor(interpolate(frame, [180, 220], [2000, 1400], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(170deg, rgba(254,236,65,0.12) 0%, rgba(252,209,209,0) 50%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Shape Layer - rotating squares */}
      {[...Array(6)].map((_, i) => {
        const r = seededRandom(i + 7);
        const size = 320 + r * 300;
        return (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920 - size / 2,
              top: seededRandom(i + 41) * 1080 - size / 2,
              width: size,
              height: size,
              borderRadius: 24,
              border: '3px solid rgba(0,0,0,0.06)',
              transform: `rotate(${frame * 0.1 + i * 40}deg)`,
            }}
          />
        );
      })}

      {/* Texture - horizontal lines */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {[...Array(20)].map((_, i) => (
          <div
            key={`hl-${i}`}
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: i * 55,
              height: 1,
              backgroundColor: '#000000',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {[...Array(18)].map((_, i) => {
        const r = seededRandom(i + 29);
        return (
          <div
            key={`pa-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (seededRandom(i + 83) * 1080 + Math.cos(frame * 0.03 + i) * 25),
              width: 5,
              height: 5,
              borderRadius: '50%',
              backgroundColor: '#000000',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* Title */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 32,
            fontWeight: 700,
            color: '#000000',
            letterSpacing: 8,
            opacity: 0.7,
          }}
        >
          SUMMARY • CLAIMING AT 62
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 119,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -3,
            marginTop: 4,
          }}
        >
          THE <span style={{ backgroundColor: '#FEEC41', padding: '0 24px', border: '5px solid #000000' }}>MATH</span> RECAP
        </div>
      </div>

      {/* Three cards highlighting the points */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          left: 80,
          right: 80,
          display: 'flex',
          gap: 28,
        }}
      >
        {/* Col 1 - 30% Penalty (FOCUS) */}
        <div
          style={{
            flex: 1.3,
            backgroundColor: '#000000',
            borderRadius: 18,
            padding: '32px 36px',
            transform: `scale(${col1}) translateY(${(1 - col1) * 40}px)`,
            opacity: col1,
            position: 'relative',
            overflow: 'hidden',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#FEEC41',
              letterSpacing: 4,
            }}
          >
            POINT 01 — PENALTY
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 204,
              fontWeight: 900,
              color: '#FEEC41',
              lineHeight: 0.9,
              marginTop: 8,
              display: 'flex',
              alignItems: 'baseline',
            }}
          >
            −30<span style={{ fontSize: 119 }}>%</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 800,
              color: '#FCD1D1',
              lineHeight: 1.1,
              marginTop: 4,
            }}
          >
            PERMANENT<br />
            CUT — FOREVER
          </div>

          {/* Baseline slash visual */}
          <div style={{ marginTop: 22, padding: '14px 16px', backgroundColor: 'rgba(254,236,65,0.12)', borderRadius: 8, border: '2px solid #FEEC41' }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700, color: '#FEEC41', opacity: 0.8 }}>
              BASELINE @ AGE 67
            </div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginTop: 4, position: 'relative' }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 51, fontWeight: 900, color: '#FCD1D1' }}>
                ${baseline2000.toLocaleString()}/mo
              </div>
              <div
                style={{
                  position: 'absolute',
                  top: '50%',
                  left: 0,
                  height: 5,
                  width: `${slashW * 280}px`,
                  backgroundColor: '#FEEC41',
                  transform: 'translateY(-50%) rotate(-6deg)',
                  transformOrigin: 'left center',
                }}
              />
            </div>
          </div>
        </div>

        {/* Col 2 - Slashes baseline */}
        <div
          style={{
            flex: 1,
            backgroundColor: '#FEEC41',
            border: '5px solid #000000',
            borderRadius: 18,
            padding: '32px 30px',
            transform: `scale(${col2}) translateY(${(1 - col2) * 40}px)`,
            opacity: col2,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.8,
              letterSpacing: 4,
            }}
          >
            POINT 02 — IMPACT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 76,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.0,
              marginTop: 8,
            }}
          >
            SLASHES<br />
            YOUR<br />
            BASELINE
          </div>

          {/* Vertical bars decreasing */}
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 6, marginTop: 22, height: 110 }}>
            {[100, 92, 84, 76, 70].map((h, i) => (
              <div
                key={`b-${i}`}
                style={{
                  flex: 1,
                  height: `${h}%`,
                  backgroundColor: '#000000',
                  borderRadius: '4px 4px 0 0',
                  opacity: interpolate(frame, [100 + i * 6, 115 + i * 6], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                }}
              />
            ))}
          </div>
        </div>

        {/* Col 3 - Today payoff illusion */}
        <div
          style={{
            flex: 1,
            backgroundColor: '#FCD1D1',
            border: '5px solid #000000',
            borderRadius: 18,
            padding: '32px 30px',
            transform: `scale(${col3}) translateY(${(1 - col3) * 40}px)`,
            opacity: col3,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
              letterSpacing: 4,
            }}
          >
            POINT 03 — ILLUSION
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 68,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1.0,
              marginTop: 8,
            }}
          >
            "SLIGHTLY<br />
            LARGER<br />
            TODAY"
          </div>
          <div
            style={{
              marginTop: 18,
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 800,
              color: '#000000',
              backgroundColor: '#000000',
              color: '#FEEC41',
              padding: '10px 16px',
              display: 'inline-block',
              borderRadius: 6,
            }}
          >
            = SHORT-TERM TRAP
          </div>

          {/* Trap icon */}
          <svg width="90" height="80" viewBox="0 0 90 80" style={{ marginTop: 16 }}>
            <path d="M 10 70 L 45 10 L 80 70 Z" stroke="#000000" strokeWidth="5" fill="none" />
            <text x="45" y="60" fontFamily="Inter" fontSize="40" fontWeight="900" fill="#000000" textAnchor="middle">!</text>
          </svg>
        </div>
      </div>

      {/* Bottom strip */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [220, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            display: 'inline-block',
            fontFamily: 'Inter, Poppins',
            fontSize: 48,
            fontWeight: 800,
            color: '#000000',
            borderTop: '4px solid #000000',
            borderBottom: '4px solid #000000',
            padding: '10px 32px',
            letterSpacing: 1,
          }}
        >
          Bringing it all together — the verdict is mathematical, not emotional.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene46BringItTogether;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene47BreakevenExceptions',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene47BreakevenExceptions: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 78 left panel
  const leftOp = interpolate(frame, [0, 22], [0, 1], { extrapolateRight: 'clamp' });
  const leftX = interpolate(sp(0, { damping: 14 }), [0, 1], [-50, 0]);

  // age counter to 78
  const ageNum = Math.floor(interpolate(frame, [10, 70], [62, 78], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Exception rows
  const row1 = sp(90, { damping: 13 });
  const row2 = sp(130, { damping: 13 });
  const row3 = sp(170, { damping: 13 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, rgba(254,236,65,0.15) 0%, rgba(252,209,209,0) 50%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Shape Layer */}
      {[...Array(7)].map((_, i) => {
        const r = seededRandom(i + 13);
        const size = 260 + r * 280;
        return (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920 - size / 2,
              top: seededRandom(i + 37) * 1080 - size / 2,
              width: size,
              height: size,
              border: '2px solid rgba(0,0,0,0.05)',
              borderRadius: i % 2 ? '50%' : 18,
              transform: `rotate(${frame * 0.06 + i * 50}deg)`,
            }}
          />
        );
      })}

      {/* Texture - circles grid */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[...Array(10)].map((_, row) =>
          [...Array(16)].map((_, col) => (
            <div
              key={`cg-${row}-${col}`}
              style={{
                position: 'absolute',
                top: row * 110 + 30,
                left: col * 125 + 30,
                width: 8,
                height: 8,
                borderRadius: '50%',
                border: '1px solid #000000',
              }}
            />
          ))
        )}
      </AbsoluteFill>

      {/* Particles */}
      {[...Array(16)].map((_, i) => {
        const r = seededRandom(i + 67);
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (seededRandom(i + 89) * 1080 + Math.sin(frame * 0.04 + i) * 30),
              width: 6,
              height: 6,
              borderRadius: '50%',
              backgroundColor: '#FEEC41',
              opacity: 0.5,
            }}
          />
        );
      })}

      {/* LEFT - 78 Break-even */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: 80,
          width: 750,
          opacity: leftOp,
          transform: `translateX(${leftX}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.7,
            letterSpacing: 5,
          }}
        >
          BREAK-EVEN AGE
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 800,
            color: '#000000',
            lineHeight: 1.0,
            marginTop: 4,
          }}
        >
          waiting finally<br />
          pays off at →
        </div>

        {/* Giant 78 */}
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 510,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.85,
            letterSpacing: -20,
            marginTop: 12,
            position: 'relative',
          }}
        >
          {ageNum}
          {/* Yellow ribbon under */}
          <div
            style={{
              position: 'absolute',
              bottom: 50,
              left: 0,
              width: '70%',
              height: 22,
              backgroundColor: '#FEEC41',
              border: '4px solid #000000',
              zIndex: -1,
            }}
          />
        </div>

        {/* Tag */}
        <div
          style={{
            display: 'inline-block',
            backgroundColor: '#000000',
            color: '#FEEC41',
            padding: '10px 22px',
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 900,
            borderRadius: 8,
            marginTop: 10,
          }}
        >
          YEARS OLD
        </div>
      </div>

      {/* RIGHT - 3 Exceptions */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          right: 80,
          width: 920,
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.7,
            letterSpacing: 5,
            opacity: interpolate(frame, [60, 80], [0, 0.7], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          THE 3 EXCEPTIONS
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.95,
            letterSpacing: -2,
            marginTop: 4,
            opacity: interpolate(frame, [60, 85], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          WHEN <span style={{ backgroundColor: '#FEEC41', padding: '0 18px', border: '5px solid #000000' }}>62</span> ACTUALLY WINS
        </div>

        {/* Row 1 - Health */}
        <div
          style={{
            marginTop: 28,
            backgroundColor: '#000000',
            borderRadius: 14,
            padding: '20px 26px',
            transform: `scale(${row1}) translateX(${(1 - row1) * 30}px)`,
            opacity: row1,
            display: 'flex',
            alignItems: 'center',
            gap: 22,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 60,
              fontWeight: 900,
              color: '#FEEC41',
              minWidth: 80,
              lineHeight: 1.0,
            }}
          >
            01
          </div>
          {/* Heart pulse icon */}
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path d="M 30 50 L 8 28 C 0 18 8 6 18 10 C 22 12 28 16 30 20 C 32 16 38 12 42 10 C 52 6 60 18 52 28 Z" stroke="#FEEC41" strokeWidth="4" fill="none" />
          </svg>
          <div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 900,
                color: '#FCD1D1',
                lineHeight: 1.0,
              }}
            >
              SEVERE HEALTH ISSUES
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#FEEC41',
                opacity: 0.8,
                letterSpacing: 2,
                marginTop: 2,
              }}
            >
              short life expectancy
            </div>
          </div>
        </div>

        {/* Row 2 - Debt */}
        <div
          style={{
            marginTop: 14,
            backgroundColor: '#FEEC41',
            border: '5px solid #000000',
            borderRadius: 14,
            padding: '20px 26px',
            transform: `scale(${row2}) translateX(${(1 - row2) * 30}px)`,
            opacity: row2,
            display: 'flex',
            alignItems: 'center',
            gap: 22,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 60,
              fontWeight: 900,
              color: '#000000',
              minWidth: 80,
              lineHeight: 1.0,
            }}
          >
            02
          </div>
          {/* Down chain icon - credit card */}
          <svg width="64" height="60" viewBox="0 0 64 60">
            <rect x="6" y="14" width="52" height="34" rx="4" stroke="#000000" strokeWidth="4" fill="none" />
            <rect x="6" y="22" width="52" height="6" fill="#000000" />
            <rect x="12" y="36" width="14" height="6" fill="#000000" />
          </svg>
          <div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.0,
              }}
            >
              CRUSHING HIGH-INT DEBT
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.8,
                letterSpacing: 2,
                marginTop: 2,
              }}
            >
              20% credit beats 8% wait credit
            </div>
          </div>
        </div>

        {/* Row 3 - Spousal Bridge */}
        <div
          style={{
            marginTop: 14,
            backgroundColor: '#FCD1D1',
            border: '5px solid #000000',
            borderRadius: 14,
            padding: '20px 26px',
            transform: `scale(${row3}) translateX(${(1 - row3) * 30}px)`,
            opacity: row3,
            display: 'flex',
            alignItems: 'center',
            gap: 22,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 60,
              fontWeight: 900,
              color: '#000000',
              minWidth: 80,
              lineHeight: 1.0,
            }}
          >
            03
          </div>
          {/* Bridge icon */}
          <svg width="70" height="60" viewBox="0 0 70 60">
            <path d="M 5 45 C 20 20, 50 20, 65 45" stroke="#000000" strokeWidth="4" fill="none" />
            <line x1="5" y1="45" x2="5" y2="55" stroke="#000000" strokeWidth="4" />
            <line x1="65" y1="45" x2="65" y2="55" stroke="#000000" strokeWidth="4" />
            <line x1="20" y1="32" x2="20" y2="50" stroke="#000000" strokeWidth="3" />
            <line x1="35" y1="26" x2="35" y2="50" stroke="#000000" strokeWidth="3" />
            <line x1="50" y1="32" x2="50" y2="50" stroke="#000000" strokeWidth="3" />
          </svg>
          <div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 51,
                fontWeight: 900,
                color: '#000000',
                lineHeight: 1.0,
              }}
            >
              SPOUSAL BRIDGE STRATEGY
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.8,
                letterSpacing: 2,
                marginTop: 2,
              }}
            >
              for couples in crisis
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene47BreakevenExceptions;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene48LockedForever',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene48LockedForever: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Form slide in
  const formY = interpolate(sp(15, { damping: 13 }), [0, 1], [50, 0]);
  const formOp = interpolate(frame, [15, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Stamp slam at ~140
  const lockScale = sp(150, { damping: 7, stiffness: 200 });
  const lockOp = interpolate(frame, [150, 165], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shake = frame > 150 && frame < 175 ? Math.sin(frame * 3) * 8 : 0;

  // FOREVER text pulse
  const forPulse = 1 + Math.sin(frame * 0.18) * 0.04;

  // SSA bot speech
  const speechOp = interpolate(frame, [70, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 40%, rgba(254,236,65,0.15) 0%, rgba(252,209,209,0) 55%)',
        }}
      />

      {/* Shape Layer */}
      {[...Array(6)].map((_, i) => {
        const r = seededRandom(i + 23);
        const size = 320 + r * 280;
        return (
          <div
            key={`s-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920 - size / 2,
              top: seededRandom(i + 47) * 1080 - size / 2,
              width: size,
              height: size,
              border: '2px solid rgba(0,0,0,0.06)',
              borderRadius: 24,
              transform: `rotate(${frame * 0.07 + i * 40}deg)`,
            }}
          />
        );
      })}

      {/* Texture - grid */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[...Array(12)].map((_, i) => (
          <div key={`g-${i}`} style={{ position: 'absolute', left: 0, right: 0, top: i * 90, height: 1, backgroundColor: '#000000' }} />
        ))}
        {[...Array(20)].map((_, i) => (
          <div key={`gv-${i}`} style={{ position: 'absolute', top: 0, bottom: 0, left: i * 100, width: 1, backgroundColor: '#000000' }} />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {[...Array(18)].map((_, i) => {
        const r = seededRandom(i + 17);
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: seededRandom(i + 71) * 1080 + Math.sin(frame * 0.04 + i) * 20,
              width: 5,
              height: 5,
              borderRadius: '50%',
              backgroundColor: '#000000',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* Top headline */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [0, 18], [0, 1], { extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 30,
            fontWeight: 700,
            color: '#000000',
            letterSpacing: 8,
            opacity: 0.7,
          }}
        >
          SSA WON'T WARN YOU
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 102,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -3,
            marginTop: 4,
          }}
        >
          ONCE YOU FILE — IT'S
        </div>
      </div>

      {/* SSA Form mockup left side */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 110,
          width: 720,
          backgroundColor: '#FCD1D1',
          border: '6px solid #000000',
          borderRadius: 14,
          padding: '28px 32px',
          opacity: formOp,
          transform: `translateY(${formY}px)`,
          boxShadow: '12px 12px 0 #000000',
        }}
      >
        {/* Form header */}
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', borderBottom: '3px solid #000000', paddingBottom: 12 }}>
          <div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700, color: '#000000', opacity: 0.7, letterSpacing: 3 }}>
              SSA FORM 521
            </div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 42, fontWeight: 900, color: '#000000', marginTop: 2 }}>
              CLAIM RETIREMENT BENEFIT
            </div>
          </div>
          {/* Stamp circle */}
          <svg width="70" height="70" viewBox="0 0 70 70">
            <circle cx="35" cy="35" r="30" stroke="#000000" strokeWidth="4" fill="none" />
            <text x="35" y="42" fontFamily="Inter" fontSize="18" fontWeight="900" fill="#000000" textAnchor="middle">SSA</text>
          </svg>
        </div>

        {/* Form fields */}
        <div style={{ marginTop: 18 }}>
          {['NAME', 'AGE AT CLAIM', 'EFFECTIVE DATE'].map((label, i) => (
            <div key={`f-${i}`} style={{ marginBottom: 14 }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 20, fontWeight: 700, color: '#000000', opacity: 0.7, letterSpacing: 2 }}>
                {label}
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 36,
                  fontWeight: 800,
                  color: '#000000',
                  borderBottom: '3px solid #000000',
                  paddingBottom: 4,
                  marginTop: 2,
                }}
              >
                {i === 0 ? '__________________' : i === 1 ? '62' : 'TODAY'}
              </div>
            </div>
          ))}
        </div>

        {/* Signature line */}
        <div style={{ marginTop: 22, display: 'flex', alignItems: 'center', gap: 16 }}>
          <div
            style={{
              flex: 1,
              borderTop: '3px solid #000000',
              paddingTop: 6,
              fontFamily: 'JetBrains Mono',
              fontSize: 20,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
            }}
          >
            CLAIMANT SIGNATURE
          </div>
          <div
            style={{
              backgroundColor: '#FEEC41',
              border: '4px solid #000000',
              padding: '10px 20px',
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 900,
              color: '#000000',
              borderRadius: 6,
            }}
          >
            FILE →
          </div>
        </div>
      </div>

      {/* SSA bot character + speech bubble */}
      <div
        style={{
          position: 'absolute',
          top: 320,
          right: 130,
          width: 600,
        }}
      >
        {/* Speech bubble */}
        <div
          style={{
            backgroundColor: '#000000',
            borderRadius: 18,
            padding: '24px 28px',
            position: 'relative',
            opacity: speechOp,
            transform: `translateY(${(1 - speechOp) * 20}px)`,
          }}
        >
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700, color: '#FEEC41', letterSpacing: 3, opacity: 0.8 }}>
            SSA SAYS:
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 51,
              fontWeight: 900,
              color: '#FCD1D1',
              lineHeight: 1.1,
              marginTop: 6,
            }}
          >
            "We'll just process whatever you hand us."
          </div>
          {/* tail */}
          <div
            style={{
              position: 'absolute',
              bottom: -18,
              left: 60,
              width: 0,
              height: 0,
              borderLeft: '20px solid transparent',
              borderRight: '20px solid transparent',
              borderTop: '20px solid #000000',
            }}
          />
        </div>

        {/* Bot/building icon */}
        <div style={{ marginTop: 40, display: 'flex', alignItems: 'center', gap: 24 }}>
          <svg width="160" height="160" viewBox="0 0 160 160">
            {/* government building */}
            <rect x="20" y="60" width="120" height="80" stroke="#000000" strokeWidth="5" fill="#FEEC41" />
            <polygon points="10,60 80,20 150,60" stroke="#000000" strokeWidth="5" fill="#FEEC41" />
            <rect x="35" y="80" width="14" height="60" fill="#000000" />
            <rect x="73" y="80" width="14" height="60" fill="#000000" />
            <rect x="111" y="80" width="14" height="60" fill="#000000" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 51, fontWeight: 900, color: '#000000', lineHeight: 1.0 }}>
              SOCIAL SECURITY
            </div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 51, fontWeight: 900, color: '#000000', lineHeight: 1.0 }}>
              ADMINISTRATION
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700, color: '#000000', opacity: 0.7, letterSpacing: 3, marginTop: 4 }}>
              NO WARNING. NO ADVICE.
            </div>
          </div>
        </div>
      </div>

      {/* LOCKED FOREVER stamp - bottom center */}
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: lockOp,
          transform: `scale(${lockScale}) translateX(${shake}px)`,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 24,
            backgroundColor: '#000000',
            padding: '14px 38px',
            border: '6px solid #FEEC41',
            borderRadius: 14,
            transform: `scale(${forPulse})`,
          }}
        >
          {/* Lock icon */}
          <svg width="70" height="80" viewBox="0 0 70 80">
            <rect x="10" y="35" width="50" height="40" rx="6" stroke="#FEEC41" strokeWidth="5" fill="none" />
            <path d="M 20 35 L 20 20 C 20 8 50 8 50 20 L 50 35" stroke="#FEEC41" strokeWidth="5" fill="none" />
            <circle cx="35" cy="55" r="5" fill="#FEEC41" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 119,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: 4,
              lineHeight: 1.0,
            }}
          >
            LOCKED FOREVER
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 50%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene48LockedForever;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene49KnowledgeBridge',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene49KnowledgeBridge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // "CANNOT UNDO" top entrance
  const topOp = interpolate(frame, [0, 18], [0, 1], { extrapolateRight: 'clamp' });
  const topY = interpolate(sp(0, { damping: 14 }), [0, 1], [-30, 0]);

  // Bridge animation - connecting You ←→ Retirement
  const bridgeWidth = interpolate(frame, [40, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // YOU pillar
  const youScale = sp(20, { damping: 12 });
  // RETIREMENT pillar
  const retScale = sp(35, { damping: 12 });

  // Subscribe CTA
  const ctaScale = sp(180, { damping: 9, stiffness: 130 });
  const ctaPulse = 1 + Math.sin(frame * 0.18) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(254,236,65,0.12) 0%, rgba(252,209,209,0) 50%, rgba(0,0,0,0.08) 100%)',
        }}
      />

      {/* Shape Layer */}
      {[...Array(6)].map((_, i) => {
        const r = seededRandom(i + 41);
        const size = 280 + r * 320;
        return (
          <div
            key={`s-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920 - size / 2,
              top: seededRandom(i + 63) * 1080 - size / 2,
              width: size,
              height: size,
              borderRadius: '50%',
              border: '2px solid rgba(0,0,0,0.06)',
              transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
            }}
          />
        );
      })}

      {/* Texture diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        {[...Array(25)].map((_, i) => (
          <div
            key={`d-${i}`}
            style={{
              position: 'absolute',
              top: -200,
              left: i * 110 - 300,
              width: 2,
              height: 1600,
              backgroundColor: '#000000',
              transform: 'rotate(28deg)',
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 79);
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (seededRandom(i + 93) * 1080 + Math.sin(frame * 0.04 + i) * 25),
              width: 6,
              height: 6,
              borderRadius: '50%',
              backgroundColor: '#FEEC41',
              opacity: 0.45,
            }}
          />
        );
      })}

      {/* Top "YOU CANNOT UNDO IT" */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: topOp,
          transform: `translateY(${topY}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -2,
          }}
        >
          YOU CANNOT <span style={{ backgroundColor: '#000000', color: '#FEEC41', padding: '0 22px', borderRadius: 8 }}>UNDO IT.</span>
        </div>
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.75,
            letterSpacing: 6,
            marginTop: 8,
          }}
        >
          KNOWLEDGE IS THE ONLY BRIDGE
        </div>
      </div>

      {/* Bridge visual - YOU ←→ SECURE RETIREMENT */}
      <div
        style={{
          position: 'absolute',
          top: 280,
          left: 80,
          right: 80,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'space-between',
          height: 360,
        }}
      >
        {/* YOU pillar */}
        <div
          style={{
            width: 380,
            height: 320,
            backgroundColor: '#000000',
            borderRadius: 16,
            padding: 28,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
            transform: `scale(${youScale})`,
            transformOrigin: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#FEEC41',
              letterSpacing: 4,
            }}
          >
            CURRENT STATE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 187,
              fontWeight: 900,
              color: '#FCD1D1',
              lineHeight: 0.9,
              letterSpacing: -6,
            }}
          >
            YOU
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 800,
              color: '#FEEC41',
            }}
          >
            Turning 62 →
          </div>
        </div>

        {/* Bridge - middle */}
        <div style={{ flex: 1, position: 'relative', height: 200, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
          {/* Bridge deck */}
          <div
            style={{
              position: 'absolute',
              left: '50%',
              top: '50%',
              transform: `translate(-50%, -50%) scaleX(${bridgeWidth})`,
              width: '100%',
              height: 16,
              backgroundColor: '#000000',
              borderRadius: 4,
            }}
          />
          {/* Bridge arches */}
          <svg width="100%" height="200" viewBox="0 0 600 200" preserveAspectRatio="none">
            <path
              d="M 20 130 C 100 50, 200 50, 300 130 C 400 50, 500 50, 580 130"
              stroke="#000000"
              strokeWidth="6"
              fill="none"
              strokeDasharray="12 6"
              strokeDashoffset={-frame * 1.5}
              opacity={bridgeWidth}
            />
            {/* Vertical cables */}
            {[80, 160, 240, 360, 440, 520].map((x, i) => (
              <line
                key={`cab-${i}`}
                x1={x}
                y1={80 + Math.abs(x - 300) * 0.2}
                x2={x}
                y2={130}
                stroke="#000000"
                strokeWidth="3"
                opacity={bridgeWidth * 0.6}
              />
            ))}
          </svg>

          {/* KNOWLEDGE label centered */}
          <div
            style={{
              position: 'absolute',
              top: 8,
              left: '50%',
              transform: 'translateX(-50%)',
              backgroundColor: '#FEEC41',
              border: '4px solid #000000',
              padding: '8px 22px',
              borderRadius: 8,
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: 2,
              opacity: bridgeWidth,
            }}
          >
            KNOWLEDGE
          </div>
        </div>

        {/* RETIREMENT pillar */}
        <div
          style={{
            width: 380,
            height: 320,
            backgroundColor: '#FEEC41',
            border: '6px solid #000000',
            borderRadius: 16,
            padding: 28,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
            transform: `scale(${retScale})`,
            transformOrigin: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.8,
              letterSpacing: 4,
            }}
          >
            DESTINATION
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 72,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.95,
              letterSpacing: -2,
            }}
          >
            SECURE<br />
            RETIREMENT
          </div>
          {/* Vault icon */}
          <svg width="80" height="80" viewBox="0 0 80 80" style={{ alignSelf: 'flex-end' }}>
            <rect x="8" y="14" width="64" height="54" rx="6" stroke="#000000" strokeWidth="5" fill="none" />
            <circle cx="40" cy="41" r="14" stroke="#000000" strokeWidth="4" fill="none" />
            <line x1="40" y1="41" x2="50" y2="31" stroke="#000000" strokeWidth="4" strokeLinecap="round" />
            <line x1="40" y1="41" x2="40" y2="55" stroke="#000000" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </div>

      {/* Bottom CTA - Subscribe + Share */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `scale(${ctaScale})`,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 20,
            backgroundColor: '#000000',
            padding: '18px 36px',
            borderRadius: 16,
            border: '5px solid #FEEC41',
            transform: `scale(${ctaPulse})`,
          }}
        >
          {/* Bell icon */}
          <svg width="50" height="50" viewBox="0 0 50 50">
            <path d="M 10 35 L 10 22 C 10 12 18 6 25 6 C 32 6 40 12 40 22 L 40 35 L 44 38 L 6 38 Z" stroke="#FEEC41" strokeWidth="4" fill="none" strokeLinejoin="round" />
            <path d="M 20 38 C 20 42 22 44 25 44 C 28 44 30 42 30 38" stroke="#FEEC41" strokeWidth="4" fill="none" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 900,
              color: '#FCD1D1',
              letterSpacing: -1,
            }}
          >
            SUBSCRIBE & SHARE
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#FEEC41',
              opacity: 0.85,
              letterSpacing: 3,
              paddingLeft: 18,
              borderLeft: '3px solid #FEEC41',
            }}
          >
            SAVE A<br />62-YEAR-OLD
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene49KnowledgeBridge;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene50OneConversation',
  durationInSeconds: 11.315,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene50OneConversation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Four chips staggered
  const chip1 = sp(10, { damping: 13 });
  const chip2 = sp(30, { damping: 13 });
  const chip3 = sp(50, { damping: 13 });
  const chip4 = sp(70, { damping: 13 });

  // ONE CONVERSATION
  const convScale = sp(95, { damping: 10, stiffness: 110 });

  // Dollar counter rolling
  const dollarAmount = Math.floor(interpolate(frame, [130, 220], [0, 200000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Final see-you fade
  const seeYouOp = interpolate(frame, [240, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, rgba(254,236,65,0.20) 0%, rgba(252,209,209,0) 60%)',
        }}
      />

      {/* Shape layer */}
      {[...Array(7)].map((_, i) => {
        const r = seededRandom(i + 51);
        const size = 280 + r * 320;
        return (
          <div
            key={`s-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920 - size / 2,
              top: seededRandom(i + 73) * 1080 - size / 2,
              width: size,
              height: size,
              borderRadius: i % 2 ? 24 : '50%',
              border: '2px solid rgba(0,0,0,0.06)',
              transform: `rotate(${frame * 0.08 + i * 30}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.04})`,
            }}
          />
        );
      })}

      {/* Texture - subtle dot grid */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {[...Array(12)].map((_, row) =>
          [...Array(20)].map((_, col) => (
            <div
              key={`dt-${row}-${col}`}
              style={{
                position: 'absolute',
                top: row * 90 + 25,
                left: col * 100 + 30,
                width: 4,
                height: 4,
                borderRadius: '50%',
                backgroundColor: '#000000',
              }}
            />
          ))
        )}
      </AbsoluteFill>

      {/* Particles drifting */}
      {[...Array(25)].map((_, i) => {
        const r = seededRandom(i + 33);
        return (
          <div
            key={`p-${i}`}
            style={{
              position: 'absolute',
              left: r * 1920,
              top: (seededRandom(i + 57) * 1080 + Math.sin(frame * 0.03 + i) * 35),
              width: 6,
              height: 6,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#FEEC41' : '#000000',
              opacity: 0.4,
            }}
          />
        );
      })}

      {/* Top label */}
      <div
        style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [0, 18], [0, 1], { extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 32,
            fontWeight: 700,
            color: '#000000',
            opacity: 0.75,
            letterSpacing: 8,
          }}
        >
          PASS IT ALONG TO →
        </div>
      </div>

      {/* Four chips row */}
      <div
        style={{
          position: 'absolute',
          top: 170,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 26,
          flexWrap: 'wrap',
          padding: '0 80px',
        }}
      >
        {[
          { label: 'A HUSBAND', anim: chip1, bg: '#000000', color: '#FEEC41' },
          { label: 'A WIFE', anim: chip2, bg: '#FEEC41', color: '#000000' },
          { label: 'A FRIEND', anim: chip3, bg: '#000000', color: '#FEEC41' },
          { label: 'A NEIGHBOR', anim: chip4, bg: '#FEEC41', color: '#000000' },
        ].map((c, i) => (
          <div
            key={`c-${i}`}
            style={{
              backgroundColor: c.bg,
              border: '5px solid #000000',
              padding: '18px 32px',
              borderRadius: 14,
              transform: `scale(${c.anim}) translateY(${(1 - c.anim) * 30}px)`,
              opacity: c.anim,
              fontFamily: 'Inter, Poppins',
              fontSize: 68,
              fontWeight: 900,
              color: c.color,
              letterSpacing: -1,
              lineHeight: 1.0,
            }}
          >
            {c.label}
          </div>
        ))}
      </div>

      {/* ONE CONVERSATION center */}
      <div
        style={{
          position: 'absolute',
          top: 370,
          left: 0,
          right: 0,
          textAlign: 'center',
          transform: `scale(${convScale})`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 170,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.95,
            letterSpacing: -6,
          }}
        >
          ONE <span style={{ backgroundColor: '#FEEC41', padding: '0 28px', border: '6px solid #000000', borderRadius: 14 }}>CONVERSATION</span>
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 55,
            fontWeight: 800,
            color: '#000000',
            marginTop: 18,
            opacity: 0.85,
          }}
        >
          can save someone →
        </div>
      </div>

      {/* Dollar counter */}
      <div
        style={{
          position: 'absolute',
          bottom: 160,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [125, 145], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            display: 'inline-block',
            backgroundColor: '#000000',
            padding: '16px 40px',
            borderRadius: 14,
            border: '5px solid #FEEC41',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#FEEC41',
              opacity: 0.8,
              letterSpacing: 5,
            }}
          >
            HUNDREDS OF THOUSANDS →
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 153,
              fontWeight: 900,
              color: '#FEEC41',
              lineHeight: 1.0,
              marginTop: 4,
            }}
          >
            ${dollarAmount.toLocaleString()}+
          </div>
        </div>
      </div>

      {/* See you in the next one */}
      <div
        style={{
          position: 'absolute',
          bottom: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: seeYouOp,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 1,
          }}
        >
          See you in the next one →
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene50OneConversation;
```

